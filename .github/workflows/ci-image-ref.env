# The one place the CI image is pinned. Read by fast-lane.yml and slow-lane.yml
# (via their `resolve-image` job), never duplicated into a `container:` literal.
#
# A007-T16: this MUST be a digest pin (`...@sha256:...`), not a moving tag.
# Changing it is a reviewed PR — the same treatment the Loro pin and the
# docker-compose base images get. Rebuild + repin with `ci-image.yml`
# (workflow_dispatch, or a push touching `.docker/ci/**`), then paste the
# digest it prints in its job summary.
CI_IMAGE=ghcr.io/shaheerjameel17/vulto-for-professional-services/ci@sha256:cdb620c58b114092ae4ba864252aabf845bfbf6ceab7eab90de23180122ba6b4

# The Postgres the slow lane's service containers run (F203). It carries the
# logical-replication flags in its default command, which a GitHub Actions
# service container cannot set. Until ci-image.yml has published
# `ghcr.io/.../ci-postgres` (a merge to main) and this is repinned to its digest,
# it stays on the stock Postgres digest the lane used before, so nothing that ran
# yesterday changes — but no lane can yet run Electric.
CI_POSTGRES_IMAGE=postgres:17-alpine@sha256:742f40ea20b9ff2ff31db5458d127452988a2164df9e17441e191f3b72252193
