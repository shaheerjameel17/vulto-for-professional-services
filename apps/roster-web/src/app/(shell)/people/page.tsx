import { NotInPrototype } from "../../../components/NotInPrototype";

export default function Page() {
  return <NotInPrototype title="People" step="Step 3 — the employee profile per VRS-F002." />;
}
