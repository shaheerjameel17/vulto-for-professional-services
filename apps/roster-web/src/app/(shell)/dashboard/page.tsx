import { NotInPrototype } from "../../../components/NotInPrototype";

export default function Page() {
  return <NotInPrototype title="Manager Dashboard" step="Step 6 — the age-ordered queue per VRS-F049." />;
}
