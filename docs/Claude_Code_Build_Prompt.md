# Vulto — Server-Authoritative Build: Instructions for Claude Code

You are starting a new build phase in this repository. Read this entire document before doing anything. It is your complete brief for seven stages of work. It was written so that you make **no product, architecture or security decisions**. Every such decision has already been made, either in the specifications or below. Your job is to implement exactly what is written, prove it with tests, and report.

---

## Part 1 — Context you must understand first

### What changed

Until 20 September 2026 this repository was building a **local-first, device-canonical** architecture: a Loro CRDT graph on each device, a Rust sync relay, a sealed encrypted device store, and end-to-end encryption of Tier 1 data (salaries, pay, contracts, HR cases) with key envelopes and 2-of-3 recovery holders.

**That architecture is retired** (finding F199). It is replaced by:

- **PostgreSQL is the single source of truth.** `services/api` is the only writer.
- **Devices hold a fast, permission-filtered cache**, fed by **Electric** (Apache-2.0) through exactly two fixed shapes filtered by a server-maintained **sync audience** (F201, F202).
- **Every write is a named mutation**: applied optimistically on the device, queued in an outbox we own, committed on the server in one transaction, idempotent and stale-state-safe.
- **Tier 1 and Tier 2 data are field-encrypted on the server** under a KMS key hierarchy, fetched on demand through an audited `protected.read`, held in memory only, and **never stored on a device**.
- **Tier 3** (wellness data) stays end-to-end encrypted, but as a **deferred module**. You do not build it in this phase.
- **Trust** is delivered through verifiable controls (VPS-A008), not through the architecture being unable to read data.

### Where the truth lives

| Question | Source |
|---|---|
| The architecture you are building | `docs/Vulto_Specs/VPS-A003_Unified_Sync_Architecture.md` |
| The stack | `docs/Vulto_Specs/VPS-A001_Technology_Stack_and_Engineering_Foundations.md` |
| The graph registry, tiers, standing rules | `docs/Vulto_Specs/VPS-A002_Master_Graph_Schema_Definition.md` |
| Permissions | `docs/Vulto_Specs/VPS-A004_Graph_Permission_Layer.md` |
| CI gates | `docs/Vulto_Specs/VPS-A007_Build_Test_and_Deployment_Pipeline.md` |
| Trust program (context only this phase) | `docs/Vulto_Specs/VPS-A008_Trust_and_Data_Protection_Program.md` |
| Why the direction changed | `docs/Foundations_Findings.md` — F199, F200, F201, F202 |
| Project rules | `CLAUDE.md` |
| Work tracking | Linear, team **Vulto Foundation**, project **Server-Authoritative Data Layer** |

### Repository state at the start of this phase

- `main` contains the rewritten specifications (commits `53803fe`, `aa1adeb`, `4e165bf`, `7b768ef`). **Main may not yet be pushed to GitHub**; Stage 1 pushes it.
- `archive/local-first-e2e` preserves the entire retired implementation **plus** FDN-68's unfinished audit work.
- `fdn-68-audit-wip` holds FDN-68's unfinished audit work alone. Stage 3 takes specific files from it.
- The retired code still lives on `main`: `services/sync-engine` (Rust), `packages/graph/src/worker/**` (Loro runtime, sealed store, Tier 1/Tier 3 envelopes), the device-unlock code in `services/api/src/auth/device-unlock.ts`, and diagnostics pages in `apps/roster-web`. **It stays, compiling and passing its unit tests, until Stage 7 deletes it.** Do not delete or refactor it earlier unless a stage says so.
- `apps/roster-web` product screens run on **fixtures**, not on the graph. Only diagnostics pages import `@vulto/graph`. **You build no product UI in this phase.**

---

## Part 2 — Rules that govern every stage

### The decision policy — read twice

1. **You do not make product, architecture, security or data-model decisions.** If the specifications or this document decide something, do exactly that.
2. **Micro-decisions are allowed only when they have no behavioral, security or data consequence**: a private function name, the split of a file into helpers, the order of test cases. Pick whatever matches existing code style, and list every such choice under "Micro-decisions" in the stage report.
3. **Anything else that is not decided is a STOP condition.** For example: the spec and this document disagree; a library does not support what is specified; a table needs a column not listed here; a test cannot be written as specified. When that happens:
   - add an **open** finding to `docs/Foundations_Findings.md`, using the next free F number and the existing table-row and section format;
   - finish nothing else in that stage that depends on it;
   - write the stage report with status **BLOCKED**, and stop.
   **Do not work around it. Do not "choose the most reasonable option."**
4. **Do not edit specifications** except to add findings. If building proves a spec wrong, stop as above.
5. **Do not gold-plate.** Build what the stage lists, nothing more. No extra abstractions for future features, no UI, no refactors of unrelated code.
6. **Do not start the next stage** until the founder writes "Stage N approved".

### Standing engineering rules (from `CLAUDE.md` and the specs)

- American English everywhere: code, comments, identifiers, docs.
- Never compute a working day. Never write a permission check in feature code; the interceptor decides.
- Tier 1 and Tier 2 values never reach device storage, logs, metrics, analytics or error reports. Tier 3 plaintext never reaches a server.
- Never write the graph except through the mutation pipeline. Never write the sync audience except through the materializer. Never add a shape beyond the two templates.
- No `localStorage` or `sessionStorage`, anywhere. IndexedDB is allowed only inside the sync client.
- Pin every new dependency to an exact version (no `^` or `~`). Pin container images by digest, and record each pin in the spec that requires it in the same commit (VPS-A001 for sync, VPS-A007 for CI images).
- UUID v4 for every ID. UTC ISO-8601 timestamps ending in `Z` in records; `timestamptz` in Postgres.
- Use the existing tools: Drizzle (`services/api/drizzle.config.ts`), Vitest, Playwright, tRPC 11 on Fastify 5, Better Auth, Zod `4.4.3`, `canonicalize`, `wa-sqlite`.

### Git workflow

- Each stage runs on its own branch from the latest `main`: `stage-<N>-<slug>`, for example `stage-2-graph-store`.
- Commit in small logical steps: `feat(FDN-94): ...`, `test(FDN-94): ...`, `docs(FDN-94): ...`.
- At the end of a stage: run the gates, push the branch, write the report, update Linear, and **stop**.
- When the founder writes "Stage N approved": merge the branch into `main` with `git merge --no-ff stage-N-...`, push `main`, set that stage's Linear issues to **Done**, then start Stage N+1 from the new `main`.
- Never force-push `main`. Never rewrite `archive/local-first-e2e` or `fdn-68-audit-wip`.

### Linear workflow

- When a stage starts, set its issues to **In Progress**.
- When the stage report is written, set them to **In Review** and add a comment on each: the report path, the branch name, and a one-paragraph summary.
- Do not set anything to **Done** before approval.
- If you raise a finding, comment it on the relevant issue.

### The stage report

At the end of every stage, write `docs/stage-reports/STAGE-<N>_<Slug>.md` with **exactly** these sections, in this order:

```markdown
# Stage <N> — <Name>

**Status:** COMPLETE | BLOCKED
**Branch:** stage-<N>-<slug> @ <commit sha>
**Linear issues:** FDN-xx, FDN-yy
**Date:** YYYY-MM-DD

## 1. Summary
Five sentences maximum, plain English, for a non-engineer founder.

## 2. Done-criteria checklist
Every done criterion listed for this stage in this document, one per line:
- [x] <criterion> — evidence: <test file>::<test name>, or <command> output
- [ ] <criterion> — why not done

## 3. Spec clauses implemented
A table: Spec ID (e.g. A003-T53) | Where implemented (file path) | Test proving it (file::test)

## 4. Files changed
Output of `git diff --stat main...HEAD`, verbatim.

## 5. Database changes
Every migration file created, with the tables, columns, indexes and constraints it adds. "None" if none.

## 6. Tests and gates
For each command in the stage's "Gates" list: the exact command, the exit code, and the pass/fail/skip counts, copied from the output. No summarizing: paste the final lines of each run.

## 7. Micro-decisions
Every micro-decision under the decision policy, one line each. "None" if none.

## 8. Findings raised
Every new F-number, one line each, with state. "None" if none.

## 9. Deviations from this brief
Anything done differently from this document, and why. This should be "None". A non-empty section without a matching finding is a defect.

## 10. Known limitations and risks
Things that work as specified but that a reviewer should know.

## 11. Readiness for the next stage
Yes or No, and what the next stage depends on from this one.
```

Keep reports factual. Do not claim anything that is not backed by a test name or command output.

### Gates that apply to every stage

Unless a stage adds more, a stage cannot be COMPLETE unless all of these pass:

```
pnpm install --frozen-lockfile
pnpm stack:up
pnpm verify
pnpm verify:full
```

Any pre-existing failure you find in Stage 1 is recorded as a baseline and must not get worse.

---

## Amendments

- **21 September 2026 — F203 closed.** GitHub Actions service containers cannot override the Postgres command. Decision: CI gets a tiny Postgres image of its own whose default command carries the replication flags, built and pinned exactly like the existing CI image. Implemented in Stage 6 item 0, because no CI suite needs replication before then. When merging Stage 1, update F203 to **Closed by founder-delegated decision, 21 September 2026 — CI Postgres image, built in Stage 6**, and move its detailed section so it follows F202.
- **21 September 2026 — F204 closed.** A `User` is stored **once per workspace** it belongs to, under the same `node_id` (its Better Auth account id). `graph_nodes` gets a composite primary key `(workspace_id, node_id)`; every row has a non-null `workspace_id`; every other node type's `node_id` stays globally unique through a partial unique index; edges reference endpoints by `(workspace_id, node_id)`. Rejected: a null `workspace_id` for `User` rows, because a row outside every workspace breaks per-workspace residency (VPS-A008 P8), export and erasure, and would let one workspace's edits to a person's node appear in another's. Stage 2 and Stage 6 below are updated. When resuming Stage 2, update F204 to **Closed by founder-delegated decision, 21 September 2026 — one `User` row per workspace under a composite key; VPS-A002 and VPS-A003 amended on `main`**.
- **21 September 2026 — F205 closed.** Your recommendation is accepted. `graph_nodes.created_at` becomes nullable, with a check constraint allowing null **only** for `PulseAggregateContribution` and `WellnessAggregateContribution`; every other type stays not-null in practice. This follows VPS-A002's closed omission policy and keeps those contributions uncorrelatable. Do it in a new migration on `stage-2-graph-store`, remove the store's refusal, extend the round-trip test to all registered types, and mark F205 **Closed by founder-delegated decision, 21 September 2026 — nullable `created_at` for the two anonymous contribution types only, by check constraint**.
- **21 September 2026 — membership changes reach the graph (added to Stage 3).** Stage 2 wrote only the founding records, so the graph's `WorkspaceMembership` node goes stale on later changes. In Stage 3, extend the membership projection so **invitation acceptance, role change and member removal** each write the matching `User`, `WorkspaceMembership` and endpoint-edge changes in the **same transaction** as the Better Auth change, the same way `workspace.create` now does. Permission decisions still read the central Better Auth row (Stage 3 item 2); the graph copy is history and the input Stage 6's audience recompute listens to.
- **21 September 2026 — generic mutations are Tier-0-only (added to Stage 4).** The registry has no field-to-partition map, so a generic mutation on a split or protected node type could put protected fields into `graph_nodes.record`. The foundation mutations `graph.createNode` and `graph.updateNodeFields` therefore accept **only node types whose every partition is Tier 0**. Split types and Tier 1/2/3 types are refused with reason `requires-feature-mutation`; the feature that owns such a type will define a named mutation that routes each field to its partition. Add a test for the refusal.
- **21 September 2026 — F206 closed.** The recommendation is accepted, with three additions. (1) `AuditEntry` gains `actor_kind` (`member` | `support` | `system`); `actor_user_id` and `actor_membership_id` stay required for `member`; `support` carries `actor_grant_id`; `system` carries `actor_system_name` from the closed enum. The journal table gains the same columns. **Correct the `AuditEntry` contract in VPS-F004 accordingly** — this is a founder-decided spec correction, recorded under F206. (2) A support scope is `{ access: "read" | "read-write", node_types: NodeType[] }`, evaluated against the same policy table as a member and capped at the scope, never above Owner. **A support principal may never write** `Workspace`, `WorkspaceMembership`, `User` or `AuditEntry`, never change roles, and never reach a Tier 3 type, whatever its scope lists. VPS-A008's "areas" means node types. (3) Each stage adds the system operations it builds: Stage 5 adds `key-rotation` and `erasure` key destruction; Stage 6 adds `audience-recompute`. Implement (1) and (2) on `stage-3-interceptor` before approval, with tests that a support principal and a system principal are each evaluated and produce correctly shaped audit entries. Mark F206 **Closed by founder-delegated decision, 21 September 2026**.
- **21 September 2026 — Stage 3 micro-decisions ruled on.**
  - **The audience materializer is not audited per evaluation.** It uses the side-effect-free `decide*` functions, and handles only Tier 0 rows, whose grants are not audited anyway. Recording "who may hold which row" is the audience table itself. No audit entries are written by the materializer. (This closes the question Stage 3 left for Stage 6.)
  - **The Client ownership disagreement** between VPS-A002's ownership table and VPS-F008's policy table must be recorded as its own finding, closed with the reading you applied (F008 governs: not applicable), and listed for the FDN-104 spec sweep. A spec disagreement is never only a micro-decision.
  - The other Stage 3 micro-decisions are accepted as written.
- **21 September 2026 — F208 closed.** Declare `governingPartitions: { Employee: "operational" }` on **both** `managed_by` and `scoped_to_entity`, matching `assignment_of` and `has_skill`. A reporting line and an employing entity are operational HR facts, not compensation, so whoever may write an Employee's operational half may move them. Record the declaration in VPS-A002's `governingPartitions` table with that one-line rationale, flip `itF208` to `it`, and mark F208 **Closed by founder-delegated decision, 21 September 2026**.
- **21 September 2026 — missing schema-version header.** Refusing a request with no `x-vulto-schema-version` header as `client-outdated` is **accepted** (fail closed). Stage 6's sync client and every tRPC client in `apps/` must always send the header; add that to Stage 6's client work and test it.
- **21 September 2026 — F125 stays open** with the current behavior: a backdated `managed_by` move is refused as `invalid-args`. VRS-F037 decides backdating when the org chart is built.
- **21 September 2026 — F209 closed.** The recommendation is accepted, with one addition. Add event type `CryptographicErasureExecuted`, operation `KeyDestroy`, and a closed target `{ erasure_domain_id, tier, destroyed_key_count, erasure_request_id: uuid | null }`, written only by the `erasure` system principal, in the same transaction as the key destruction. `erasure_request_id` is `null` until VPS-F007's ErasureRequest exists, and then it is required, so every erasure traces back to an approved request. Record the event in VPS-F004's vocabulary as a founder-decided correction under F209, replace the fail-closed seam with the real audit call, keep a test that a failed audit destroys no key, and mark F209 **Closed by founder-delegated decision, 21 September 2026**.
- **21 September 2026 — key provider has no default (Stage 5 review).** `createKeyProvider` must **not** default an unset `VULTO_KEY_PROVIDER` to `local`. An unset or empty value is a startup error in every environment, and `local` is refused unless `NODE_ENV` is `development` or `test` (not merely "not production"), so a production server missing `NODE_ENV` cannot silently fall back to the development key. Update `.env.example`, CI env and the tests accordingly.
- **21 September 2026 — noted, not required now.** KEK rotation (VPS-A003 "Rotation") has no issue yet; it is added to FDN-96's follow-up list rather than built in Stage 5. `protected.read` resolves each fragment's node and key one query at a time; that is acceptable at the 500-item cap and is revisited only if measured slow.
- **21 September 2026 — pushing.** The founder has allowed `git push` to `origin` for `main`, `stage-*`, `archive/local-first-e2e` and `fdn-68-audit-wip`. A refused push is still a STOP condition.
- **21 September 2026 — Stage 6 approved and merged (`961830d`). Stage 7 amendments:**
  - **F210 (device revoke survives).** Before dropping `device_unlock_secret` (item 4), create the control-plane table `device_workspace_revocation (workspace_id, device_id, revoked_at, revoked_by, reason)`, primary key `(workspace_id, device_id)`, identifiers only, never synced. Backfill it from `device_unlock_secret` in the same migration, repoint every revoke check (session path and shape proxy) to it, then drop the old table. A test proves a device revoked before the migration is still revoked after it. "Keep the device registry, revoke and retire" in item 1 includes this.
  - **F212 (retired browser job).** Delete the disabled `device-store-browser` job, its config and its suite together with the retired code. After Stage 7 no job in either lane may be disabled or skipped except `publish-artifacts` off `main`.
  - **Accessibility smoke pass restored.** Disabling `device-store-browser` in Stage 6 stopped the accessibility smoke pass. Stage 7 moves it into a live slow-lane suite (the auth browser suite or the sync suite) against the current pages, and it must pass in CI.
  - **Check the merge-commit run first.** Confirm the fast and slow lanes on `main` at `961830d` are green before branching. If red, stop and report.
  - **Device-side rules carried from Stage 6, not to be undone:** `vulto:device` survives sign-out and holds only the device id and the pending-erase list; the outbox is versioned separately from the cache and is never wiped by a version change; every client API request carries `x-vulto-workspace-id`, and a mismatch is refused with nothing applied.
- **22 September 2026 — Stage 7 approved (`bc55274`/`d749c3c` on `stage-7-retire`). Merge and follow-ups:**
  - **Verdict: approved for merge.** Read directly against the code, not just the report. Migration `0019` creates `device_workspace_revocation`, backfills it from `device_unlock_secret` joined to the most recent matching `device_trust_event`, then drops six retired tables, in that order, inside `drizzle-kit migrate`'s transaction. Its test builds a genuine pre-0019 schema, revokes devices the old way across two workspaces (including one revoked only in a second workspace, to prove workspace scoping survives too), migrates, and proves every revoked pair is still revoked and nothing unrevoked was swept in — exactly what F210 required. `git grep -n -i -E "loro|sync-engine|sealed-store|shamir|tier1-envelope"` returns only `CLAUDE.md`'s archive line and two historical comments in the migration; no `.rs` files are tracked; `loro-crdt` and `shamir-secret-sharing` are gone from every `package.json` and the lockfile; `sync-ticket.ts` and `device-unlock.ts` are deleted with no live references outside tests and historical comments. The slow lane has exactly the five jobs claimed, no `device-store-browser` job anywhere, and `publish-artifacts` is the only conditional one (pre-existing `continue-on-error`, `main`-only, unrelated to Stage 7). The accessibility smoke pass is a real, live gate against `/sign-in`, `/sign-up` and `/devices` with a shrink-only baseline. The FDN-54 adversarial suite has the seven cases and eleven tests claimed; cases 4 (mid-session demotion), 5 (stale transition) and 6 (an audit-append failure) were read in full and all three fail closed correctly. The findings recount was independently reproduced by parsing every `| F<n> |` row of `docs/Foundations_Findings.md`: 159 rows, F54–F213, one gap (F198), 143 closed, 10 open (F70, F71, F73, F85, F91, F118, F125, F129, F130, F213), 6 recorded — an exact match to the report. `CLAUDE.md`, `Bootstrap.md` and `CONTRIBUTING.md` read as described. `git diff --shortstat main...bc55274` is 198 files, +4,263, −48,484, matching the report exactly. `workspace.create`, `changeWorkspaceRole` and `revokeMembershipForActor` are each one `db.transaction`, with no device-side step, confirming the single-transaction claim.
  - **One question resolved, no finding needed.** `device.revoke` no longer gates `protected.read` or any tRPC mutation: `requireCurrentWorkspaceSession` (used by every `protectedProcedure`) carries no device id and never touches `device_workspace_revocation`; only the shape proxy reads it. Checked against `VPS-F001`'s own contract for `device.revoke` ("revokes this workspace's trust and erases this workspace's cache only") and G04 (the device cache holds Tier 0 only; revocation erases the cache) — this is the spec as written, not a gap introduced in Stage 7. Tier 1/2 access was never meant to be device-gated in the server-authoritative model; it is membership-gated and server-audited in `protected.read`, which is the correct control surface. No code change follows. Worth knowing for the founder: "Revoke" on the Devices screen only cuts a device's sync/cache trust to one workspace, not its session's API access — a stolen device with a live browser session is stopped by suspending the account or ending the session, not by device revoke. Consider a one-line clarification on that screen's copy when the Devices UI is next touched; not a Stage 7 blocker.
  - **F213 ruling — keep `member.projection_state`, close as a recorded simplification.** The column and its check constraint are vestigial (`revocation-pending` is declared in the check constraint and the type union but never written anywhere in `services/**` or `packages/**` outside tests and Drizzle snapshots — confirmed by direct grep), but removing it touches Better Auth's member model and every query that reads it, for a column with no functional cost today. Not worth Stage 7 risk. Mark F213 **Closed by founder-delegated decision, 22 September 2026 — `member.projection_state` and its check constraint stay; the column is dead in practice, not in the schema. Revisit during the FDN-104 spec sweep, not before.**
  - **Merge.** CI is green on `stage-7-retire` at `d749c3c` (run `35637057091`), and `main` was green at `961830d`/`4d91705` before branching (confirmed). Merge `stage-7-retire` into `main` with `--no-ff`. In the same pass on `main`: update F213's row in `docs/Foundations_Findings.md` to the ruling above and fold it into the next programmatic recount.
  - **CI image repin.** Once merged, `.docker/ci/**` changed, so the `ci-image` workflow republishes without Rust. Read the new digest from that run and repin `CI_IMAGE` in `.github/workflows/ci-image-ref.env`, per the report's section 10 and the existing repin procedure.
  - **Stop after that.** Part 4 stands: the next brief is the FDN-104 spec sweep and RST-33 (canonical Employee profile). Wait for "Stage 8 go" before starting either.
- **22 September 2026 — Stage 8 reviewed (`c733763` on `stage-8-employee-profile`). Rulings, merge and follow-ups:**
  - **Verdict: approved for merge**, once the fixes below land. Read directly against the code, not just the report. `rowScopeSatisfied` and `decideRead` in `services/api/src/permission/interceptor.ts` were read in full: subject exclusion is layered on top of the role-based outcome and correctly overrides a full/read grant when caller === subject, and correctly does nothing when the link or the subject can't be resolved (F215 a/b, below). `resolveReaderSet` resolves `own` from the real link and leaves every manager-derived scope unresolvable, which is harmless because `Employee:compensation`'s manager cell is `restricted`, not a grant, so Gate 3 never reaches it. `employeeLinkUser` enforces the bidirectional one-Employee-per-login uniqueness the report claims, checked in both directions. The `Employee` registry entry (`packages/schema/src/registry/nodes.ts`) is byte-for-byte unchanged from before Stage 8, confirmed with `git show`. `FEATURE_LIFECYCLE_NODE_TYPES` refuses `graph.createNode`, `graph.updateNodeFields` and `graph.transitionLifecycle` for `Employee` symmetrically on client and server, and the test at `employee.integration.test.ts` proves all three. The four F130 subject-exclusion tests were read in full, including one that asserts a literal sentinel string is absent from `protected.read`'s response to the excluded subject and present to everyone else — end-to-end proof, not an access-flag check. `transitionStatus` was read against its test: the status table is enforced server-side (Active↔Inactive requires `end_date`, `Converted` is terminal), and a stale `expected_version` is reported as `stale-state` ahead of transition validation, exactly as the code comment says it must be. The import-equivalence test was read in full: an imported row and a manually created one produce identical canonical records (case-normalized email, coerced numeric fields, same lifecycle status, same `scoped_to_entity` edge), and one malformed row is rejected without harming the batch. `VPS-D004`'s F214 marker is present exactly as described, and the section's original body is otherwise untouched. The findings recount was independently reproduced by parsing every `| F<n> |` row: 161 rows, F54–F215, one gap (F198), 146 closed, 9 open — an exact match to the report. The mutation-testing pass/fail counts in the report (removing subject exclusion fails 2 tests, `direct-reports` never satisfied fails 3, `own` never satisfied fails 2, `own-plus-team` satisfied for anyone fails 5) were not independently re-run — running them would mean breaking the code and re-running the suite on the linked machine, which has no `pnpm` available in this session — but they are consistent with the actual test bodies at lines 483, 500, 519, 540 (exclusion), 582, 670, 685 (direct-reports) and 622, 685 (own) read in full, and are accepted on that basis.
  - **F130 — closure confirmed.** The report's claim is correct: `decideRead` now applies the exclusion for every node type that registers one, feeding the interceptor, `protected.read` and the sync audience from the one decision, as A004-T16 requires. No further action.
  - **F215 — all three decisions confirmed as written.** (a) A login with no linked Employee record is not treated as the subject of anything — correct, because denying access whenever the link can't be resolved would blind an Owner to every case, including ones about people other than themselves, for a data-completeness reason that has nothing to do with who the case concerns; not excluding is the conservative direction here, since it grants nothing beyond the existing role-based grant. (b) A case with no recorded subject excludes no one — correct, since the write gate already refuses a subject-less case, so this is unreachable in practice and, if it were ever reached by a data defect, still leaves HRCase gated at the role level. (c) `own-plus-team` means "shares my active manager" — accepted as the definition going forward. One follow-up: `VPS-A004_Graph_Permission_Layer.md`'s own matrix is where `Read (own + team)` is written (line 128 and 159) and "team" is not defined anywhere in that document — checked directly, there is no definition to conflict with, only a gap. Add one sentence to `VPS-A004`, next to the matrix or in Role definitions, stating the definition this finding settled: team member scope's "team" means "shares the caller's active manager, derived the same way Manager status is (`VPS-F001` G06)." Cite F215. This is a documentation fix only, no code change, and belongs in this same pass.
  - **F214 — the design is ruled, the shell is not built yet.** The proposed replacement is accepted as written: one `Reconnect` state, triggered only by `401`/`UNAUTHORIZED` on an authenticated call or `access-revoked` from the shape proxy, never by a network failure, timeout, 5xx or rate limit; full-bleed, no illustration, one `Retry` that re-issues the failed call and says why nothing else. Both tradeoffs are decided as recommended: **tradeoff 1** — a call carrying no session at all (never authenticated on this device, or already signed out) goes to sign-in, not `Reconnect`; `Reconnect` is reserved for a call that had a session and was refused. This does not weaken non-enumeration: within the "had a session, got refused" bucket, an expired session and a revoked one still render identically, which is the actual guarantee F148 protects. Whether a device ever had a session in the first place is not information about any workspace or person — every never-authenticated visitor gets the same sign-in screen regardless of what happened to anyone else. **Tradeoff 4** — nothing is erased on a plain `401`; cached rows stay until a call succeeds or the person signs out. Accepted, and only because it is scoped to what the cache can ever hold: Tier 0 only, by the tier system's own design, so an expired session sitting on a shared device exposes nothing beyond what that design already decided is safe to leave there. If a future stage ever widens what the device cache holds, this decision must be revisited — record that as a note next to the ruling, not as an open question now. Tradeoffs 2 and 3 are accepted as stated, unchanged from the proposal. **Implement the design in `VPS-D004` now**: replace the "Locked shell" section's body with the Trigger / Rendering / What Retry does / Data / Kept / Removed structure from the F214 write-up (adapted to the document's own voice), remove the F214 marker blockquote, and update the Decisions Recorded entry to record the ruling and this date rather than pointing at an open question. **Do not build the `Reconnect` component, the tRPC/shape-proxy wiring, or its tests in this pass.** Stage 8 touched no `apps/` code, and a first client shell feature — with its own browser test proving cold-start and mid-session render identically and that Retry behaves correctly — deserves its own reviewed stage rather than being folded into this merge. Name it **Stage 9 — the Reconnect shell state** in `docs/Reviewer_Handoff.md`'s follow-ups; it is briefed separately once "Stage 9 go" is given. Mark F214 **Closed by founder-delegated decision, 22 September 2026 — one `Reconnect` state, non-enumerating, `401`/`access-revoked` only; missing-session routes to sign-in; no erase on a plain `401`, scoped to Tier 0. `VPS-D004` corrected to match. Build tracked separately as Stage 9.**
  - **Merge.** Confirm CI is green on `stage-8-employee-profile` at its current head (and that `main` was green before branching, as it was at Stage 7's merge). Apply the `VPS-D004` and `VPS-A004` doc fixes above, update F214's and F215's rows in `docs/Foundations_Findings.md`'s status table to the rulings above, and fold both into the next programmatic recount (161 rows becomes 160 open-minus-one: 147 closed, 8 open — F70, F71, F73, F85, F91, F118, F125, F129 — recount it programmatically, do not hand-count). Merge `stage-8-employee-profile` into `main` with `--no-ff`.
  - **Linear.** Move `RST-33` to Done — its own done criteria (canonical record, validated relationships, history-preserving lifecycle, protected fields, import/manual equivalence) are each verified above. Leave `FDN-104` **in progress, not Done** — this was its priority slice only (`VPS-D004`, `VPS-F004`, `VPS-F002`, `VPS-A002`'s `Client` row, `VRS-F002`); 39 of its 44 affected specs are still uncorrected and are scoped for just-in-time correction ahead of their own features, per FDN-104's own text. Add a comment on `FDN-104` noting the priority slice is complete and pointing at this stage's commits.
- **22 September 2026 — F216 ruled; Stage 9 redefined, original Reconnect brief renumbered Stage 10.** The Stage 9 (Reconnect) brief went to OpenAI Codex this round, a fresh session against the same repo, not Claude Code — the loop is agent-agnostic (see `Reviewer_Handoff.md` section 1). Codex traced the brief before writing UI and stopped with F216 rather than inventing a bootstrap design locally, which is the correct call under this project's decision policy. Verified directly: `apps/roster-web/src/app/(shell)/layout.tsx` wraps only the fixture `Shell.tsx`, which hardcodes `workspaceName="Northgate Studio"` and `syncStatus="Synced"` as literals; `createGraphClient` is mounted nowhere but `/sync-harness`; `authClient.useSession()` appears only on `/auth-ready` and `/devices`, outside the shell, and `/auth-ready`'s own copy says workspace access stays closed until the server confirms the membership projection; `SyncEngine.#handleRevoked` stops every source and publishes `signedOut: true` before any retry could reuse that worker. Every screen under `(shell)/` renders from fixtures (`EMPLOYEES`, a fixed viewer, a client-side `canSeeCompensation()` helper unrelated to the real interceptor). This is `FDN-69`'s scope — the shared shell and workspace bootstrap — never built, sitting in Backlog since before Stage 1; Reconnect assumed it already existed.
  - **The ruling is not invented from scratch — it's already half-built server-side.** `services/api/src/auth/config.ts` already configures Better Auth's `organization()` plugin; the session already carries `activeOrganizationId`, and `services/api/src/auth/workspace-session.ts` already clears it to `null` on revocation — `requireCurrentWorkspaceSession` already treats it as the workspace claim. The only gap is client-side: `apps/roster-web/src/lib/auth-client.ts` never added `organizationClient()`, so nothing in the shell can read what the server already tracks.
  - **Founder's sequencing call.** Given three options (a minimal real bootstrap now; deferring shell-wiring and continuing the backend-only `VRS-001` order; or a full `FDN-69` migration of every fixture screen in one pass), the founder chose the minimal bootstrap. Stage 9 becomes that: authenticate, resolve the workspace via `activeOrganizationId`, mount the real graph client, replace `Shell.tsx`'s fixture props with real state, keep offline cold start working from `session_hint` with no network call, and surface (not render) a `401`/`access-revoked` at the shell boundary. No fixture screen (Home, People, Timesheets, Foundations) is touched — that migration is explicitly out of scope and not yet briefed as its own stage.
  - **The original Reconnect brief is unchanged in substance and renumbered Stage 10**, now depending on Stage 9's exposed refusal signal and bootstrap re-invocation (Retry, once `access-revoked` has stopped a client, means re-running the bootstrap from scratch — there is nothing left inside a stopped client to retry). `FDN-114` (Codex's issue) stays Stage 10's; it already carries the F216 discovery and the blocked report.
  - **F216 marked Closed by founder-delegated decision, 22 September 2026** — the shell bootstrap contract above, with Stage 9/10 as its build. Findings table and recount updated (162 rows, F54–F216, 148 closed, 8 open).
  - **Branch state.** `stage-9-reconnect-shell` is pushed and holds only the F216 discovery commits and the blocked stage report — no Reconnect or bootstrap code. Start Stage 9 from a **new** branch off `main` (`stage-9-shell-bootstrap` or similar); don't build the bootstrap on top of the old branch's name, since Stage 9 and Stage 10 are now different, separately-reviewed stages.
- **22 September 2026 — F217 ruled; the Stage 9 brief updated in place.** A first Stage 9 attempt (Codex, `stage-9-shell-bootstrap`, abandoned) traced F216's ruling against the live workspace lifecycle before writing code and stopped correctly: `session.activeOrganizationId` is read correctly per F216, but nothing populates it. Verified directly: `createWorkspace` → `createPendingWorkspaceAdmission` → `confirmWorkspaceAdmission` never touches the field; the only production write to it anywhere is the revocation path's clear-to-null. Offline, each `(workspace, user)` pair has its own cache with no marker of which was last active, and `device-identity.ts`'s database is deliberately, test-enforced narrow (device id and pending-erase names only) — not a place to add one without reopening that contract. F216's ruling should have checked this and didn't; that gap is this review's, not the builder's.
  - **Ruling.** `confirmWorkspaceAdmission` sets `session.activeOrganizationId` in its existing transaction, only when the confirmed membership is the person's sole active one — mirroring how `revokeWorkspaceAdmission` already checks membership state. A person already active in one workspace who creates or joins a second is never auto-switched. A null active organization with exactly one active membership resolves to it (no real ambiguity); with more than one, or none, the shell must not guess — a minimal holding state, distinct from F214's `Reconnect`, covers it until workspace switching (already tracked as undesigned) is built. Offline, the identical rule: exactly one cached workspace boots from it; more than one requires coming online. `F217` closed.
  - **The Stage 9 section in Part 3 is updated in place** with these two additions (its point 2 and point 4) rather than kept as a separate amendment to reconcile later — read Part 3 directly, not this bullet, when building.
  - **Branch state.** `stage-9-shell-bootstrap` is pushed and holds only the F217 discovery and blocked report — no bootstrap code. Start Stage 9 from a fresh branch off `main` again (a third name, e.g. `stage-9-shell-bootstrap-v2`), same as the Stage 9/10 split did for the Reconnect branch.
  - **Stop after that.** Wait for "Stage 9 go" (the real shell bootstrap, now covering both F216 and F217) before starting either Stage 9 or Stage 10.
- **22 September 2026 — F218 ruled; the Stage 9 brief updated in place again.** A second Stage 9 attempt (Codex, `stage-9-shell-bootstrap-v2`, abandoned) stopped correctly before writing code: the F217-updated brief's point 1 ("no data after `isPending` clears" unconditionally means sign in) and point 4 (a cold offline boot must mount from cache with no network call) both fire on the same event — a cold offline load also resolves `useSession()` to "no data" — and the brief never said which wins. Verified directly against the pinned client: Better Auth's session atom (`session-atom.mjs`, 1.6.29) sets `data: null` with a structured `error` (a `BetterFetchError`, always carrying a numeric `.status`) when `/get-session` reaches the server — this covers both a genuine no-session answer (`error: null`) and any HTTP-level failure — and sets `data: null` with an *unstructured* error (whatever the fetch layer itself threw; never a `BetterFetchError`, no numeric `.status`) only when the request never reaches the server at all. That is exactly the offline case, and it is distinguishable from a confirmed no-session answer by checking whether `error` carries a numeric `.status`. The sync engine already makes this same split for the same reason (`engine.ts`'s `failure.kind === "network"` vs `"unauthenticated"`).
  - **Ruling.** Point 1 fires only on a confirmed answer: `data` null and `error` either `null` or carrying a numeric `.status`. An `error` present without one is indeterminate — the request never reached the server — and falls through to point 4 instead of sign-in. Point 4 is extended to cover the case its wording didn't anticipate: when point 1 couldn't resolve at all, there is no `user.id` to filter cached workspaces by, so enumerate every `vulto:<workspaceId>:<userId>` cache on the device unfiltered; the same one-cache-mounts, more-than-one-requires-online rule applies regardless of whether "more than one" means two workspaces for one person or two people sharing the device. `F218` closed.
  - **The Stage 9 section in Part 3 is updated in place** with these changes to points 1 and 4, plus matching updates to Done criteria and Gates, rather than kept as a separate amendment to reconcile later — read Part 3 directly, not this bullet, when building.
  - **Branch state.** `stage-9-shell-bootstrap-v2` is pushed and holds only the F218 discovery and blocked report — no bootstrap code. Start Stage 9 from a fresh branch off `main` again (a fourth name, e.g. `stage-9-shell-bootstrap-v3`).
  - **Stop after that.** Wait for "Stage 9 go" (the real shell bootstrap, now covering F216, F217 and F218) before starting either Stage 9 or Stage 10.
- **22 September 2026 — F219 ruled; Stage 9 continues on its existing branch, not a new one.** A third Stage 9 attempt (Codex, `stage-9-shell-bootstrap-v3`, FDN-117, in progress) implemented the F216/F217/F218-corrected brief and, for the first time, actually exercised it in a real browser — and stopped correctly on a gap only real execution surfaces: point 3's `organizationClient()` design calls Better Auth's `organization.list()`/`listMembers()`, and `services/api/src/auth/http.ts` returns 404 for every `/api/auth/organization/*` path, reads included, by deliberate design (FDN-85/FDN-86's "no second write path" boundary, stated in the route's own comment). A real single-membership browser test reached the 404 and stayed stuck in the unresolved holding state that's supposed to be point-3's unambiguous case.
  - **Ruling.** No `/api/auth/organization/*` route opens, for reads or writes — that boundary is correct as built. One new route instead: `POST /workspace/list-active-memberships` in `services/api/src/auth/http.ts`, session-gated with no workspace scope (mirrors the existing `/devices/retire`), returning `{ workspaceId, workspaceName }` for the caller's `active`/`confirmed` memberships only — the exact predicate `requireCurrentWorkspaceSession` and `confirmWorkspaceAdmission` already use, so nothing new is invented, and Better Auth's own `member.status` values (`pending`/`active`/`revoked` sit directly on the row the closed plugin would have read) are never surfaced unfiltered. `F219` closed. `VPS-F001`'s "Workspace admission control" section gets one new bullet recording the boundary and the one exception, so the route's code comment isn't the only place this is written down.
  - **The Stage 9 section in Part 3 is updated in place** — point 3 replaces `organizationClient()` with the new route — rather than kept as a separate amendment to reconcile later.
  - **Branch state — do not restart.** Unlike F216/F217/F218, this finding does not invalidate the branch it was found on. `stage-9-shell-bootstrap-v3`'s preliminary work — the F218 `hasServerAnswer` split, the offline cached-workspace fallback, the graph client construction, the refusal-signal wiring in `Shell.tsx`/`layout.tsx`, the `packages/graph/src/sync-client/*` changes — is sound and was read directly, not just reported. Only `auth-client.ts`'s `organizationClient()` import and the preliminary `soleActiveWorkspace` function's Better-Auth calls need replacing with a call to the new route. Continue on `stage-9-shell-bootstrap-v3`, `FDN-117` stays In Progress.
  - **Stop after that.** Wait for "Stage 9 go" (the real shell bootstrap, now covering F216, F217, F218 and F219) before merging Stage 9 or starting Stage 10.
- **22 September 2026 — F220 ruled; Stage 9 continues on the same branch a second time.** Still on `stage-9-shell-bootstrap-v3`, FDN-117, after applying the F219 fix (its route works, server test 45/45), the browser test that was failing on F219 still failed: the resolved workspace name never rendered. F219's route correctly names the sole active workspace, but nothing made that name the server's actual active-session claim — `/devices/register` still rejected the workspace header with 403, `trpc.ts`'s context builder still returned `principal: null`, both because `session.activeOrganizationId` was still `null`. Checked directly: `confirmWorkspaceAdmission` (F217's fix) is the *only* production write to that field for the ordinary case, and it fires once, at confirmation — it cannot reach a session created afterward. A fresh sign-in on a new device or browser, for someone already an active confirmed member, gets a brand-new session row at the column's `null` default, and nothing before this ruling ever gave it a way forward. This is not a rare edge case — it is the ordinary state of every fresh sign-in for an already-confirmed single-workspace person.
  - **Ruling.** A second new route, `POST /workspace/activate-sole-membership`, beside F219's route in `services/api/src/auth/http.ts`: session-gated, no request body, trusts nothing the client asserts — it re-derives the caller's active/confirmed memberships itself. Exactly one → the identical write `confirmWorkspaceAdmission` already performs (`update(session).set({ activeOrganizationId })`, filtered by that user's id, covering every session row they hold), returning `{ workspaceId }`. Zero or more than one → writes nothing, returns `{ workspaceId: null }` — the same "unresolved" state point 3 already defines, not a new error shape. It only ever writes from `null`; an already-set `activeOrganizationId` is never its target, so this cannot become a hidden workspace-switcher. `F220` closed. `VPS-F001` gets one more clause naming both F219's and F220's routes together as the session's active-organization boundary.
  - **The Stage 9 section in Part 3 is updated in place** — point 3 now calls both new routes in sequence before constructing the graph client — rather than kept as a separate amendment to reconcile later.
  - **Branch state — still no restart.** Same as F219: `stage-9-shell-bootstrap-v3`'s preliminary work (the F218 split, offline fallback, graph client construction, refusal-signal wiring, sync-client changes, and now the F219 list route with its passing test) is sound and untouched by this finding. Only the missing activation call and its route need adding. Continue on `stage-9-shell-bootstrap-v3`, `FDN-117` stays In Progress.
  - **Stop after that.** Wait for "Stage 9 go" (the real shell bootstrap, now covering F216 through F220) before merging Stage 9 or starting Stage 10.
- **22 September 2026 — Stage 9 completion report reviewed and approved for merge.** `stage-9-shell-bootstrap-v3` at `08e2873` (FDN-117, In Review) was read directly against the code, not just the report. Both new routes (F219, F220), `auth-client.ts`, `shell-bootstrap.tsx`'s full boot sequence, the `packages/graph/src/sync-client` refusal-signal wiring (`engine.ts`, `host.ts`, `client.ts`, `protocol.ts`, `status.ts`), `Shell.tsx`/`(shell)/layout.tsx`'s final diffs, all eight browser tests in `shell-bootstrap.spec.ts`, and the key `auth.integration.test.ts` tests were each read in full and match the ruled contract exactly; the two new routes exceed the minimal requirement (transactional, row-locked, belt-and-suspenders `isNull` guard). The non-doc diff is 13 files with no fixture screen among them. The findings recount was independently reproduced against this branch's own `docs/Foundations_Findings.md`: 166 rows, F54–F220, one gap (F198), 152 closed, 8 open, 6 recorded — an exact match. No discrepancy found. **Verdict: approved for merge, no further findings.**
  - **Merge.** `--no-ff` merge `stage-9-shell-bootstrap-v3` into `main`, push both. Move `FDN-117` to Done. Update `docs/Reviewer_Handoff.md`'s Stage status table (section 5) with the merge commit.
  - **Proceed straight into Stage 10** — already briefed below, and in `Reviewer_Handoff.md` section 10 — on the merged `main`. No further "go" is needed for Stage 10 specifically; it was unblocked the moment Stage 9 was approved.
  - **Full brief in Part 3 below reflects the final, F220-updated Stage 9 contract** — read it directly if anything here is ambiguous, don't reconstruct it from this amendment.
- **22 September 2026 — Stage 9 merged (`5a0e9ee`); Stage 10 completion report reviewed and approved for merge.** `stage-10-reconnect-shell` at `cf59ed5` (FDN-114, In Review) was read directly against the code: `ReconnectState.tsx`'s copy matches `VPS-D004` byte-for-byte; `Shell.tsx` gates the whole tree on `state.refusal`; `shell-bootstrap.tsx`'s Retry re-runs the full Stage 9 bootstrap and correctly guards against an unrelated session auto-refresh redirecting to sign-in mid-Reconnect, and against a refetch/attempt race stranding the shell on its holding copy; `host.ts`'s new `notifyOnline()` call on re-init correctly restarts an `unauthorized` source that `engine.ts` deliberately gives no restart timer, distinct from the existing `access-revoked` teardown path. All eight browser tests in `reconnect.spec.ts` were read in full, including a literal byte-for-byte `outerHTML` comparison proving cold-start and mid-session render identically. Exactly 8 files touched, no fixture screen among them, no finding or spec correction needed. **Verdict: approved for merge, no further findings.**
  - **Merge.** `--no-ff` merge `stage-10-reconnect-shell` into `main`, push both. Move `FDN-114` to Done. Update `docs/Reviewer_Handoff.md`'s Stage status table (section 5) with the merge commit.
  - **Housekeeping, now that Codex has full permissions on this machine:** delete the two stray, locked worktrees left over from earlier reviewer sessions (`.wt-stage9-check`, `.wt-f218-main`, both at the repo root) with `git worktree remove --force` for each, followed by `git worktree prune`; and remove the untracked leftover file `docs/stage-reports/STAGE-9_Reconnect_Shell.md` (from an old, abandoned Stage 9 attempt, not part of any branch's current history) with `git clean -fd -- docs/stage-reports/STAGE-9_Reconnect_Shell.md`. Confirm `git status --short` is clean afterward and mention in the next report if anything unexpected turned up.
  - **Stop after that.** Both Stage 9 and Stage 10 are done. The next brief (the FDN-104 spec sweep's next feature, per `Reviewer_Handoff.md` section 11 — VRS-F003 or VRS-F004 in the prior MVP order) waits for the founder's go, same as every stage boundary.
- **22 September 2026 — Stage 11 briefed: Multi-Entity and Jurisdiction Foundation (`VRS-F003`), F221 and F222 ruled.** `VRS-001`'s Feature Register settles the MVP-order question left open above: `VRS-F003` sits at position three, directly after `VRS-F002` (RST-33, built), and before `VRS-F004`. Checked directly against the code before writing this brief: `Entity`, `scoped_to_entity` and Entity's ownership row are already fully registered (`packages/schema/src/registry/{nodes,edges,ownership}.ts`) — nothing schema-level is missing — but no mutation, no permission-matrix row, and no bootstrap path exists anywhere; `VRS-F002`'s own `employeeCreate` already requires a valid `entity_id` and would reject on a workspace with none. Two findings surfaced while confirming the brief and are both ruled the same day (full reasoning in `docs/Foundations_Findings.md`; `VRS-F003`'s own text corrected to match): **F221** — Entity transfer is not a `VPS-F004`-audited event; `scoped_to_entity`'s own edge history already records every transfer, actor and timing, and `VPS-F004`'s closed, access-focused `event_type` taxonomy is not extended for it. **F222** — no code path creates a workspace's first Entity (`VPS-F006`, the spec's stated source, is not built); `services/api/src/graph/founding.ts`'s `writeFoundingRecords` gains a sixth founding record — a default Entity named after the workspace, `jurisdiction: "Global"`, `default_currency: "USD"` — so G04 holds from workspace creation onward without waiting on `VPS-F006`.
  - **Full brief below (Part 3, new Stage 11 section).** Server-side only, following Stage 8's own precedent exactly — Stage 8 (`VRS-F002`) touched no `apps/` code, and this stage doesn't either. The Entities screen (`VPS-F005`'s host) and the entity selector on the Employee profile both wait for the fixture-migration work already on the follow-up list, rather than being built piecemeal ahead of it.
  - **File a Linear issue for this stage** before starting, same convention as every prior stage (`RST-39` is the next number as of this writing; confirm before filing in case another issue landed first).
  - **Stop after that.** Wait for the founder's go before starting Stage 11.
- **22 September 2026 — Stage 11 correctly stopped once before any code, on F223; now ruled and unblocked.** Tracing the brief before implementation (`RST-39`), the builder found that `entity.deactivate(entityId)`'s stated contract left two things undefined: which field deactivation changes (`VRS-F003` listed both `is_active` and `lifecycle_status: Active | Dissolved`) and how it carries the caller's base version, which `VPS-A003` A003-T54 requires of every state-transition mutation. That is a real gap — `VRS-F003` never resolved it — and stopping before writing code was correct. **F223, ruled and closed the same day:** `is_active` is dropped from Entity's schema (a `featureOwnedLifecycle`-convention leftover; Entity is `fixedLifecycle`, like `VRS-F004`'s WorkingCalendar/Holiday/WorkingPattern, which carry `lifecycle_status` alone). Deactivation transitions `lifecycle_status` from `Active` to `Dissolved`. `Entity` joins `packages/schema/src/mutations/employee.ts`'s `FEATURE_LIFECYCLE_NODE_TYPES` alongside `Employee`, so the generic `graph.transitionLifecycle` correctly refuses it; `entity.deactivate`'s corrected signature is `entity.deactivate(entityId, expectedVersion)`, checking the version first (`stale-state`, A003-T54) and only then G04/G05 — exactly `employee.transitionStatus`'s own pattern. Full reasoning in `docs/Foundations_Findings.md`; `VRS-F003` and the Stage 11 section below are both corrected to match.
  - **Resume Stage 11** on the corrected brief below. No other part of the brief changes.
- **22 September 2026 — Stage 11 stopped a second time before optimistic mutators, on F224; now ruled and unblocked.** Implementing the brief, the builder found that the four new mutations live in the shared `packages/schema/src/mutations/foundation.ts` registry `packages/graph` also consumes, and `OPTIMISTIC_MUTATORS`'s exhaustive type requires a client handler for every registered name — `pnpm verify` fails typecheck without one for each. `employee.setEntity`'s handler needs to find and close the device's currently-open `scoped_to_entity` edge, which the builder read as a second direct traversal G06 forbids outside `entity.resolveForEmployee`. That is a real, if narrower, gap — the brief specified the server-side resolver but never the client optimistic halves. **F224, ruled and closed the same day:** G06 protects `entity.resolveForEmployee`'s *temporal* (`asOf`-based) resolution only; it does not reach the mechanical, non-temporal "find the one currently-open edge" lookup a single-active-edge-with-history mutation's own optimistic half must perform on its own state — already precedented by `org.moveEmployee`'s existing optimistic mutator for `managed_by`, with no comparable restriction. `entity.create`/`entity.update` get ordinary node-write optimistic handlers; `employee.setEntity`'s handler closes the open edge and reopens a new one via the already-shared `moveEmployeeEdgeId`; `entity.deactivate`'s handler checks only `expected_version` and leaves G04/G05's workspace-wide counts to the server, relying on the sync outbox's existing rejection/undo path rather than replicating them client-side. Full reasoning in `docs/Foundations_Findings.md`; `VRS-F003`'s G06 and the Stage 11 section below are both corrected to match.
  - **Resume Stage 11** on the corrected brief below (new Do item 7). No other part of the brief changes.
- **22 September 2026 — Stage 11 completion report reviewed and approved for merge.** `codex/stage-11-multi-entity-jurisdiction` at `fb4ef35` (RST-39, In Review) was read directly against the code: all four optimistic mutators match the F224 ruling exactly (`entity.create`/`entity.update` as ordinary node writes; `employee.setEntity` closing only the currently-open `scoped_to_entity` edge via `moveEmployeeEdgeId`; `entity.deactivate` checking only `expected_version`, no G04/G05 logic); the server-side `entity.deactivate` validates in the ruled order — stale version, then G04, then G05 — under `serializable` isolation; `founding.ts` writes the sixth founding record inside the existing transaction; `policy-table.ts`'s `MATRIX_OVERRIDES.Entity` matches the workspace-configuration pattern exactly; and the full test suite (the rewritten `foundation.test.ts` and `engine.test.ts` cases, and all seven `entity.integration.test.ts` cases, including both G04/G05 refusals and the UK→Pakistan transfer) was read in full, not sampled. No conflict markers, no stub handler, no stray `is_active` reference, no file under `apps/`. **Verdict: approved for merge, no further findings.**
  - **Merge.** `--no-ff` merge `codex/stage-11-multi-entity-jurisdiction` into `main`, push both. Move `RST-39` to Done. Update `docs/Reviewer_Handoff.md`'s Stage status table (section 5) with the merge commit, same as the Stage 10 precedent (`88ab14f`).
  - **Stop after that.** The next brief (per `Reviewer_Handoff.md` section 12's MVP-order note — `VRS-F004`, Working Calendar) waits for the founder's go, same as every stage boundary.
- **22 September 2026 — Stage 11 merged; Stage 12 briefed: Working Calendar and Working Patterns (`VRS-F004`).** Codex merged `codex/stage-11-multi-entity-jurisdiction` `--no-ff` as `96d53eb`, pushed both branches, moved `RST-39` to Done, and recorded the merge commit in `docs/Reviewer_Handoff.md`'s Stage status table (`e0c05e4`) — all independently confirmed directly against `main`, `origin/main` and the merge commit's own diff (no conflict markers; the merge carries the approval-commit's own two files forward untouched). `VRS-001`'s Feature Register confirms `VRS-F004` as the correct next MVP-order feature after `VRS-F003`. Checked directly against the code before writing this brief: `WorkingCalendar`, `Holiday`, `WorkingPattern`, their ownership rows and their edges are already fully registered in `packages/schema/src/registry/{nodes,edges,ownership}.ts` — nothing schema-level is missing — but no mutation, no permission-matrix row, and no `workingDays.*` API exists anywhere. Two findings surfaced while confirming the brief and are both ruled the same day (full reasoning in `docs/Foundations_Findings.md`; `VRS-F004` corrected to match): **F225** — Holiday (and `ReducedHoursPeriod`) confirmation is not actually `VPS-F004`-audited; its closed, access-focused `event_type` taxonomy has no category for an ordinary business write, the exact gap F221 already found for Entity transfer — each node's own `confirmed_at`/`confirmed_by` fields are the record instead. **F226** — the spec's G06/G07 materialized working-day index (client-side, Worker-rebuilt, sized entirely against `VRS-F005`'s Bench Forecast render budget) predates the September 2026 server-authoritative pivot and has no caller yet, since `VRS-F005` is not built; Stage 12 computes `workingDays.*` directly against the graph, unindexed, and the index itself is deferred in full to whichever stage builds `VRS-F005`.
  - **Build Stage 12** on the brief above. Branch `codex/stage-12-working-calendar`. `RST-40` already exists in Linear (Backlog) — move it In Progress and log findings there the same way `RST-39` was used for Stage 11.
  - **Stop after the report and Linear update.** The next brief waits for the founder's go, same as every stage boundary.
- **22 September 2026 — Stage 12 stopped before implementation, on F227 through F231; now ruled and unblocked.** Tracing the corrected brief before writing any code, Codex found five contract gaps the brief left open and stopped correctly rather than choosing behavior in code (`fcfe213`, `RST-40` moved to Blocked): **F227** — no initial working week is named for `Global`, `IN` or `SG`, though every Entity (including Stage 11's founding `Global` Entity) requires an Active `WorkingCalendar` at creation. **F228** — `calendar.update`, `holiday.confirm`/`cancel` and `pattern.set`/`clear` carry no caller base version, though `VPS-A003` A003-T54 requires one for a state transition, per F223's own precedent. **F229** — the stated `pattern.set` overlap check rejects the very replacement it exists to allow, since every valid replacement date falls inside the open range the same call is about to close. **F230** — neither the spec nor the brief says whether a calendar's holidays carry forward when `calendar.update` supersedes it. **F231** — G07's PayRun/LeaveRequest/Contract recalculation exemptions were wrongly said (F226) to be provable in Stage 12; none of those three features exists yet to prove an exemption against. All five ruled the same day, founder-delegated, full reasoning in `docs/Foundations_Findings.md`; this correction also narrows F226's own overreach. `VRS-F004` is corrected to match on all five points:
    - **F227.** `Global`, `IN` and `SG` all default to Monday-Friday, `standard_daily_hours: 8` (the plurality template already used by UK/US/AE), written atomically with the Entity by a new shared `createInitialCalendar` helper, used by both `entity.create` and Stage 11's founding bootstrap. Editable afterward like every other founding default (F222).
    - **F228.** A003-T54 applies to a `lifecycle_status` transition, not every field write (F223's own reading, reapplied). `calendar.update`, `holiday.cancel`, `pattern.set` (only when superseding an existing pattern) and `pattern.clear` each require `expectedVersion` of the row being closed, checked first. `holiday.add` (create) and `holiday.confirm` (an ordinary field update — a Holiday's `lifecycle_status` never changes on confirmation) carry none, matching `entity.create`/`entity.update`.
    - **F229.** `WorkingPattern` reuses the `closeAt` shared-boundary convention Stage 11 built for `scoped_to_entity`: superseding a pattern sets its `effective_to` to the new pattern's own `effective_from`, never the day before. The one real overlap guard is that the new `effective_from` may not precede the pattern it supersedes — an earlier date overlaps already-closed history instead and is refused.
    - **F230.** `calendar.update` copies every still-Active Holiday of the superseded version onto the new one (fresh `holiday_in` edge, never a Canceled holiday), in the same transaction as the calendar write. The old version's own Holiday nodes are untouched, so a historical `calendar.get(asOf)` resolves the holiday set that was true then. `holiday.confirm`/`cancel` refuse a holiday on a `Superseded` calendar version.
    - **F231, correcting F226.** Stage 12 proves only G07's live-view default — `workingDays.*` returns newly-confirmed values on its next call, nothing cached. The PayRun/LeaveRequest/Contract exemptions are recorded as binding rules for whichever stage later builds each of `VRS-F062`, leave and `VRS-F020`; they are not Stage 12's to build or test.
  - **Resume Stage 12** on the corrected brief below (revised Do items 1-4 and 7, Done criteria and report checklist). No other part of the brief changes.
- **22 September 2026 — Stage 12 stopped a third time before implementation, on F232 through F234; now ruled and unblocked.** Resuming Stage 12 on the F227-F231 rulings, Codex traced the corrected brief into the code and spec before writing any and found three more gaps, stopping correctly a third time (`bc21a52`, `RST-40` moved to Blocked again): **F232** - `VRS-F004`'s own setup-flow prose named AE's initial template as Monday-to-Friday, but the feature's own first acceptance criterion requires an AE entity's Friday to be excluded as non-working - the two cannot both be true of the same template. **F233** - F227 made `entity.create` write a calendar and ownership edge atomically on the server, but nobody extended `entity.create`'s existing optimistic mutator to match, and no portable deterministic ID or shared template-selection logic existed for the client half to use. **F234** - `ReducedHoursPeriod` is inline WorkingCalendar data the required UAE Ramadan test depends on, but no named mutation (including the corrected `calendar.update`) accepts, edits or confirms one, so no product path can create the state the test needs. All three ruled the same day, founder-delegated, full reasoning in `docs/Foundations_Findings.md`; this round also corrects a piece of F227's own already-closed rationale that AE's move broke:
    - **F232.** AE's named template is corrected from Monday-to-Friday to Sunday-to-Thursday, matching Saudi Arabia's own template and this document's own framing of a historically Friday/Saturday Gulf weekend. The acceptance criterion was right; the template line was the error.
    - **F233.** `entity.create`'s optimistic handler now creates the calendar and ownership edge too. Two new deterministic ID derivations join `moveEmployeeEdgeId` in `packages/schema/src/mutations/foundation.ts` (same one-to-one bit-flip shape, distinct constants), and the jurisdiction-template table `createInitialCalendar` uses is extracted into one portable pure function both the server helper and the client's optimistic handler call, so they can never diverge.
    - **F234.** `reduced_hours_periods` is edited exclusively through a new optional `reducedHoursPeriods` parameter on `calendar.update` - omitted, it carries forward unchanged (the same convention F230 gave Holiday); provided, it replaces the array outright. No new node, no separate confirmation mutation.
  - **Resume Stage 12** on the corrected brief below (Do items 1, 2 and 7 revised again; Done criteria and report checklist extended). No other part of the brief changes.
- **23 September 2026 — Stage 12 stopped a fourth time, this time inside real implementation, on F235; now ruled and unblocked.** Resuming on the F232-F234 rulings, Codex built the calendar/pattern mutation module and reached the first real `pattern.set` integration test against Postgres, which failed: `pattern_for` connects `WorkingPattern` to split-protection `Employee` (`operational`/`compensation` partitions) with no `governingPartitions` entry in the edge registry, so F136's conservative rule refuses the edge for every role before any Full-write check runs. This is the same class of gap F208 found for `managed_by`/`scoped_to_entity` — the Stage 12 brief's "already complete... none of that needs touching" claim about the four working-calendar edges was wrong on this one point. Verified directly against `packages/schema/src/registry/edges.ts`, the node registry (`WorkingPattern` is single-partition Tier 0 Standard), and the interceptor's `edgeRoleDecision` (`services/api/src/permission/interceptor.ts`). Recorded as F235 (`e4a9599`), `RST-40` moved to Blocked, partial implementation preserved on `codex/stage-12-working-calendar` at `7e64708` (schema and graph tests passing, API typechecking clean).
  - **Ruled.** `governingPartitions: { Employee: "operational" }` is declared on `pattern_for`, following F208 exactly: which working pattern applies to an employee is operational scheduling data, not compensation. Recorded in `VPS-A002`'s `governingPartitions` table (already updated on `main`, above `membership_in`'s row).
  - **Do not restart the branch.** Unlike the two prior rounds, `codex/stage-12-working-calendar` now carries real, tested implementation (`7e64708`) that must be kept. Pull the new `main`, then bring the four ruled docs (`docs/Foundations_Findings.md`, `docs/Vulto_Specs/VPS-A002_Master_Graph_Schema_Definition.md`, `docs/Claude_Code_Build_Prompt.md`, `docs/Reviewer_Handoff.md`) onto the branch from `main` as-is — they are reviewer-authoritative and fully supersede the branch's own F235-open findings-doc edit — while keeping every code file exactly as `7e64708` left it. A plain merge will conflict only on `docs/Foundations_Findings.md`'s F235 region; resolve by taking `main`'s version of that file wholesale.
  - **Resume Stage 12** on the corrected brief below (Do item 4 revised, Done criteria and report checklist extended once more). No other part of the brief changes.

---

## Part 3 — The stages

### Stages 1–22 — archived

Stages 1 to 22 are merged and their briefs are preserved verbatim in `docs/archive/Build_Prompt_Stages_1-22.md`. Only the most recent stages are kept below.

### Stage 23 — Local-First Search (`VPS-F002`)

`VRS-F015` is permanently vacant (promoted to the suite as `VPS-F002`, per `VRS-001`'s "Promoted to the suite" table), and `VRS-001`'s MVP order runs `VRS-F014` → `VPS-F002`. This stage builds the local half of the `Cmd+K` palette's data layer: a trigger-maintained search index inside the device cache, `search.query` as a local query, and the command registry and matcher. It changes no server code: no file under `apps/` or `services/api/` changes, no new mutation, no new policy row, no new system principal, no router procedure. (One test-only exception, F302/F304: an optional parameter on `addNode` in `services/api/src/permission/test-support.ts`, see Do item 8.) The palette UI (`VPS-D002`'s Command Palette component, the keystroke, keyboard navigation, the Panel view, the group layout) is reference material for whichever later stage wires it into the shell, the same deferral every prior Interface Specification has received; `apps/roster-web/src/fixtures/command-palette.ts` is prototype data and is not touched.

**Findings F297 through F300, closed before this brief was written:**

- **F297** — The spec's index is SQLite FTS5 virtual tables, but the pinned `wa-sqlite@1.0.0` (both shipped builds) has no FTS module at all (`no such module: fts5`; `fts3`/`fts4` fail identically; `json_extract` and `LIKE` work). Genuine architectural fork, decided by the founder: a trigger-maintained derived table `cache_search` inside the existing cache, queried by prefix and substring; no dependency change. See "Do" items 1 and 2.
- **F298** — `search.query` cannot return `skillMatcher.adHocSearch`'s output locally; `adHocSearch` has been a server read since F286. Ruled on F286's and F199's precedent: `search.query` returns `skillMatched: boolean` instead of `skillMatches`; the palette calls the existing server `adHocSearch` unmodified when it is true; offline, that group alone degrades to `requires-connection`. No server change.
- **F299** — Only `Employee`, `Skill`, `Project` and `Client` can be registered now. `Document` (inherited protection, no `file_name` schema), `Policy` and `Deliverable` (no `title` schema) are forward dependencies. `search.queryProtected` is deferred until a built node type has a Tier 1–3 searchable name (owned by whichever of `VRS-F020`/`VRS-F022`/`VRS-F023` builds it first). Ruled on F296's and F248's forward-dependency precedent and the spec's own G05.
- **F300** — The device's audience-filtered cache is the local filter (Stage 22's own G03 ruling; no second check reimplemented client-side); commands are a static registry with no role gating this stage, the server authorizing whatever a command executes.

**Finding F301, raised by the builder tracing this brief before writing product code, ruled the same day, no founder consultation needed:**

- **F301** — This brief originally said `verify:full` already runs a device-store browser suite. It does not: `verify:full` is `pnpm verify && pnpm verify:preflight && pnpm --filter @vulto/api test`. The device-store browser suite is the separate `pnpm test:sync-browser`, run by `.github/workflows/slow-lane.yml` as its own `sync-browser` job. Ruled: it is an additional required gate for this stage, because `cache_search`'s triggers will run in the real IndexedDB-VFS database that only this suite exercises. See Do item 8 and Gates.
- **F302** — Resuming on F301, the builder found Do item 8 named a fixture the suite never had: `seedProtected`'s Employee is created by `addNode` with universal fields only, no `full_name`. Tracing it also exposed that a trigger inserting `label` (`NOT NULL`) from an absent `full_name` would abort `putNode` and break replication of that row. Ruled: the triggers index a node only when its registered label field is a non-empty string (Do item 2); `seedProtected` gets a named Employee via one re-export line in `services/api/src/test/sync-browser-support.ts`, the single exception to the `services/api/` boundary; the assertions live in the existing role-removal test (Do item 8).
- **F303** — F302's guard (label from `full_name` only) excluded every real Ghost Employee: `ghostEmployeeOperationalRecord` deliberately sets `full_name: null` and puts the role title in `job_title` (`VRS-F007`), yet this brief requires finding Ghosts by role title. Ruled: the registry's `labelField` becomes an ordered `labelFields` list (`Employee`: `full_name`, then `job_title`), the label is the first non-empty text, `VRS-F007`'s Ghost identity fields do not change, and tests build the Ghost with `ghostEmployeeOperationalRecord`. Also: `operationalShape` is not exported from `employee.ts` and must be (or a key list derived from it) for the registry's Tier 0 validation (Do item 1).
- **F304** — F302's authorized re-export of `nodeRecord`/`insertNode` from `services/api/src/test/sync-browser-support.ts` fails `pnpm verify`: `scripts/arch-check.mjs` (A003-T52) forbids importing or re-exporting `graph/store` outside the graph, permission, mutations, protected, audience and jobs folders. Ruled: `addNode` in `services/api/src/permission/test-support.ts` (an allowed folder, and the file the gate treats as test support) gains an optional `fields` parameter; `sync-browser-support.ts` is restored to `main`'s version; `seedProtected` calls `addNode(tx, workspaceId, "Employee", {...})`; no gate changes. This reverses F302's "do not change `addNode`" clause, which was written to avoid a wider effect a defaulted parameter does not have.
- **F305** — The full `pnpm test:sync-browser` ended 18 passed, 9 failed, none of them Stage 23's: `seedEntities` and `newEntity` create an `Entity` through `graph.createNode`, which has rejected that type with `requires-feature-mutation` since Stage 11 (`82c0b31`, 22 September), and the suite has not been repaired since Stage 10 — so F301's "full suite passes" gate was unsatisfiable, and the A003-T56 no-protected-data-on-device test has not run green for eleven stages. Ruled (open finding, repair deferred to its own Linear issue): the gate becomes a baseline comparison against `main`, and the passing role-removal test gains a real-browser sentinel check for `cache_search`. See Gates and Do item 8(d).

`docs/Vulto_Specs/VPS-F002_Local-First_Search.md` is corrected to match (Technical Architecture, the registry table note, Permission filtering, Skill delegation, the API contract, G01/G02/G03/G05, S01, the offline criterion, the NFR, and four Decisions Recorded entries). See each finding's own detail section in `docs/Foundations_Findings.md` for the full reasoning.

1. **The searchable-type registry.** New file `packages/schema/src/search.ts`, exported from `packages/schema/src/index.ts`: `SEARCHABLE_NODE_TYPES`, a readonly list of `{ nodeType, labelFields, indexedFields, secondaryField? }` (`labelFields` is an ordered, non-empty list of indexed fields, each provably Tier 0; F303), registering exactly: `Employee` (`labelFields: ["full_name", "job_title"]`, `indexedFields: ["full_name", "preferred_name", "job_title"]`, `secondaryField: "job_title"`), `Skill` (`labelFields: ["name"]`; `["name", "category"]`; `secondaryField: "category"`), `Project` (`labelFields: ["name"]`; `["name"]`) and `Client` (`labelFields: ["name"]`; `["name"]`). Registration must validate, at module load and in a conformance test, that (a) every `nodeType` exists in the node registry (`packages/schema/src/registry/nodes.ts`); (b) the type is fixed Tier 0 or, for a tier-split type (Employee), every indexed field belongs to that type's own Tier 0 partition schema (`operationalShape` in `packages/schema/src/employee.ts`, a module-private constant today — export it, or a key list derived from it, from that file; that is in scope, F303) — a field that cannot be shown to be Tier 0 makes registration throw, so G02 is a property of registration, not of query-time filtering; (c) `AuditEntry` is refused by name (G08); and (d) every field name matches `/^[a-z_]+$/`, because the trigger SQL below is generated from these constants. Also export the registry's serialized form as a fingerprint constant used by item 2's version-pin test. `Document`, `Policy` and `Deliverable` are not registered (F299) — state that in the module's own comment so a later stage adds them by adding one entry, not by editing this stage's mechanism.
2. **The index table and its triggers.** In `packages/graph/src/sync-client/schema.ts`, add `cache_search` to `CREATE_REPLICATED_SCHEMA`: `node_id TEXT PRIMARY KEY, node_type TEXT NOT NULL, label TEXT NOT NULL, lifecycle_status TEXT NOT NULL, search_text TEXT NOT NULL`, plus an index on `node_type`. Add `cache_search` to `REPLICATED_TABLES` (so `prepareCacheSchema` drops and refills it with the rest on a version change) and bump `CACHE_SCHEMA_VERSION` from 2 to 3 (`database.ts::prepareCacheSchema` needs no logic change). Generate three triggers on `cache_nodes` from the registry (never hand-written per type): after insert, after update and after delete — on insert/update, delete the node's index row and re-insert it when the node's type is registered, `is_soft_deleted = 0` **and the node has a resolvable label** (F302, F303: `label` is the first of the registered `labelFields`, in order, for which `json_type(record_json, '$.<field>') = 'text'` and the extracted value `<> ''` — for `Employee`, `full_name`, else `job_title`, so a real Ghost, whose `full_name` is deliberately null and whose role title is its `job_title` (`ghostEmployeeOperationalRecord`, `VRS-F007`), is indexed under its role title; treat a non-text value in any other indexed field as absent the same way) — a node that fails the guard gets no index row, any existing row for it is deleted, and the trigger must have no path that can raise, because it would otherwise violate `label NOT NULL`, abort `putNode` and break replication of that row; a nameless node is unsearchable, never an error — with `search_text` = SQLite's own `lower()` of the node's registered indexed fields read through `json_extract(record_json, '$.<field>')`, `coalesce`d and joined by a single space, `label` = that resolved label; on delete, delete the index row. `SqliteCache.putNode` (`ON CONFLICT DO UPDATE`), `deleteNode` and `resetTemplate`'s bulk `DELETE FROM cache_nodes` all fire ordinary triggers — this was verified against a probe of exactly those statements, including soft-delete and hard-delete; prove it again in tests, do not assume it. No separate erasure work: `erasure.ts::eraseDatabases` deletes the whole per-workspace IndexedDB database, index included — state this in the module comment rather than adding an erase path. Because a registry change alters the generated SQL, add a test pinning the registry fingerprint (item 1) to an expected literal with a comment saying a change to either requires bumping `CACHE_SCHEMA_VERSION` — a forgotten bump must fail a test, not silently leave devices on stale triggers.
3. **`searchQuery`, the palette's data query.** New file `packages/graph/src/queries/search.ts` (exported from `queries/index.ts`), following `skill-matrix.ts`'s own shape: a plain async function taking the device's `SyncDatabase`, reading only local tables, no interceptor call, no server round trip (F300; G03). Contract: `searchQuery(database, { text, limit? }) -> { commandMatches, entityMatches, skillMatched }`. Normalize the text with SQLite's own `lower()` (bind the raw text as a parameter and apply `lower(?)` in SQL) — never JavaScript's `toLowerCase()` — so index and query can never disagree on a character; escape `%`, `_` and the escape character in the bound text and use an explicit `ESCAPE`, so a name containing `%` or `_`, and a query of `%`, match only literally. Blank or whitespace-only text returns all-empty results. A match is a substring of `search_text`. Rank each row: class 0 if its `label` starts with the text, class 1 if any word of `search_text` starts with the text, class 2 otherwise; within a class order `Active` before every other lifecycle status, then by label (binary order), then `node_id`, so the ordering is deterministic. `limit` applies per node type and defaults to 8 (this brief's own choice; record it as a micro-decision). Return `entityMatches` ordered by node type — `Employee`, `Skill`, `Project`, `Client` — then rank, each as `{ nodeType, nodeId, label, secondaryLabel, lifecycleStatus, isGhost?, availabilityStatus?, nextRolloffDate? }`: `secondaryLabel` is the registered `secondaryField` (`Employee`: `job_title`, or `null` when that same `job_title` is already the resolved label, as for a Ghost — F303; `Skill`: `category`) and, for a `Project`, the name of the Client its `belongs_to` edge points to (the lookup `bench-forecast.ts` already performs for `clientId`), else `null`. `isGhost` is `record["employee_type"] === "Ghost"` on an `Employee`-typed row — textually the same check `matchCandidates` and `getSkillMatrix` already use (`VRS-F007`/G06), never a check against the separately-registered `GhostResource` node type. Deprecated Skills and Inactive Employees stay findable (nothing is dropped; `lifecycleStatus` carries the state). `skillMatched` is true when at least one `Skill` row matches the text, computed from the complete match set before any per-type limit is applied (F298).
4. **The person context line's availability, from the one shared rule.** For `Employee` rows only, `availabilityStatus` (`"Assigned"` | `"Available"`) and `nextRolloffDate` (the earliest `end_date` among that employee's Active Assignments whose `end_date` is on or after today; `null` when none) come from a single helper factored out of `getSkillHolders` in `packages/graph/src/queries/skill-matrix.ts`, whose inline `coveredIds` computation (`assignmentCoversDate` over Assignment nodes, matched to an employee through `record["employee_id"]`) becomes the shared implementation — exactly the way Stage 22 factored `assignmentCoversDate` itself out of `bench-forecast.ts`. `getSkillHolders`'s behavior is preserved and `skill-matrix.test.ts` passes unchanged; there must be exactly one implementation of "is this employee covered by an Assignment today" in the repository, not a second one written for search, and no per-row call to `getBenchForecast` (a full bench computation per result row is not the cost this query should pay per keystroke). Load Assignment nodes once per call, and only when the result set contains at least one Employee. The formatted line ("Available Aug 19 · next rolloff Aug 18") is UI copy for the later shell stage; this query returns the structured fields.
5. **The command registry and matcher.** New file `packages/graph/src/queries/search-commands.ts`: `SEARCH_COMMANDS`, a static readonly list of `{ commandId, label, keywords, shortcut?, target }` where `target` is `{ kind: "navigate", destination }` or `{ kind: "action", actionId }`, using symbolic ids the shell resolves (no route strings from `apps/`, no import from `apps/`): navigation destinations `bench-forecast`, `people`, `timesheets` and `home` (`Go to Bench Forecast`, `Go to People`, `Go to Timesheets`, `Go to Home`), and action ids `employee.create` and `assignment.create` (`Create employee`, `Add assignment`) — the two the spec names and the prototype shell already models. Match by case-insensitive substring over `label` and `keywords` in JavaScript (commands are static; there is no index-side normalization to keep consistent), ordered by label, returning `[{ commandId, label, shortcut? }]` per the contract. No role gating and no execution: command execution (`VPS-F002-S05`) belongs to the shell, and every executed command is authorized by the server on the mutation or read it triggers (F300).
6. **The skill-delegation boundary, stated and tested — not built.** No server code, no wrapper. `searchQuery`'s module comment must state that the skill-match group is the palette's own call to the existing server `skillMatcher.adHocSearch`, unmodified, made only when `skillMatched` is true, arriving after the local results and degrading to `requires-connection` offline (F298), so a later UI stage does not invent a local skill-match path or a second implementation (G04).
7. **Tests.** New `packages/graph/src/queries/search.test.ts`, following `skill-matrix.test.ts`'s own precedent exactly: `SqliteCache`/`openTestDatabase`, no Postgres, no `services/api` involvement, `fetch` spied to reject so any network attempt fails the test. Cover: the spec's acceptance criterion at its own scale (150 employees and 50 projects; a three-character query returns type-grouped matches, the local index only — record the measured p95 for the query in the stage report and assert only a deliberately loose ceiling, 100ms, so an algorithmic regression fails without a timing flake); trigger maintenance for insert, rename (update), soft-delete, re-activation, hard delete (`deleteNode`) and `resetTemplate("nodes")`, each visible in the very next query (G01, the no-lag NFR); G02, proven both ways — a row carrying unregistered fields (`email`, `phone`) is never findable by them and the `cache_search.search_text` column is read directly to show only registered fields are present; registration validation — a non-Tier-0 node type, a field outside Employee's operational half, `AuditEntry`, an unknown node type and a field name that fails the pattern each throw; nameless, role-titled and malformed rows (F302, F303) — an Employee with neither `full_name` nor `job_title`, with empty-string values and with non-text values is written through `putNode` without error and yields no index row; an Employee with `full_name: null` and a `job_title` — the real Ghost shape, built with `ghostEmployeeOperationalRecord`, never an invented `full_name` — is indexed with `label` equal to its `job_title`, is found by that role title, returns `isGhost: true` and `secondaryLabel: null`, and a later `putNode` adding a real `full_name` re-labels it; wildcard and character safety — a name containing `%` and one containing `_` matched literally, a query of `%` matching nothing spuriously, and a non-ASCII (for example Urdu-script) name matching itself, proving index and query normalize identically; ranking — label-prefix before word-prefix before other substring, `Active` before `Inactive` within a class, ties broken deterministically, and the per-type `limit`; `skillMatched` true for a Skill-name match and false otherwise, including when the matching Skill row is beyond the per-type limit; a Ghost's role title returns `isGhost: true` through the `employee_type` check; the context line — an Active Assignment covering today reports `Assigned` with its `end_date` as `nextRolloffDate`, a Canceled Assignment covering the same dates reports `Available`, and a person with none reports `Available` with `nextRolloffDate: null` (proving the shared helper, with `skill-matrix.test.ts` unchanged and green); a `Project` result's client name; commands — `create emp` returns `create-employee` ahead of any entity result, a verb query returns commands and entities together, an unmatched query returns the same all-empty shape as a blank one (the empty-state rule leaves nothing to distinguish locally, since a restricted row is simply not on the device); and, in `packages/graph/src/sync-client/database.test.ts`, that `cache_search` is in `REPLICATED_TABLES`, that a database at cache version 2 has `cache_search` created with its triggers and cache tables dropped and refilled by replication, and that the fingerprint pin from item 2 holds.
8. **Assertions in the existing sync browser suite, and its one fixture change (F301, F302, F304).** (a) `packages/graph/sync-browser-tests/helpers.ts`: `seedProtected` creates its Employee with operational Tier 0 fields instead of a nameless one — replace `addNode(tx, workspaceId, "Employee")` with `addNode(tx, workspaceId, "Employee", { employee_type: "Employee", full_name: SEARCH_FIXTURE_EMPLOYEE_NAME, job_title: "Search Fixture Engineer" })`, keep the returned node id and leave the `writeProtected` and `recomputeWorkspace` calls unchanged, and export `SEARCH_FIXTURE_EMPLOYEE_NAME` (a fixed constant carrying no Tier 1 or Tier 2 sentinel text). Its two existing callers (the role-removal test and the A003-T56 test) are otherwise unaffected: it is still one Employee row and the seeded counts do not change. (b) The single test-only exception to this stage's `services/api/` boundary (F304): in `services/api/src/permission/test-support.ts`, `addNode(tx, workspaceId, nodeType)` gains an optional fourth parameter `fields`, default `{}`, merged beneath `nodeRecord`'s own fields (`insertNode(tx, { ...fields, ...record })`), so it cannot override `node_id`, `node_type`, `workspace_id` or the universal provenance fields; every existing three-argument caller is unchanged. That is the only `services/api/` file this stage changes. `services/api/src/test/sync-browser-support.ts` stays exactly as on `main` — it already re-exports `addNode` — so add no re-export of `nodeRecord` or `insertNode` there (if your branch already has them from an earlier reading of this item, remove them), import nothing from `graph/store` outside the folders `scripts/arch-check.mjs` allows, deep-import no API internals into `helpers.ts`, and change neither `arch-check.mjs` nor any gate: `pnpm verify` must pass on the diff. (c) Assertions, in the existing test "removing the member's role removes the rows they may no longer read from the cache" (it already uses `seedProtected` and `dump()` and exercises an Electric move-out): after the first `Synced`, and again after the role change and audience recompute, `expect.poll` (replication is eventual, and `dump()` reads tables one after another) until the `node_id` set of `dump()["cache_search"]` equals the `node_id` set of `cache_nodes` rows that are not soft-deleted, whose `node_type` is in `SEARCHABLE_NODE_TYPES` and that have a resolvable label (the first non-empty text among the registered `labelFields`, F303); and at the first point assert the seeded Employee's `search_text` contains `SEARCH_FIXTURE_EMPLOYEE_NAME` lowercased. Team Member keeps read of `Employee:operational` (F292), so the Employee is not itself the row that moves out; the second check proves the index stays consistent through the move-out deletes for whatever rows left, and you build no new scenario for it. `dump` reads every table in `sqlite_master`, so the existing "no Tier 1 or Tier 2 value reaches any browser storage" test would scan `cache_search` with no change — but that test calls `seedEntities` and cannot currently run (F305); do not weaken it, and do not attempt to repair it here. (d) Instead, add a real-browser sentinel check for the new table to the same role-removal test (F305): call `seedProtected` with unique Tier 1 and Tier 2 sentinel strings (for example `SENTINEL-tier1-${randomUUID()}`, not the current `"irrelevant"`, and containing no fixture name text), and after the first `Synced` assert that neither sentinel appears anywhere in `JSON.stringify(dump())`, `cache_search` included.

**Done criteria:**
- `cache_search` exists in `CREATE_REPLICATED_SCHEMA`, is in `REPLICATED_TABLES`, and `CACHE_SCHEMA_VERSION` is 3 (items 1, 2).
- The registry refuses any type or field it cannot show to be Tier 0, and `AuditEntry` by name; each refusal is a passing test (item 1, G02, G08).
- Insert, rename, soft-delete, re-activation, hard delete and `resetTemplate("nodes")` are each reflected in the index by the very next query, proven through the real `SqliteCache` write methods, not raw SQL (item 2, G01).
- Names containing `%` or `_`, a query of `%`, and a non-ASCII name all match exactly as literal text, with index and query normalized by SQLite's own `lower()` on both sides (item 3).
- A three-character query at 150 employees and 50 projects returns type-grouped results from the local index with no network call, and the measured p95 is reported (item 3, the spec's acceptance criterion).
- Results are ranked label-prefix, then word-prefix, then substring, `Active` before other statuses, deterministically, with `limit` applied per type (item 3).
- `skillMatched` is computed before any limit and `search.query` returns no `skillMatches` field; no code in this diff calls or wraps `skillMatcher.adHocSearch` (items 3, 6, F298, G04).
- A Ghost appears with `isGhost: true` through the identical `employee_type` check `matchCandidates` uses — no new Ghost-detection logic anywhere in the diff (item 3, G06).
- An Employee's `availabilityStatus` and `nextRolloffDate` come from one helper shared with `getSkillHolders`; `getSkillHolders`'s tests pass unchanged; the repository contains exactly one implementation of Assignment coverage for a date (item 4).
- Commands are a static registry with symbolic targets, matched by text, ordered above entity results by the caller; no role check, no execution, no import from `apps/` (item 5, F300).
- The registry fingerprint pin test fails if the registry changes without the cache-version constant being reviewed alongside it (item 2).
- `pnpm test:sync-browser` on this branch fails exactly the tests, for exactly the reasons, that it fails on `main` (the recorded baseline), and every other test passes — including the role-removal test with its `cache_search` set-equality, name and sentinel assertions, running against the triggers in the real IndexedDB-VFS database (item 8, F301, F305); the baseline and the comparison are both in the report by test name.
- A node with no resolvable label (no non-empty text in any registered `labelFields`) gets no index row and never makes a trigger fail, while a real Ghost (`full_name: null`, role title in `job_title`) is indexed under its role title; the suite's own former nameless Employee shape replicates without error (item 2, F302, F303).
- No file under `apps/` appears in the diff, and no file under `services/api/` other than `services/api/src/permission/test-support.ts`, changed only by `addNode`'s optional `fields` parameter (item 8, F302, F304); no new policy row, mutation, router procedure or system principal; `packages/graph/package.json` and the `wa-sqlite` pin are unchanged.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). `verify:full` does NOT run any browser suite (F301): the root script is `pnpm verify && pnpm verify:preflight && pnpm --filter @vulto/api test`. Because `cache_search`'s triggers now run inside the real IndexedDB-VFS device database, which only the sync browser suite exercises, this stage adds a fifth required gate: `pnpm test:sync-browser` — the full suite, not only the A003-T56 `--grep` — after `pnpm stack:up` (Electric is in `docker-compose.yml`) and `pnpm --filter @vulto/api db:migrate`, with the environment `.github/workflows/slow-lane.yml`'s `sync-browser` job uses (`SYNC_BROWSER_DATABASE_URL`, `ELECTRIC_URL=http://localhost:5133`, `ELECTRIC_SECRET`). Report the exact command and result. If your environment genuinely cannot run it (no Chromium or no Electric), say so plainly and record the pushed branch's slow-lane `sync-browser` job conclusion with its run URL instead; a silently skipped gate is not acceptable. **Baseline rule (F305): the full suite is red on `main`.** `seedEntities` and `newEntity` create an `Entity` through `graph.createNode`, which has rejected that type with `requires-feature-mutation` since Stage 11 (`82c0b31`), so a full pass is impossible and is NOT required. The gate is instead a baseline comparison, per Part 2's rule that a pre-existing failure is recorded as a baseline and must not get worse: (1) on a clean checkout or worktree of `main` at `11e404a` or later, run the same full command in the same environment and record the failing tests by exact name and failure message; (2) on this branch, run it again — the failing set must be identical by name and reason (the seed or mutate step's `requires-feature-mutation`), every other test must pass, including the role-removal test with all its added assertions, and a test that fails only on the branch or for a different reason is a Stage 23 regression you fix; the two-tabs failure is settled the same way, by showing it identical on `main`; (3) if you cannot run the baseline, say so and do not claim the gate. Do not change `seedEntities`, `newEntity`, `graph.createNode`, `FEATURE_LIFECYCLE_NODE_TYPES`, the suite configuration or any gate, and do not skip, tag or remove a test. No new browser suite is added — only item 8's assertions and fixture change. The Postgres-backed half of `verify:full` passes trivially untouched, since nothing in this stage's diff reaches `services/api/` — do not treat an empty diff there as a gap to fill.

**What to check in the Stage 23 report:** that the trigger SQL is generated from `SEARCHABLE_NODE_TYPES` and no per-type trigger is hand-written anywhere (F297, G05), and that every field name reaching the generator passes the `/^[a-z_]+$/` guard, so no runtime value is ever interpolated into SQL; that `cache_search` is in `REPLICATED_TABLES` and `CACHE_SCHEMA_VERSION` is 3, read directly off `schema.ts`, and that the fingerprint pin test is real (change the registry locally and confirm it fails); that the index is maintained through `putNode`/`deleteNode`/`resetTemplate` by triggers alone — grep the diff for any application-code write to `cache_search` — and that the tests drive those real methods rather than raw `INSERT`s; that normalization is SQLite's `lower()` on both sides, with no `toLowerCase()`/`toLocaleLowerCase()` on the entity-matching path, proven by a non-ASCII case, and that LIKE metacharacters are escaped with an explicit `ESCAPE` rather than stripped; that the registry's Tier 0 validation is a real check against the protection registry and Employee's operational schema, not a hardcoded allow-list that merely happens to agree today — read the negative tests; that `search.query`'s result has no `skillMatches` and `skillMatched` is computed from the pre-limit match set (F298); that the `employee_type === "Ghost"` check is textually the one `matchCandidates` uses; that `getSkillHolders` now calls the shared availability helper and `skill-matrix.test.ts` is byte-unchanged, and that `git grep assignmentCoversDate` shows no second coverage traversal; that no result row's availability came from a per-row `getBenchForecast` call; that the command registry imports nothing from `apps/`, has no role gating and no executor, and that `Document`/`Policy`/`Deliverable` and `search.queryProtected` were not built (F299); that the triggers resolve a label from the first non-empty text among the registered `labelFields`, that a nameless, empty-named or non-text-named Employee replicates without error, and that a real Ghost built with `ghostEmployeeOperationalRecord` is indexed under its role title, found by it and returned `isGhost: true` (F302, F303); that the measured p95 is reported with the fixture scale it was measured at; and that no file under `apps/` appears in the diff and the only file under `services/api/` in it is `services/api/src/permission/test-support.ts`, changed only by `addNode`'s optional `fields` parameter (default `{}`, unable to override identity or provenance fields, every existing caller unchanged), with `services/api/src/test/sync-browser-support.ts` unchanged and `pnpm arch:check` (and all of `pnpm verify`) passing with no gate or allowlist change (F304), `packages/graph/package.json` and the `wa-sqlite` pin are unchanged, and the `pnpm test:sync-browser` baseline from `main` and the branch comparison are both recorded by test name and failure message with the failing set identical and every other test passing (F305; or the slow-lane `sync-browser` job's conclusion and run URL where the suite cannot be run locally, per F301), including the added `cache_search` assertions, the sentinel check and the `seedProtected` fixture change (F302, F305).

---

### Stage 24 — Sync browser suite repair (`FDN-126`, F305)

A repair stage, not a feature: nothing in `apps/`, `services/api/`, `packages/schema/` or any workflow or gate changes, and no product code changes. `VPS-F003` (Notification and Alert Center), the register's next MVP-order feature, becomes Stage 25 once this merges.

**Why, checked directly against the code and CI before writing this brief.** The sync browser suite (`pnpm test:sync-browser`, `packages/graph/sync-browser-tests`) has been red since Stage 11. `seedEntities` and `newEntity` in `helpers.ts` create a node of type `Entity` through `graph.createNode`; Stage 11 (`82c0b31`, 22 September) put `Entity` in `FEATURE_LIFECYCLE_NODE_TYPES`, and both the server (`services/api/src/mutations/foundation.ts`) and the client's optimistic mutators (`packages/graph/src/mutators/foundation.ts`) refuse every type in that set with `requires-feature-mutation`. GitHub Actions confirms it: the slow lane's last green `main` run is `250a60a` (22 September 09:34 UTC), the first red is `e0c05e4` (Stage 11, 12:57 UTC, its `sync-browser` job failing at the step "Gate — no protected data on the device (A003-T56)"), and it has been red on every slow-lane push to `main` since, `publish-artifacts` skipped throughout. From about 24 September 20:34 UTC every job in both workflows also failed without running a step (the private-repository Actions allowance); the repository is now public, so CI should run. Codex's Stage 23 run showed 18 passed and 9 failed, all nine at the seed or mutate step. `Entity` is coupled into the suite in exactly five places: `helpers.ts` lines 141, 157, 174 and 242 and `sync.spec.ts` line 200. `packages/graph/tsconfig.json` includes only `src/**/*.ts` and its `test` script is `vitest run src`, so no hermetic gate ever looked at the browser suite's helpers. See F305 and its 26 September update in `docs/Foundations_Findings.md`.

**Ruling F305 asked for, made:** seed the suite with `Client`, not a genuine `entity.create`. `Client` is fixed Tier 0 and not feature-owned, so `isTier0Only` and the feature-owned check pass on both server and client; its lifecycle is `Active`/`Inactive` (the stale-transition test transitions to `Inactive`; `Entity`'s is `Active`/`Dissolved`); the policy table gives the owner `Full` and every other role `Read`. Its ownership is provisional (a later Vulto Sales or Projects stage will claim it), which is why item 2's guard exists.

1. **The seed type in one place.** New file `packages/graph/sync-browser-tests/seed-type.ts` (no imports beyond types): `export const SYNC_SUITE_SEED_NODE_TYPE = "Client" as const;`, with a comment stating why it was chosen and what to do when it stops being valid (pick another registered Tier 0 type with no owning mutation and an `Active`/`Inactive` lifecycle).
2. **The fast-lane guard.** New unit test `packages/graph/src/sync-client/browser-suite-seed.test.ts` (inside `src/`, so `pnpm verify`'s `test:fast` runs it), importing `SYNC_SUITE_SEED_NODE_TYPE` from `../../sync-browser-tests/seed-type` and `isNodeType`, `isTier0Only`, `getNodeRegistration` and `FEATURE_LIFECYCLE_NODE_TYPES` from `@vulto/schema`, asserting that the type is a registered node type, is Tier 0 only, is not in `FEATURE_LIFECYCLE_NODE_TYPES`, and has a fixed lifecycle containing both `Active` and `Inactive`. Every assertion carries a message saying that the sync browser suite seeds this type through `graph.createNode` and `graph.transitionLifecycle`, that a feature has claimed it (or it is otherwise unsuitable), and to change `seed-type.ts`. Prove it bites: locally set the constant to `"Entity"`, confirm the test fails with those messages, and restore it (do not commit the perturbation). The reviewer prototyped exactly this test on a scratch copy before writing this item: it passes for `Client`, fails for `Entity` with the "A feature has claimed it" message, and `tsc --noEmit` accepts the import of `seed-type.ts` from outside `src/`.
3. **The fixture and the suite.** In `helpers.ts`, replace the four `Entity` references with the constant (the created node's `node_type`, `clientQuery`'s `nodeType`, `newClient`'s `node_type`) and rename the four helpers to say what they now are — `seedEntities` to `seedClients`, `newEntity` to `newClient`, `entityQuery` to `clientQuery`, `listEntities` to `listClients` — updating their doc comments and `untilSynced`'s. In `sync.spec.ts` rename all 22 uses and make the stale-state test's `node-get` query use the constant. Keep every assertion's intent, every expected value and every timeout exactly as they are; add no `test.skip`, `test.fixme`, `retries` or `--grep` exclusion; touch no other test.
4. **Run the whole suite for the first time since Stage 10.** After `pnpm stack:up` and `pnpm --filter @vulto/api db:migrate`, with the same environment as Stage 23 (`set -a; source .env; set +a; SYNC_BROWSER_DATABASE_URL="$DATABASE_URL" ELECTRIC_URL="$ELECTRIC_URL" ELECTRIC_SECRET="$ELECTRIC_SECRET" pnpm test:sync-browser`), the target is 27 of 27. The nine formerly failing tests have not executed past their seed step for four days, so a failure now is information, not necessarily a defect in the fix: classify each one. (a) Suite or fixture drift inside `packages/graph/sync-browser-tests` (an assertion naming something that legitimately changed since Stage 10) — fix it there, keeping the assertion's intent, and record the change. (b) A genuine product defect from Stages 11–23 (for example a real permission, audience or sync regression) — STOP: commit a short finding note on the branch, set `FDN-126` to Blocked, and report it; do not fix product code in this stage. (c) An environment flake — do not add retries; run the test three times to establish that it is a flake and report it.
5. **Prove the A003-T56 gate bites.** Once the suite is green, as a temporary local perturbation only: change `seedProtected` so that its Employee carries the Tier 1 sentinel in a Tier 0 field (for example `full_name: tier1`), run `pnpm test:sync-browser --grep 'no Tier 1 or Tier 2'`, and confirm it FAILS because the sentinel is found in the cache; revert the perturbation and confirm it passes. Report both results. Do not commit the perturbation.
6. **CI is the acceptance gate.** Push the branch. The `fast-lane` and `slow-lane` workflows must each end `success` on the branch's head commit — jobs `resolve-image`, `verify`, `api-integration`, `production-build`, `auth-browser` and `sync-browser`. Report each run's URL and conclusion. The reviewer will read them through the public Actions API. If a job fails without executing any step, that is an Actions-availability problem (the repository was made public on 26 September so it should not recur): report it and stop; do not claim CI. The Stage 20 to 23 merges have never been through CI's `pnpm verify` or `api-integration`, so a failure in a job other than `sync-browser` may be a real earlier defect: classify it exactly as in item 4 and stop on anything that is not suite or fixture drift.

**Done criteria:**
- `SYNC_SUITE_SEED_NODE_TYPE` is `"Client"` in `seed-type.ts` and is the only place the seed type is named; `git grep -n '"Entity"' packages/graph/sync-browser-tests` returns nothing.
- The guard test exists under `packages/graph/src`, runs in `pnpm verify`, passes, and was shown to fail with an instructive message when the constant is set to `"Entity"`.
- The full `pnpm test:sync-browser` passes 27 of 27 locally with no skip, fixme, retry or grep exclusion added, and every test not named in a classification kept its assertions, values and timeouts unchanged; any drift fix under item 4(a) is listed in the report by test name.
- The A003-T56 test runs and passes, and the item 5 negative control failed with the perturbation and passed without it.
- The branch's `fast-lane` and `slow-lane` runs are both `success` on its head commit, with URLs in the report.
- No file outside `packages/graph/sync-browser-tests/` and the one new unit test under `packages/graph/src/sync-client/` appears in the diff (report files under `docs/stage-reports/` aside); no product code, workflow, gate, dependency or `docs/` governing-file change.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`), plus items 4 to 6. There is no baseline comparison in this stage: a full pass is the target and F305's baseline rule is retired when this stage merges.

**What to check in the Stage 24 report:** that `seed-type.ts` is the single source and `git grep '"Entity"'` in the suite is empty; that the guard test is inside `src/` (so `test:fast` runs it), asserts all four properties with instructive messages, and was actually shown to fail against `"Entity"`; that the diff to `sync.spec.ts` and `helpers.ts` is a pure rename plus the constant, with no assertion, expected value or timeout changed except any drift fix the report itemizes and justifies by test name, and no `skip`, `fixme`, `retries` or `grep` exclusion; that the report gives the local 27-of-27 result (or classifies each failure per item 4) and both results of the item 5 negative control; that the branch's `fast-lane` and `slow-lane` runs are read directly from the Actions API by the reviewer, not accepted from the report; and, after merge, that `main`'s next slow-lane run is green and `publish-artifacts` runs.


### Stage 25 — Notification and Alert Center (`VPS-F003`)

`VRS-001`'s MVP order runs `VPS-F002` → `VPS-F003` (Notification and Alert Center, platform, `VRS-F016` in the Roster register). This stage builds the feature's data layer: the Notification node and its recipient-only access, the subscription registry and its delivery engine, the recipient's read-state mutations, and the device queries the Inbox will render. It ships two event-driven rules and delivers the `hrCompliance.sendReminder` hook that Stage 18 deferred. It builds no email, no push, no muting and nothing under `apps/`: the Inbox screen, the sidebar unread indicator, the keyboard map and the inline actions are `VPS-D004`'s shell and are reference material for whichever later stage wires them, the same deferral every prior stage has made. The spec's central warning governs everything below: **a notification that says the wrong thing to the wrong person is the same leak as a wrong permission row, delivered with worse timing.** Every rule is a security decision.

**Findings F307 through F320, closed before this brief was written** (each has its own file in `docs/findings/`):

- **F307** — Notification's `Recipient-only` cell resolves to `none` for every role, including the recipient, through a `recipient-only-unresolvable` scope the interceptor cannot decide, so nobody could read their own notification and the audience materializer would place none on any device. Ruled: a new resolvable scope, `recipient` (the row's `delivered_to` edge target equals the principal's `userId`), `full` for all five roles, no role sees another person's row (G04); writes stay closed (see Do items 1 and 3).
- **F308** — The pipeline exposes only `changedRowIds` (no created-versus-updated, no before value), so "created or escalated" cannot be detected, and running rules inside the source transaction would let a notification failure abort the source (G03). Ruled: one generic post-commit hook; an engine in its own transaction as a new system principal that never fails the source; idempotency by two new Tier 0 fields, `rule_id` and `dedupe_key`, with a rule-supplied discriminator (`severity` for RevenueGapAlert) and an advisory lock on the key (Do items 2 and 4).
- **F309** — Only RevenueGapAlert (created or escalated) and TimesheetAnomalyFlag (created) have built event sources; six other rules wait on unbuilt features, three on a scheduler (FDN-57). Ruled: the registry ships with those two, and Stage 18's deferred `hrCompliance.sendReminder` is delivered here as a manual producer (Do item 5).
- **F310** — Email needs `EmailService` (`VPS-A006`, unbuilt), push is `VPS-F011`, and the Inbox is UI. Ruled: none built; `category` is recorded so email can be added later without touching producers (G08 holds by construction).
- **F312** — `TimesheetAnomalyFlag` is fixed `Manager-restricted` Tier 2, so a rule cannot read its fields, and a message naming the employee draws that name from the Employee's Tier 0 partition, not from the source. Ruled: `messageFields` are `{ from: "subject" | "source", field }`; a `source` field requires a fixed Tier 0 source; the flag contributes existence only (Do items 2 and 3).
- **F313** — Raised by the builder tracing this brief, ruled the same day: the brief put the engine in a new `services/api/src/notifications/` folder, which `scripts/arch-check.mjs` (A003-T52) forbids, and it also said only `pipeline.ts` may change under `mutations/`, which its own read-state and reminder mutations contradict. Ruled: the engine lives in `services/api/src/mutations/notification-delivery.ts`; the boundary is "no existing source feature's mutation file changes" (Do items 3, 8e and the Done criteria).
- **F314** — Raised the same way: the criterion that `recipient-only-unresolvable` no longer exists "anywhere in the repository" matches the ledger, findings and archived documents that record it as history and that the builder may not edit. Ruled: the criterion is product code only (`packages/`, `services/`, `apps/`, `scripts/`).
- **F315** — Raised by the builder tracing Do item 5, ruled the same day: the brief said `hrCompliance.sendReminder` reuses "the role gate `hrCompliance.listSubmissionStatus` already enforces", but that query has no role gate (it filters by what the caller can read, and a Team Member sees their own row). Ruled: the sender must hold a `full`-scope-`any` `TimesheetEntry` cell in the policy table (Owner and HR Admin), derived from the table, and may name only employees the query returns for that caller (Do item 5).
- **F316** — Raised by the builder tracing Do item 3, ruled the same day: the watched rows are created by standalone engines in their own transactions after the source commit, by paths that never reach `applied.changedRowIds` (`timesheet.ts` discards the anomaly engine's `flagIds`; `assignment.ts` discards the alert engine's ids; `revenueGapAlert.sweep` never enters `applyMutation`; the anomaly flag never calls the audience seam), so the brief's `changedRowIds` hook would have missed both watched paths. Ruled: an `AsyncLocalStorage` write log fed by additive `recordWrite` calls in the store's write functions, drained after success by a scope wrapped around `applyMutation` and every tRPC procedure (Do items 3 and 8).
- **F317** — Raised by the builder tracing Do item 1, ruled the same day: the new `recipient` grant makes the caller `full` on their own Notification, and nothing reserves the `delivered_to` edge (`FEATURE_OWNED_EDGE_TYPES` holds only `has_skill` and `requires_skill`; the generic edge mutations consult that set, not the node set), so an Owner or HR Admin recipient (also `full` on every `User`) could attach a second `delivered_to` edge and redirect or widen access, and the first-edge resolver would follow a caller-chosen `effective_from`. Ruled: `delivered_to` joins `FEATURE_OWNED_EDGE_TYPES`, and the `recipient` scope fails closed on anything but exactly one edge agreeing with `recipient_user_id` (Do items 1 and 8).
- **F318** — Raised by the builder tracing F317's client requirement, ruled the same day: F317 said the client optimistic edge mutators read `FEATURE_OWNED_EDGE_TYPES`; they do not (`packages/graph/src/mutators/foundation.ts` imports only `FEATURE_LIFECYCLE_NODE_TYPES`; `rg FEATURE_OWNED_EDGE_TYPES packages/graph/src` is empty). Ruled: the guard is added to the three client edge mutators, covering `has_skill` and `requires_skill` too (Do item 1).
- **F319** — Raised by the builder tracing the F317 and F318 assertions, ruled the same day: the server authorizes before it validates and the generic edge target omits endpoint ids, so a generic `createNode` for a `Notification` and all three generic `delivered_to` edge mutations are already refused with `role` before any feature guard runs, and F317's predicted re-addressing path was denied in practice (the guard remains as defense in depth). Ruled: no change to authorization order or `edgeTarget`; the guards stay; the tests assert what each path really does (Do item 8g).
- **F320** — Raised by the builder at Do item 7, ruled the same day: the spec's `notification.listForUser(userId, filter?)` never defines `filter`, and neither did this brief. Ruled: not built; `notificationListForUser(database)` takes no `filter` and no user id, and a later stage that needs one defines it with its consumer (Do item 7).
- **F311** — Muting has no valid target (every shippable rule is `ActionNeeded`, G05) and no home (`WorkspaceMembership` is projection-owned with fixed fields). Ruled: not built; a conformance test proves no `ActionNeeded` rule is mutable (Do item 6).

`docs/Vulto_Specs/VPS-F003_Notification_and_Alert_Center.md` is corrected to match (the schema block carries `rule_id` and `dedupe_key`, and five Decisions Recorded entries record F307 through F311). See each finding's own file in `docs/findings/` for the full reasoning.

1. **The Notification record and its access.** New file `packages/schema/src/notification.ts` (exported from `packages/schema/src/index.ts`), following `revenue-gap-alert.ts`'s shape: a Zod fields schema for the `Notification` node carrying the universal node fields plus `recipient_user_id` (UUID), `source_node_type` (a registered node type name), `source_node_id` (UUID), `category` (`"ActionNeeded" | "Informational"`, deliberately two values), `message` (non-empty string, max 300), `read_at` and `dismissed_at` (nullable timestamps), `rule_id` and `dedupe_key` (non-empty strings, F308). Every field is Tier 0 (the registry entry `Notification`, owner `VPS-F003`, is already fixed `Recipient-only` Tier 0 in `packages/schema/src/registry/nodes.ts`, and the `delivered_to` edge, `Notification` to `User`, is already registered in `registry/edges.ts`; register the fields schema wherever the other feature-owned schemas are registered, and change neither registry entry). Then, in `packages/schema/src/policy/policy-table.ts`: replace the `recipient-only-unresolvable` scope with `recipient` in `PolicyScope`, and change the five cells of the `Recipient-only` class to `cell("full", "recipient", "Own only")` (F307). In `services/api/src/permission/interceptor.ts::rowScopeSatisfied`, decide `recipient` for a member principal by reading the row's active `delivered_to` edges (as `resolveStoredSubjectEmployeeId` reads `triggered_by`), and fail closed (F317): it is satisfied only when the row has **exactly one** active outgoing `delivered_to` edge, that edge's target node id equals `principal.userId`, **and** the row's own `recipient_user_id` field equals `principal.userId`; any other principal kind, no edge, two or more edges, a mismatch between the edge and the field, or a mismatch with the principal is `false`. Never take the first edge, never match any edge. \1 Add `delivered_to` to `FEATURE_OWNED_EDGE_TYPES` in the same file (F317, the same mechanism Stage 21 used for `has_skill` and `requires_skill`), so the server's `createEdgeMutation`, `closeEdgeMutation` and `updateEdgeMetadataMutation` (which already consult the set in `services/api/src/mutations/foundation.ts`) refuse it with `requires-feature-mutation`. The client optimistic mutators do **not** consult the set today (F318: `packages/graph/src/mutators/foundation.ts` imports only `FEATURE_LIFECYCLE_NODE_TYPES`), so add the check there too: in `createEdge` (before the cache write) refuse when `FEATURE_OWNED_EDGE_TYPES` has `String(edge["edge_type"])`, and in `closeEdge` and `updateEdgeMetadata` (after the edge is loaded) refuse when it has the loaded edge's type, each with `throw new OptimisticRejection("requires-feature-mutation")`, mirroring the node guards at `foundation.ts:200`, `:214`, `:233` and `:326`. The shared set now covers `has_skill` and `requires_skill` on the client as well, which the server already refused (a behavior alignment, not a new rule: the server stays the authority and the client guard only stops an optimistic write the server would reject); if an existing client test relied on the client accepting one of those edges through the generic mutators, report it in the stage report and adjust only that assertion. Without the set entry, the new `recipient` grant (`full` on the caller's own Notification) plus a `full`-scope-`any` grant on `User` would let an Owner or HR Admin recipient attach a second `delivered_to` edge and redirect or widen who can read a notification. The engine writes its one `delivered_to` edge through `authorizeWrite` as its system principal, never through `graph.createEdge`, so the guard does not affect it. Update `policy-table.test.ts` (and any test asserting the old scope) so the fidelity test still reads the literal matrix text; this is the only permission-layer change in the stage, and it must not alter any other type's resolution (run the whole policy and interceptor suites unchanged).
2. **The subscription registry.** New file `packages/schema/src/notification-rules.ts`: `NOTIFICATION_RULES`, a readonly list of `{ ruleId, sourceNodeType, category, recipient, discriminatorField?, messageFields, messageTemplate }` (each `messageFields` entry is `{ from: "subject" | "source", field }`, the subject being the Employee the notification is about), with `recipient` from the closed set `RECIPIENT_RESOLVERS = ["employee-manager", "employee-self"]` (no other resolver exists; Owner, HR Admin and broadcast resolvers are added by the stage that needs them, with their boundary). Registration validates at module load and in a conformance test: (a) `sourceNodeType` is a registered node type, and every `messageFields` entry is a provably Tier 0 field of the node it names: a `subject` field must be in `operationalShape` (the Employee's Tier 0 partition), and a `source` field is allowed only when the source type is fixed Tier 0 (F312: `TimesheetAnomalyFlag` is fixed `Manager-restricted` Tier 2, so a rule on it can draw nothing from the flag but its existence). The proof already exists as the last block of `validateSearchableNodeTypes` in `packages/schema/src/search.ts`; factor it into one exported helper used by both registries and leave the search tests unchanged, do not write a second copy. So no message can carry Tier 1, Tier 2 or Tier 3 content (G02, G07, and the spec's "message text is a disclosure surface"); (b) `discriminatorField`, when present, is a Tier 0 field of the source; (c) `ruleId` is unique; (d) `messageTemplate` renders only its declared `messageFields` (a template referencing an undeclared field fails registration). Register exactly two rules: `revenue-gap-alert` (`RevenueGapAlert`, `ActionNeeded`, `employee-manager`, discriminator `severity`, active alerts only) and `timesheet-anomaly-flag` (`TimesheetAnomalyFlag`, `ActionNeeded`, `employee-manager`, no discriminator). Messages carry a person's name and the fact, never money: `revenue-gap-alert` may use the subject's `full_name` and `bench_days`, never `daily_cost` or `accumulated_cost`; `timesheet-anomaly-flag` says the person's timesheet needs review and names no flag detail; its source is Tier 2, so its message uses `subject` fields only. Each rule's subject employee is `RevenueGapAlert.employee_id`, and for the flag the `triggered_by` edge target (the same path the interceptor already resolves).
3. **The delivery principal and engine.** In `packages/schema/src/policy/principal-policy.ts`: add the system principal `notification-deliver` (display name `Notifications`) with one operation, `notification.deliver`, added only because the engine performing it exists in this diff (the table's own rule). New file `services/api/src/mutations/notification-delivery.ts` (F313: the engine writes the graph, so `scripts/arch-check.mjs`'s A003-T52 rules allow it only under `services/api/src/mutations/`, where `revenue-gap-alert.ts`, `timesheet-anomaly.ts` and `utilization-snapshot.ts` already sit; a `notifications/` or `jobs/` folder fails the gate): `deliverForRows(changedRowIds)` opens its own transaction as that principal, loads the touched nodes, and for each node whose type has a rule: resolves the subject employee; resolves the recipient (`employee-manager` is the subject's active `managed_by` target as of now; `employee-self` is the subject itself), then the recipient's `User` through `resolveUserForEmployee` in `services/api/src/permission/employee-link.ts`; **if there is no manager, or the recipient Employee has no linked User, delivers nothing** and reports the skip (no fallback to Owner or anyone else: G02, a wrong recipient is worse than a missing one; the count of skipped deliveries is in the stage report). Computes `dedupe_key` as `ruleId:sourceNodeId:discriminator` (the discriminator is the source's `discriminatorField` value, empty when none), takes `pg_advisory_xact_lock` on a hash of `workspaceId + recipient + dedupe_key`, and inserts the Notification and its `delivered_to` edge only if no Notification with that recipient and `dedupe_key` exists (F308). The engine reads a watched node's universal row (id, type, created and updated stamps) and its `triggered_by` edge, and never a Tier 1 or Tier 2 field (F312), so watching a Tier 2 source such as the flag needs no protected read and creates no protected-read audit entry; if tracing shows the interceptor forces a protected read for that existence check, stop and report before adding a system operation. Writes go through `authorizeWrite` as the system principal like every other engine (`revenue-gap-alert.ts` is the precedent), and the Notification node id and the edge id both reach `audienceMaterializer.onRowsChanged` in the same transaction, so the recipient's audience row exists before any device reads it. Deliver through one generic write-log seam, not the mutation's own ids (F316: the watched rows are created by standalone engines in their own transactions, after the source commit, by paths that never reach `applied.changedRowIds` or `audienceMaterializer.onRowsChanged`; `TimesheetAnomalyFlag` writes never call the audience seam at all, and `revenueGapAlert.sweep` never enters `applyMutation`). (i) New file `services/api/src/graph/write-log.ts` (the graph folder may import the store): an `AsyncLocalStorage`-scoped `Set<string>` with `recordWrite(id)` (a no-op outside a scope) and a `runInWriteScope(fn)` helper. (ii) In `services/api/src/graph/store.ts`, one additive `recordWrite` call in each write function (`insertNode`, `insertUserNode`, `updateNodeFields`, `softDeleteNode`, `insertEdge`, `updateEdgeMetadata`, `closeEdge`) recording the node or edge id after the row is written; nothing else in the store changes, and no source feature file changes. A transaction that later rolls back or is retried leaves ids for rows that do not exist; the engine loads each id and skips any that is absent, so that is harmless. (iii) The delivery scope, defined in `mutations/notification-delivery.ts`: `withNotificationDelivery(fn)` opens a write scope if none is active (an already-active outer scope wins and drains), runs `fn`, and only after `fn` has resolved successfully drains the recorded ids into `deliverForRows`, outside every source transaction; any delivery error is caught and logged, and the result of `fn` is returned regardless (F308). The delivery's own writes run with the scope cleared (`AsyncLocalStorage.exit`/`run(undefined, ...)`) so they cannot re-trigger delivery. (iv) Wrap in that scope, in exactly two places: the whole of `applyMutation` in `mutations/pipeline.ts` (so the plan's `afterCommit` and the standalone engines it calls are inside the scope), and every tRPC procedure through one generic middleware in `services/api/src/trpc.ts` (so standalone paths such as `revenueGapAlert.sweep`, which never enter `applyMutation`, are covered); a later jobs service (FDN-57) calls `withNotificationDelivery` itself. At-least-once delivery after a crash between commit and drain is not solved here (F308): the jobs service's reconciliation sweep will be safe to add because every delivery is idempotent. No existing source feature's mutation file changes (G03, F313): the diff modifies none of `revenue-gap-alert.ts`, `timesheet-anomaly.ts`, `timesheet.ts` or any other file under `services/api/src/mutations/` that a watched feature owns. The files under `services/api/src/mutations/` in the diff are limited to `pipeline.ts` (the scope wrapper), the new Notification files (the engine, the read-state mutations of item 4, the reminder mutation of item 5), and additive registration lines in whichever registry file already lists named mutations; outside `mutations/` the diff adds `graph/write-log.ts`, the additive `recordWrite` calls in `graph/store.ts`, and the one tRPC middleware in `trpc.ts` (F316).
4. **Read-state mutations.** Three named mutations, `notification.markRead(notificationId)`, `notification.dismiss(notificationId)` and `notification.markAllRead()`, on both sides following the `graph.transitionLifecycle` precedent (the schema-package mutation definition, the server `ServerMutation`, and the client optimistic mutator, each running the same checks; look at how Stage 11's and Stage 12's mutations are registered in both places rather than inventing a shape). Each sets only `read_at` (and `dismissed_at` for dismiss) to now on the caller's own row; is idempotent (a second call changes nothing and returns success); is authorized only through the `recipient` scope, so a call naming another person's notification is rejected exactly as a nonexistent one is (no existence leak); and `markAllRead` touches only the caller's own unread Notifications. No mutation creates or deletes a Notification, and none can set any other field: a test attempts each and asserts the rejection.
5. **`hrCompliance.sendReminder` (F309).** The mutation Stage 18 deferred: `hrCompliance.sendReminder(weekStartDate, employeeIds)` in the same registration shape as item 4. Authorization (F315: `hrCompliance.listSubmissionStatus` has no role gate; it returns whatever rows the caller can read, so a Team Member legitimately sees their own status, and there is nothing to reuse): the caller must hold, in at least one of their roles, a `full` cell with scope `any` for `TimesheetEntry` (partition `record`) in the policy table, resolved through `resolvePolicyCell` and never through a hard-coded role list (today that is Owner and HR Admin; Manager is `read`, Finance Admin is `read`, Team Member is `full` but `own`, so none of those may send a reminder, including to themselves), matching the spec's "an HR Admin view ... with a reminder action" and "nobody but the employee and an HR Admin writes". A caller without it is rejected before anything is read. It may then name only employees that `listSubmissionStatus` itself returns for the caller and whose status resolves to `Draft` or `Not-Started` for that week; any other id is rejected exactly as a nonexistent one is (no existence leak). It delivers, through the item 3 engine, one `ActionNeeded` Notification per named employee, recipient `employee-self`, `source_node_type` `Employee`, `source_node_id` the employee, `dedupe_key` `timesheet-reminder:<employeeId>:<weekStartDate>:<UTC calendar day>` (so one reminder per person per week per day), message a fixed sentence naming the week and nothing else. It is not an event rule and not in `NOTIFICATION_RULES`. Correct `VRS-F010`'s Out of Scope line about `hrCompliance.sendReminder` to say it is delivered by `VPS-F003` Stage 25 and does not alter the compliance view's read contract.
6. **Muting is not built (F311), and the conformance test that proves G05 by construction.** No `notification.mute`, no preference field. One conformance test over `NOTIFICATION_RULES` asserts the registry cannot express an `Informational`-muted-by-default path: every shipped rule has `category: "ActionNeeded"`, and the test's comment states the deferral and where the first `Informational` rule must decide the preference's home.
7. **The device queries.** New file `packages/graph/src/queries/notifications.ts` (exported from `queries/index.ts`), following `skill-matrix.ts` and `search.ts`: plain async functions over the device's `SyncDatabase`, reading only local tables, no interceptor call and no network. `notificationListForUser(database)` returns (F320: no `filter` parameter; the spec's `filter?` is undefined anywhere and the three groups are the Inbox's whole read, so it is deferred until a consumer defines it) `{ needsYou, today, earlier }`: `needsYou` is `ActionNeeded` with `dismissed_at` null, `today` is the remaining undismissed rows created on the current local calendar day, `earlier` the rest, each ordered by `created_at` descending, and the function takes **no user id parameter** (G04: the device cache holds only its own principal's audience, F300's precedent; the query must not add a second filter that could disagree with the audience, and its module comment says so). `notificationUnreadActionCount(database)` counts `ActionNeeded` rows with `read_at` null and `dismissed_at` null and counts nothing else (G06). Read `packages/graph/src/queries/search.ts` for how a query reads `cache_nodes` records and mirror it; add no table, no trigger and no schema-version bump (`CACHE_SCHEMA_VERSION` stays 3).
8. **Tests.** Server, following the `*.integration.test.ts` precedents (`revenue-gap-alert.integration.test.ts`, `audience.integration.test.ts`, `interceptor.integration.test.ts`), against the real Postgres stack: (a) a RevenueGapAlert created for an employee notifies the manager's linked user and nobody else, and the employee, another manager, an HR Admin and the Owner each get `none` from `decideRead` on that notification and have no audience row for it (F307, G04, acceptance criterion 1 and 3); (b) the same for a TimesheetAnomalyFlag; (c) escalation Low to Medium to High creates exactly one notification per severity and re-running at the same severity creates none (F308), and two concurrent deliveries for one key produce one row (the advisory lock); (d) a manager with no linked user, and an employee with no manager, yield no notification and no fallback recipient, and the skip is counted; (e) the source mutation still commits and returns when the engine throws (inject a failing engine through the existing dependency seam), and no existing source feature's mutation file changed (G03, F313); (e2) delivery is proven through the paths that really produce the watched rows, never by handing `deliverForRows` an id (F316): a `timesheet.submitWeek` through `applyMutation` that trips an anomaly check notifies the manager; an `assignment` change through `applyMutation` that creates and then escalates a RevenueGapAlert notifies the manager once per severity; and `revenueGapAlert.sweep` called through the tRPC caller (never `applyMutation`) notifies the manager; each fails if the write-log or its wrappers are removed; (f) a message rendered for each rule contains the subject's name and no money field, no Tier 1 or Tier 2 sentinel value even when the source record carries one, and the registry refuses a rule whose `messageFields` includes a non-Tier-0 field (including a `source` field of a Tier 1 or Tier 2 source), an unknown source type, a duplicate `ruleId` and a template referencing an undeclared field; (g) the read-state mutations: recipient-only, idempotent, cannot touch other fields, a foreign id is rejected identically to a missing id, `markAllRead` touches only own rows, and the generic write paths are closed (F319; the server authorizes before it validates, `pipeline.ts` `authorizeWrite` then `plan.validate`, and `edgeTarget` omits endpoint ids, so which refusal fires depends on the path, and the tests assert exactly what each path really does): (i) `graph.createNode` for a `Notification`, and `graph.createEdge`, `graph.closeEdge` and `graph.updateEdgeMetadata` for a `delivered_to` edge, are refused on the server with reason `role` or `requires-feature-mutation` (either is acceptable; assert the refusal, that nothing was written and that no Notification or edge exists afterwards, and record the observed reason for each in the stage report); (ii) `graph.updateNodeFields`, `graph.softDeleteNode` and `graph.transitionLifecycle` on an existing Notification, called by its own recipient (who passes authorization through the `recipient` scope, so the feature guard is the only thing standing), are refused with exactly `requires-feature-mutation`, and the row is unchanged; (iii) on the client optimistic mutators (which have no authorization step) all of these, and the `has_skill` alignment of F318, are refused with exactly `requires-feature-mutation`; and the `recipient` scope fails closed when a row is seeded with two active `delivered_to` edges or with an edge target that disagrees with `recipient_user_id` (F317; seed the conflict through the store in the test, since the generic mutation is refused); (h) `hrCompliance.sendReminder`: rejected for a Manager, a Finance Admin and a Team Member (including for themselves) and accepted for an HR Admin and an Owner, rejected for an employee whose week is `Submitted`, one notification per person per week per day, recipient is the employee themselves and no one else, and the manager gets nothing. Device, in `packages/graph/src/queries/notifications.test.ts` with `SqliteCache`/`openTestDatabase` and `fetch` spied to reject: grouping into the three groups and ordering, the unread count counting `ActionNeeded` only (three unread action items and eleven unread informational ones read 3, acceptance criterion 5), dismissed items leaving `needsYou`, empty input returning three empty groups, and the absence of a user id parameter. Conformance: the registry validations of item 2 and the deferral test of item 6. The existing policy, interceptor, audience and sync suites pass unchanged except for the scope rename in item 1.
9. **Docs and the stage report.** Correct `docs/Vulto_Specs/VPS-A004_Graph_Permission_Layer.md`'s `Recipient-only` clause to say the scope is now resolved through the `delivered_to` edge (whatever wording the clause uses today; keep its class name), and `VRS-F010`'s Out of Scope line (item 5). Write `docs/stage-reports/STAGE-25_Notification_and_Alert_Center.md` following the eleven-section report format in Part 2, with the measured facts the reviewer will check: the count of deliveries skipped for no manager or no linked user in the fixtures, the list of files under `services/api/src/mutations/` in the diff (expected: `pipeline.ts`, the new Notification files, and additive registration lines only), and the exact rule list registered.

**Done criteria:**
- `Notification` reads `full` for its own recipient and `none` for every other principal, Owner included, through the new `recipient` scope, and `recipient-only-unresolvable` no longer exists in any file under `packages/`, `services/`, `apps/` or `scripts/` (item 1, F307, F314, G04); the ledger, findings and archived documents keep the name as history and are not edited.
- The generic node and edge write paths are closed for `Notification` and `delivered_to` as item 8(g) states (server refusal by `role` or `requires-feature-mutation` where authorization comes first, exactly `requires-feature-mutation` for a recipient's generic update, soft-delete or transition and on the client), and no user-facing path creates or deletes a Notification or re-addresses one; the `recipient` scope requires exactly one active `delivered_to` edge whose target and the row's `recipient_user_id` both equal the caller, and fails closed on any conflict (items 1, 4, F317).
- A recipient's notification reaches only their device's audience and no one else's, proven in the audience integration test with the real materializer (items 3, 8a, G04).
- The registry refuses a non-Tier-0 message field, an unknown source type, a duplicate rule and an undeclared template field, each a passing test, and every shipped message carries a name and a fact but no money and no Tier 1 or Tier 2 value (item 2, 8f, G02, G07, G08).
- Exactly two rules are registered, `revenue-gap-alert` and `timesheet-anomaly-flag`, both `ActionNeeded` to the employee's manager only (item 2, acceptance criterion 1).
- Delivery is idempotent by `dedupe_key`: escalation notifies once per severity, a repeat notifies none, and concurrent runs insert once (items 3, 8c, F308).
- A missing manager or an unlinked recipient delivers nothing and falls back to no one (items 3, 8d).
- A source mutation is unaffected when the engine fails, and the diff modifies no existing source feature's mutation file (items 3, 8e, G03, F313).
- The two real watched creation paths and the standalone sweep each produce their notification through the write-log scope, proven end to end, and the additive `recordWrite` calls plus the `trpc.ts` middleware are the only diff outside `mutations/` and the new Notification files apart from `graph/write-log.ts` (items 3, 8e2, F316).
- `markRead`, `dismiss` and `markAllRead` work on the caller's own rows only, change only their two fields, are idempotent, and reject a foreign id exactly as they reject a missing one (item 4, 8g).
- `hrCompliance.sendReminder` requires a `full`-scope-`any` `TimesheetEntry` cell resolved from the policy table (Owner and HR Admin today), reaches only `Draft` or `Not-Started` employees, and notifies only the employee themselves, once per person per week per day (item 5, 8h).
- `notificationListForUser` returns the three groups from local state alone with no user id parameter, and `notificationUnreadActionCount` counts `ActionNeeded` only (item 7, 8, G04, G06).
- No `notification.mute`, no email, no push, no file under `apps/`, no new cache table or trigger, no `CACHE_SCHEMA_VERSION` change, and no new policy row other than the five `Recipient-only` cells (items 1, 6, F310, F311).
- `pnpm verify` and `pnpm verify:full` pass, and the branch's `fast-lane` and `slow-lane` GitHub Actions runs at the head commit both end green with every job executing (the reviewer reads them from the public API, as for Stage 24).

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). `verify:full` runs `pnpm verify && pnpm verify:preflight && pnpm --filter @vulto/api test` and no browser suite. This stage adds no device SQL, so `pnpm test:sync-browser` is not a required local gate, but the branch's `slow-lane` `sync-browser` job must stay green: run it locally only if your environment can, and otherwise say so and rely on CI, which is the reviewer's evidence. Push the branch and quote both workflow run ids and their conclusions in the report.

**What to check in the Stage 25 report:** that `recipient-only-unresolvable` is gone from product code (`git grep -n recipient-only-unresolvable -- packages services apps scripts` returns nothing; documents keep it as history) and the fidelity test still reads the literal matrix; that the `recipient` scope resolves through the `delivered_to` edge only, with no role special case, and that the Owner really gets `none` in a real test rather than an asserted constant; that the audience materializer is untouched and `graph/store.ts` differs only by one additive `recordWrite` call per write function (read the diff, and confirm removing them makes the end-to-end delivery tests fail); that no message can contain a non-Tier-0 field (read the registry validation, then change a template locally to reference `daily_cost` and confirm registration fails); that no rule falls back to Owner, HR Admin or any other recipient when a manager or link is missing; that the engine runs after commit in its own transaction and a failure cannot fail or roll back the source (read the scope wrapper in `pipeline.ts`, the `trpc.ts` middleware and the injection test); that `dedupe_key` includes the discriminator and the advisory lock is real; that `hrCompliance.sendReminder` derives its authorization from the policy table's `TimesheetEntry` cell rather than a role list, and that a Manager, a Finance Admin and a Team Member are each rejected in a real test; that `git diff --stat` shows nothing under `apps/`, nothing else under `services/api/src/mutations/`, no new cache table, and no `CACHE_SCHEMA_VERSION` change; that the workflow run ids in the report are real and green, read from the public API rather than from the report; and that the stage report names the skipped-delivery count and the exact registered rules.

### Stage 26 — Silent Audit Log (`VPS-F004`)

`VRS-001`'s MVP order runs `VPS-F003` → `VPS-F004` (Silent Audit Log, Compliance). Most of this feature already exists from Stages 3 and 5 (the journal, the interceptor's single logging call site, the closed entry schema, actor pseudonymization) and Stage 23 excluded it from search. What is missing is the feature's only API contract, `auditLog.query`, and proof of the properties the spec claims. This stage builds the query, proves the rest by test, and builds nothing under `apps/`: the review screen (`VPS-F004-S03`) needs `VPS-F005`'s console and the shell and is deferred like every prior UI. It adds no database mechanism, no new event type, no new system principal and no cache table.

**Read this before anything else: report every contradiction once.** Stage 25 stopped eight times, one contradiction per stop, each a defect of the brief. This time, before writing product code, trace every Do item, every named file and every claim below against the repository, then write **one** report listing **all** the contradictions you found together (not one at a time), and stop for a ruling only if something blocks the whole stage. Where a contradiction has an obvious minimal resolution that stays inside this brief's scope, take it, record it in the stage report under "Micro-decisions" and continue.

**Findings F323 through F326, closed before this brief was written** (each has its own file in `docs/findings/`):

- **F323** — `VPS-A004` and the code log every successful grant of Tier 1, Tier 2 and Tier 3; `VPS-F004` says Tier 1 and Tier 3 only. Ruled: `VPS-A004` and the code stand (Tier 2 grants are logged, Tier 0 is not); `VPS-F004` is corrected.
- **F324** — `auditLog.query` does not exist and its contract is open (pagination, event filter shape, what reading the log writes). Ruled: the shape in Do item 1.
- **F325** — Immutability is structural only and its one test checks exported names. Ruled: stronger structural tests, no database trigger this stage; a database-level guard is deferred behind the hash chain (`FDN-108`) and recorded as a limitation.
- **F326** — S01, S04 and G07 are built; S03 (UI) and `VPS-F007` orchestration are not buildable. Ruled: deferred, and the parts that exist are confirmed by test where the existing suites do not already prove them.

`docs/Vulto_Specs/VPS-F004_Silent_Audit_Log.md` is corrected to match (its overview, "What is logged", G04, Out of Scope and three Decisions Recorded entries). See each finding's own file in `docs/findings/`.

1. **`auditLog.query` (F324).** A tRPC query on the app router (new `auditLog` router entry in `services/api/src/router.ts`, following the pattern of `hrCompliance` and `revenueGapAlert`) with a Zod input schema in `packages/schema` (follow how `hrSubmissionStatusInputSchema` is defined and exported): `{ workspace_id, start_date?, end_date?, actor_user_id?, event_types? (array of the closed `AuditEventType` values, any of), target_node_type?, target_tier? (0 to 3), cursor?, limit? }`, `.strict()`. Output `{ entries: AuditEntry[], next_cursor: string | null }`, the full entries parsed through `auditEntrySchema`, ordered `occurred_at` descending then `audit_entry_id` descending, keyset-paginated: `cursor` is an opaque base64url string encoding the last row's `(occurred_at, audit_entry_id)`, an invalid cursor is a validation error, `limit` defaults to 50 and is capped at 200. Add the query to a new `services/api/src/audit/query.ts` (the only place besides `journal.ts` allowed to import `audit/schema`; check `scripts/arch-check.mjs` rule 3 and, per the standing lesson, run `node scripts/arch-check.mjs` against a scratch file at that exact path first), filtering on the indexed columns (`occurred_at`, `actor_user_id`, `event_type`, `target_node_type`, `target_tier`), always scoped to `workspace_id`. `start_date` and `end_date` are inclusive calendar dates in UTC. Nothing here may hand-roll a role check: authorization is the interceptor's decision for `AuditEntry` through `authorizeRead(tx, principal, { workspaceId, nodeType: "AuditEntry", nodeId: null }, context, "NodeList")` (find the existing callers of `"NodeList"` and mirror the closest one; if `NodeTarget` with a null node id is not acceptable in practice, use the `QueryTarget` shape with `query_kind` `node-list`, and record which you used). A caller that is not Owner or HR Admin must receive a refusal identical in shape to the other procedures' permission refusals and must cause exactly one `PermissionDenied` entry (target tier 2); a permitted call causes exactly one `SensitiveAccessGranted` entry per call, not per row, and the page returned must not include that call's own entry (read first, append after). A `workspace_id` that is not the caller's must be refused the way `hrCompliance.listSubmissionStatus` and the other procedures treat it (read them, do not invent). The query is a server call only: nothing here is replicated to a device, and no `packages/graph` code changes.
2. **Tier 2 grants are logged, Tier 0 are not (F323).** No interceptor change. Add tests, if the existing interceptor suites do not already prove each (grep `interceptor.integration.test.ts`, `protected.integration.test.ts` and `adversarial.integration.test.ts` first and cite what you found): a successful Tier 2 read writes one `SensitiveAccessGranted` entry with `target_tier` 2; a Tier 0 read writes none; a denial at each of Tier 0, 1, 2 and 3 writes one `PermissionDenied` entry; no entry of any kind contains a content value (seed a record with a sentinel value in a Tier 1 field and assert the sentinel appears in no entry's JSON).
3. **Immutability, structurally (F325).** (a) A test scans `services/api/src/**/*.ts` (excluding test files) and asserts that `.update(auditJournal)` appears only in `audit/pseudonymizer.ts`, that `.delete(auditJournal)` appears nowhere, and that `audit/schema` is imported only where `arch-check` rule 3 permits; make it fail with an instruction, and prove it bites by a temporary negative control reported in the stage report. Replace, do not keep, the weak `has no update and no delete path` name-check, or keep it and add the scan beside it. (b) A runtime test, using the real mutation pipeline as Owner, proves `graph.createNode`, `graph.updateNodeFields`, `graph.softDeleteNode`, `graph.transitionLifecycle`, `graph.createEdge`, `graph.closeEdge` and `graph.updateEdgeMetadata` are each refused for `AuditEntry` (create, and update or delete against an existing journal entry id), with nothing written and the journal unchanged; assert refusal, and record the observed reason for each in the stage report (the server authorizes before it validates, so `role` or `requires-feature-mutation` are both acceptable, as in Stage 25). (c) A test proves `AuditEntry` is not replicable (`isNodeTypeReplicable("AuditEntry")` is false and no sync audience row can exist for it) and is not in `SEARCHABLE_NODE_TYPES` and is refused by `validateSearchableNodeTypes` (the Stage 23 test may already prove this; cite it rather than duplicating).
4. **Pseudonymization and correlation (F326).** Read `audit/audit.integration.test.ts` (the `actor pseudonymization` block) and confirm it proves the spec's acceptance criterion: after `pseudonymizeActor` the erased actor's entries persist, their `actor_user_id` is the same opaque token on every one of them (so they stay correlatable), no entry is deleted, no other person's entry changes, and a Tier 1 read by another person is unaffected. Add only what is missing. `auditLog.query` filtered by the opaque token must return exactly those entries: add that test.
5. **The overhead measurement (F326).** Measure `appendAudit`'s added time to one interceptor decision (median and p95 over at least 200 decisions on the CI-shaped database, denial and Tier 1 grant separately) and report it against the spec's 10 ms requirement. It is a report, not a gate: assert nothing tighter than a deliberately loose ceiling (for example 100 ms p95) so an algorithmic regression fails without a timing flake.
6. **Docs and the stage report.** Correct `docs/Vulto_Specs/VPS-A004_Graph_Permission_Layer.md` only if its interceptor sentence needs a clarifying reference to `VPS-F004` (it already states the Tier 1, 2 and 3 rule; change nothing that is already correct). Write `docs/stage-reports/STAGE-26_Silent_Audit_Log.md` in the eleven-section format of Part 2, with the measured facts the reviewer will check: the query's input and output schemas, the observed refusal reason for each generic write path, the overhead figures, the list of contradictions you found before coding (or "none"), and the exact files changed.

**Done criteria:**
- `auditLog.query` exists with the F324 input and output shapes, is keyset-paginated with a capped limit, is scoped to the caller's workspace, and is refused for every role except Owner and HR Admin through the interceptor's own decision, not a local role check (item 1).
- A denied call writes exactly one `PermissionDenied` entry and a permitted call exactly one `SensitiveAccessGranted` entry per call, and the returned page never contains that call's own entry (item 1).
- Each filter returns exactly the matching entries, including a date range, an actor, several event types together, a target node type and a target tier; the pagination cursor walks a journal larger than one page without a gap or a repeat, in the stated order; a filter on the opaque pseudonym token works (items 1, 4).
- A Tier 2 grant is logged, a Tier 0 grant is not, a denial at every tier is logged, and no entry contains a content value (item 2, F323).
- The structural scan proves `.update(auditJournal)` only in the pseudonymizer and no `.delete(auditJournal)`, with a reported negative control (item 3a, F325); every generic write path is refused for `AuditEntry` even for the Owner with the journal unchanged, and the observed reasons are recorded (item 3b); `AuditEntry` is not replicable and not searchable (item 3c).
- The pseudonymization acceptance criterion is proven end to end and through `auditLog.query` (item 4).
- The overhead figures are reported against the 10 ms requirement (item 5).
- No file under `apps/`, no change to `packages/graph`, no interceptor change, no new event type, no new system principal, no migration, no dependency change (items 1 to 6).
- `pnpm verify` and `pnpm verify:full` pass, and the branch's `fast-lane` and `slow-lane` runs at the head commit both end green with every job the branch can run executing (`publish-artifacts` is main-only and is skipped on a branch).

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). `verify:full` runs `pnpm verify && pnpm verify:preflight && pnpm --filter @vulto/api test`; the api integration tests run against the real Postgres stack. Push the branch and quote both workflow run ids and their conclusions in the report. If the native vitest binding breaks after a reinstall, rely on CI rather than reinstalling repeatedly.

**What to check in the Stage 26 report:** that authorization goes through `authorizeRead`/the interceptor and no role list appears in `audit/query.ts` or the router entry (grep for `owner`, `hr-admin`); that the query is scoped to `workspace_id` in SQL and cannot read another workspace's entries (read the WHERE clause and the test); that the cursor is stable across entries sharing an `occurred_at` (read the tie-break and the test that seeds equal timestamps); that a denied call and a permitted call each wrote exactly one entry and the permitted call's own entry is absent from its own page; that the structural scan really fails when an `.update(auditJournal)` is added elsewhere (read the reported negative control, and change it locally); that no interceptor, `packages/graph`, `apps/`, migration or dependency file changed (`git diff --stat`); that the overhead numbers are real measurements with the sample size stated; and that the branch's fast-lane and slow-lane run ids in the report are real and green, read from the public API rather than from the report.

### Stage 27 — Leave Policy Engine, part 1: policy, resolution and accrual (`VRS-F018`)

`VRS-001`'s MVP order runs `VPS-F004` → `VRS-F018` (Leave Policy Engine, Roster, Compliance). It is one feature but two stages (F327): this stage builds the policy layer (`S01`, `S04`), policy resolution and the accrual and carry-over engine for `Immediate`, `Monthly` and `Annual` leave (`S03`, without `Earned`), server-side and pure-engine only. The overtime and TOIL half (`S05`: the `clearance_outcome` field, `overtime.approve`, `Earned` accrual, the re-flagging exemption) is Stage 28 and waits on a founder decision about where a TOIL accrual lives (F327); do not build any of it here. There is no UI (`S02` needs the shell and `VPS-F005`'s console) and no local/offline query (F333). It adds no node type, no edge type, no system principal and no cache table.

**Read this before anything else: report every contradiction once.** As in Stage 26: before writing product code, trace every Do item, every named file and every claim below against the repository (open each "existing X"; grep both server and client for every claim that something is shared or reused; run `node scripts/arch-check.mjs` against a scratch file at every new path with its exact imports; check which gate refuses first when a refusal reason is asserted), then write **one** report listing **all** the contradictions you found, take the obvious minimal resolution inside this brief's scope for each and record it under Micro-decisions, and stop for a ruling only if something blocks the whole stage.

**Findings F327 through F334, closed before this brief was written** (each has its own file in `docs/findings/`):

- **F327** — The overtime half cannot be built as specified (the only record of an overtime approval is a Tier 2 Manager-restricted flag the employee cannot read, yet TOIL is a live Tier 0 balance with no new node type). Ruled: split; Stage 27 is the policy layer and non-Earned accrual; S05 is Stage 28 behind a founder decision.
- **F328** — `LeavePolicy` resolves to `none` for Manager and Team Member under the `Standard` default. Ruled: an override row identical to `WorkingCalendar`'s.
- **F329** — A policy version has no effective date. Ruled: `effective_from` added to the schema.
- **F330** — The policy year is undefined. Ruled: the UTC calendar year at MVP.
- **F331** — Engine details are unspecified (partial first month, carry-over consumption, `expiringSoon`, negative balances). Ruled: Do item 4's rules.
- **F332** — `LeaveRequest` has no field schema, so `used` has nothing to read. Ruled: usage is an input; the server query passes none until `VRS-F019` (forward dependency).
- **F333** — The local, offline queries cannot be built yet. Ruled: server queries only, figures measured and reported.
- **F334** — The schema permits invalid TOIL and Earned combinations. Ruled: Earned if and only if TOIL, one entry per leave type, and a plain zero-with-a-note for Earned until Stage 28.

`docs/Vulto_Specs/VRS-F018_Leave_Policy_Engine.md` is corrected to match (the schema block carries `effective_from`, and eight Decisions Recorded entries record F327 to F334). See each finding's own file in `docs/findings/`.

1. **The `LeavePolicy` record and its access (F328, F329, F334).** New file `packages/schema/src/leave-policy.ts`, exported from `packages/schema/src/index.ts`, following the shape of the existing feature schemas (for example the rate-card one; find where `RateCard`'s fields schema and `rateCard.create` are defined and mirror them, do not invent a different layout): a strict Zod fields schema for the `LeavePolicy` node with the universal fields plus `name` (non-empty), `jurisdiction` (the `JURISDICTIONS` enum from `packages/schema/src/mutations/entity.ts`), `employment_type_scope` (`EMPLOYMENT_TYPES` from `employee.ts` or `"All"`), `leave_types` (a non-empty array of `{ leave_type: Annual | Sick | Casual | Unpaid | Parental | Bereavement | TOIL | Other, accrual_method: Immediate | Monthly | Annual | Earned, annual_entitlement_days (non-negative decimal), carryover_max_days (default 0), carryover_expiry_days (integer, default 90), minimum_notice_days (integer, default 0), requires_approval (default true), is_encashable (default false) }`, one entry per `leave_type`, `Earned` if and only if `TOIL`), `overtime_policy` (`{ requires_pre_approval false, toil_accrual_rate 1.0, toil_expiry_days 90, toil_max_accrued_days 10 }`, carried and validated only; nothing reads it this stage), `blackout_periods` (`{ start_date, end_date, reason, applies_to_leave_types: array or "All" }`, `start_date <= end_date`), `version` (integer from 1), `supersedes_id` (nullable UUID), `is_active` (default true) and `effective_from` (ISO date, F329). The registry entry (`LeavePolicy`, owner `VRS-F018`, fixed `Standard` Tier 0) and the `supersedes` edge pair (`LeavePolicy` to `LeavePolicy`) already exist; change neither. In `packages/schema/src/policy/policy-table.ts` add the matrix override row `LeavePolicy` identical to `WorkingCalendar`'s (Owner and HR Admin `FULL_ANY`, Finance Admin, Manager and Team Member `READ_ANY`), keeping the fidelity test reading the literal matrix. Add `LeavePolicy` to `FEATURE_LIFECYCLE_NODE_TYPES` (`packages/schema/src/mutations/employee.ts`) so the generic node mutations refuse it. Do not add `supersedes` to `FEATURE_OWNED_EDGE_TYPES` unless tracing shows the rate-card lineage relies on the opposite; report what you find.
2. **The policy mutations.** `leavePolicy.create` and `leavePolicy.update`, defined in `packages/schema` beside the other definitions and implemented in a new `services/api/src/mutations/leave-policy.ts` (a path under `mutations/`, the only folder the graph-write gate allows), mirroring `rateCardCreate` and `rateCardUpdate` in `services/api/src/mutations/rate-card.ts` (read them first): `create` writes one version-1 node (`supersedes_id` null, `is_active` true, `effective_from` per F329); `update` never edits a node: it writes a new node with `version` plus one, `supersedes_id` set to the prior, a `supersedes` edge as the rate-card mutation writes it, `effective_from` (default today's UTC date, never earlier than it), and sets the prior version's `is_active` false, all in one transaction, and rejects a stale or non-latest prior with the stale-state result the pipeline already uses. Both are Tier 0, `onlineOnly` (configuration is written online, like the rate card; confirm how `defineMutation` treats `onlineOnly` for a Tier 0 mutation before choosing). Authorization is the interceptor's decision for `LeavePolicy` (`full` for Owner and HR Admin only), never a role list in the mutation. No client optimistic mutator this stage unless the rate card has one and the offline declaration requires it (report what you find).
3. **Policy resolution.** `leavePolicy.getApplicable(employee_id)` and `leavePolicy.listConflicts(workspace_id)` as tRPC queries on the app router in a new `leavePolicy` entry, with input schemas in `packages/schema`, backed by new `services/api/src/permission/leave-queries.ts` (the folder rate-card, timesheet and skill-matcher queries live in). A policy matches an employee when it is `is_active`, its `jurisdiction` equals the jurisdiction of the entity in force for the employee (`resolveForEmployee` in `services/api/src/graph/entity-resolution.ts`, as of the query date) and its `employment_type_scope` is the employee's `employment_type` or `All`. Among matches take the most recently created (by `created_at`, then `node_id` for a tie) and report the others as a conflict. `getApplicable` returns `{ policy: LeavePolicy | null, conflicting_policy_ids: string[] }`, `null` when nothing matches or the employee has no entity; it must refuse an employee the caller may not read exactly as it refuses a nonexistent one, using `filterReadable` and the existing derived-read pattern (mirror `listSubmissionStatus` in `timesheet-queries.ts`; read it). `listConflicts` returns `{ employee_id, policy_ids }[]` for the employees the caller can read who match more than one active policy; it is a configuration view, so it requires the same `full` on `LeavePolicy` that writing does (Owner and HR Admin), derived from the policy table through `resolvePolicyCell` and not a role list (F315's lesson: open the code that would provide any gate before relying on it). Report the measured p95 of `getApplicable` against the 20 ms requirement.
4. **The accrual and carry-over engine (F330, F331, F332, F334).** New pure module `packages/schema/src/leave-balance.ts`, exported from the index, no database and no I/O: `computeLeaveBalance(input, dependencies) -> { entitledYtd, carriedOver, used, remaining, expiringSoon, notes }` where `input` is `{ employee: { start_date }, policyVersions (the lineage, each with its fields and effective_from), leave_type, as_of_date, usage: { leave_type, start_date, end_date }[] }` and `dependencies.countWorkingDays(from, to)` returns the employee's working days in an inclusive range (the server passes `countWorkingDays` from `services/api/src/graph/working-days.ts`, which returns `{ days, hours }`; use `days`). All dates are UTC calendar dates and every count is working days: no calendar-day arithmetic anywhere in the module (`G05`; a test greps the module for `86400000` and `getTime` differences and fails on them, and a second test proves a UAE Friday and Saturday weekend request deducts only working days through an injected counter). The policy year is the UTC calendar year (F330). Rules, per policy year: **Immediate** grants the full entitlement of the version in force on the later of the year's first day and the employee's start date; **Annual** grants the full entitlement once, on the year's first day (or the start date if later), by the version in force then; **Monthly** accrues, for each complete month on or before the as-of date, `entitlement ÷ 12` by the version in force at that month's end, and prorates a partial first month by working days (`entitlement ÷ 12 × working days from start date to month end ÷ working days in that month`, F331a); the month in progress accrues nothing. **Carry-over:** at a policy year's end, unused days (entitled plus carried, less used) up to `carryover_max_days` roll into the next year and stop counting at `policy-year start + carryover_expiry_days`; the excess is forfeited, never retained (F331b). Usage is attributed by date and consumes carried days first, then the year's entitlement. `expiringSoon` is the unused carried days that expire within 30 days after the as-of date (F331c). A negative `remaining` is returned with a `notes` entry naming the cause and size (F331d). A policy change applies forward only: each step uses the version in force on its own date, so a balance viewed after an update leaves earlier accrual unchanged (F329). An `Earned` type returns `entitledYtd: 0` and a `notes` entry that earned accrual is not built yet (F334). Never cache or store a balance (`G04`).
5. **The balance query.** `leaveBalance.compute(employee_id, leave_type, as_of_date?)` as a tRPC query (new `leaveBalance` router entry, input schema in `packages/schema`), backed by `leave-queries.ts`: authorize through the same derived-read pattern as item 3 (the caller must be able to read the employee, and refuse an unreadable employee exactly as a nonexistent one), resolve the applicable policy and its lineage (the versions linked by `supersedes_id`), build the input, pass an **empty usage list** (F332: `LeaveRequest` has no schema; the query's module comment and the stage report say so plainly) and call the engine with the server's `countWorkingDays` and the employee's `start_date`. `as_of_date` defaults to today's UTC date. Report the measured p95 against the 200 ms requirement, stating the fixture size, and state that it is a server figure (F333).
6. **Tests.** Engine unit tests in `packages/schema/src/leave-balance.test.ts` (no database) for every acceptance criterion the stage can prove: 14 days monthly, six complete months in, is 7 days entitled to date; carried days expire on day 91 of a 90-day expiry; a policy update leaves prior accrual unchanged and applies forward; the UAE-weekend deduction counts working days only; a partial first month is prorated by working days; carry-over caps and forfeits; carried days are consumed first; `expiringSoon`; a negative balance carries a note; Immediate and Annual; an Earned type returns zero with its note. Schema tests for `leavePolicy` validation (`Earned` only for `TOIL`, one entry per type, blackout ordering, defaults). Server integration tests in the `*.integration.test.ts` style against the real Postgres stack: create and update version the lineage (the prior node is unchanged except `is_active`, the `supersedes` edge exists, a stale update is rejected); only Owner and HR Admin can write, every role can read a policy (Manager and Team Member get `full`-less `read` through the new row), a non-writer's `graph.*` and `leavePolicy.*` attempts are refused; `getApplicable` picks the most recent policy and reports the conflict, returns `null` for an employee with no matching policy or entity, and refuses an unreadable employee like a missing one; `listConflicts` is refused for a Manager and a Team Member and lists the right employees for HR Admin; `leaveBalance.compute` end to end through the router with a real employee, entity, calendar and policy lineage. Policy-table fidelity and conformance suites pass unchanged except for the new row.
7. **Docs and the stage report.** Write `docs/stage-reports/STAGE-27_Leave_Policy_Engine_Part_1.md` in the eleven-section format of Part 2 with the measured facts the reviewer will check: the contradictions you found before coding (or "none"), the input and output schemas, the measured p95s and their fixture sizes, what you found in the rate-card precedent for the questions items 1 and 2 leave open, and the exact files changed. Do not edit `VRS-F018` (the reviewer corrected it) or any reviewer-owned document.

**Done criteria:**
- `LeavePolicy` has its strict schema (with `effective_from`, Earned-if-and-only-if-TOIL, one entry per type), the override policy row, and the feature-lifecycle guard, and every role can read a policy while only Owner and HR Admin can write (items 1, 2).
- `leavePolicy.create` and `update` version the lineage through `supersedes` in one transaction, never edit a prior version but for `is_active`, reject a stale update, and take their authorization from the interceptor, not a role list (item 2).
- `getApplicable` and `listConflicts` implement the F-ruled matching, return `null` and conflicts as specified, refuse an unreadable employee like a nonexistent one, and gate `listConflicts` by the policy table (item 3).
- The engine is pure, uses working days only with a passing guard test, and satisfies every listed acceptance criterion (item 4, 6).
- `leaveBalance.compute` runs end to end through the router, passes no usage until `VRS-F019`, says so, and reports its measured p95 (item 5).
- No UI, no client mutator or query, no `S05` code (no `clearance_outcome`, no `overtime.approve`), no new node or edge type, no system principal, no cache table, no `CACHE_SCHEMA_VERSION` change, no interceptor change, no dependency change (items 1 to 7).
- `pnpm verify` and `pnpm verify:full` pass, and the branch's `fast-lane` and `slow-lane` runs at the head commit both end green with every job the branch can run executing (`publish-artifacts` is main-only and is skipped on a branch).

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). Push the branch and quote both workflow run ids and their conclusions in the report. If the native vitest binding breaks after a reinstall, rely on CI rather than reinstalling repeatedly.

**What to check in the Stage 27 report:** that no role list appears in `leave-policy.ts`, `leave-queries.ts` or the router entries (grep for `owner` and `hr-admin`); that `update` leaves the prior node untouched except `is_active` and writes the `supersedes` edge as the rate card does (diff the two mutations); that `leave-balance.ts` has no I/O import and no calendar-day arithmetic, and that the guard test really fails when one is added (change it locally); that the acceptance-criterion tests compute the spec's own numbers (7 days at six months, 91-day expiry, forward-only change, Friday and Saturday weekend) rather than restating the implementation; that a partial first month is prorated by working days; that `getApplicable` and `leaveBalance.compute` refuse an unreadable employee identically to a missing one; that the balance query passes no usage and the report says so; that nothing of `S05` leaked in (`git grep clearance_outcome overtime.approve`); and that the branch's fast-lane and slow-lane run ids in the report are real and green, read from the public API rather than from the report.

---

## Part 4 — After Stage 7

---

## Part 4 — After Stage 7

## Part 4 — After Stage 7

Stage 8 was the priority slice of the feature-spec sweep (FDN-104) and the first Roster feature, the canonical Employee profile (RST-33); it merged as `5897b7b`, F130 closed, F215 confirmed, F214 ruled. Stage 9 (above) was meant to be that ruling's build, the Reconnect shell state — but tracing it before implementation surfaced F216: the shell was never wired to the real backend at all (`FDN-69`, never built). Stage 9 is now the minimal real shell bootstrap F216 ruled, and the original Reconnect brief is renumbered Stage 10, unblocked once Stage 9 lands. The remaining 40-odd FDN-104 specs are corrected just-in-time, before each one's own implementation stage — never in one large pass, per FDN-104 itself.

---

## Part 5 — What to do right now

Stages 1–11 are merged (Stage 11 as `96d53eb`). Stage 12 — Working Calendar and Working Patterns (`VRS-F004`) — is briefed above (Part 3), server-side only. F225 and F226 were ruled the day the brief was written; F227 through F231 were raised and ruled resuming the brief a second time; F232 through F234, raised resuming it a third time (`bc21a52`), and F235, raised during real implementation once the first `pattern_for` write reached the server interceptor (`e4a9599`), are now ruled too and folded into the brief above — AE's template corrected (F232), `entity.create`'s optimistic handler extended to match its server-side calendar write (F233), reduced-hours periods given a write path through `calendar.update` (F234), and `pattern_for` given the `governingPartitions: { Employee: "operational" }` declaration F208 already established the pattern for (F235). `VRS-001`'s Feature Register confirms it as the correct next MVP-order feature after `VRS-F003`. Resume implementation on `codex/stage-12-working-calendar` (currently at `e4a9599`, real partial implementation preserved at `7e64708`) on the fully corrected brief — do not restart the branch this time; move `RST-40` back from Blocked to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**23 September 2026 — Stage 12 reviewed directly against the finished code and merged `--no-ff` as `0249d5c`.** `RST-40` is Done. `VRS-001`'s Feature Register confirms `VRS-F005` (The Bench Forecast) as the correct next MVP-order feature. Briefing it surfaced and closed F236, F237 and F238 before any code was written; resuming four more times surfaced and closed F239 (the Pitch-day exclusion's unbuilt `VRS-F009`/`VRS-F010` dependency), F240 (`assignment.cancel`'s missing state-transition version), F241 (the bench aggregate locality contradiction with `VPS-A004`'s server-only disclosure-control rule) and F242 (the undefined `filters` schema shared between the local query and the new server aggregate) — see the Stage 13 section above and `docs/Foundations_Findings.md`. `codex/stage-13-bench-forecast` already exists (pushed at `1a45be1`, `RST-41` moved to Blocked when F242 was raised) — resume on it rather than restarting; bring `docs/Foundations_Findings.md`, `docs/Vulto_Specs/VRS-F005_The_Bench_Forecast.md`, `docs/Claude_Code_Build_Prompt.md` and `docs/Reviewer_Handoff.md` onto the branch from `main` as-is (only `docs/Foundations_Findings.md` will conflict with the branch's own F242-open edit — resolve by taking `main`'s version, the same resolution Stage 12 used for F235 and Stage 13 has now used three times, for F239, F240 and F241). Move `RST-41` back from Blocked to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**23 September 2026 — Stage 13 reviewed directly against the finished code and merged `--no-ff` as `d9b6d74`.** `RST-41` is Done. Codex resumed on the F239–F242 rulings, built the Bench Forecast for real (branch `codex/stage-13-bench-forecast`, tip `ab2708b`), and reported all gates green. Independent review confirmed F237's `resolveWorkingDay`, F238's disclosure control, F240's version-check-first `assignment.cancel` and F242's `matchesBenchForecastFilters`/employee-ID-free `getAggregate` all directly against the code; one test-coverage gap (HR Admin never exercised against the Contextual Intelligence Panel's protected fields) was found and closed at `9f553ef` before merge. `docs/Reviewer_Handoff.md` section 13 records the full review outcome. `VRS-001`'s Feature Register confirms `VRS-F006` (Rate Card Engine) as the correct next MVP-order feature — checking it directly against the code before writing its brief surfaced and closed F243 (Assignment's registered but unwritten `governed_by` edge to RateCard) and F244 (`VRS-F006`'s stale local-first sync/retention-window language) — see the Stage 14 section above and `docs/Foundations_Findings.md`. Start a new branch `codex/stage-14-rate-card-engine` from the current `main` (`990e902`, carrying the F243/F244 corrections). Move `RST-42` to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**23 September 2026 — Stage 14 reviewed directly against the finished code and merged `--no-ff` as `04f2a7a`.** `RST-42` is Done. Independent review read all 23 changed non-doc files and the real-Postgres integration suite's own assertions line by line, confirmed F243 through F247 exactly as ruled, and additionally verified one unflagged but correctly-scoped shared-code fix Codex made along the way (`services/api/src/permission/reader-set.ts`'s "own"-scope resolution, previously poisoning any subject-less fixed-class node — dormant until RateCard, the first Finance-restricted node type any stage had actually built reads/mutations for, exercised it). `docs/Reviewer_Handoff.md` section 14 records the full outcome. `origin/main` and all local/remote branches were then brought current and cleaned (26 branches down to the ones still in flight or deliberately archived) — see `docs/Reviewer_Handoff.md`'s top summary. `VRS-001`'s Feature Register confirms `VRS-F007` (Ghost Resources) as the correct next MVP-order feature — checking it directly against the code before writing its brief surfaced and closed F248 (the undocumented forward dependency on `VRS-F028`'s unbuilt `OpenRole`) and F249 (the promotion API's underspecified `existingEmployeeId` branch) — see the Stage 15 section above and `docs/Foundations_Findings.md`. Start a new branch `codex/stage-15-ghost-resources` from the current `main`. Move `RST-43` to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**23 September 2026 — Stage 15 blocked once, resumed the same day.** Codex traced the brief into the mutation registry and stopped on F250 before writing any product code: `ghostResource.cancel`/`promote` carried no `expectedVersion` (A003-T54), and the brief's "the transition guard gives G07's exclusivity for free" reasoning only holds for sequential requests, not concurrent ones under `pipeline.ts`'s default transaction isolation. Ruled the same day (full reasoning above and in `docs/Foundations_Findings.md`): both mutations gain `expectedVersion`; `ghostResource.promote` joins the serializable-isolation list; a version mismatch on `promote` returns G07's actor-and-time rejection when the cause is an existing promotion, the ordinary `stale-state` rejection otherwise. `codex/stage-15-ghost-resources` (currently at `355a08f`, F250's own finding commit, no product code yet) resumes on the corrected brief — do not restart the branch. Move `RST-43` back from Blocked to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**23 September 2026 — Stage 15 blocked a second time, resumed the same day.** Codex traced the brief's permission claims into the actual policy table before writing any product code and stopped on F251: the brief said `GhostResource`/`OpenRole` had zero `MATRIX_OVERRIDES` entries, but Stage 13 had already added and tested explicit overrides for both (`f2d928d`), and deleting them would drop Manager/Team Member to `none`, not read, while leaving Manager unable to create a Ghost at all — contradicting `VRS-F007`'s own Creating-a-Ghost flow. Ruled the same day (full reasoning above and in `docs/Foundations_Findings.md`): the Stage 13 overrides stay; `GhostResource`'s `manager` cell alone is extended to `FULL_ANY()`; `team-member` and all of `OpenRole` are untouched; the resulting Manager write access to `promote`/`cancel` is accepted as this codebase's node-type-scoped permission model working as designed, not a new gap; the shared policy test is split so `GhostResource` gets its own assertions. `codex/stage-15-ghost-resources` (currently at `dcdfc88`, F251's own finding commit, still no product code) resumes on the corrected brief — do not restart the branch. Move `RST-43` back from Blocked to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**24 September 2026 — Stage 15 blocked three more times, resumed each same day, then reviewed and merged.** Resuming a third time surfaced F252 (the required `VPS-F004` promotion audit event has no home in the closed taxonomy, ruled on F221/F225's own precedent); resuming a fourth time surfaced F253 (Ghost creation and promotion could not satisfy Employee's required identity fields, resolved with a Ghost-only record constructor and an extended promotion contract). Codex then built the real implementation (preserved at `dae1d01`) and its first real permission-interceptor integration test exposed F254 (`promoted_to` declared no governing Employee partition, structurally refused by the interceptor for every role), closed the same day on F208/F235's own precedent. `codex/stage-15-ghost-resources` resumed each time without a branch restart. Independent review then read the finished code directly against every ruling — not the completion report alone: the F254 fix, zero `appendAudit` calls, `requireUnique` reuse, the full promotion transaction, and genuine (not mocked) real-PostgreSQL concurrency/rollback tests were all re-verified. Approved and merged `--no-ff` as `4081e9f`. `RST-43` is Done. `VRS-001`'s Feature Register confirms `VRS-F008` (Capacity Conflict Resolution) as the correct next MVP-order feature — checking it directly against the code and against `VPS-A006`'s own architecture decisions before writing its brief surfaced and closed F255 (`Assignment.manager`'s policy contradicting `VPS-A004`'s own table), F256 (`validateCapacity`'s calendar-day aggregation contradicting G02 and the "reused, not reimplemented" claim), F257 (the audit claim with no home in `VPS-F004`'s closed taxonomy) and F258 (the Depends-On omission of `VPS-A006`'s unbuilt job-scheduling infrastructure) — see the Stage 16 section above and `docs/Foundations_Findings.md`. Start a new branch `codex/stage-16-capacity-conflict-resolution` from the current `main` (carrying the F255–F258 corrections). Move `RST-44` to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**24 September 2026 — Stage 16 reported complete by Codex (`58c02be`, `pnpm verify:full` green, 278 tests), but independent review of the finished implementation directly against every one of F255–F262's rulings found one genuine gap before merge: F263.** `services/api/src/permission/conflict-resolution-queries.ts`'s `sweepOvercommitted` reads every Active Employee/Assignment in the workspace directly via `getNodes` and returns every overcommitted employee to any authenticated workspace member — no `filterReadable` call, no role check, at either the query or its `router.ts` wiring (`conflictResolution.sweepOvercommitted`, gated only on workspace match). `services/api/src/permission/employee-queries.ts`'s `listEmployees` (the identical `Employee` node type) and `ghost-resource-queries.ts`'s `listGhostResources` both already pipe their rows through `filterReadable(tx, principal, rows, context)` before returning them; `sweepOvercommitted` is the only workspace-wide listing query in the codebase to skip it, exposing `Employee:operational` data — row-scoped for Manager (`Full (direct reports)`) and Team Member (`Read (own + team)`) per `policy-table.ts` — to every workspace member regardless of role or relationship to the employee. Every other file checked in this review (interceptor, edge registry, `assignment.ts`, `conflict-resolution.ts`, `mutators/foundation.ts`, `queries/conflict-check.ts`, `policy-table.ts`, `pipeline.ts`, `router.ts`, `founding.ts`, the schema package's new files, and the full integration/unit test suites) matched its ruling exactly, several with more rigorous proof than required — F263 is the sole finding.

Traced directly to `listEmployees`/`listGhostResources`'s own precedent — unlike F262, no founder consultation needed. Ruled the same day: `sweepOvercommitted` gains a `context: InterceptorContext = {}` parameter matching those two queries' own signatures, and filters its `employees` array through `filterReadable(tx, principal, employees, context)` before iterating — the identical call `listEmployees` already makes on the identical node type. No policy-table change, no new mechanism. Extend `services/api/src/permission/capacity-conflict.integration.test.ts` with two cases: `sweepOvercommitted` called as the fixture's existing `outsider` (team-member, no relationship to the overcommitted employees) omits every employee the outsider cannot read; called as a genuine Manager, it returns only that Manager's own direct reports among the overcommitted set. Full reasoning in `docs/Foundations_Findings.md`'s F263 and `docs/Vulto_Specs/VRS-F008_Capacity_Conflict_Resolution.md`'s corrected Security Considerations.

`codex/stage-16-capacity-conflict-resolution` (currently at `58c02be`) resumes on this one-file fix — do not restart the branch. `RST-44` stays In Review; move it back to In Progress while the fix lands, then back to In Review once `pnpm verify:full` is green again. When the fix is pushed and gates pass, stop and wait for the founder to complete final review and merge, same as every stage boundary.

**24 September 2026 — F263 fixed by Codex (`56581cf`), independently re-verified directly against the code, and Stage 16 merged `--no-ff` as `e705aad`.** `sweepOvercommitted` now filters its Employee list through `filterReadable`, matching `listEmployees`'s own call exactly; new PostgreSQL coverage proves an unrelated team-member outsider sees no overcommitted employees and a genuine Manager sees only their own direct reports. The existing Owner-scoped read-only/Ghost-parity/local-server-parity tests were confirmed unmodified. `RST-44` is Done. `docs/Reviewer_Handoff.md` section 16/17 records the full outcome.

**24 September 2026 — Stage 16 fully closed (see above).** `VRS-001`'s Feature Register confirms `VRS-F009` (Time Classification Taxonomy) as the correct next MVP-order feature. Checking it directly against the code before writing its brief surfaced and closed F264 (Pitch had no creation mutation, no staffing relationship, and no read-access policy row, despite the spec's flows depending on all three) — traced directly to F251's and F262's own precedent, no founder consultation needed — see the Stage 17 section above and `docs/Foundations_Findings.md`. `docs/Vulto_Specs/VRS-F009_Time_Classification_Taxonomy.md` corrected to match. Start a new branch `codex/stage-17-time-classification-taxonomy` from the current `main`. File `RST-45` and move it to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**24 September 2026 — Codex closed the one verification gap Stage 17's review had disclosed rather than glossed over.** The reviewing environment had no live PostgreSQL and a Node/native-binding mismatch, so `pnpm verify:full` was not independently re-run there; every test's assertions were read directly and judged sound, but the live run itself was taken on the report's word. Codex re-ran `pnpm verify:full` in its own correctly-provisioned environment (Node `v24.18.0`, live PostgreSQL) at `8a91a01`, reporting the same 284 API tests passed / 2 skipped the Stage 17 report originally claimed — genuine terminal output, not re-asserted from the report. Also acknowledged the founder's ruling: escalate-and-wait is standing practice for every future stage; Codex reports build-time findings in the stage report and Linear only, and does not edit `docs/Foundations_Findings.md`, `docs/Claude_Code_Build_Prompt.md`, or any `docs/Vulto_Specs/*.md` file directly going forward.

**24 September 2026 — Stage 17 fully closed (see above).** `VRS-001`'s Feature Register confirms `VRS-F010` (Timesheet Speed-Run) as the correct next MVP-order feature. Checking it directly against the code before writing its brief surfaced and closed two more findings the same day, no founder consultation needed either time: F267 (`logged_against`'s Pitch pair declared no `governingPartitions`, the identical omission F265 found in `staffed_on`) and F268 (`rowScopeSatisfied` had no resolution branch for TimesheetEntry's or TimesheetAnomalyFlag's subject Employee, the identical structural gap F264 found for Pitch) — see the Stage 18 section above and `docs/Foundations_Findings.md`. `docs/Vulto_Specs/VRS-F010_Timesheet_Speed-Run.md` corrected to match, along with four scoping decisions settled on established precedent (the `overtime_flag_threshold` hardcoded-pending-`VPS-F005` note, `hrCompliance.sendReminder`'s full deferral to `VPS-F003`, the `ApprovedOvertime`/`VRS-F018` boundary, and the post-transaction anomaly-evaluation timing). Start a new branch `codex/stage-18-timesheet-speed-run` from the current `main`. File `RST-46` and move it to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 — Stage 18 reviewed directly against the finished code and merged `--no-ff` as `1389d18`.** `RST-46` is Done. `VRS-001`'s Feature Register confirms `VRS-F011` (Billable vs Non-Billable Pulse) as the correct next MVP-order feature. Checking it directly against the code before writing its brief surfaced and closed F277 and F278 the same day; Codex then traced the brief into the interceptor before writing product code and stopped on F279, F280 and F281, all ruled the same day — F281 escalated as a genuine architectural fork, resolved after a robustness challenge corrected the reviewer's first-pass recommendation — see the Stage 19 section above and `docs/Foundations_Findings.md`. `docs/Vulto_Specs/VRS-F011_Billable_vs_Non-Billable_Pulse.md` and `docs/Vulto_Specs/VPS-A004_Graph_Permission_Layer.md` corrected to match. Start a new branch `codex/stage-19-billable-non-billable-pulse` from the current `main`. File `RST-47` and move it to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 — Stage 19 reviewed directly against the finished code and merged `--no-ff` as `0e7e4d9`.** Codex's completion report (`0631c91`, all four gates passed, `verify:full` at 301 tests passed / 2 skipped) was independently verified against the actual diff and both integration test suites, not accepted from the report's prose — every one of F279–F281's rulings, the `SYSTEM_OPERATION_TARGETS` registry, `calculateUtilization`'s formula and the shared `expectedWeek` helper all confirmed directly, no discrepancies found. `RST-47` is Done. `docs/Reviewer_Handoff.md` section 20 records the full outcome. `VRS-001`'s Feature Register confirms `VRS-F012` (Revenue Gap Alert) as the correct next MVP-order feature — checking it directly against the code before writing its brief surfaced and closed F282, F283 and F284 the same day, no founder consultation needed for any of them — see the Stage 20 section above and `docs/Foundations_Findings.md`. `docs/Vulto_Specs/VRS-F012_Revenue_Gap_Alert.md` corrected to match. Start a new branch `codex/stage-20-revenue-gap-alert` from the current `main`. File `RST-48` and move it to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 — Stage 20 reviewed directly against the finished code and merged `--no-ff` as `c2583a5`.** Codex's completion report (`1a7071b`, all four gates passed, `verify:full` at 308 tests passed / 2 skipped) was independently verified against the actual diff and every new/modified test file, not accepted from the report's prose — F283's shared `benchDaysForWindow` Pitch exclusion, the `revenue-gap-alert-evaluate` system-principal write path, the `SYSTEM_OPERATION_TARGETS`/`triggered_by` edge-clause generalization (a correct extension not explicitly specified in the brief), and G05–G09 all confirmed directly against real Postgres-backed tests, no discrepancies found. `RST-48` is Done. `docs/Reviewer_Handoff.md` section 21 records the full outcome, including a stale `.git/index.lock` from an interrupted first merge attempt, cleared and resolved with no data loss. `VRS-001`'s Feature Register confirms `VRS-F013` (Skill-to-Project Matcher) as the correct next MVP-order feature — checking it directly against the code before writing its brief surfaced and closed F285, F286 and F287 the same day, no founder consultation needed for any of them — see the Stage 21 section above and `docs/Foundations_Findings.md`. `docs/Vulto_Specs/VRS-F013_Skill-to-Project_Matcher.md` and `packages/schema/src/registry/edges.ts` corrected to match. Start a new branch `codex/stage-21-skill-to-project-matcher` from the current `main`. File `RST-49` and move it to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 — Stages 21 and 22 merged; Stage 23 briefed.** Stage 21 (`VRS-F013`, Skill-to-Project Matcher) merged `--no-ff` as `414b8df` (`RST-49` Done) and Stage 22 (`VRS-F014`, Skill Matrix) merged `--no-ff` as `cd17db0` with no findings on completion review (`RST-50` Done); `docs/Reviewer_Handoff.md` sections 22 and 23 record both. `VRS-001`'s Feature Register confirms `VPS-F002` (Local-First Search) as the correct next MVP-order feature after `VRS-F014` — `VRS-F015` is permanently vacant, promoted to the suite. Checking it directly against the code before writing its brief surfaced and closed F297 through F300 the same day: F297 (the spec's SQLite FTS5 index cannot be built — the pinned `wa-sqlite` has no FTS module; a genuine architectural fork, decided by the founder in favor of a trigger-maintained `cache_search` table, no dependency change), F298 (`search.query` cannot return the server-side `adHocSearch`'s result locally), F299 (only four of the spec's seven searchable types can be registered now, and `search.queryProtected` has no eligible node type) and F300 (the interceptor-filtering language and command availability have no local mechanism) — the last three closed on existing precedent, no founder consultation needed — see the Stage 23 section above and `docs/Foundations_Findings.md`. `docs/Vulto_Specs/VPS-F002_Local-First_Search.md` corrected to match. Start a new branch `codex/stage-23-local-first-search` from the current `main`. File `RST-51` and move it to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 (later) — Stage 23 stopped before product code on F301, ruled the same day.** Codex traced the brief against `package.json` and `.github/workflows/slow-lane.yml` and found the brief's claim that `verify:full` runs a device-store browser suite false (the brief, not the code, was wrong). Ruled: `pnpm test:sync-browser` is an additional required gate for Stage 23, with one added assertion in the existing `sync.spec.ts` — see F301 in `docs/Foundations_Findings.md` and Stage 23's Do item 8 and Gates above. `codex/stage-23-local-first-search` (currently at `fa51a3c`, finding note and blocked report only, no product code) resumes on the corrected brief — do not restart the branch. `RST-51` moves back from Blocked to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 (later still) — Stage 23 stopped a second time before product code on F302, ruled the same day.** Resuming on F301, Codex traced Do item 8's named Employee fixture and found it does not exist (`seedProtected`'s Employee is nameless), and tracing it exposed that a trigger inserting a `NOT NULL` label from an absent name would abort `putNode` and break replication. The brief, not the code, was wrong again. Ruled: the triggers index a node only when its label field is a non-empty string; `seedProtected` gets a named Employee via one re-export line in `services/api/src/test/sync-browser-support.ts` (the single `services/api/` exception); the assertions live in the existing role-removal test — see F302 in `docs/Foundations_Findings.md` and Stage 23's Do items 2, 7 and 8. `codex/stage-23-local-first-search` (currently at `7f87ae6`, finding notes and blocked report only, no product code) resumes on the corrected brief — do not restart the branch. `RST-51` moves back from Blocked to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 (later still) — Stage 23 stopped a third time before product code on F303, ruled the same day.** Resuming on F302, Codex traced the label guard against the real Ghost writer and found it excluded every real Ghost Employee (`full_name` is deliberately null; the role title is `job_title`). The brief, not the code, was wrong a third time, and the reviewer audited the rest of the brief's field assumptions against the code in the same pass. Ruled: `labelFields` is an ordered list (`Employee`: `full_name`, then `job_title`), the label is the first non-empty text, Ghost tests use `ghostEmployeeOperationalRecord`, and `operationalShape` (or a key list derived from it) is exported from `employee.ts` — see F303 in `docs/Foundations_Findings.md` and Stage 23's Do items 1, 2, 3, 7 and 8. `codex/stage-23-local-first-search` (currently at `11684a6`, finding notes and blocked report only, no product code) resumes on the corrected brief — do not restart the branch. `RST-51` moves back from Blocked to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 (later still) — Stage 23 stopped a fourth time on F304, ruled the same day.** Codex built the partial implementation (preserved at `90ba633`, focused tests 24/24), ran `pnpm verify`, and it failed at `arch:check`: the test re-export F302 authorized violates A003-T52's `graph/store` import boundary. The brief, not the code or the gate, was wrong a fourth time. Ruled: `addNode` in `services/api/src/permission/test-support.ts` gains an optional `fields` parameter (the only `services/api/` change), `services/api/src/test/sync-browser-support.ts` is restored to `main`'s version, `seedProtected` calls `addNode(..., { ... })`, and no gate or allowlist changes — see F304 in `docs/Foundations_Findings.md` and Stage 23's Do item 8. `codex/stage-23-local-first-search` (currently at `90ba633`, partial implementation preserved) resumes on the corrected brief — do not restart the branch or discard that work. `RST-51` moves back from Blocked to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 (later still) — Stage 23 stopped a fifth time, on F305, ruled the same day; F305 stays open.** The F304 build passed `pnpm verify` and `pnpm verify:full` and its new assertions passed in the real browser, but the full `pnpm test:sync-browser` ended 18 passed, 9 failed: the suite's `seedEntities` and `newEntity` create an `Entity` through `graph.createNode`, rejected as `requires-feature-mutation` since Stage 11, so the suite has been red on `main` since 22 September and F301's "full suite passes" gate was unsatisfiable. Ruled: not repaired in Stage 23 (its own Linear issue); the gate becomes a baseline comparison against `main` (identical failing set, every other test passing), and the role-removal test gains a real-browser sentinel check for `cache_search` — see F305 in `docs/Foundations_Findings.md`, Stage 23's Gates paragraph and Do item 8(d). `codex/stage-23-local-first-search` (currently at `9f50931`, F304 correction and blocked report, partial implementation complete) resumes on the corrected brief — do not restart the branch. `RST-51` moves back from Blocked to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**25 September 2026 (later still) — Stage 23 reviewed directly against the finished code and merged `--no-ff` as `b50ab85`.** Codex's completion report (`95115d5`; `pnpm verify` and `pnpm verify:full` green, 319 API tests passed / 2 skipped, graph 97 and schema 111 unit tests, search p95 0.90 ms; `pnpm test:sync-browser` recorded as an identical-baseline comparison, the same nine failures by name and message shape on clean `main` and on the branch, 18 passed on each) was independently verified against the actual diff and the checks the reviewer's shell can run (`arch-check`, `tsc --noEmit`, graph and schema vitest, prettier, eslint) — no findings; see `docs/Reviewer_Handoff.md` section 24. `RST-51` is Done. F305 (the sync browser suite has been red on `main` since Stage 11) stays open; its repair is `FDN-126`.

**26 September 2026 — F305 settled into a brief: Stage 24, the sync browser suite repair (`FDN-126`).** The founder made the repository public (the private-repository Actions allowance had run out), which let the reviewer read the Actions history through the public API: the slow lane's last green `main` run was `250a60a` (22 September 09:34 UTC), it went red at `e0c05e4` (Stage 11) in the `sync-browser` job's A003-T56 gate step and stayed red on every slow-lane push to `main` since, with `publish-artifacts` skipped; and from about 24 September 20:34 UTC every job in both workflows also failed to start (the Actions allowance), so the fast lane's `pnpm verify` has not run on CI for the Stage 20 to 23 merges. Ruled: the suite is seeded with `Client`, not a feature-owned type, a fast-lane guard test fails the moment that stops being valid, the A003-T56 gate gets a negative control, and CI itself is the acceptance gate — see the Stage 24 section above and F305's 26 September update. Start a new branch `codex/stage-24-sync-browser-suite-repair` from the current `main`. Move `FDN-126` to In Progress. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary. `VPS-F003` (Notification and Alert Center) becomes Stage 25.

**26 September 2026 (later) — Stage 24 reviewed directly against the finished code and CI, merged `--no-ff` as `cd73619`.** Codex's report (`3f31993`: 27 of 27 locally after one browser-closure correction, the Entity guard and the A003-T56 sentinel negative control both shown to fail as intended and then pass) was verified against the actual diff — a pure rename plus the seed-type constant, no skip, fixme, retry or grep exclusion — and against the branch's GitHub Actions runs read directly from the public API: `fast-lane` and `slow-lane` both green at the head commit, every job executing, `sync-browser` passing its A003-T56 gate and its full suite for the first time since 22 September, `api-integration` passing on CI over the Stage 20 to 23 code. No findings. `FDN-126` is Done. F305 closes when `main`'s own next slow lane is green with `publish-artifacts` running, which needs `main` pushed. `VPS-F003` (Notification and Alert Center) is Stage 25 and is briefed after that.

**26 September 2026 — Stage 25 briefed (`VPS-F003`, Notification and Alert Center); F305 and F306 closed.** `main`'s slow lane at `70a4af1` ended green with `publish-artifacts` executing for the first time. Checking `VPS-F003` against the code before writing the brief surfaced F307 through F312, all closed on the spot: `Notification`'s recipient-only access is unresolvable by any code (a new `recipient` scope is added), the pipeline exposes only changed row ids (a post-commit, idempotent, failure-isolated engine with `dedupe_key`), only two of the eleven rules have a built event source, and email, push, muting and the Inbox screen are deferred. Stage 25 builds the data layer and delivers Stage 18's deferred `hrCompliance.sendReminder`. See Part 3. A fifth check while writing the brief added F312 (`TimesheetAnomalyFlag` is Tier 2, so its rule can carry no flag field).

**26 September 2026 — Stage 25 reviewed directly against the finished code and merged `--no-ff` as `e52fef0`.** `RST-52` is Done. Codex stopped before code eight times on findings F313 to F320, every one a defect in this brief, each ruled the same day, and then built the stage; the independent review found no blocking finding (F321 recorded, F322 open and low). Fast-lane 36249811914 and slow-lane 36249811907 at `095c190` were green. See `docs/Reviewer_Handoff.md` section 26.

**26 September 2026 — Stage 26 briefed (`VPS-F004`, Silent Audit Log).** Most of the feature already exists from Stages 3, 5 and 23; the brief builds `auditLog.query` and proves the rest by test. Checking the spec against the code surfaced F323 through F326, all closed on the spot: `VPS-A004` and `VPS-F004` disagree on whether successful Tier 2 access is logged (the code logs it; `VPS-F004` is corrected), `auditLog.query` does not exist and its contract is open, immutability is structural only (a database guard is deferred behind the hash chain), and the review screen and erasure orchestration are not buildable. The brief asks the builder to report every contradiction once, before coding, rather than stopping at each. See Part 3.

**26 September 2026 — Stage 26 reviewed directly against the finished code and merged `--no-ff` as `fc7ef42`.** Linear `FDN-127` is Done. Asking the builder to trace everything and report all contradictions once produced one honest report (nine micro-decisions, none needing a ruling) instead of eight single-issue stops; no finding. Fast-lane 36252277110 and slow-lane 36252277118 at `1c90a91` were green. See `docs/Reviewer_Handoff.md` section 27. `VRS-F018` (Leave Policy Engine) is Stage 27 and is briefed next.

**26 September 2026 — Stage 27 briefed (`VRS-F018`, part 1: policy, resolution and accrual).** The feature is split in two (F327): the overtime and TOIL half cannot be built as specified (the only record of an overtime approval is a Tier 2 Manager-restricted flag the employee cannot read, yet TOIL is a live Tier 0 balance with no new node type) and waits on a founder decision. Checking the spec against the code surfaced F327 through F334, all closed by ruling. See Part 3.
