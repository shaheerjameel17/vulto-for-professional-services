# Build prompt — Stages 1 to 22 (archived)

Merged stages, moved verbatim from `docs/Claude_Code_Build_Prompt.md` Part 3 on 26 September 2026. The live document keeps the current and most recent stages.

### Stage 1 — Alignment, baseline and local stack

**Linear:** none moved to In Progress. Comment on FDN-102 when done.
**Branch:** `stage-1-alignment`

**Read, in this order:** `CLAUDE.md`; VPS-002; VPS-A003 in full; VPS-A001; VPS-A004 "Where enforcement runs" plus its technical specifications; VPS-A007 gates 5 and 6; Foundations_Findings F199–F202; this document again.

**Do:**

1. **Push existing history:** `git push origin main archive/local-first-e2e fdn-68-audit-wip`. If the push fails for credentials, stop and report BLOCKED.
2. **Baseline:** run `pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify` and `pnpm verify:full`. Record every result verbatim in the report as the baseline. Fix nothing yet.
3. **Postgres logical replication.** In `docker-compose.yml`, give the `postgres` service:
   `command: ["postgres", "-c", "wal_level=logical", "-c", "max_replication_slots=10", "-c", "max_wal_senders=10"]`
   **CI is not changed in Stage 1.** GitHub Actions service containers cannot take a command (F203); Stage 6 item 0 gives CI its own Postgres image.
4. **Retire the Rust relay from the default stack without deleting it.** Give the `sync-engine` compose service `profiles: ["retired"]`, so `pnpm stack:up` no longer builds Rust. Stage 7 deletes it.
5. **Add Electric** to `docker-compose.yml`:
   - service name `electric`; image `electricsql/electric`, using the latest stable **1.x** release as of the day you run this;
   - pin it by digest: `docker pull electricsql/electric:<version>`, then `docker inspect --format='{{index .RepoDigests 0}}'`;
   - host port `5133` mapped to container port `3000` (host port 3000 is Next.js's);
   - environment: `DATABASE_URL: postgres://vulto:vulto@postgres:5432/vulto`, `ELECTRIC_SECRET: ${ELECTRIC_SECRET}`;
   - `depends_on` Postgres healthy;
   - if the image contains a shell with `curl` or `wget`, add a healthcheck on `/v1/health`; otherwise document the host-side check, as the old sync-engine comment did.
   - Record the version and digest in VPS-A001's "Sync client selection" section, replacing "Versions are pinned exactly…" with the actual pins.
6. **Environment variables.** Add to `.env.example` with safe local defaults, and to `turbo.json` `globalPassThroughEnv`:
   - `ELECTRIC_URL=http://localhost:5133`
   - `ELECTRIC_SECRET=` a 32-character local-only value
   - `VULTO_KEY_PROVIDER=local`
   - `VULTO_LOCAL_ROOT_KEY=` a base64 32-byte local-only value, labeled as never for production
   - `AWS_REGION=`, `VULTO_KMS_ROOT_KEY_ARN=` (empty locally)
7. **Update the compose header comment** to describe the new stack: Postgres with logical replication, Redis, Electric; sync-engine retired under the `retired` profile.
8. **Create `docs/stage-reports/`** and write the Stage 1 report into it.

**Done criteria:**
- `main`, `archive/local-first-e2e` and `fdn-68-audit-wip` exist on origin.
- `pnpm stack:up` starts Postgres, Redis and Electric, and does not build Rust.
- `docker compose exec postgres psql -U vulto -c "SHOW wal_level"` returns `logical`.
- Electric's `/v1/health` responds from the host on port 5133.
- The baseline of `pnpm verify` and `pnpm verify:full` is recorded, and neither is worse than before the stage.

**Gates:** the standard four, plus the psql and health checks above.

---

### Stage 2 — The canonical PostgreSQL graph store

**Linear:** FDN-94.
**Branch:** `stage-2-graph-store`

**Read:** VPS-A003 "The canonical store"; VPS-A002 "Universal node conventions", "Universal edge conventions", "The edge storage contract", "Privacy classes and tiers", "Schema Evolution Protocol"; `packages/schema/src/records.ts`; `packages/schema/src/registry/*`; `services/api/src/auth/workspace-projection.ts`; `services/api/src/auth/schema.ts`.

**Build:**

1. **New Drizzle schema file** `services/api/src/graph/schema.ts`. Add it to `drizzle.config.ts` so `schema` is an array: `["./src/auth/schema.ts", "./src/graph/schema.ts"]`.
2. **One migration**, generated with `pnpm --filter @vulto/api db:generate`, then hand-appended with the SQL Drizzle cannot express. It creates:
   - `CREATE EXTENSION IF NOT EXISTS btree_gist;`
   - **`graph_nodes`**
     - `node_id uuid not null`, `workspace_id uuid not null`, **primary key `(workspace_id, node_id)`**, `node_type text not null`, `lifecycle_status text not null`, `schema_version integer not null`, `version bigint not null default 1`
     - `is_soft_deleted boolean not null default false`
     - `created_at timestamptz not null`, `created_by uuid`, `updated_at timestamptz`, `updated_by uuid`, `soft_deleted_at timestamptz`, `soft_deleted_by uuid`
     - `record jsonb not null`
     - check constraints: `record->>'node_id' = node_id::text`; `record->>'lifecycle_status' = lifecycle_status`
     - index `(workspace_id, node_type, lifecycle_status) where not is_soft_deleted`
     - partial unique index on `(node_id) where node_type <> 'User'` — every node ID except a `User`'s is globally unique
     - check `node_type <> 'Workspace' or workspace_id = node_id`
   - **`graph_edges`**
     - `edge_id uuid primary key`, `workspace_id uuid not null`, `edge_type text not null`
     - `from_node_id uuid not null`, `to_node_id uuid not null`, with foreign keys `(workspace_id, from_node_id)` and `(workspace_id, to_node_id)` referencing `graph_nodes(workspace_id, node_id)`, so an edge can never cross workspaces
     - `effective_from timestamptz`, `effective_to timestamptz`, `version bigint not null default 1`, `is_soft_deleted boolean not null default false`
     - the same created/updated/soft-deleted columns as `graph_nodes`
     - `record jsonb not null`, with check `record->>'edge_id' = edge_id::text`
     - indexes `(workspace_id, edge_type, from_node_id, effective_from, effective_to)` and `(workspace_id, edge_type, to_node_id, effective_from, effective_to)`
     - partial unique index `(edge_type, from_node_id) where effective_to is null and not is_soft_deleted and edge_type in ('managed_by','scoped_to_entity')`
     - exclusion constraint `exclude using gist (from_node_id with =, edge_type with =, tstzrange(effective_from, effective_to, '[)') with &&) where (not is_soft_deleted and edge_type in ('managed_by','scoped_to_entity'))`
   - **The Workspace node's `workspace_id` equals its own `node_id`.**
   - **A `User` node is one row per workspace**, `node_id` = the Better Auth `user.id`, its `record` exactly as `records.ts` defines it (no `workspace_id` field). Only the membership projection writes `User` rows; the store refuses a `User` write from any other caller.
   - **No foreign key to Better Auth tables.** Consistency with them is enforced in code, in the same transaction.
3. **What goes in `record`.** The complete Tier 0 partition record, validated by the existing Zod record schemas in `packages/schema/src/records.ts`:
   - for a node type whose registry tier is 0, the whole record;
   - for a split node type, the Tier 0 partition only;
   - for a node type whose every partition is Tier 1, 2 or 3, only the universal fields (no content). Content arrives in Stage 5.
   - Edges: every edge is a row in `graph_edges`, including edges with a protected endpoint. Metadata that belongs to a protected tier is not written in this stage.
4. **Store module** `services/api/src/graph/store.ts` — internal, not an API. Every function takes a Drizzle transaction as its first argument:
   - `insertNode(tx, record)`, `updateNodeFields(tx, nodeId, expectedVersion | null, patch)` (increments `version`), `softDeleteNode(tx, nodeId, actor)`
   - `insertEdge(tx, record)`, `closeEdge(tx, edgeId, effectiveTo, actor)`
   - `getNode(tx, workspaceId, nodeId)`, `getNodes(tx, workspaceId, filter)` — **every** read and write takes `workspaceId`; there is no workspace-less lookup
   - `outgoing(tx, nodeId, edgeType, at?)`, `incoming(...)`
   - `traverse(tx, startNodeId, edgeTypes, maxDepth)`, implemented with a recursive CTE
   - Every write validates against the registry: node type registered, edge triple registered, record passes its Zod schema. Invalid input throws a typed `GraphValidationError`, never a raw database error.
   - **No permission logic here.** Stage 3 adds the interceptor above this layer.
5. **Architecture rule.** Extend `scripts/arch-check.mjs` so that `services/api/src/graph/store.ts` may be imported only from `services/api/src/{graph,permission,mutations,protected,audience,jobs}/**`. Anything else fails `pnpm arch:check`.
6. **Workspace creation writes the Postgres graph.** In the existing `workspace.create` flow, write the five founding records into `graph_nodes` and `graph_edges` (Workspace node, the Owner's User node, the WorkspaceMembership node, `membership_of`, `membership_in`), **in the same Postgres transaction** as the central Better Auth membership row. Leave the existing device-projection path in place: **it is a temporary dual write that Stage 7 removes.** Mark it with a `// F199: removed in Stage 7 (FDN-103)` comment.

**Tests** — Vitest integration tests against the real Postgres, following the existing `db-preflight` pattern:
- every node type in `NODE_REGISTRY` round-trips through `insertNode`/`getNode`, using a minimal valid record built from the registry;
- inserting an edge with an unregistered triple throws `GraphValidationError`;
- two open `managed_by` edges from one employee → rejected by the database;
- overlapping closed intervals for `scoped_to_entity` → rejected by the exclusion constraint;
- `updateNodeFields` with a stale `expectedVersion` → typed `StaleVersionError`, and `version` unchanged;
- `workspace.create` rolls back graph rows if the Better Auth insert fails, and the reverse;
- `traverse` returns correct depth-limited results on a five-level `managed_by` chain;
- one person creating two workspaces produces two `User` rows with the same `node_id`, each in its own workspace, and an edge from workspace A to a node in workspace B is rejected by the foreign key;
- inserting a non-`User` node whose `node_id` already exists in another workspace is rejected;
- the arch-check rule fails when a file outside the allowed folders imports the store (a fixture file created in the test's temp directory).

**Done criteria:**
- The migration applies cleanly on an empty database and on the current development database.
- All tests above pass.
- `pnpm arch:check` enforces the import rule.
- `workspace.create` is atomic across Better Auth and graph rows.

**Gates:** the standard four, plus `pnpm --filter @vulto/api db:migrate` on a fresh database.

---

### Stage 3 — The server-side permission interceptor and audit journal

**Linear:** FDN-98; FDN-89 (partial — see item 5); FDN-68 (server half).
**Branch:** `stage-3-interceptor`

**Read:** VPS-A004 in full; VPS-F004; `packages/graph/src/worker/permission/*`; on branch `fdn-68-audit-wip`: `packages/schema/src/audit.ts`, `packages/schema/src/audit.test.ts`, `services/api/src/auth/audit-journal.ts`, `services/api/src/auth/audit-pseudonymizer.ts`, `services/api/drizzle/0010_nosy_zzzax.sql`, `services/api/drizzle/0011_wild_next_avengers.sql`, and the FDN-68 comments on Linear.

**Build:**

1. **Move the pure policy code into `packages/schema/src/policy/`:** `policy-table.ts`, `matrix-coverage.ts` and every pure helper they need, plus their unit tests. Export them from `@vulto/schema`. Replace the originals in `packages/graph/src/worker/permission/` with **re-export shims**, so the retired worker still compiles until Stage 7. Behavior must be byte-for-byte identical: the moved unit tests pass unchanged.
2. **Principals** — `services/api/src/permission/principal.ts`. One discriminated union:
   - `member { userId, workspaceId, membershipId, roles }` — roles come from the central Better Auth member row, read on every request (this closes F127);
   - `support { grantId, workspaceId, scope, expiresAt }` — type and evaluation only; nothing issues support grants yet (FDN-106);
   - `system { name, workspaceId }` — `name` is one of a closed enum: `audience-recompute`, `retention-sweep`, `erasure`, `key-rotation`.
   Each system principal's permitted operations are listed in the policy table and nowhere else.
3. **Derived Manager scope.** Manager is derived from active `managed_by` edges, read from Postgres at request time. It applies only where a User↔Employee identity link exists. **That link does not exist yet (VRS-F002 / RST-33)**, so Manager scope currently resolves to nothing. Implement the lookup against a single function `resolveEmployeeForUser(tx, workspaceId, userId): Promise<string | null>` that returns `null` today, with a comment naming RST-33. Do not invent a link.
4. **The interceptor** — `services/api/src/permission/interceptor.ts`:
   - `authorizeRead(tx, principal, target)`, `authorizeWrite(tx, principal, target, change)`, `filterReadable(tx, principal, rows)`, `authorizeTraversal(...)`;
   - VPS-A004's graph traversal rules;
   - `None` versus `Restricted` outcomes per A004-T18/T19;
   - the three write gates in order: role → write authority → empty reader set (A004-T17);
   - aggregate disclosure control stays as currently implemented; port it if it lives in the moved code.
5. **FDN-89, partial.** Implement subject exclusion and concrete reader-set resolution against `resolveEmployeeForUser`. While it returns `null`, any protected write needing a concrete reader set is refused with reason `reader-set-unresolvable`, per A004 ("refused or deferred rather than guessed at"). F130 remains open and must be restated in the report.
6. **tRPC wiring.** A tRPC context that resolves the Better Auth session and the active workspace into a `member` principal; a `protectedProcedure` that requires one; the principal is never taken from client input.
7. **Server audit journal** (FDN-68, server half):
   - Take, with `git checkout fdn-68-audit-wip -- <path>`, **only**: `packages/schema/src/audit.ts`, `packages/schema/src/audit.test.ts`, `services/api/src/auth/audit-journal.ts`, `services/api/src/auth/audit-pseudonymizer.ts`, and the two migrations.
   - Move the journal and pseudonymizer to `services/api/src/audit/`.
   - Re-generate the migration numbering so it follows Stage 2's migration. Keep the SQL content.
   - Change the append path so an audit event is written **inside the caller's transaction** (`appendAudit(tx, event)`), per A003-T59. Remove the device outbox and sealed-journal assumptions; keep idempotency on `audit_entry_id` and "no update or delete path".
   - Every denial, and every Tier 1, 2 or 3 grant decided by the interceptor, calls `appendAudit` in the same transaction.
   - F198: reserve `AuditEntry` from every generic write path in the new interceptor. The only writer is `appendAudit`; the only post-append change is the pseudonymization operation.
   - **Do not build the hash chain.** That is FDN-108, later.
8. **Port the permission matrix suite** (A004-T07): every role × privacy class in the default mapping, and every role × node type in the matrix, now against `services/api/src/permission`. Coverage must not fall below the current suite's; report both counts.

**Done criteria:**
- The moved unit tests pass unchanged; the retired worker still compiles.
- The ported matrix suite passes against the server interceptor, with a case count ≥ the old suite's.
- A role change takes effect on the very next request, with no cache and no restart.
- Every denial and every protected grant produces exactly one audit row, in the same transaction.
- `AuditEntry` cannot be written through any generic path.
- Support and system principals are evaluated and audited.
- The report restates F130 as open.

**Gates:** the standard four.

---

### Stage 4 — The named-mutation pipeline

**Linear:** FDN-95.
**Branch:** `stage-4-mutations`

**Read:** VPS-A003 "Writes", "Conflict resolution", A003-T53, T54, T64, T69, T71.

**Build:**

1. **Definitions** — `packages/schema/src/mutations/`:
   `defineMutation({ name, input: ZodSchema, tier: 0 | 1 | 2, onlineOnly: boolean, stateTransition: boolean })`. A registry `MUTATIONS` of all definitions. `onlineOnly` is forced to `true` whenever `tier > 0`.
2. **The foundation mutation set** — exactly these, no feature mutations:
   - `graph.createNode`, `graph.updateNodeFields`, `graph.softDeleteNode`, `graph.createEdge`, `graph.closeEdge` — Tier 0, registry-validated, the building blocks features will wrap;
   - `graph.transitionLifecycle` — `stateTransition: true`, carries `expected_version`;
   - `org.moveEmployee` — serializable transaction; rejects a cycle; closes the prior `managed_by` edge's `effective_to` and opens the new edge's `effective_from` together; effective date from input, never the clock.
3. **Mutation log table** `graph_mutations`: `mutation_id uuid primary key`, `workspace_id uuid not null`, `actor_user_id uuid`, `name text not null`, `args_sha256 text not null`, `outcome jsonb not null`, `created_at timestamptz not null default now()`.
4. **Server pipeline** — `services/api/src/mutations/pipeline.ts`. For each mutation, in one transaction, in this order:
   1. idempotency check (step 1 below);
   2. authorize (Stage 3 interceptor write gates);
   3. registry and Zod validation;
   4. domain rule (the mutation's server implementation);
   5. `appendAudit` where required;
   6. `audience.onRowsChanged(tx, changedRowIds)` — an interface with a **no-op** implementation now, implemented in Stage 6;
   7. insert into `graph_mutations`;
   8. commit.
   On any failure the whole transaction rolls back, and a rejection outcome is recorded in its own small transaction.
   - **Idempotency:** same `mutation_id` and same `args_sha256` → return the stored outcome with status `duplicate`. Same id with different args → reject `mutation-id-conflict`.
   - **Stale state:** `stateTransition` mutations compare `expected_version` with `graph_nodes.version`; a mismatch → reject `stale-state`. Every successful update increments `version`.
   - **Schema version:** clients send header `x-vulto-schema-version`. Below `MIN_CLIENT_SCHEMA_VERSION` (a new exported constant in `packages/schema`, set to `1`) → tRPC error code `PRECONDITION_FAILED`, reason `client-outdated`.
5. **tRPC** — `graph.applyMutations({ mutations: Array<{ mutation_id, name, args }> })`, maximum 100 per call. Mutations are processed **in order**, each in its own transaction. Returns `Array<{ mutation_id, status: 'applied' | 'duplicate' | 'rejected', reason?, result? }>`. Processing stops at the first rejection; the remaining mutations are returned as `rejected` with reason `blocked-by-earlier-rejection`, so client ordering is preserved.
6. **Client halves** — `packages/graph/src/mutators/`: an optimistic implementation for each foundation mutation, operating on the Stage 6 cache interface. In this stage, test them against an in-memory implementation of that interface only.

**Tests:**
- replay of the same `mutation_id` applies once and returns `duplicate`;
- same id with different args → `mutation-id-conflict`;
- an approve/reject race on one node via `graph.transitionLifecycle` → exactly one `applied`, the other `stale-state`;
- `org.moveEmployee` rejects A→B→A and a longer cycle, and keeps history half-open and non-overlapping;
- a mutation denied by the interceptor writes an audit row and no graph change;
- an ordered batch stops at the first rejection with the specified statuses;
- `client-outdated` is returned below the minimum version;
- a Tier 1 mutation definition is `onlineOnly` even if declared otherwise.

**Done criteria:** all tests pass; no path writes `graph_nodes` or `graph_edges` except the pipeline and the Stage 2 `workspace.create` transaction. Extend `pnpm arch:check` to enforce this.

**Gates:** the standard four.

---

### Stage 5 — Protected data and field-level encryption

**Linear:** FDN-96; FDN-97 (erasure only — blob encryption waits for object storage, FDN-70).
**Branch:** `stage-5-protected-data`

**Read:** VPS-A003 "Data protection tiers", "The encryption architecture", "Cryptographic erasure", A003-T55, T56, T59–T62, T73; VPS-A006 logging rules; VPS-F007 "What erasure actually does, per tier".

**Build:**

1. **Tables:**
   - **`protected_data_keys`**: `key_id uuid primary key`, `workspace_id uuid not null`, `kind text not null check (kind in ('kek','dek'))`, `tier smallint check (tier in (1,2))`, `erasure_domain_id uuid`, `parent_key_id uuid references protected_data_keys(key_id)`, `root_key_ref text`, `wrapped_key bytea`, `created_at timestamptz not null default now()`, `destroyed_at timestamptz`.
     - A KEK has `root_key_ref` and no parent. A DEK has a parent KEK.
     - Unique: one live Tier 2 DEK per workspace; one live Tier 1 DEK per `(workspace_id, erasure_domain_id)`.
   - **`graph_protected_fragments`**: `fragment_id uuid primary key`, `workspace_id uuid not null`, `owner_kind text not null check (owner_kind in ('node','edge'))`, `owner_id uuid not null`, `schema_partition text not null`, `tier smallint not null check (tier in (1,2))`, `erasure_domain_id uuid not null`, `data_key_id uuid not null references protected_data_keys(key_id)`, `nonce bytea not null`, `ciphertext bytea not null`, `header jsonb not null`, `version bigint not null default 1`, created/updated columns; unique `(owner_kind, owner_id, schema_partition)`.
2. **Key providers** — `services/api/src/crypto/`:
   - interface `KeyProvider { wrapKek(plain: Uint8Array, ctx): Promise<{ wrapped, rootKeyRef }>; unwrapKek(wrapped, rootKeyRef, ctx): Promise<Uint8Array> }`;
   - `AwsKmsKeyProvider` using `@aws-sdk/client-kms` (pinned exactly), `Encrypt`/`Decrypt` with `EncryptionContext = { workspace_id }`;
   - `LocalKeyProvider`: AES-256-GCM under `VULTO_LOCAL_ROOT_KEY`, with the workspace ID as AAD;
   - the factory selects by `VULTO_KEY_PROVIDER`; it **throws at startup** if `NODE_ENV=production` and the provider is `local` (A003-T73);
   - no other code imports `@aws-sdk/client-kms`. Add an arch-check rule.
3. **Envelope encryption:**
   - DEK: 32 random bytes. Content: AES-256-GCM with a fresh 12-byte random nonce per encryption, via Node `crypto`.
   - AAD = `canonicalize(header)` (RFC 8785), with header fields exactly: `format: "vulto:protected-fragment:v1"`, `workspace_id`, `owner_kind`, `owner_id`, `owner_type` (node or edge type), `schema_partition`, `tier`, `erasure_domain_id`, `data_key_id`.
   - DEKs are wrapped by the KEK with AES-256-GCM and AAD `canonicalize({ format: "vulto:dek:v1", key_id, workspace_id, tier, erasure_domain_id })`.
4. **Erasure domain:** default = the owner node's ID. Add `ERASURE_DOMAIN_OVERRIDES` to `packages/schema`, empty today; features add entries when built.
5. **Key cache:** unwrapped KEKs and DEKs held in process memory only; TTL ≤ 5 minutes; maximum 1,000 entries; never logged or serialized.
6. **Workspace key provisioning:** `workspace.create` creates the workspace KEK inside its existing transaction. It shows the Owner nothing.
7. **Write path:** `writeProtected(tx, owner, partition, value)` used by the pipeline; Tier 1 and Tier 2 mutation input is written here, never into `graph_nodes.record`.
8. **Read path** — tRPC `protected.read({ node_ids: uuid[] (max 500), partitions?: string[] })`. Per fragment:
   1. interceptor `authorizeRead`;
   2. `appendAudit` (a failure withholds the data);
   3. decrypt;
   4. return.
   Set the HTTP response header `Cache-Control: no-store`. A destroyed key → the fragment is returned as state `erased`, with no content.
9. **Job principal wrapper** — `services/api/src/jobs/principal.ts`: `runAsPrincipal(principal, fn)`. A `member` principal is re-resolved at execution; if the grant was withdrawn, throw `PrincipalWithdrawnError` before any read. There is no jobs service yet (FDN-57); build the wrapper and its tests only.
10. **Cryptographic erasure** — `services/api/src/protected/erasure.ts`, callable only by the `erasure` system principal: set `wrapped_key = NULL` and `destroyed_at = now()` for the domain's DEKs, evict them from the cache, and `appendAudit`. **Backup key expiry is an operations task (FDN-58)**; list it under Known limitations.
11. **Decryption reachability** (A007-T08): arch-check rule — the decrypt function is importable only from `protected/read.ts`, `jobs/principal.ts` and `protected/erasure.ts`.
12. **Logging:** if `services/api` has a logging redaction allowlist, extend it to every protected field. If it does not, add a test that captures Fastify logger output during `protected.read` and asserts that no plaintext sentinel value appears.

**Tests:**
- a dump of both tables (`SELECT *`) contains no plaintext sentinel;
- a fragment's ciphertext copied onto another owner fails authentication;
- a denied read writes an audit row and returns nothing;
- a forced audit-append failure withholds the data;
- erasing one employee's domain leaves another employee's content readable;
- `runAsPrincipal` for a demoted member throws before reading;
- the production guard rejects `LocalKeyProvider`;
- `AwsKmsKeyProvider` is unit-tested with a mocked KMS client (no network in tests).

**Done criteria:** all of the above; the arch-check rules pass; VPS-A003's acceptance criteria for dump safety, fragment moving and erasure are cited with test names.

**Gates:** the standard four.

---

### Stage 6 — Sync: audience, shape proxy, device cache, outbox, offline

**Linear:** FDN-100, FDN-99, FDN-101.
**Branch:** `stage-6-sync`

**Read:** VPS-A003 "Reads", "Offline behavior", "Revocation", A003-T56–T58, T63–T67, T72; VPS-A001 "Sync client selection" and "Local graph query layer"; VPS-F001 "Session expiry and offline access", "Device revocation", G04; Electric's documentation on shapes, subqueries, auth proxy and the TypeScript client, for the pinned version.

**Server:**

0. **CI Postgres image (F203).** Create `.docker/ci-postgres/Dockerfile`: `FROM` the same `postgres:17-alpine@sha256:742f40ea…` digest the workflows use today, with `CMD ["postgres", "-c", "wal_level=logical", "-c", "max_replication_slots=10", "-c", "max_wal_senders=10"]`. Extend `.github/workflows/ci-image.yml` to build and publish it as `ghcr.io/<repo>/ci-postgres`, triggered by changes under `.docker/ci-postgres/**`, exactly as the CI image is. Add `CI_POSTGRES_IMAGE=<digest pin>` to `.github/workflows/ci-image-ref.env`, and have `slow-lane.yml`'s `resolve-image` job output it, so every Postgres `services:` entry uses `${{ needs.resolve-image.outputs.postgres_image }}` instead of the literal digest. Publishing needs a merge to `main` followed by a repin, as the CI image README describes: do the Dockerfile and workflow change first, stop and report so the founder can merge it, then repin once the digest exists. Until the repin, the Stage 6 browser suite runs locally only; say so in the report.

1. **Audience tables:** `sync_node_audience (workspace_id uuid, user_id uuid, node_id uuid, primary key (workspace_id, user_id, node_id))` and `sync_edge_audience (workspace_id uuid, user_id uuid, edge_id uuid, primary key (workspace_id, user_id, edge_id))`. Identifiers only. (Keyed by workspace because a `User` node's `node_id` repeats across workspaces, per F204.)
2. **Materializer** — `services/api/src/audience/`:
   - implements `audience.onRowsChanged` from Stage 4: for each touched Tier 0 node or edge, and each active member of the workspace, insert or delete audience rows so they equal `interceptor.authorizeRead` for that member;
   - `recomputeWorkspace(tx, workspaceId)` — a full recompute, run as the `audience-recompute` system principal, called on every membership, role or `managed_by` change;
   - only Tier 0 node types, and edges whose both endpoints are Tier 0, are ever eligible.
3. **Electric database role and publication:**
   - a migration creates the role `vulto_electric` with `REPLICATION`, `LOGIN` and `SELECT` on exactly `graph_nodes`, `graph_edges`, `sync_node_audience` and `sync_edge_audience`, and a publication `electric_publication_default` containing exactly those four tables;
   - Electric connects as `vulto_electric`, with manual table publishing enabled, using the environment variable Electric documents for that in the pinned version;
   - **if the pinned version cannot run with a pre-created publication and a SELECT-only role, STOP with a finding.**
   - The role's password comes from a new environment variable `ELECTRIC_DB_PASSWORD`.
4. **Shape proxy** — Fastify route `GET /v1/shape/:template`, with `template ∈ { nodes, edges }`:
   - requires a valid Better Auth session, an active membership in the session's workspace, and a non-revoked device;
   - sets `table`, `where` and `params` server-side:
     - nodes: `workspace_id = $1 AND node_id IN (SELECT node_id FROM sync_node_audience WHERE workspace_id = $1 AND user_id = $2)`
     - edges: the same, over `graph_edges` and `sync_edge_audience`;
   - forwards only Electric's protocol parameters (`offset`, `handle`, `live`, `cursor`) from the client, adds `ELECTRIC_SECRET` server-side, and streams the response back;
   - any client-supplied `table`, `where`, `columns` or `params` → `400`;
   - a revoked device or removed membership → `401` with body `{ "code": "access-revoked", "erase": true }`.

**Client** — inside `packages/graph/src/sync-client/`. The public API is exported from `@vulto/graph`:

5. **Host:** a SharedWorker `sync-worker.ts` shared by every tab. Where `SharedWorker` is unavailable, a dedicated Worker that holds `navigator.locks.request("vulto-sync:<workspaceId>:<userId>")`; non-holding tabs read the same database and receive change notifications over `BroadcastChannel("vulto-sync:<workspaceId>:<userId>")`.
6. **Cache database:** `wa-sqlite` with `IDBBatchAtomicVFS`, database name `vulto:<workspaceId>:<userId>`, with tables:
   - `cache_nodes (node_id primary key, node_type, lifecycle_status, is_soft_deleted, version, record_json)`
   - `cache_edges (edge_id primary key, edge_type, from_node_id, to_node_id, effective_from, effective_to, is_soft_deleted, version, record_json)`
   - `sync_cursor (template primary key, handle, offset)`
   - `outbox (seq integer primary key autoincrement, mutation_id unique, name, args_json, undo_json, status check in ('pending','inflight','rejected'), reason, created_at)`
   - `session_hint (singleton primary key check (singleton = 1), user_id, workspace_id)` — identifiers only, used to open the cache offline. **No tokens, ever.**
7. **Replication:** `@electric-sql/client` `ShapeStream` against the proxy for both templates. Apply inserts, updates and deletes into the cache tables. Persist `handle` and `offset`. On `must-refetch`, clear that table and resync.
8. **Outbox:** `mutate(name, args)`:
   1. generate a `mutation_id`;
   2. run the optimistic client mutator (Stage 4) against the cache, recording before-images in `undo_json`;
   3. append to `outbox`;
   4. upload in `seq` order via `graph.applyMutations`.
   - `applied` or `duplicate` → delete the outbox row (replication delivers the authoritative row).
   - `rejected` → apply `undo_json`, mark `rejected` with the reason, and surface `NeedsAttention`.
   - Network failure → leave the row `pending` and retry with exponential backoff (1 s doubling to 60 s).
   - `onlineOnly` mutations are refused immediately while offline, with reason `requires-connection`.
9. **Query layer:** port `packages/graph/src/query.ts`'s typed query API so it runs against `cache_nodes` and `cache_edges`, preserving its public types. Recursive traversal via recursive CTE. Availability outcome per A001-T07: `mid-sync | requires-connection | permission-absence | ready`.
10. **Protected values:** `protectedRead(nodeIds)` calls `protected.read`. Results are held **only in worker memory**, keyed by node and partition, and cleared on sign-out, workspace switch, role change and `access-revoked`. `prefetchProtected(nodeIds)` warms the same memory store.
11. **SyncStatus observable:** `Synced | Syncing | PendingChanges | Offline | NeedsAttention`.
12. **Erasure on revocation and sign-out:** delete the IndexedDB database and the in-memory protected store, then emit `Offline` with a signed-out state.
13. **Public API:** `createGraphClient({ workspaceId, userId, apiOrigin })` → `{ query, subscribe, mutate, protectedRead, prefetchProtected, syncStatus, signOut }`.

**Tests:**
- **Server:**
  - the audience conformance suite (A003-T57): for every role × privacy class on a synthetic fixture workspace, audience ⊆ interceptor-permitted, and interceptor-permitted Tier 0 ⊆ audience;
  - the publication allowlist test (queries `pg_publication_tables`);
  - the proxy rejects `table` and `where`;
  - the proxy returns `access-revoked` for a removed member.
- **Browser** — new Playwright config `packages/graph/playwright.sync.config.ts`, against the real stack:
  1. first sign-in replicates and a query returns the member's rows;
  2. **cold offline boot**: after the network is cut and the page reloaded, queries still return rows and SyncStatus is `Offline`;
  3. three offline Tier 0 mutations, then reconnect → each applied **exactly once** (assert on `graph_mutations`);
  4. two tabs: a mutation in tab A appears in tab B without a reload;
  5. a queued state transition rejected as `stale-state` → reverted locally and `NeedsAttention`;
  6. removing the member's role → the forbidden rows disappear from the cache on next connection;
  7. device revocation → the IndexedDB database is gone;
  8. **no protected data on the device**: seed Tier 1 and Tier 2 sentinel values, exercise `protectedRead`, then scan every IndexedDB database and every cache table for the sentinels — none are found.

**Done criteria:** every test above passes; VPS-A003's acceptance criteria for offline boot, queued writes, stale-state, the device-storage scan and revocation are cited with test names.

**CI:** add an `electric` service to the slow-lane job that runs this suite, using the same pinned digest and the `vulto_electric` role, and run the suite there once item 0's repin has landed.

**Gates:** the standard four, plus `pnpm exec playwright test --config packages/graph/playwright.sync.config.ts`.

---

### Stage 7 — Retire the local-first implementation and harden CI

**Linear:** FDN-103, FDN-54.
**Branch:** `stage-7-retire`

**Do:**

1. **Delete:**
   - `services/sync-engine/`, `rust-toolchain.toml`, the `sync-engine` compose service, every Rust CI job, and the sync-engine image publish step;
   - `packages/graph/src/worker/**` except anything Stage 6 still imports (there should be nothing);
   - the re-export shims from Stage 3;
   - `packages/graph/src/sync/**` (the old relay client);
   - `services/api/src/auth/device-unlock.ts` and the per-workspace unlock secret (keep the device registry, revoke and retire);
   - `services/api/src/auth/sync-ticket.ts` if nothing in Stage 6 uses it;
   - the diagnostics pages under `apps/roster-web/src/app/*-diagnostics/`, `LockedShellGate`, and the `next.config.ts` aliases for them;
   - `services/api/browser-tests-device-store/`, its Playwright config and its root scripts;
   - the `pretest:sync-engine`, `test:sync-engine`, `stage5:opacity-fixtures`, `*device-store*` and `*worker-browser*` root scripts.
2. **Remove the temporary dual write** in `workspace.create` (Stage 2 item 6).
3. **Remove dependencies** `loro-crdt` and `shamir-secret-sharing`. Keep `wa-sqlite`, `canonicalize` and `zod`.
4. **Drizzle:** a migration dropping the `device_unlock_secret` table and any other table that only the deleted code used. List each one in the report.
5. **CI**, per VPS-A007: the fast lane runs `pnpm verify`; the slow lane runs `pnpm verify:full` plus the Stage 6 Playwright suite and the existing auth browser suite. Add the gates:
   - audience conformance
   - publication allowlist
   - shape proxy tests
   - the no-protected-data-on-device browser test
   - the arch-check rules from Stages 2, 4 and 5
6. **FDN-54 adversarial suite** — `services/api/src/test/adversarial/`, covering:
   - a forged `mutation_id` replay;
   - a principal supplied in client input (must be ignored);
   - client-supplied shape parameters;
   - a Tier 1 value requested by a demoted member mid-session;
   - a stale transition after reconnect;
   - an audit-append failure;
   - decryption attempted from a non-allowed module (arch-check).
7. **Documentation:** rewrite `docs/Bootstrap.md` and `CONTRIBUTING.md` for the new stack (Node, pnpm, Docker; `pnpm stack:up` starts Postgres, Redis and Electric). Remove the `The archive` section's "removed from main by a tracked Linear issue" sentence from `CLAUDE.md`, and replace it with "Removed from main in Stage 7; preserved on `archive/local-first-e2e`."
8. **Findings:** recount the findings summary table programmatically, the way the file describes, and update the count paragraph.

**Done criteria:**
- `git grep -n -i -E "loro|sync-engine|sealed-store|shamir|tier1-envelope"` on `main` returns only historical documentation.
- No Rust files remain.
- `pnpm verify`, `pnpm verify:full` and the slow-lane suites pass locally.
- CI passes on the pushed branch.
- The adversarial suite passes.

**Gates:** the standard four, plus the Stage 6 Playwright suite and a green CI run on the branch (link it in the report).

---

### Stage 8 — Spec sweep (priority slice) and the canonical Employee profile

**Linear:** FDN-104 (the priority slice below only — not all 44 specs, per FDN-104's own "not in one large pass"), RST-33.
**Branch:** `stage-8-employee-profile`

**Do:**

1. **Correct `VPS-D004` (Application Shell Navigation and System States) first — everything else in this slice, and RST-33 itself, renders through it.**
   - **The "Locked shell" section describes retired architecture, not a wording fix.** It gates on "the local store... stays sealed until a server-authorized online unlock succeeds" (F106), with Cold-start and Mid-session variants keyed to a role-refresh checkpoint (F127) sealing a live Worker store. None of that exists after Stage 7: there is no local store to seal, no unlock endpoint, no Worker. Read the whole section before touching it. **This is a design question, not a find-and-replace — raise it as a numbered finding with your recommended replacement; do not decide it unilaterally.** The replacement must preserve the property the old design protected: a denied session and an unreachable server render identically (non-enumeration, per F148) — `requireCurrentWorkspaceSession`'s `UnauthorizedWorkspaceSessionError` still does not distinguish "no session," "wrong workspace" or "revoked membership," so the client still cannot either. A plausible shape: one full-bleed "reconnect" state triggered by any `401`/`UNAUTHORIZED` response, same Retry action, same refusal to say why — propose it with the tradeoffs, don't assume it's right.
   - **The "Aged out — Fetch" state no longer describes what happens.** It reads "data outside the local retention window... present on the server, simply not cached here," dashed border, a **Fetch** action, "remains locally cached for the retention period" afterward. Tier 1 and Tier 2 data is never cached on a device at all now — every render of a protected field is a live `protected.read` call, not a cache-age question. Correct this to a `requires-connection` state with a **Retry** action (FDN-104's own wording), shown whenever a protected field's fetch is pending or has failed for lack of connectivity, with no claim about caching once it succeeds. Update the state-comparison table at the end of the section.
   - Leave Syncing and Restricted alone — they describe the audience-filtered Electric cache and the permission interceptor, both unchanged by Stage 7.

2. **Correct `VPS-F004` (Silent Audit Log).** Its core design — append-only, every denial at every tier, every successful Tier 1/3 access, excluded from search — is architecture-agnostic and stays. Remove the "entries older than the local retention window are not materialized on device... render as the aged-out state and fetch on demand" language (two places): audit history was never Tier 0, so it was never eligible for the local cache under the current architecture either. State instead that reviewing Tier 1/3 access history is always a server call, gated the same way the record itself is, rendering through whatever item 1's ruling produces.

3. **Correct `VPS-F002` (Local-First Search).** It claims to index "every nameable entity in the graph entirely from the local index with no network round-trip." Per FDN-104: scope the always-local, always-offline guarantee to Tier 0 entities only — what the device cache actually holds. A query that could match a Tier 1/2/3 entity is a server-backed search call: slower, online-only, itself an access the interceptor and (where the matched type is Tier 1/3) the audit log govern, and it degrades on item 1's `requires-connection` state when offline. `AuditEntry`'s total exclusion from the index (G07) is unaffected. Re-read the whole document — the "no spinner, no *searching* state, no difference the user can perceive" framing is only true for Tier 0 now and needs qualifying everywhere it appears, not once.

4. **Correct `VPS-A002`'s Client ownership row**, per F207's closure: reconcile the Cross-Suite Node Ownership table's `Client` row with `VPS-F008`'s "not applicable" so the two tables state one answer. This is the only outstanding correction in `VPS-A002` — the rest is already updated for F199 (`managed_by`'s single-writer mutation, the retired-Loro history, the deprecated retention-window field are all already correct; do not re-touch them).

5. **Correct `VRS-F002` (Atomic Employee Profiles).** It is written throughout as writing to "the local store" first and syncing outward, with client-side validation ("rejected before reaching the local store") and a Tier 1 "local wipe" on offboarding. Correct to: a named server mutation is the only writer (per `VPS-A002`'s already-updated rule); status-transition validation happens server-side before the mutation commits; Tier 0 fields then reach the device cache through the ordinary Electric shape sync, so the 200ms-offline-directory claim stays true, scoped explicitly to the Tier 0 half; Tier 1 (`base_compensation_amount` and the rest) was never on a device, so offboarding's "Tier 1 access revoked" step is the `device_workspace_revocation`/audience-recompute machinery Stage 6/7 already built, not a wipe. Update each corrected clause's Decisions Recorded section with a pointer to F199 — don't silently reword the body text.

6. **Implement RST-33 on the corrected specs.**
   - Register `Employee` as a real node type in `packages/schema/src/registry.ts`: the Tier 0 identifying half and the Tier 1 compensation half, exactly as `VPS-A002` already documents them (it uses Employee as its running example for the tier-split pattern throughout — read that document's Employee entries directly rather than re-deriving the field list). Both halves' privacy classes and the tier split are already specified; this stage wires the registry entry, it does not redesign the schema.
   - Named mutations for create, update, and the lifecycle transition (Active → Inactive → Converted, `VRS-F002` G06's transition table, validated server-side per item 5). Employee is tier-split, so update needs its own named mutations, not `graph.updateNodeFields` — Stage 4's rule restricts the generic mutations to Tier-0-only node types.
   - Relationships: `managed_by` already carries `governingPartitions: { Employee: "operational" }` from F208's closure — confirm the mutation that moves a report honors it; don't re-decide it. `scoped_to_entity` the same way. Link to `User`/`WorkspaceMembership` for workspace, role and identity rather than duplicating those facts on Employee.
   - Wire `services/api/src/permission/employee-link.ts`'s `resolveEmployeeForUser` to a real lookup. Its two callers (`reader-set.ts`'s subject exclusion, `roles.ts`'s Manager derivation) already treat `null` as "cannot tell, resolve conservatively" and need no changes themselves — once this returns a real Employee id, both go live for the first time. This is exactly the kind of seam this project's review process has caught before (Stage 2/3's membership-graph gap, Stage 4's generic-mutation leak): write paired tests proving subject exclusion actually excludes the subject now, and a Manager's derived scope actually widens to their reports and nowhere else — not only that the code compiles with a non-null id.
   - Permission-aware typed queries for view/directory: Tier 0 fields through the ordinary graph read path, Tier 1 fields through `protected.read`, both filtered by the reader-set logic subject exclusion now participates in.
   - Import and manual creation must produce equivalent canonical records (RST-33's own done criterion). If `VPS-F006` (Workspace Setup and Data Import) isn't corrected yet, route its Employee-shaped import rows through the same named mutation as manual creation rather than a separate path; raise a finding if that isn't achievable without also touching `VPS-F006`.

**Done criteria:**
- `VPS-D004`, `VPS-F004`, `VPS-F002`, `VRS-F002` each have their Decisions Recorded section updated with a pointer to F199; a grep for "local store", "authorized device", "retention window" and "client-side" across these four documents returns only historical Decisions Recorded entries.
- `VPS-A002`'s Client row matches `VPS-F008`.
- The Locked-shell replacement is a ruled finding, not a silent decision.
- `Employee` is registered, with create/update/lifecycle mutations, `managed_by`/`scoped_to_entity` wired through the existing governing-partitions and mutation machinery, and permission-aware queries.
- F130 is closeable: paired tests prove subject exclusion and Manager-scope both fire correctly once `resolveEmployeeForUser` is real, not merely that it returns non-null.
- Import and manual creation produce equivalent canonical records, or a finding says why not yet.

**Gates:** the standard four (format, lint, typecheck, `pnpm verify`), plus `pnpm verify:full`, the arch-check rules, and a green CI run on the branch (link it in the report). Name the subject-exclusion and Manager-scope tests individually in the report, the way Stage 7 named its gates.

---

### Stage 9 — the real shell bootstrap

Raised by F216 while tracing the original Stage 9 (now Stage 10) before implementation: the shell has never been wired to the real backend Stages 1–8 built. Every screen under `apps/roster-web/src/app/(shell)/` renders from hardcoded fixtures (`EMPLOYEES`, a fixed viewer, a client-side `canSeeCompensation()` toy). `createGraphClient` is mounted nowhere but the `/sync-harness` diagnostic route. This is `FDN-69`'s actual scope, narrowed to the minimum that unblocks Stage 10 — **not** a migration of every screen off fixtures. Home, People, Timesheets and the rest stay on fixtures after this stage; only the shell's own bootstrap becomes real.

A first attempt at this brief (branch `stage-9-shell-bootstrap`, now abandoned) stopped correctly on F217: F216's ruling assumed `session.activeOrganizationId` was already populated somewhere; it wasn't. A second attempt (branch `stage-9-shell-bootstrap-v2`, now abandoned) stopped correctly on F218: the F217-updated point 1 (unconditional sign-in on "no data") and point 4 (offline boot from cache, no network call) contradicted each other on a cold offline load, where `useSession()` also resolves to "no data." A third attempt (branch `stage-9-shell-bootstrap-v3`, in progress) implemented the corrected brief and stopped correctly on F219, found only once the bootstrap was actually exercised in a browser: `organizationClient()` (point 3's original design) calls Better Auth's `organization/list`, which `services/api/src/auth/http.ts` deliberately 404s along with every other `/api/auth/organization/*` route. Continuing on the same branch after F219's fix, it stopped a second time, correctly, on F220: the new list route correctly names the sole active workspace, but nothing made that the server's active-session claim — `/devices/register`, `trpc.ts`'s context builder and the shape proxy all still saw `activeOrganizationId: null` and refused. F217 through F220 are all ruled now — F217's two steps are points 2 and 3 below; F218's split is folded into points 1 and 4; F219's and F220's fixes (two new server routes, replacing `organizationClient()` and closing the establish-session gap) are both folded into point 3. **None of F219 or F220 restarts the branch.** `stage-9-shell-bootstrap-v3`'s preliminary code is correct apart from the calls this ruling adds and replaces — continue on that same branch, applying only the point-3 fixes below to what's already there, and keep the branch's existing commits and working tree rather than starting a new branch.

**Linear:** `FDN-117` (the fourth attempt, `stage-9-shell-bootstrap-v3`) is already filed and In Progress — continue on it, do not file a new issue. `FDN-114` (Stage 10's Reconnect issue), `FDN-115` and `FDN-116` (the two earlier abandoned attempts) all stay as-is.

**The ruled contract (F216 + F217 + F218 + F219 + F220).** Checked directly against the code: `services/api/src/auth/config.ts` already configures Better Auth's `organization()` plugin server-side, and the session already carries `activeOrganizationId` — `requireCurrentWorkspaceSession` already treats it as the workspace claim, and `workspace-session.ts`'s revocation path already clears it to `null`. But nothing populates it: a repository-wide search found only that one write (the clear-to-null). `apps/roster-web/src/lib/auth-client.ts` also has no `organizationClient()` plugin, so nothing in the shell can read the field even once it holds something.

1. **No session → sign-in (F218: only on a confirmed answer).** This needs no workspace call at all, and is checked first, purely client-side — but "no data after `isPending` clears" is not by itself confirmation of "no session." Better Auth's pinned session atom (`session-atom.mjs`) only sets `data: null` with a structured error — `error` is a `BetterFetchError`, always carrying a numeric `.status` (401, 5xx, whatever the server answered) — when `/get-session` actually reached the server; a genuine no-session response also lands here, with `error: null`. It sets `data: null` with an *unstructured* error — whatever the fetch layer itself threw (a `TypeError`, an abort `DOMException`; never a `BetterFetchError`, no numeric `.status`) — only when the request never reached the server at all: offline. Sign in only when `error` is `null` or carries a numeric `.status`. When `error` is present without one, the answer is indeterminate, not "no session" — fall through to point 4's offline path instead. This is the same split the sync engine already makes for the same reason (`engine.ts`'s `failure.kind === "network"` vs `"unauthenticated"`); reuse that shape, don't invent a new one.
2. **Populate the active organization server-side (F217).** In `services/api/src/auth/workspace-session.ts`'s `confirmWorkspaceAdmission`, in the same transaction, set `session.activeOrganizationId` to the confirmed workspace **only when it is that person's sole active membership** — check the same way `revokeWorkspaceAdmission` already checks membership state. Do **not** auto-switch a person who already has an active workspace when they create or join a second one; leave their current active organization untouched. This covers the ordinary first-workspace case without inventing workspace switching, which is already deferred (see `Reviewer_Handoff.md`'s follow-ups).
3. **A session → resolve the workspace, establish it server-side, then mount the real client (F219 + F220).** Do **not** add `organizationClient()` to `auth-client.ts` — every `/api/auth/organization/*` route, reads included, is deliberately closed (FDN-85/FDN-86's "no second write path" boundary), and the plugin has nothing legitimate to call. Read `user.id` and `session.activeOrganizationId`. If `activeOrganizationId` is already set, use it directly — skip the two calls below, it's already the server's active-session claim. If it's `null`, resolve it by calling `POST /workspace/list-active-memberships` — a session-gated, workspace-independent route (add it to `services/api/src/auth/http.ts`, same shape as the existing `/devices/retire`: session-only, no workspace header). It returns `{ workspaceId, workspaceName }` for each of the caller's memberships that are `active`/`confirmed` on both the membership and its organization, using the exact predicate `requireCurrentWorkspaceSession` and `confirmWorkspaceAdmission` already use — nothing else (no roles, no pending/revoked rows). Exactly one result → **before** constructing the graph client, call a second new route, `POST /workspace/activate-sole-membership` (same file, same session-gated shape, no request body) — it independently re-derives the caller's sole active confirmed membership itself (it does not trust the `workspaceId` the list call returned) and, only when there really is exactly one, sets `session.activeOrganizationId` on every session row that user holds — the exact write `confirmWorkspaceAdmission` already performs for the confirming session, now reachable from a later sign-in too. Proceed to construct the client only if the activation call confirms the same workspace; if it returns `{ workspaceId: null }` (the membership picture changed underneath — a race, not the common case), or the list call returned zero or more than one result to begin with, this is unresolved workspace selection, not this stage's to invent — render a minimal holding state (plain copy is enough; this is **not** F214's `Reconnect` and must not reuse its trigger, copy, or component). Construct the real graph client (`createGraphClient`, the same one `/sync-harness` already exercises) with the resolved workspace and `user.id`, sending `x-vulto-workspace-id` per the existing convention (`packages/schema/src/mutations/define.ts`'s `WORKSPACE_HEADER`). Mount it in the shell layout, replacing `Shell.tsx`'s hardcoded `workspaceName` and `syncStatus` props with the client's real state.
4. **Offline boot stays offline, single-cache only (F217, extended by F218).** `VPS-A003`: "booting offline is restored." If there is no network, enumerate cached workspaces. When point 1 resolved a `user.id` (a structured error reached the server, or a genuine no-session answer that was somehow followed by an offline retry), filter to that user's `vulto:<workspaceId>:<userId>` caches. When point 1 could not resolve at all — the indeterminate case, a cold boot with no server reachable and so no known user — there is no user to filter by yet: enumerate every `vulto:<workspaceId>:<userId>` cache on the device via `listIdbDatabases()`, unfiltered. Either way, exactly one eligible cache → mount from it via `session_hint`, no network call; its `workspaceId` and `userId` are enough to construct the graph client even with no live session. More than one → this is the same unresolved-selection case as point 3, not a silent pick (whether that means one user with two workspaces or two different people sharing the device); require coming online. Do not add anything to `device-identity.ts`'s allowed-key list — that database's narrowness is a deliberate, test-enforced contract, and neither F217 nor F218 reopens it.
5. **Surface refusals, don't render them.** This stage's job is to make a `401`/`UNAUTHORIZED` from an authenticated call, and an `access-revoked` from the shape proxy, reach the shell as a signal — a callback, an exposed field on the client's public state, whatever fits the existing `SyncState` shape best. **Do not build the Reconnect screen itself here** — that is Stage 10, unblocked once this lands.
6. **Retry, for Stage 10, means re-running this bootstrap.** `#handleRevoked` stops every source; there is nothing left inside a stopped client to retry. Design the bootstrap so it can be re-invoked from scratch (construct a fresh client, same resolution above) — that re-invocation *is* what Stage 10's Retry button calls. Say this explicitly in the report so Stage 10 doesn't have to rediscover it.
7. **Do not touch `Offline`.** It already exists from Stage 6 and is out of scope here.

**Done criteria:**
- A real, authenticated user creating or accepting their first workspace membership gets `activeOrganizationId` set server-side, proved by a test — not asserted from the diff.
- That same user's shell mounts the live graph client, not fixtures — proven with a browser test.
- A user with no Better Auth session is sent to sign-in without any workspace call being attempted, and only on a confirmed answer (`error` null or carrying a numeric `.status`) — never on a network-level failure standing in for "no session" (F218).
- A cold offline boot with exactly one cached workspace mounts from cache with no network call, including when `useSession()` never resolved at all; more than one cached workspace does not silently pick and does not crash.
- A `401` and an `access-revoked` are each observably surfaced to the shell (a test asserting the signal fires is enough; rendering it is Stage 10's job, not this stage's).
- No existing screen's fixture data is migrated in this stage — Home, People, Timesheets and Foundations are explicitly out of scope, and any change to them is a deviation to flag, not a bonus.
- A second workspace created or joined by a person already active elsewhere does not change their active organization.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`), plus a real browser test proving: (a) a first-time workspace creation/join sets the active organization and mounts the real client; (b) no session redirects to sign-in with zero workspace calls; (c) offline cold start with exactly one cached workspace mounts without network, and with more than one does not silently pick — proven with a real network-level failure (not a mocked 401), and separately that the same network-level failure never redirects to sign-in; (d) a `401`/`access-revoked` from the client is observable at the shell boundary; (e) creating a second workspace while already active elsewhere does not switch the active organization (an integration test on `confirmWorkspaceAdmission` is enough for this one, a browser test isn't required).

**What to check in the Stage 9 report:** that `POST /workspace/list-active-memberships` returns exactly the active/confirmed rows the ruling specifies and nothing else (test a pending and a revoked membership are both excluded, not just the happy path); that `POST /workspace/activate-sole-membership` independently re-derives sole membership rather than trusting the caller, only ever writes from `null` (never switches an already-set `activeOrganizationId`), and is proven with a real end-to-end browser run — device registration succeeds, the shape proxy accepts the workspace, and the real workspace name renders, not just that the two new routes return the right JSON in isolation; that `organizationClient()` and every `/api/auth/organization/*` call are gone from the final diff, not merely unused; that no screen outside the shell's own bootstrap was touched; that the refusal-signal shape is documented clearly enough that Stage 10 can consume it without re-deriving anything; that offline boot was tested, not just reasoned about; and that the sign-in-vs-offline split (F218) was tested with an actual network-level failure (request aborted or refused before any response), not merely a mocked 401 or a mocked empty response standing in for "offline."

---

### Stage 10 — the Reconnect shell state

Depends on Stage 9 (the real shell bootstrap). Originally briefed as "Stage 9" before F216 discovered the shell had no real authenticated call to refuse in the first place; renumbered once that dependency was ruled. `FDN-114` is its Linear issue, already filed and In Progress.

**Source of truth for the design — read it, do not re-derive it.** `docs/Vulto_Specs/VPS-D004_Application_Shell_Navigation_and_System_States.md`, section **"The reconnect state"** (just above "## The three system states"), is the ruled design: F214, closed 22 September 2026. Every rule below is already written there in full — Trigger, Rendering, What Retry does, Data, Kept, Removed, and the note about revisiting if a future stage widens the device cache. Build exactly that, nothing more, nothing reinterpreted.

**Do:**
1. Consume the refusal signal Stage 9 exposed at the shell boundary — do not re-derive `401`/`access-revoked` classification from scratch; Stage 9's report says where that signal lives.
2. Build the `Reconnect` component per the spec's **Rendering** section: full-bleed, centered, no illustration, the exact copy given, one **Retry** action.
3. Wire the trigger exactly as specified: only a `401`/`UNAUTHORIZED` from an authenticated call, or `access-revoked` from the shape proxy, ever shows it. A network failure, timeout, 5xx or rate limit must leave the shell exactly as it is (the existing `Offline` treatment already covers connectivity loss from Stage 6 — do not touch that path).
4. Wire the sign-in split: this is already Stage 9's job (no session → sign-in, before any workspace call). Confirm it holds; don't rebuild it.
5. Wire **Retry** to Stage 9's bootstrap re-invocation (Stage 9's point 5) — not a literal re-issue of a call on a stopped client, which cannot work; re-running the bootstrap is what a correct Retry does.
6. Confirm the **Data** behavior: `access-revoked` already erases that workspace's cache (Stage 6's existing machinery) — verify this still fires, don't rebuild it. A plain `401` erases nothing; cached Tier 0 rows stay but are not rendered until a call succeeds or the person signs out.
7. Guard against reuse: nothing else in the codebase may trigger this treatment. If you add any check or flag that could let a future feature reuse it, that's a deviation from the brief — don't.

**Done criteria:**
- The four building blocks above (trigger, rendering, Retry, sign-in split) exist and are exercised by tests, not just present in the diff.
- A cold-start refusal and a mid-session refusal render identically — same component, same copy, no branch on which one it was.
- `Offline` (already built) and `Reconnect` (this stage) are visibly distinct and never triggered by the same condition.
- A network failure, timeout, 5xx, or rate limit never shows `Reconnect`.

**Gates:** the standard four, plus a real browser test that proves: (a) a `401` on an authenticated call with no prior session goes to sign-in; (b) a `401` with a prior session shows `Reconnect`, and Retry re-issuing a now-successful bootstrap mounts the shell; (c) `access-revoked` shows `Reconnect` and the workspace's cache is gone when it does; (d) a simulated network failure or 5xx does not show `Reconnect` and leaves the existing state alone.

**What to check in the Stage 10 report:** that no code path outside the one trigger can show `Reconnect`; that the sign-in-vs-Reconnect branch was tested with an actual no-session call, not asserted from reading the code; that `Offline` was not modified beyond what distinguishing it from `Reconnect` required; and that Retry genuinely re-runs Stage 9's bootstrap rather than something that only works on a client that was never stopped.

---

### Stage 11 — Multi-Entity and Jurisdiction Foundation (`VRS-F003`)

Depends on `VRS-F002` (Employee, built in Stage 8) and the schema-level registration `VRS-F003` already owns — `Entity`, `scoped_to_entity` and Entity's `OWNERSHIP_REGISTRY` row are already complete in `packages/schema/src/registry/{nodes,edges,ownership}.ts`; none of that needs touching. Read `docs/Vulto_Specs/VRS-F003_Multi-Entity_and_Jurisdiction_Foundation.md` directly — its Technical Architecture, Graph Specifications (G01–G07) and its own corrected Non-Functional Requirements, Security Considerations and Decisions Recorded sections (F221, F222) are the source of truth this brief summarizes, not the reverse.

**Do:**
1. **Entity's own mutations.** Following `services/api/src/mutations/employee.ts`'s exact pattern (checks/validate/apply, `ServerMutation<MutationArgs<...>>`, argument schema in a new `packages/schema/src/mutations/entity.ts`, registered in `services/api/src/mutations/pipeline.ts`): `entity.create(name, jurisdiction, defaultCurrency, fields?)`, `entity.update(entityId, fields)`, `entity.deactivate(entityId, expectedVersion)`. `entity.create` validates `jurisdiction` against the closed enum (`PK, UK, US, AE, SA, IN, SG, Global`) and `defaultCurrency` as ISO 4217. Add `"Entity"` to `packages/schema/src/mutations/employee.ts`'s `FEATURE_LIFECYCLE_NODE_TYPES` (alongside `Employee`) so the generic `graph.transitionLifecycle` correctly refuses it (F223) — `entity.deactivate` is the only path that may change Entity's `lifecycle_status`, and there is no separate `is_active` field to update; `VRS-F003`'s schema no longer has one. `entity.deactivate`'s `validate()` checks `expected_version` against the current row first, rejecting a mismatch `stale-state` (`VPS-A003` A003-T54, exactly `employeeTransitionStatus`'s own first check), then refuses (`MutationRejection`, a new reason string each) in **two distinct business-rule cases, checked after the version check, never before it, and never collapsed into one:** any Active employee remains scoped to it (G05, naming the count), or it is the workspace's sole remaining Active Entity (G04 — this invariant must hold even when the entity being deactivated happens to have zero employees scoped to it). `apply()` writes `lifecycle_status: "Dissolved"` through `updateNodeFields` under the checked version, the same call `transitionLifecycle` and `employeeTransitionStatus` both make.
2. **The transfer mutation.** `employee.setEntity(employeeId, entityId, effectiveFrom)`: closes the employee's current active `scoped_to_entity` edge with `services/api/src/graph/store.ts`'s existing `closeEdge` (already exactly this primitive — row-locked, rejects a double-close) at `effectiveFrom`, then inserts a new active edge to the new entity, following the same single-active-edge-with-history shape `employeeCreate` already uses to create the first one. No audit-log call — see F221; `scoped_to_entity`'s own edge fields are the record.
3. **Temporal resolution.** `entity.resolveForEmployee(employeeId, asOf?)`: one implementation in `services/api`, usable directly server-side. This stage has no real caller yet (contract generation, leave policy and payroll are later features), so prove it with tests against `scoped_to_entity`'s own history rather than a real caller — including the UK→1 March→Pakistan transfer example from the spec's own acceptance criteria. `asOf` defaults to now. G06: nothing else may traverse `scoped_to_entity` directly; if anything in this stage's own code needs the employee's entity, it calls this function too.
4. **The founding-record bootstrap (F222).** Extend `services/api/src/graph/founding.ts`'s `writeFoundingRecords` (or add a same-transaction sibling call from `createWorkspace` in `services/api/src/auth/workspace-projection.ts`) to also write a default Entity: name = the workspace's own name (already available, no new input needed), `jurisdiction: "Global"`, `default_currency: "USD"`, `lifecycle_status: "Active"`. Must commit or roll back with the other five founding records — never a follow-up write that could leave a workspace with zero entities if it failed partway.
5. **The permission matrix.** Add an explicit `MATRIX_OVERRIDES["Entity"]` row to `packages/schema/src/policy/policy-table.ts`, matching `VPS-A004`'s already-written "workspace-configuration pattern" (the same pattern the document already applies in prose to LeavePolicy, WorkingCalendar and others — it names Entity explicitly): `owner`/`hr-admin` → `FULL_ANY()`, `finance-admin`/`manager`/`team-member` → `READ_ANY()`. Without this row, Entity silently falls through to `Standard`'s default class mapping, which is person-scoped (`own-plus-team` for Team Member, `direct-reports` for Manager) and wrong for a workspace-wide record with no owner or reports of its own — write a test that fails before the override and passes after, so the gap can't silently reopen.
6. **No UI.** Stage 8 (`VRS-F002`) touched no `apps/` code, and this stage follows the same discipline. The Entities screen (`VPS-F005`'s host, not built) and the conditional entity selector on the Employee profile (the People/Employee screen is still fixture data per `Reviewer_Handoff.md`'s open follow-ups) both wait for that work rather than being built piecemeal now. Nothing under `apps/roster-web` changes in this stage.
7. **The four optimistic mutators (F224).** `packages/graph/src/mutators/foundation.ts`'s shared `OPTIMISTIC_MUTATORS` registry requires a client handler for every registered mutation name; add the four this stage introduces. `entity.create` and `entity.update` are ordinary node-write handlers, the same shape `employeeCreate`/`employeeUpdate` already use. `employee.setEntity`'s handler finds the employee's currently-open `scoped_to_entity` edge directly in the device cache (`effectiveTo === null` — the same non-temporal lookup `moveEmployee`'s existing handler already performs on `managed_by`; this is not the G06-restricted `asOf` question), closes it at `effective_from`, and opens a new edge to the destination Entity with its id derived from `moveEmployeeEdgeId(mutationId)` — the same portable, deterministic function the server's own `employee.setEntity` implementation already uses for this edge, so client and server compute the identical id. `entity.deactivate`'s handler checks only `expected_version` (`stale-state` on a mismatch) and writes `lifecycle_status: "Dissolved"` optimistically; it must NOT re-check G04 or G05 client-side — those are workspace-wide counts a single device's cache cannot answer reliably offline, and a server-side refusal the client couldn't predict is handled by the existing outbox `reject()`/`undo` path, not a new mechanism.

**Done criteria:**
- `entity.create`/`update`/`deactivate` and `employee.setEntity` each exist as named server mutations, permission-interceptor-gated, proven by integration tests — not asserted from the diff.
- `entity.deactivate` is refused, with the correct distinct reason each time, both when an Active employee remains scoped and when it is the workspace's sole remaining Active Entity (G04, G05) — two separate tests, including the case where the sole remaining entity has zero employees scoped to it.
- `entity.deactivate` rejects a stale `expectedVersion` with `stale-state` before it ever reaches the G04/G05 checks (F223, A003-T54) — a version-mismatch test distinct from the two G04/G05 tests.
- `entity.resolveForEmployee` returns the entity in force at a given `asOf`, proven with the spec's own UK→Pakistan transfer example (or an equivalent), and a later Entity change never alters an already-closed edge's own recorded history (G03).
- A newly created workspace has exactly one Active Entity, named after the workspace, with no manual step (F222) — proven by a test on workspace creation, not read off the code.
- Team Member and Manager get Read, never Full, on Entity; Owner and HR Admin get Full — proven by a test that fails against `Standard`'s unmodified default mapping and only passes with the new `MATRIX_OVERRIDES` row (item 5).
- No file under `apps/` appears in the diff.
- `pnpm verify` passes typecheck with real optimistic handlers for all four mutations — no stub, no-op, or placeholder handler (F224).
- `employee.setEntity`'s optimistic handler closes the correct currently-open `scoped_to_entity` edge and derives the new edge's id from `moveEmployeeEdgeId`, proven by a device-cache test mirroring `moveEmployee`'s own coverage of `managed_by` — not asserted from the diff.
- `entity.deactivate`'s optimistic handler does not evaluate G04 or G05 client-side — proven by a test that an optimistically-applied deactivation the server later refuses is rolled back through the outbox's existing rejection path, not by a client-side re-check.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 11 report:** that `entity.create`/`update`/`deactivate` and `employee.setEntity` each go through the same checks/validate/apply pipeline every other mutation does, not a shortcut; that the two distinct deactivation refusals (G04, G05) are each independently tested, including the sole-remaining-entity-with-zero-employees case; that `entity.resolveForEmployee` is called by nothing that bypasses it — grep the diff for any direct `scoped_to_entity` traversal outside this one function (G06); that the founding-record bootstrap runs in the same transaction as the other five founding records; that the new `MATRIX_OVERRIDES["Entity"]` row is tested against a Team Member and a Manager specifically, not just Owner/HR Admin; that `entity.deactivate` checks `expected_version` before G04/G05, not after and not skipped (F223); that nothing anywhere reads or writes a field called `is_active` on Entity (F223 dropped it — grep the diff); that no file under `apps/` appears in the diff; that all four Stage 11 mutations have real optimistic handlers in `packages/graph/src/mutators/foundation.ts`, none of them stubs (F224); that `employee.setEntity`'s handler only looks up the open `scoped_to_entity` edge and takes no `asOf` argument; and that `entity.deactivate`'s optimistic handler contains no G04/G05 count logic, relying on the outbox's rejection path instead.

---

### Stage 12 — Working Calendar and Working Patterns (`VRS-F004`)

Depends on `VRS-F002` (Employee), `VRS-F003` (Entity, Stage 11, merged `96d53eb`) and the schema-level registration `VRS-F004` already owns — `WorkingCalendar`, `Holiday`, `WorkingPattern`, their ownership rows and their edges (`Entity`→`WorkingCalendar`, `WorkingPattern`→`Employee`, `Holiday`→`WorkingCalendar`, `WorkingCalendar`→`WorkingCalendar` for `supersedes`) are already complete in `packages/schema/src/registry/{nodes,edges,ownership}.ts` — none of that needs touching. Read `docs/Vulto_Specs/VRS-F004_Working_Calendar_and_Working_Patterns.md` directly — its Technical Architecture, Resolution order, Graph Specifications (G01–G08) and its own corrected Non-Functional Requirements, Security Considerations and Decisions Recorded sections (F225, F226) are the source of truth this brief summarizes, not the reverse.

**Do:**
1. **The calendar/pattern mutation module.** New `packages/schema/src/mutations/calendar.ts`, mirroring `entity.ts`'s exact pattern (checks/validate/apply, `ServerMutation<MutationArgs<...>>`): argument schemas for `calendar.update`, `holiday.add`, `holiday.confirm`, `holiday.cancel`, `pattern.set`, `pattern.clear`. Register all six in the shared `MUTATIONS` registry (`packages/schema/src/mutations/index.ts`) and `services/api/src/mutations/pipeline.ts`'s `IMPLEMENTATIONS`. Add a shared `createInitialCalendar(tx, entityId, jurisdiction)` helper in the same module: PK/UK/US/AE/SA get their named template (AE is Sunday-Thursday, corrected by F232 - not Monday-Friday); every other jurisdiction (including `Global`, `IN`, `SG`) gets Monday-Friday, `standard_daily_hours: 8` (F227). Both `entity.create` and `services/api/src/graph/founding.ts`'s `writeFoundingRecords` call this one helper, writing the calendar atomically with the Entity, never as a follow-up. The jurisdiction-to-template lookup itself is a plain, portable, pure function with no I/O - export it (e.g. `initialWorkingWeekFor(jurisdiction)`) so `packages/graph`'s client-side optimistic handler can import and call the identical function (F233), never a duplicated copy of the table.
2. **`calendar.get`/`calendar.update`.** `calendar.get(entityId, asOf?)` reads the Entity's Active `WorkingCalendar`, or, with `asOf`, the version active at that date by following the `supersedes` chain — a small dedicated resolver in `services/api`, the same placement `entity.resolveForEmployee` established in Stage 11. `calendar.update(calendarId, workingWeek, dailyHours, expectedVersion, reducedHoursPeriods?)` checks `expectedVersion` against the calendar being superseded first (`stale-state` on a mismatch, F228), then never edits in place (G01): it writes a new `WorkingCalendar` with `lifecycle_status: "Active"`, connects it from the prior version via the existing `supersedes` edge, and marks the prior version `lifecycle_status: "Superseded"` — both writes, plus copying every still-Active Holiday of the prior version onto the new one (fresh `holiday_in` edge, never a Canceled holiday, F230), committed together. `reducedHoursPeriods` omitted carries the prior version's array forward unchanged; provided, it replaces the array outright on the new version — the only write path `ReducedHoursPeriod` data has, since it carries no lifecycle of its own (F234). Validate `working_week` as exactly seven entries keyed 1–7, each with a boolean `is_working` and a non-negative decimal `hours`.
3. **Holiday mutations.** `holiday.add(calendarId, fields)` creates a `Holiday` scoped to the calendar; when `is_provisional` is true, `date` defaults to `estimated_date`. No version argument (F228) — there is nothing to supersede. `holiday.confirm(holidayId, actualDate)` sets `date` to `actualDate` and stamps `confirmed_at`/`confirmed_by`, leaving `estimated_date` untouched as the record of the shift — no `VPS-F004` `AuditEntry` call anywhere in this path (F225), and no version argument, since a Holiday's `lifecycle_status` never changes on confirmation (F228). `holiday.cancel(holidayId, expectedVersion)` checks `expectedVersion` first (`stale-state`, F228), then sets `lifecycle_status: "Canceled"`. Both `holiday.confirm` and `holiday.cancel` refuse `not-found` against a holiday belonging to a `Superseded` calendar version (F230). None of the three resolve `applies_to_locations` themselves — that belongs to `workingDays.*`'s read-time resolution order (item 5), not to these writes.
4. **`pattern.set`/`pattern.clear`.** Single-active-with-history for a versioned node, the same shape `employee.setEntity` used for an edge in Stage 11, reusing the `closeAt` shared-boundary convention Stage 11 built for `scoped_to_entity` (F229): `pattern.set(employeeId, workingWeek, effectiveFrom, expectedVersion?)` — `expectedVersion` of the current Active pattern, required whenever one exists to supersede, omitted only for an employee's first-ever pattern (F228) — closes it at exactly the new `effective_from` (`effective_to` set to that same date, `lifecycle_status: "Superseded"`) and creates a new Active one. The write-time guard (G04) is that the new `effective_from` may not precede the pattern it supersedes — an earlier date would overlap already-closed history instead and is refused (`invalid-args`); a same-day replacement is allowed and simply supersedes outright. `pattern.clear(employeeId, effectiveFrom, expectedVersion)` — always required, since clearing always closes an existing pattern — closes the current pattern at `effectiveFrom` with no replacement, under the same guard. A weekday omitted from `workingWeek` is stored as omitted, never defaulted to a zero-hours row (G03) — item 5's read-time resolution performs the inheritance, not this write. **Both mutations write a `pattern_for` edge, which requires `governingPartitions: { Employee: "operational" }` declared on `pattern_for` in `packages/schema/src/registry/edges.ts` (F235) — the same one-line fix F208 made for `managed_by` and `scoped_to_entity`, since `edgeRoleDecision` refuses a split endpoint with no declared partition for every role, not only some. `VPS-A002`'s table already carries the corresponding row; this is the code side of that ruling.**
5. **The `workingDays.*` read API — no materialized index (F226).** One implementation in `services/api`: `workingDays.count(employeeId, from, to)`, `isWorking(employeeId, date)`, `hoursOn(employeeId, date)`, `next(employeeId, from, n?)`, `addWorkingDays(employeeId, from, n)`. Each evaluates the spec's resolution order directly against the graph for the dates it needs — a matching Holiday (full, or half at `entity daily hours × 0.5`, respecting `applies_to_locations`), then the employee's Active `WorkingPattern` for that date (inheriting any day it omits from the entity calendar), then the entity's Active `WorkingCalendar`, then any `ReducedHoursPeriod` factor on top — with no caching or precomputed table anywhere. G08: this is the only code in the stage, and going forward the only code anywhere, that touches this resolution order.
6. **No UI, no jurisdiction holiday seeding.** Same discipline as Stage 8 and Stage 11: nothing under `apps/roster-web` changes. `holiday.importForJurisdiction` is not built this stage — it is a content task (curating actual per-jurisdiction public-holiday dates), independent of this stage's architecture, and nothing downstream needs it yet: a workspace is fully usable with `holiday.add` alone, exactly as the spec's own NFRs already require. Scope it as a follow-up when a consuming feature or the founder needs seeded calendars.
7. **Optimistic mutators for all six new mutations, plus `entity.create`'s own.** The same shared-registry requirement Stage 11 found (F224): `packages/graph/src/mutators/foundation.ts`'s `OPTIMISTIC_MUTATORS` needs a client handler for `calendar.update`, `holiday.add`, `holiday.confirm`, `holiday.cancel`, `pattern.set`, `pattern.clear`. All six are ordinary node-write/supersede handlers with no workspace-wide aggregate check like `entity.deactivate`'s G04/G05 — write them the shape `employeeUpdate`/`entity.update` already use, including the supersede pair for `calendar.update`, `pattern.set` and `pattern.clear`; the four that now carry `expectedVersion` (F228) pass it straight through the same way `entity.deactivate`'s own handler already does, with no client-side re-derivation; `calendar.update`'s handler also carries `reducedHoursPeriods` through the identical copy-forward-or-replace logic (F234). **Extend the existing `entityCreate` handler too (F233):** after writing the Entity node exactly as today, it now also writes a `WorkingCalendar` node (`lifecycle_status: "Active"`, fields from item 1's shared `initialWorkingWeekFor(jurisdiction)`) and the `Entity`→`WorkingCalendar` ownership edge, using two new deterministic ID derivations added beside `moveEmployeeEdgeId` in `packages/schema/src/mutations/foundation.ts` (identical one-to-one bit-flip shape, a distinct constant each, so all IDs derived from one `mutationId` stay collision-free). Returns three undo entries instead of one. Stage 11's server-only founding bootstrap has no client cache counterpart and needs no change.

**Done criteria:**
- `calendar.update` never edits in place — proven by a test that the prior `WorkingCalendar` version is `Superseded`, not deleted, and a range computed before the edit still resolves against the old working week (G01).
- Every Entity has an Active `WorkingCalendar` immediately on creation, whether Stage 11's founding-bootstrap default Entity or one created later via `entity.create` — proven by a test, not asserted (G02), including `Global`, `IN` and `SG` each getting the Monday-Friday/8-hour default and AE the Sunday-Thursday default (F227, F232) through the shared `createInitialCalendar` helper.
- `entity.create`'s optimistic handler creates the same calendar and ownership edge the server does, from the same shared template function and deterministic IDs — proven offline: an optimistically-created Entity has its calendar immediately, with nothing to reconcile once the server mutation lands (F233).
- `workingDays.isWorking`/`hoursOn` inherit an omitted `WorkingPattern` weekday from the entity calendar rather than treating it as zero hours or non-working (G03).
- A `pattern.set` that supersedes the current Active pattern closes it exactly at the new `effective_from` (`closeAt`, no gap, no overlap) and is rejected only when its `effective_from` precedes the pattern it supersedes (G04, F229) — proven with a same-day replacement (allowed) and a backdated one (refused), not just the ordinary case.
- `pattern.set`'s `pattern_for` edge write succeeds against the real interceptor, not a mock — proven by the integration test that originally failed and exposed F235, now passing once `governingPartitions: { Employee: "operational" }` is declared on `pattern_for` (F235).
- A `Holiday` scoped to one `applies_to_locations` value does not remove a working day for an employee at a different location — proven with the spec's own Sindh/Lahore-vs-Karachi example, tested against two employees in two different locations, not one (G05).
- `calendar.update`, `holiday.cancel`, `pattern.set` (when superseding) and `pattern.clear` each refuse a stale `expectedVersion` before any other check; `holiday.add` and `holiday.confirm` take none (F228) — proven by a test per mutation, not asserted.
- `calendar.update` copies every still-Active Holiday of the superseded calendar onto the new version, and a historical `calendar.get(asOf)` against the old version still resolves the old holiday set; `holiday.confirm`/`cancel` refuse a holiday on a `Superseded` calendar version (F230).
- `calendar.update`'s `reducedHoursPeriods` parameter, omitted, carries the prior version's array forward unchanged, and, provided, replaces it outright — proven with both cases, plus a `calendar.get(asOf)` against a superseded version still resolving its own frozen array (F234).
- `holiday.confirm` on a provisional holiday updates `date`/`confirmed_at`/`confirmed_by`, retains `estimated_date`, and writes no `VPS-F004` `AuditEntry` (F225) — grep the diff for any audit-journal call in this path.
- The resolution order is proven end to end against the spec's own acceptance criteria: the AE Friday exclusion against the corrected Sunday-Thursday template (F232), the PK 5.5-day/44-hour week, the UK→March pattern-effective-date split, the UAE Ramadan 0.75-factor example, and the reduced-hours period written through `calendar.update` (F234) — the PayRun and LeaveRequest examples are out of scope this stage (F231).
- `workingDays.*` is the only code path anywhere in the diff that reads `WorkingCalendar`, `Holiday` or `WorkingPattern` to decide whether a day is a working day — grep the diff to confirm nothing else does (G08); the live-view default is what this stage proves for G07, not the PayRun/LeaveRequest/Contract exemptions themselves (F231).
- No file under `apps/` appears in the diff; no `holiday.importForJurisdiction` implementation is added; no materialized index, cache table or precomputed row appears anywhere (F226).
- `pnpm verify` passes typecheck with real optimistic handlers for all six new mutations and `entity.create`'s extended handler — no stub, no-op, or placeholder handler (F233).

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 12 report:** that `calendar.update`/`pattern.set`/`pattern.clear` all supersede rather than edit in place (G01, G04); that a newly created Entity gets an Active `WorkingCalendar` with no manual step, `Global`/`IN`/`SG` on the Monday-Friday default and AE on the corrected Sunday-Thursday default (G02, F227, F232); that `entity.create`'s optimistic handler creates the same calendar and edge the server does, from the shared template function and deterministic IDs, with a test proving parity rather than asserting it (F233); that an omitted `WorkingPattern` weekday inherits at read time rather than being defaulted at write time (G03); that `pattern.set`'s overlap guard is tested with a same-day replacement (allowed) and a backdated one (refused), not only the ordinary case (F229); that G05's location scoping is tested against employees in two different locations, not asserted; that `calendar.update`, `holiday.cancel`, `pattern.set` (when superseding) and `pattern.clear` each check `expectedVersion` first, and `holiday.add`/`holiday.confirm` take none (F228); that `calendar.update` copies forward the prior version's still-Active holidays and that a historical `calendar.get(asOf)` still resolves the old set (F230); that `calendar.update`'s `reducedHoursPeriods` parameter is tested both omitted (carries forward) and provided (replaces outright), with no separate reduced-hours mutation anywhere in the diff (F234); that `pattern_for` carries a `governingPartitions: { Employee: "operational" }` entry in `packages/schema/src/registry/edges.ts` and that `pattern.set`'s integration test actually creates the edge rather than mocking the interceptor (F235); that `holiday.confirm` writes no `VPS-F004` `AuditEntry` anywhere in the diff (F225) — grep for it; that no materialized index, cache table or precomputed row appears anywhere (F226) — grep the diff for anything resembling one; that `workingDays.*` is the sole resolution-order code path (G08); that the report does not claim to prove the PayRun/LeaveRequest/Contract exemptions — only the live-view default (F231); that no file under `apps/` appears in the diff; and that `holiday.importForJurisdiction` is not implemented.

---

### Stage 13 — The Bench Forecast (`VRS-F005`)

Depends on `VRS-F002` (Employee, Stage 8), `VRS-F003` (Entity, Stage 11, merged `96d53eb`) and `VRS-F004` (WorkingCalendar/Holiday/WorkingPattern, Stage 12, merged `0249d5c`) — every day count in this feature resolves through `workingDays.*`. Read `docs/Vulto_Specs/VRS-F005_The_Bench_Forecast.md` directly — its Technical Architecture, Graph Specifications (G01–G07), API contracts and its own corrected Interface Specification, acceptance criteria, NFRs, Security Considerations and Decisions Recorded sections (F236, F237, F238) are the source of truth this brief summarizes, not the reverse. Checked directly against the code before writing this brief: `Assignment`, `Project`, `Client`, `GhostResource`, `OpenRole`, `BurnoutAlert`, `FlightRiskSignal` and `WellnessTriggerEvent` are already fully registered in `packages/schema/src/registry/{nodes,edges}.ts` (`assignment_of` already carries `governingPartitions: { Employee: "operational" }`) — nothing schema-level is missing — but no mutation, no permission-matrix row, no portable working-day function, and no aggregate disclosure-control mechanism exist anywhere.

Seven findings surfaced on this stage, all ruled the same day each was raised (full reasoning in `docs/Foundations_Findings.md`; `VRS-F005` and `VRS-F004` corrected to match):

- **F236** — the spec's Contextual Intelligence Panel, `benchForecast.get` and its offline/NFR claims describe Tier 2 fields (BurnoutAlert, FlightRiskSignal) and the compensation-derived bench cost figure as "entirely local," contradicting F199's architecture, where only Tier 0 replicates to the device. Ruled: the timeline's bars/dates/bench-day counts and the Panel's Tier 0 fields stay genuinely local; Tier 2 Panel content and bench cost become separate, on-demand, non-blocking fetches.
- **F237** — `VRS-F004`'s G06/G07 deferred a materialized working-day index to "whichever stage builds `VRS-F005`," but the index as specified assumes the retired CRDT materialization pipeline. Ruled: no index is built; the resolution-order logic becomes one portable, pure function shared by the server and the client, evaluated live against the already-local Tier 0 cache.
- **F238** — the k-anonymity disclosure-control mechanism this feature's own acceptance criteria require has never been built anywhere, and its threshold settings have no home (`VPS-F005_Workspace_Configuration_Console` isn't built). Ruled: two fields added to `Workspace`'s schema now (no settings UI), and one shared, reusable disclosure-control function built for real — this feature's first caller, not a bespoke check.
- **F239** — the spec's own bench-time formula and one of its acceptance criteria require excluding days carrying Pitch-categorized time, but `TimesheetEntry`, `Pitch` and the edge connecting them (owned by `VRS-F009`/`VRS-F010`, both later in MVP order) have no mutation anywhere in the repository — found tracing Stage 13's brief into the query layer, before any code was written. Ruled: the same partial-forward-dependency treatment the spec already gives `VRS-F006`/`VRS-F007` — the Pitch exclusion is deferred until `VRS-F009`/`VRS-F010` exist; bench periods compute from Assignment coverage alone until then.
- **F240** — `assignment.cancel(assignmentId)` transitions Assignment from `Active` to `Canceled` but carries no caller base version, violating `VPS-A003` A003-T54 exactly as F223 and F228 already established for Entity, Holiday, WorkingCalendar and WorkingPattern lifecycle transitions — found resuming Stage 13 on F239's ruling, before any code was written. Ruled: `assignment.cancel(assignmentId, expectedVersion)`, version checked first (`stale-state`), then the transition, the same ordering F223/F228 already require; `assignment.create`/`update` are unaffected.
- **F241** — `benchForecast.get`'s contract claims `aggregateUtilization`/`cohortSize` resolve entirely locally, while Stage 13 Do item 4 already computes them server-side and returns them "from `benchForecast.get`'s aggregate portion," naming no endpoint — found resuming Stage 13 a third time, before any code was written. Ruled: `VPS-A004` prohibits a client-side disclosure-control check, so the aggregate moves to a new named endpoint, `benchForecast.getAggregate`, mirroring `benchForecast.getCost`'s existing non-blocking split; `benchForecast.get` returns `rows` only.
- **F242** — `benchForecast.getAggregate`'s `filters?` argument has no defined schema: `VRS-F005`'s Filtering section names skill, seniority, department, entity and availability window in prose only, with no field names, value types, AND/OR rules, or availability-window meaning — found resuming Stage 13 a fourth time, before any code was written. Ruled: one shared `BenchForecastFilters` type and one portable `matchesBenchForecastFilters` function, called identically by the local row query and `benchForecast.getAggregate`, so the two can never derive different cohorts; the server derives both the filtered and unfiltered disclosure-control cohorts itself, never from client-supplied IDs.

**Do:**

1. **Assignment mutations and the 100% capacity constraint.** New `packages/schema/src/mutations/assignment.ts`, mirroring `calendar.ts`'s exact pattern: `assignment.create(employeeId, projectId, startDate, endDate, billablePercentage, rateCardId?)`, `assignment.update(assignmentId, fields)`, `assignment.cancel(assignmentId, expectedVersion)`. `validate()` sums the employee's other Active assignments' `billable_percentage` over the write's date range and rejects with `ConflictError` (naming the current total and the attempted addition) when the combined total would exceed 100 on any overlapping date — this is the default and is never silently bypassed; `VRS-F008`'s override path is out of scope this stage, but store `capacity_override_reason`/`capacity_override_by`/`capacity_override_at` as plain nullable fields now, per the spec's own schema, so `VRS-F008` has somewhere to write later. `assignment.cancel`'s `validate()` checks `expectedVersion` against the row's current version first, rejecting `stale-state` before anything else, then transitions `status` from `Active` to `Canceled` — the same version-first ordering F223 gave `entity.deactivate` and F228 gave `holiday.cancel`/`calendar.update`/`pattern.set`/`pattern.clear` (F240). `assignment.create` and `assignment.update` carry no version argument; `update` never touches `status`, the same ordinary-field-write exemption F228 gave `holiday.confirm`. `effective_billing_rate` is written at create time as the employee's existing `billing_rate_default` (`packages/schema/src/employee.ts` already has this field) — `rate_card_id`/`rate_override_hourly`/`rate_override_reason` stay nullable and unused; `VRS-F006` is not built (G07). Register in the shared `MUTATIONS` registry and `services/api/src/mutations/pipeline.ts`'s `IMPLEMENTATIONS`.
2. **The portable working-day resolution function (F237).** Extract the four-step resolution order (`Holiday` → `WorkingPattern` → `WorkingCalendar` → `ReducedHoursPeriod`) that Stage 12's `workingDays.*` already implements in `services/api` into one pure, no-I/O function in `packages/schema` (e.g. `resolveWorkingDay(calendar, pattern, holidays, reducedHoursPeriods, date)`), taking already-fetched rows and returning `{ isWorking, hours }` for one date — no database or cache access inside it. `services/api`'s existing `workingDays.*` calls it unchanged, fetching rows from Postgres exactly as today. Do not alter `workingDays.*`'s existing behavior or tests.
3. **The Bench Forecast's client-side query (no server round-trip).** New client-side query function in `packages/graph` (not `apps/`) — e.g. `packages/graph/src/queries/benchForecast.ts` — that reads `Assignment`, `Project`, `Client`, `GhostResource`, `OpenRole`, `Entity`, `WorkingCalendar`, `Holiday` and `WorkingPattern` directly from the device's local Electric-replicated `wa-sqlite` cache (all Tier 0) and calls item 2's shared `resolveWorkingDay` per employee per day in the requested window to derive bench periods, day counts and bar positions, narrowed by `filters?` through a new shared `matchesBenchForecastFilters(candidate, filters)` function in `packages/schema` (F242) — matching `benchForecast.get`'s corrected contract (F236, F241): bars, dates and bench-day counts only, no cost figure, no aggregate figure, no network call. `assignment.create`'s deterministic-ID and optimistic-write conventions follow `calendar.ts`'s own precedent (item 7 below) — this query function is read-only and has no mutation counterpart of its own.
4. **Bench cost and aggregate utilization, each a separate server call gated by disclosure control (F238, F241).** `services/api`: `benchForecast.getCost(employeeIds, window)` — a new endpoint reading each employee's compensation (Tier 1, via the existing `protected.read` path) and returning the accumulated cost per the spec's own compensation-type formulas (annual/monthly/hourly, calendar-year-aware), `null` for a caller without compensation access. `benchForecast.getAggregate(workspaceId, window, filters?)` — a second new endpoint computing aggregate utilization (the sum of `billable_percentage`/100 over cohort working days, divided by cohort working-day count) server-side per the spec's formula and passing it through the new shared disclosure-control function (item 5), returning `{ aggregateUtilization, cohortSize }`. It derives the filtered cohort and the unfiltered comparison cohort itself, from the caller's own authorized base cohort, using item 3's shared `matchesBenchForecastFilters` function against its own server-side data (F242) — never from a client-supplied employee-ID list. Both are single, batched, on-demand calls issued after item 3's local render and never block it; `benchForecast.get` itself returns only `rows` (F241) — filtering which employees match stays the local query in item 3, only the aggregate figure crosses the network, because `VPS-A004` requires disclosure control to run through the server-side interceptor and explicitly prohibits a client-side check.
5. **The shared disclosure-control function (F238).** New function in `packages/schema`'s policy module, e.g. `applyDisclosureControl(cohortSize, unfilteredCohortSize, computeAggregate, threshold)`: returns `Suppressed` when `cohortSize < threshold`; returns the unfiltered aggregate (computed against the full unfiltered cohort, with an `unfiltered: true` indicator) when the filtered cohort is smaller than the unfiltered cohort by fewer than `threshold` members; otherwise computes and returns the real aggregate over the filtered cohort. Add `k_anonymity_minimum` (default 5) and `k_anonymity_minimum_sensitive` (default 8) as plain nullable-with-default fields on `Workspace`'s schema, enforcing the documented floor of 3 wherever either is read — no settings UI (`VPS-F005_Workspace_Configuration_Console` still owns that later). `benchForecast.getAggregate` is this function's first caller (F241), using `k_anonymity_minimum` (Tier 0/2 default). The differencing-attempt audit trail is out of scope this stage (F238) — do not add an `AuditEntry` call for an ordinary or suppressed aggregate read.
6. **The Contextual Intelligence Panel.** Split per F236: a client-side function in `packages/graph` (e.g. `packages/graph/src/queries/contextualIntelligence.ts`) resolves `skills`, `openRoles` and `references` directly from the local cache (all Tier 0, via the existing `has_skill`/`placeholder_for`-style edges) with no network call. A new `services/api` endpoint (e.g. `contextualIntelligence.getProtected(employeeId)`) resolves `burnoutAlert`/`flightRiskSignal` through the permission interceptor exactly as `VPS-A004` already gates every Tier 2 read — present only when the caller's role passes, absent (never null) otherwise. The client function calls the local query and the protected endpoint together and merges them into the one return shape `contextualIntelligence.get` documents, matching the corrected API contract. `WellnessTriggerEvent` is never requested by either half — no suppression logic is written for it (F236).
7. **Optimistic mutators.** `packages/graph/src/mutators/foundation.ts`'s `OPTIMISTIC_MUTATORS` needs handlers for `assignment.create` and `assignment.update` (the same ordinary node-write shape `employeeUpdate`/`entity.update` already use) and `assignment.cancel` (a version-checked transition, mirroring `holidayCancel`'s exact shape: compare the cached Assignment's version to `expectedVersion`, throw `OptimisticRejection("stale-state")` on mismatch, otherwise write `status: "Canceled"` locally — F240). `assignment.create`'s handler does **not** re-check the 100% capacity constraint client-side — like `entity.deactivate`'s workspace-wide counts (F224), an overlapping-assignment total is not something a single device's cache can answer reliably offline (another device may hold an assignment this one hasn't synced yet); a server refusal is handled by the existing outbox rejection/undo path.
8. **The permission matrix.** Add explicit `MATRIX_OVERRIDES` rows for `Assignment`, `Project`, `Client`, `GhostResource` and `OpenRole` in `packages/schema/src/policy/policy-table.ts`, matching the same workspace-configuration pattern `Entity` and `WorkingCalendar` already use: `owner`/`hr-admin` → `FULL_ANY()`, `finance-admin`/`manager`/`team-member` → `READ_ANY()`. Without these rows every one of the five falls through to `Standard`'s person-scoped default, which is wrong for workspace-wide staffing data — write a test per node type that fails before the override and passes after, the same proof Stage 11 required for `Entity`.
9. **No UI, no Ghost rows, no rate cards, no Pitch-day exclusion.** Nothing under `apps/` changes — same discipline as Stage 8, 11 and 12. Ghost rows are out of scope this stage per the spec's own "until [`VRS-F007`] exists, none are rendered" allowance; do not build Ghost creation, promotion, or any `GhostResource` write path. Rate card resolution is out of scope per the same allowance for `VRS-F006`; `effective_billing_rate` is written from `billing_rate_default` only, never a rate card lookup. The Pitch-day exclusion is out of scope per F239's ruling — item 3's client-side bench query correctly omits `TimesheetEntry`; do not add `TimesheetEntry`, `Pitch`, or the `logged_against` edge, and do not build any read or write path for either node type. Bench periods are Assignment coverage only this stage.

**Done criteria:**
- `assignment.create`/`update`/`cancel` exist as named server mutations, permission-interceptor-gated, proven by integration tests.
- The 100% capacity constraint is proven with a `ConflictError` naming the current total and the attempted addition, on an overlapping-date attempt that would exceed 100 — not asserted from the diff.
- `resolveWorkingDay` (item 2) is a pure function with no I/O, called identically by `services/api`'s existing `workingDays.*` (unchanged behavior, existing tests still pass) and by the new client-side bench query (item 3) — proven by a test that both callers agree on the same inputs, not by inspection.
- The client-side bench query (item 3) computes bars, bench periods and day counts entirely from local cache rows with zero network calls — proven by a test run with the network cut, not asserted.
- `benchForecast.getCost` returns `null` for a caller without compensation access and the correct compensation-type-aware figure (annual/monthly/hourly, calendar-year-boundary-aware) for one who has it — proven with the spec's own examples, including a bench region crossing a calendar year.
- `benchForecast.getAggregate` never resolves on-device — proven by a test asserting it requires the network and the permission interceptor, the mirror image of item 3's local-with-the-network-cut proof — and `benchForecast.get`'s own return type carries no `aggregateUtilization`/`cohortSize` field (F241).
- `matchesBenchForecastFilters` (item 3) is called identically by the local bench query and `benchForecast.getAggregate` — proven by a test that both agree on cohort membership for the same filter input — and `getAggregate` derives its cohorts from the caller's own authorization rather than a client-supplied employee-ID list, proven by a test, not asserted (F242).
- `applyDisclosureControl` (item 5) is proven against both of `VPS-A004`'s own examples: a four-person cohort suppressed at the default threshold of 5, and a twelve-person cohort's aggregate returned unfiltered when a filter would reduce it to eleven — two independent tests, not one.
- `Workspace.k_anonymity_minimum`/`k_anonymity_minimum_sensitive` default to 5/8 and cannot be read or written below the floor of 3 — proven by a test, not asserted.
- The Contextual Intelligence Panel's Tier 0 half resolves with no network call and its Tier 2 half (`burnoutAlert`/`flightRiskSignal`) is proven against the real interceptor — a manager sees a report's `burnoutAlert` and not another manager's report's; `flightRiskSignal` is Owner/HR Admin only — not mocked.
- `WellnessTriggerEvent` is never queried anywhere in the diff — grep to confirm (F236).
- `MATRIX_OVERRIDES` rows for `Assignment`, `Project`, `Client`, `GhostResource` and `OpenRole` are each proven with a test that fails before the row and passes after (item 8), against a Team Member and a Manager specifically, not just Owner/HR Admin.
- `pnpm verify` passes typecheck with real optimistic handlers for `assignment.create`/`update`/`cancel` — no stub, no-op, or placeholder handler.
- `assignment.create`'s optimistic handler contains no capacity-constraint logic, relying on the outbox's existing rejection/undo path instead (item 7) — proven by a test, mirroring `entity.deactivate`'s own coverage (F224).
- `assignment.cancel` rejects a stale `expectedVersion` with `stale-state` before evaluating anything else, both server-side and in its optimistic handler, before any `Canceled` transition — proven by a test, mirroring `holidayCancel`'s own coverage (F240).
- No file under `apps/` appears in the diff; no `GhostResource` write path is added; no rate-card lookup is added; `effective_billing_rate` is written only from `billing_rate_default`; no `TimesheetEntry`, `Pitch` or `logged_against` read or write path is added (F239) — grep the diff to confirm.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 13 report:** that `assignment.create`/`update`/`cancel` go through the same checks/validate/apply pipeline every other mutation does; that the 100% capacity constraint is tested against a genuinely overlapping date range, not just an exact-match one; that `resolveWorkingDay` is a real shared function with one implementation, not two copies that happen to agree today — grep the diff for a second copy of the resolution order; that the client-side bench query (item 3) is proven offline, with the network cut, not merely coded to look local; that `benchForecast.getCost` and `benchForecast.getAggregate` are both separate calls from `benchForecast.get`'s bars/dates, never blocking the local render; that `benchForecast.getCost` returns `null` rather than a `billing_rate_default` substitute for an unauthorized caller; that `benchForecast.getAggregate` is gated by the permission interceptor and never resolves offline (F241); that `matchesBenchForecastFilters` is one implementation called by both the local query and `benchForecast.getAggregate`, not two copies that happen to agree today, and that `getAggregate` never accepts a client-supplied employee-ID list as its cohort (F242); that `applyDisclosureControl` is tested against both of `VPS-A004`'s own worked examples (four-person suppression, twelve-to-eleven differencing); that the two new `Workspace` k-anonymity fields have their floor of 3 enforced, not just documented; that the Contextual Intelligence Panel's Tier 2 fetch is proven against the real interceptor with a Manager who does and does not manage the subject employee, not a single always-permitted test; that `WellnessTriggerEvent` appears nowhere in the diff's query code; that each of the five new `MATRIX_OVERRIDES` rows has its own failing-before/passing-after test; that `assignment.create`'s optimistic handler has no capacity logic, relying on the outbox's rejection path; that no file under `apps/` appears in the diff; that neither `GhostResource` nor a rate-card lookup is implemented this stage; that `TimesheetEntry`/`Pitch`/`logged_against` appear nowhere in the diff, with bench periods computed from Assignment coverage alone (F239); and that `assignment.cancel` checks `expectedVersion` first, server-side and in its optimistic handler, before any `Canceled` transition (F240).

---

### Stage 14 — Rate Card Engine (`VRS-F006`)

Depends on `VRS-F002` (Employee, Stage 8 — `billing_rate_default`, `seniority_level`), `VRS-F003` (Entity, Stage 11, merged `96d53eb` — `default_currency`) and `VRS-F005` (Assignment, Stage 13, merged `d9b6d74` — the node this feature prices). Read `docs/Vulto_Specs/VRS-F006_Rate_Card_Engine.md` directly — its Technical Architecture, Graph Specifications (G01–G07), API contracts and its own corrected Interface Specification, NFRs, Security Considerations and Decisions Recorded sections (F243, F244) are the source of truth this brief summarizes, not the reverse. Checked directly against the code before writing this brief: `RateCard` and `RateCardLine` are already fully registered in `packages/schema/src/registry/nodes.ts` (`fixedProtection("Finance-restricted", 1)`, Tier 1), the `governed_by` (Assignment → RateCard) and `supersedes` (RateCard → RateCard) edges are already registered in `edges.ts`, and `RateCard` already carries Cross-Suite ownership alongside `Invoice` in `ownership.ts` — nothing schema-level is missing — but no mutation, no rate-resolution logic, and no `governed_by` edge write path exist anywhere. `RateCard`/`RateCardLine` need no new `MATRIX_OVERRIDES` row: both already fall through to the Finance-restricted default class mapping in `packages/schema/src/policy/policy-table.ts`, which is correct per the spec's own Security Considerations ("No new permission row was required").

Five findings surfaced on this stage, all closed the same day each was raised (full reasoning in `docs/Foundations_Findings.md`; `VRS-F006` corrected to match):

- **F243** — `VPS-A002`'s edge registry declares `governed_by: Assignment → RateCard`, owned by this feature and backed by real registry code, but `VRS-F006`'s own contracts described resolution purely against Assignment's scalar `rate_card_id` field, with no mutation ever writing the registered edge — leaving `rateCard.usageCount` and the version-history panel's active-assignment count with no traversal path, which A002-T03 already forbids as an unmaterialized FK join. Ruled: Assignment keeps `rate_card_id` as its own resolution field (unchanged, already built by Stage 13); `assignment.setRateCard`/`assignment.create` additionally write the registered `governed_by` edge, mirroring `assignment_of`/`assigned_to`'s existing dual scalar-plus-edge shape, managed manually (close-then-open) the same way `governed_by_calendar` is already managed by `calendar.ts`.
- **F244** — `VRS-F006`'s NFRs, System States and Depends-On line described RateCard/RateCardLine as syncing to devices and aging out of a Tier 1 window — `VPS-A003` says Tier 1 is memory-only, fetched on demand, never persisted on any device, and withdraws the retention window entirely. The same class of stale local-first language F236 already corrected once in `VRS-F005`. Ruled: corrected to fetch-on-demand phrasing throughout; "Aged out" marked Not applicable, the same answer `VRS-F005` already gives its own Tier 2 Panel content.
- **F245** — found tracing the corrected brief into the mutation registry, before any code was written: `rateCard.update`'s signature carries no `currency`/`name` parameter despite the brief calling `currency` caller-supplied on update, and marking the prior card `is_active: false` on supersession carried no `expectedVersion` despite A003-T54. Ruled: `rateCard.update(rateCardId, expectedVersion, lines)`, version checked first, on `calendar.update`'s own precedent (F228); `name`/`currency` copy forward from the prior card, never accepted by `update`; the `is_active` transition is a permitted lifecycle-only exception to G02, the same shape Stage 12's `WorkingPattern` supersession already uses.
- **F246** — found in the same trace: card-bearing `assignment.create` was made responsible for a `governed_by` edge write and a Tier 1 `RateCardLine` read, but Stage 13's existing optimistic handler for it was never revisited. Ruled: the `governed_by` edge is written server-side only, in `apply()`, never optimistically — nothing on-device ever reads it; `assignment.create`'s optimistic handler is unchanged from Stage 13 (scalar `rate_card_id`, `effective_billing_rate` always defaulting to `billing_rate_default` locally, corrected once the server confirms), the same server-only-resolution discipline F224/F241 already established. No conditional `onlineOnly` mechanism is needed.
- **F247** — found resuming Stage 14 a second time, once Codex reached the RateCardLine lookup itself: `rate_card_id`/`seniority_level` are protected Tier 1 fields with no edge and no index, but `rateCard.getPreview` and `resolveAssignmentRate` both need a point lookup by `(rateCardId, seniority)`, and `readProtected` only accepts node ids — never a predicate over decrypted content. The only alternative was a workspace-wide decrypt-and-filter scan, exactly what A002-T03 already forbids. Ruled: `RateCardLine.line_id` is generated deterministically from `(rate_card_id, seniority_level)` — the same `deterministicUuid(seed)` scheme `membership-edge-ids.ts` already uses for `membership_of`/`membership_in`, and F233's `initialCalendarId` already uses for `WorkingCalendar` — so both callers compute the id they need instead of searching for it. No edge, no index, no new mechanism; G05 ("at most one line per seniority") becomes automatic, since a duplicate seniority always computes the identical id and overwrites the same fragment.

**Do:**

1. **`RateCard`/`RateCardLine` mutations and versioning.** New `packages/schema/src/mutations/rateCard.ts`, mirroring `calendar.ts`'s exact supersession pattern: `rateCard.create(name, currency, lines)` creates a new `RateCard` (version 1) plus its `RateCardLine` rows in one write; `currency` is caller-supplied — the mutation does no server-side Entity lookup or default-resolution of its own; `RateCard` has no `entity_id` field or edge (confirmed against the schema), so "defaults to the Entity's `default_currency`" (G06) is UI-time behavior for whichever future stage builds the Rate cards screen, out of scope here. `rateCard.update(rateCardId, expectedVersion, lines)` checks `expectedVersion` first — `stale-state` before anything else, on `calendar.update`'s own precedent (F245/G08) — then creates a new `RateCard` (`version` incremented, `supersedes_id` set to the prior card's id) with the given lines, copying `name` and `currency` forward from the prior card unchanged (`update` accepts neither as a parameter), and marks the prior card's `is_active: false` — a permitted lifecycle-only exception to G02's content immutability (F245/G02), the same shape Stage 12's `WorkingPattern` supersession already uses. Each `RateCardLine`'s `line_id` is computed as `deterministicUuid(\`rate_card_line:${rateCardId}:${seniorityLevel}\`)` (F247/G10) — the same SHA-256-derived, UUID-v4-shaped scheme `services/api/src/auth/membership-edge-ids.ts` uses for `membership_of`/`membership_in` and F233's `initialCalendarId` uses for `WorkingCalendar` — never a random UUID v4. Within one write, at most one `RateCardLine` per `seniority_level` (G05): this is enforced by the id scheme itself — a duplicate `seniority_level` in the same `lines` array computes the identical `line_id` and overwrites the same protected fragment, so no separate duplicate-detection code is needed. Register both in the shared `MUTATIONS` registry and `services/api/src/mutations/pipeline.ts`'s `IMPLEMENTATIONS`.
2. **The rate-resolution engine (G04).** A pure function in `packages/schema` (e.g. `resolveAssignmentRate(input)`), run server-side on every write that could change it: 1) `rate_override_hourly` if set; 2) else the `RateCardLine` matching the employee's `seniority_level` under the Assignment's `rate_card_id`, read via a single-id `readProtected` call against `line_id = deterministicUuid(\`rate_card_line:${rateCardId}:${seniorityLevel}\`)` (F247/G10) — a point lookup, never a scan, and no RateCardLine caching, ever; 3) else the employee's `billing_rate_default` (Tier 0, already on Assignment's create path since Stage 13), converted from daily to hourly at 8 hours. A seniority with no line under the card computes an id that was never written, so the lookup returns nothing and resolution falls through to rule 3, exactly as the spec describes. The resolved figure writes to `effective_billing_rate` atomically with the same transaction as the triggering write (NFR) — never a separate step, never recomputed retroactively when a card is later superseded (G04). `assignment.create` calls it once at creation; `assignment.setRateCard`, `assignment.setRateOverride` and `assignment.clearRateOverride` (item 4) each call it again after their own field change.
3. **The `governed_by` edge, alongside the scalar field (F243).** `assignment.create` (when `rate_card_id` is provided) and `assignment.setRateCard` write a `governed_by` edge from the Assignment to the chosen RateCard: close any existing active `governed_by` edge from that Assignment first (`effective_to` at the write's timestamp), then open the new one (`effective_from` at the same instant) — the same manual close-then-open shape `calendar.ts` already uses for `governed_by_calendar`. No `historyPolicy` is added to the shared `governed_by` edge type (F243's own ruling). **This write happens exclusively in each mutation's server-side `apply()` step** — never in an optimistic handler (F246/G09); item 7 covers what the optimistic half of each of these mutations does instead.
4. **Three new Assignment mutations.** `assignment.setRateCard(assignmentId, rateCardId) -> { effectiveBillingRate }` (item 3's edge write, then item 2's resolution). `assignment.setRateOverride(assignmentId, hourly, reason) -> { effectiveBillingRate }` — `reason` is required, matching `capacity_override_reason`'s existing required-when-set pattern; writes `rate_override_hourly`/`rate_override_reason`, then resolves (always to `hourly`, per rule 1 — this call never touches the `governed_by` edge). `assignment.clearRateOverride(assignmentId) -> { effectiveBillingRate }` — nulls both override fields, then resolves through rules 2–3. All three follow the existing checks/validate/apply mutation pipeline shape; none takes an `expectedVersion` (they set specific fields, the same ordinary-field-write exemption F228 gave `holiday.confirm` and F240 gave `assignment.update`).
5. **Read-side server queries.** `rateCard.list(workspaceId) -> RateCard[]` — a plain permission-interceptor-gated read. `rateCard.getPreview(rateCardId, seniority) -> { hourlyRate, dailyRate, monthlyRate }` computes `line_id` the same deterministic way (item 1/F247/G10) and reads that one node id — never a scan, never an index over encrypted content. `rateCard.usageCount(rateCardId) -> { activeAssignments }` and the version-history "how many assignments still point at it" figure both resolve via `incoming(tx, workspaceId, rateCardId, "governed_by")`, counting only edges with `effective_to: null` — never an Assignment table scan (F243).
6. **Permission verification, no new row.** Write a test proving `RateCard`/`RateCardLine` already resolve Owner/HR Admin/Finance Admin → full, Manager/Team Member → none, through the existing Finance-restricted default class mapping with zero `MATRIX_OVERRIDES` entry — the same "prove the fallback, don't add a row" shape the spec's own Security Considerations describes.
7. **Optimistic mutators, split by what a device can know.** `rateCard.create`/`rateCard.update` get **no** optimistic handler at all — RateCard/RateCardLine are Tier 1, never cached on device (F244), so these are online-only, server-resolved mutations; mark them `onlineOnly: true` in `defineMutation`, the same convention `benchForecast.getAggregate` already established for a Tier-1-dependent operation. `assignment.create`'s existing Stage 13 optimistic handler is **unchanged** by this stage (F246): it already writes `rate_card_id` as a plain scalar field and always sets `effective_billing_rate` to the employee's cached `billing_rate_default`, whether or not a card is supplied — do not add a local `governed_by` edge write here, and do not attempt to resolve the true card-based rate on-device. `assignment.setRateOverride`'s optimistic handler writes `rate_override_hourly`/`rate_override_reason` and sets `effective_billing_rate` to `hourly` directly — rule 1 is unconditional and needs no RateCardLine lookup, so this is always safely computable on-device. `assignment.setRateCard`'s optimistic handler writes only the scalar `rate_card_id` field — no local edge write (item 3) — and leaves `effective_billing_rate` at its previous cached value, unchanged, until the server's confirmed response updates it (the true value always needs a RateCardLine here, since a card was just selected). `assignment.clearRateOverride`'s optimistic handler updates `effective_billing_rate` locally **only** when the true resolution is knowable without a RateCardLine — falling back to the employee's cached `billing_rate_default` (Tier 0, already local) when the Assignment's cached `rate_card_id` is null; otherwise it too leaves `effective_billing_rate` unchanged until the server confirms — the same "deliberately server-only, no client precompute" discipline F224 established for capacity and F241 established for disclosure control.
8. **No UI.** Nothing under `apps/` changes — same discipline as Stage 8, 11, 12 and 13.

**Done criteria:**
- `rateCard.create`/`update` exist as named server mutations, permission-interceptor-gated (Finance-restricted, no override row needed — item 6), proven by integration tests; `update` creates a new versioned node linked by `supersedes` and never edits the prior one in place (G02), proven by a test reading the prior version's fields unchanged after the update.
- `rateCard.update` checks `expectedVersion` first — `stale-state` before anything else, before any new node is written — proven by a test that calls `update` with a stale `expectedVersion` against a card already superseded by a concurrent call and asserts no third `RateCard` version is created (F245/G08).
- `rateCard.update` accepts no `name` or `currency` parameter; the new version's `name` and `currency` are copied forward from the prior card unchanged — proven by a test that updates only `lines` and asserts the resulting card's `name`/`currency` match the prior version exactly, not a caller-supplied value (F245).
- A duplicate `seniority_level` within one `create`/`update` call replaces rather than creating a second `RateCardLine` (G05) — proven by a test, not asserted; the test should confirm this by construction (the id scheme, F247/G10), not by separate duplicate-detection code appearing in the diff.
- `RateCardLine.line_id` is `deterministicUuid(\`rate_card_line:${rateCardId}:${seniorityLevel}\`)`, never a random UUID v4 — proven by a test that creates the same `(rateCardId, seniority)` twice (in one `lines` array and across two separate writes) and asserts the resulting `line_id` is identical both times (F247/G10).
- `rateCard.getPreview` and `resolveAssignmentRate` both reach a `RateCardLine` via a single-id `readProtected` call computed from `(rateCardId, seniority)` — proven by a test that no query or grep-detectable code path enumerates or decrypts more than one RateCardLine per lookup; a seniority with no line under the card resolves to "no line" (falls through to `billing_rate_default` in `resolveAssignmentRate`, or an empty/not-found result from `getPreview`), not an error (F247).
- `resolveAssignmentRate` (item 2) is proven against all three of the spec's own resolution branches, including the case where an override and a rate card are both present (the override wins) and the calendar-agnostic daily-to-hourly conversion for the `billing_rate_default` fallback.
- `effective_billing_rate` is written atomically with its triggering mutation's own transaction, never as a separate step — proven by a test asserting no intermediate state is externally observable, not asserted from the diff.
- Updating a rate card does **not** change `effective_billing_rate` on Assignments already pointing at the prior version (G02, G04) — proven by a test that reads an existing Assignment's resolved rate before and after an unrelated `rateCard.update`.
- `assignment.setRateCard` and `assignment.create` (when `rate_card_id` is given) write a `governed_by` edge from the Assignment to the RateCard, closing any prior active one first — proven by a test asserting exactly one edge with `effective_to: null` per Assignment at any time (F243). The edge write happens only in each mutation's server-side `apply()` step — proven by a diff review (not a runtime test) confirming no `OPTIMISTIC_MUTATORS` entry touches a `governed_by` edge (F246/G09).
- `assignment.create`'s optimistic handler is unchanged from Stage 13 — proven by a diff showing no new lines touching its optimistic branch beyond what Stage 13 already built: `rate_card_id` still writes as a plain scalar, `effective_billing_rate` still defaults unconditionally to `billing_rate_default` until the server confirms (F246).
- `rateCard.usageCount` and the version-history active-assignment figure both resolve via `incoming(..., "governed_by")` — proven by a test with several Assignments pointing at different card versions, not a table scan (F243).
- `assignment.setRateOverride` requires a non-empty `reason`; `assignment.clearRateOverride` nulls both override fields and re-resolves through the card/default fallback — both proven by tests, not asserted.
- `rateCard.create`/`update` have no optimistic mutator and are marked `onlineOnly: true` — proven by a test that the mutation is rejected or deferred when attempted offline (F244).
- `assignment.setRateOverride`'s optimistic handler always resolves `effective_billing_rate` to the given `hourly` value locally — proven with the network cut.
- `assignment.setRateCard`'s and `assignment.clearRateOverride`'s optimistic handlers never fabricate a RateCardLine-dependent `effective_billing_rate` on-device — proven by a test asserting the cached value is left unchanged (not zero, not null, not guessed) when the true resolution requires a card lookup, and correctly resolved locally when it does not (the `billing_rate_default`-fallback case).
- `pnpm verify` passes typecheck with real handlers for every new mutation — no stub, no-op, or placeholder.
- No file under `apps/` appears in the diff; no `entity_id` field or edge is added to `RateCard`; no `MATRIX_OVERRIDES` row is added for `RateCard`/`RateCardLine` (item 6) — grep the diff to confirm.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 14 report:** that `rateCard.update` creates a genuinely new node (a fresh `rate_card_id`) rather than mutating the prior version's own row; that `rateCard.update` rejects a stale `expectedVersion` before creating that new node — not after, and not merely logged (F245); that `rateCard.create`'s signature accepts `name`/`currency` while `rateCard.update`'s does not, and that `update`'s new version carries the prior card's `name`/`currency` forward untouched even when the diff shows no explicit copy statement (it should); that a Manager or Team Member calling any RateCard mutation or query is rejected by the existing Finance-restricted default, with no new permission row anywhere in the diff; that `resolveAssignmentRate` is one implementation called by all four sites that can trigger it (`assignment.create`, `setRateCard`, `setRateOverride`, `clearRateOverride`), not a copy re-implemented per call site; that the `governed_by` edge write closes the prior active edge before opening the new one, proven with a caller that changes a card twice in a row, not just once; that the edge write appears only inside each mutation's server-side `apply()`, never in `OPTIMISTIC_MUTATORS` (F246); that `assignment.create`'s optimistic-handler diff is empty or near-empty against its Stage 13 form — any new lines there beyond what Stage 13 already built are a finding, not a feature; that `rateCard.usageCount` genuinely traverses `incoming(..., "governed_by")` rather than scanning Assignment rows — grep the diff for a `SELECT`/query filtering Assignment by `rate_card_id` outside the resolution engine itself; that `RateCardLine.line_id` is computed with `deterministicUuid`, not `randomUUID` or similar — grep the diff for the line-creation call site (F247); that `rateCard.getPreview` and `resolveAssignmentRate` both call `readProtected` with a single computed node id, never a loop over multiple RateCardLine ids and never a call to any code path that lists or enumerates RateCardLines for a card (F247); that `rateCard.create`/`update` carry `onlineOnly: true` and no entry in `OPTIMISTIC_MUTATORS`; that the three optimistic Assignment-rate handlers are tested with the network cut, and specifically that the RateCardLine-dependent case leaves `effective_billing_rate` untouched rather than approximating it; and that no file under `apps/` appears in the diff.

### Stage 15 — Ghost Resources (`VRS-F007`)

Depends on `VRS-F002` (Employee, Stage 8 — `employee_type`, full schema) and `VRS-F005` (Assignment, Stage 13, merged `d9b6d74` — the model Ghosts participate in identically, G04). Read `docs/Vulto_Specs/VRS-F007_Ghost_Resources.md` directly — its Technical Architecture, Graph Specifications (G01–G08), API contracts and its own corrected Depends-On, API contracts and Out of Scope sections (F248, F249) are the source of truth this brief summarizes, not the reverse. Checked directly against the code before writing this brief: `GhostResource` is already fully registered in `packages/schema/src/registry/nodes.ts` (`fixedProtection("Standard", 0)`, Tier 0, lifecycle `Active`/`Promoted`/`Canceled`); `OpenRole` is already registered too (same file, owner `VRS-F028`, Tier 0); the `placeholder_for` (GhostResource → OpenRole) and `promoted_to` (GhostResource → Employee) edges are already registered in `edges.ts`; `GhostResource → Employee, Promoted` is already the registered conversion in `conversions.ts` (F88's own prior ruling — `Promoted`, not `Converted`); `employee_type: z.enum(["Employee", "Ghost"])` is already the live field on Employee in `packages/schema/src/employee.ts`; and `GhostResource`/`OpenRole` already carry explicit `MATRIX_OVERRIDES` rows in `packages/schema/src/policy/policy-table.ts` (Owner/HR Admin full, Finance Admin/Manager/Team Member read — Stage 13's own workspace-staffing rows, `f2d928d`). But no mutation, no read query, and no promotion transaction exist anywhere: a repository-wide search for `ghostResource` across `packages/schema`, `services/api` and `packages/graph` returns nothing. Seven gaps surfaced across briefing and implementation, all closed the same day each was raised (full reasoning in `docs/Foundations_Findings.md`; `VRS-F007` corrected to match):

- **F248** — `VRS-F007`'s Depends-On section named `VRS-F002`, `VRS-F005`, `VPS-A002`, `VPS-A003` and `VPS-A004` but not `VRS-F028`, although the optional `openRoleId` on `ghostResource.create`, `ghostResource.linkOpenRole` and the registered `placeholder_for` edge all target `OpenRole` — a node type with no mutation, field schema or create path anywhere in the repository, owned by `VRS-F028` (MVP position F17, well after this feature's F04). Ruled: the same F239 treatment — a Partial forward dependencies line added; `openRoleId` is accepted and the edge write built now, simply unreachable until `VRS-F028` lands.
- **F249** — `ghostResource.promote`'s contract names a second path, `{ existingEmployeeId }`, for "linking an existing Employee node" — described as the common case, a candidate hired through `VRS-F028` — but no acceptance criterion, G-spec or field-level detail defines how the Ghost's own placeholder Employee node, or its Assignment/skill edges, reconcile with a second, already-existing Employee node. Ruled: Stage 15 builds the fully-specified path only (`{ fullName, email, employmentType, startDate }`, filling the Ghost's own linked Employee node in place — every acceptance criterion, G06 and G07 describe exactly this); `existingEmployeeId` is deferred, rejected with a clear "not yet supported" error rather than a half-built cross-node merge invented on no more grounding than "the common case."
- **F250** — found resuming Stage 15 once Codex traced the corrected brief into the mutation registry: `ghostResource.cancel`/`promote` transition `GhostResource.lifecycle_status` but carried no `expectedVersion`, contradicting A003-T54; and the brief's own "the transition guard gives G07's exclusivity for free" reasoning only holds for sequential requests — `services/api/src/mutations/pipeline.ts` uses default transaction isolation unless a mutation name is explicitly placed on its serializable list, so two concurrent `promote` calls could both read `Active` and both proceed. Ruled: both mutations gain `expectedVersion`, checked first, on `calendar.update`/`rateCard.update`'s own precedent (F228, F245); `ghostResource.promote` (a genuinely multi-row transaction) joins the serializable list alongside `calendar.update`/`assignment.cancel`/`rateCard.update`, `ghostResource.cancel` (single-row) does not, the same distinction `holiday.confirm`'s own exemption already establishes; a version mismatch on `promote` returns G07's actor-and-time rejection when the cause is an existing promotion, the ordinary `stale-state` rejection otherwise — one check, two messages, no competing mechanism.
- **F251** — found resuming Stage 15 a second time: this brief claimed three times that `GhostResource`/`OpenRole` have zero `MATRIX_OVERRIDES` entries and resolve through the `Standard` default's fallback. The live code says the opposite — Stage 13 (`f2d928d`, Do item 8) already added explicit, tested overrides for both, and the `Standard` default's `direct-reports`/`own-plus-team` scopes can't resolve a subject for either node type, so deleting the overrides would drop Manager/Team Member to `none`, not to read. Worse, the existing override and this brief's own Done criteria both left Manager read-only, contradicting `VRS-F007`'s own Creating-a-Ghost flow (Owner, HR Admin **or Manager**), and `authorizeWrite` requires a literal `full` outcome with no finer-grained mechanism anywhere in this codebase. Ruled: the Stage 13 overrides are kept, not removed; `GhostResource`'s `manager` cell is extended from `READ_ANY()` to `FULL_ANY()` (leave `team-member` and all of `OpenRole` unchanged); the resulting full Manager access to `promote`/`cancel` is accepted as a node-type-scoped, not mutation-scoped, consequence of the existing permission model, matching `VRS-F007`'s own Security Considerations disclaiming confidentiality concerns here; `policy-table.test.ts`'s shared `it.each` test is split so `GhostResource` gets its own dedicated assertions.
- **F252** — found resuming Stage 15 a third time: this brief's Do item 5 requires a successful-promotion `VPS-F004` audit entry, but `VPS-F004` logs successful access only at Tier 1/3, both GhostResource and Employee are Tier 0, and the closed five-value event taxonomy has no ordinary successful-operation category — the identical shape of gap F221 (Entity transfer) and F225 (Holiday confirmation) already found and closed. Ruled on that same precedent: `VPS-F004`'s taxonomy is not extended, no `appendAudit` call is added; `GhostResource`'s own `updated_by`/`updated_at`, the `promoted_to` edge's `created_by`/`created_at` (created once, never modified, G03) and the linked Employee's own `updated_by`/`updated_at` — all mandatory per `VPS-A002`'s Universal Node/Edge Conventions — already give a permanent who-and-when promotion record with no new write. `VRS-F007`'s Security Considerations bullet is corrected to cite that provenance instead of a `VPS-F004` event.
- **F253** — found resuming Stage 15 a fourth time: `employeeOperationalRecord`'s `EmployeeCreateFields` requires `employee_code`, `full_name`, `email` and `employment_type`, none of which `ghostResource.create`'s inputs supply, and promotion never supplied `employee_code` either, so the codebase could not build the null-field Ghost record the brief specified nor leave a valid real Employee after promotion. Ruled: `packages/schema/src/employee.ts` gains a second, Ghost-only `ghostEmployeeCreateFieldsSchema`/`ghostEmployeeOperationalRecord` pair that nulls exactly `employee_code`/`full_name`/`email`/`employment_type` (the real-Employee schema and constructor are untouched); `contracted_hours` defaults to 40 like real creation already does, since it is a capacity number, not an identity fact; promotion's contract gains a required `employeeCode` and optional `contractedHours`, validated with `employee.create`'s own already-exported `requireUnique` helper rather than a new mechanism.
- **F254** — found once Codex began real implementation and its first `ghostResource.promote` integration test reached the actual permission interceptor: `promoted_to` (GhostResource → Employee) declares no `governingPartitions` entry, and Employee is tier-split, so `edgeRoleDecision` structurally refuses the edge write with `reason: "role"` for every role before any code runs — the identical class of gap F208 found for `managed_by`/`scoped_to_entity` and F235 found for `pattern_for`. Ruled on that same precedent: `promoted_to` is declared `{ Employee: "operational" }` in `packages/schema/src/registry/edges.ts` and in `VPS-A002`'s governing-partitions table — promotion writes only Employee's Tier 0 identity fields, never compensation, the same reasoning already used for six other Employee-endpoint edges.

One integration point surfaced checking `VRS-F005` (Stage 13)'s own code directly, already ruled and recorded by `VRS-F005` itself, not a new finding: `VRS-F005`'s Partial forward dependencies line states "`VRS-F007` supplies Ghost rows; until it exists, none are rendered" (F239's own closing text), and Stage 13's `packages/graph/src/queries/bench-forecast.ts` correctly implements that deferral today with a `node.record["employee_type"] !== "Ghost"` filter over the row list. Now that `VRS-F007` exists, that filter's condition has been met — item 6 below removes it, per `VRS-F005`'s own stated contract, not a change to `VRS-F005`'s scope.

**Do:**

1. **`GhostResource` schema and the two-node atomic create — a Ghost-specific Employee record constructor (F253).** New `packages/schema/src/ghost-resource.ts`: `ghost_id` (UUID v4, random — no deterministic-id requirement here, since both nodes are Tier 0 and reached by plain `getNode`/`getNodes`, never `readProtected`'s point-lookup-only constraint), `workspace_id`, `ghost_employee_id` (FK, set once at creation, never changed — G01), `lifecycle_status` (`Active`/`Promoted`/`Canceled`, matches the registry), `notes` (nullable rich text). `employeeOperationalRecord`'s existing `EmployeeCreateFields` input requires `employee_code`, `full_name`, `email` and `employment_type` — facts a Ghost genuinely does not have yet — so `packages/schema/src/employee.ts` gains a second, Ghost-only pair used only here: `ghostEmployeeCreateFieldsSchema` (`job_title`, `start_date` required; `seniority_level`, `billing_rate_default` optional — exactly `ghostResource.create`'s own inputs, nothing else, `.strict()`) and `ghostEmployeeOperationalRecord(fields)`, returning the same record shape `employeeOperationalRecord` returns but with `employee_type: "Ghost"`, and `employee_code`/`full_name`/`email`/`employment_type` explicitly `null`, `contracted_hours: 40` (the same default `employeeOperationalRecord` already uses when unspecified). `employeeCreateFieldsSchema` and `employeeOperationalRecord` themselves are untouched. New `packages/schema/src/mutations/ghostResource.ts`: `ghostResource.create(roleTitle, projectedStartDate, seniorityLevel?, targetSkills?, expectedRate?, openRoleId?) -> { ghostId, employeeId }` creates both nodes atomically in one write — an Employee node built via `ghostEmployeeOperationalRecord`, `lifecycle_status: "Active"`, and a `GhostResource` node (`ghost_employee_id` pointing at it, `lifecycle_status: "Active"`) — mirroring the existing multi-node-atomic-create precedent (F195's workspace/membership projection, F233's calendar bootstrap). `targetSkills`, if given, write `has_skill` edges from the new Employee node identically to how `VRS-F002`'s own employee-create path writes them (G05 — no special-casing). `openRoleId`, if given, writes the `placeholder_for` edge (item 3). Register in `MUTATIONS` and `services/api/src/mutations/pipeline.ts`'s `IMPLEMENTATIONS`.
2. **`ghostResource.cancel(ghostId, expectedVersion) -> { success }` (F250).** `expectedVersion` checked first — `stale-state` before anything else, `calendar.update`'s own precedent (F228) — then a plain lifecycle transition, `Active -> Canceled`, following the existing fixed-lifecycle-transition guard shape (`isValidEmployeeTransition`'s pattern, applied to `GhostResource`'s own registered transitions). A single-row write: not added to the serializable-isolation list (item 4a). The linked Employee node is untouched — Out of Scope's own text gives no separate employee-side effect, and none of G01–G08 describes one.
3. **`ghostResource.linkOpenRole(ghostId, openRoleId) -> { success }` (F248, partial forward dependency).** Writes the `placeholder_for` edge: close any existing active `placeholder_for` edge from that GhostResource first, then open the new one (G02's "relinking replaces; it never duplicates" — the same manual close-then-open shape `calendar.ts` uses for `governed_by_calendar`). Build and register this now; it has no real caller until `VRS-F028` creates its first `OpenRole` node, per F248's ruling.
4. **The promotion transaction (G06, G07) — the one operation in this product requiring server confirmation.** `ghostResource.promote(ghostId, expectedVersion, { employeeCode, fullName, email, employmentType, startDate, contractedHours? }) -> { status: 'promoted' | 'pending_confirmation' }` (F249 — the `existingEmployeeId` variant is rejected with a clear "not yet supported" error, not built; F253 — `employeeCode` is newly required: there is no employee-code auto-generation mechanism anywhere in this codebase, `employee.create` already requires the caller to supply it, so promotion asks for the same human decision real hiring already asks for; `contractedHours` is newly optional — given, it overwrites the Ghost's current value, omitted, the value already on the node from creation, 40 by default, carries forward unchanged, the same "every other field untouched" principle G06 already states for everything else). Mark `onlineOnly: true` (item 7) — this is what makes promotion the deliberate, spec-named exception to `VPS-A003`'s last-write-wins (no optimistic completion; the caller sees `pending_confirmation` until the server round-trip resolves it). **4a. Concurrency (F250).** Add `"ghostResource.promote"` to `services/api/src/mutations/pipeline.ts`'s literal serializable-isolation list, alongside `calendar.update`/`assignment.cancel`/`rateCard.update` — this is a genuinely multi-row transaction (GhostResource's own transition, the linked Employee's field update, the `promoted_to` edge write), the same shape every other listed mutation already has, and it is what makes the one-winner guarantee real: a transaction that loses the serializable race gets a `40001` and is retried by the pipeline's existing retry loop against fresh state, so the losing request's own version check then correctly sees the new `Promoted` state. `expectedVersion` is checked first, inside that serializable transaction — `stale-state` before anything else. On a version mismatch, re-read the GhostResource's current `lifecycle_status` and its `updated_by`/`updated_at` provenance (already recorded on every write per `VPS-A002`'s universal node conventions): if the current status is `Promoted`, return a dedicated "already promoted" rejection naming that actor and time (G07); for any other reason the version moved, return the ordinary generic `stale-state` rejection A003-T54 requires — one check, one code path, two messages depending on what it finds. **4b. The transaction itself**, atomic across every step (G06 — any failure rolls back entirely, the Ghost remains `Active` with no partial state committed): validate first with `services/api/src/mutations/employee.ts`'s already-exported `requireUnique(ctx, { email, employee_code: employeeCode }, employeeId)` (F253) — the identical helper `employee.create` already uses, excluding the Ghost's own linked Employee node id since this updates that node rather than creating a new one; a clash returns the same `duplicate-email`/`duplicate-code` rejection `employee.create` already returns. Once the version check and uniqueness check both pass, transition `GhostResource.lifecycle_status` from `Active` to `Promoted`; update the linked Employee node (found via `ghost_employee_id`) with `employee_type: "Employee"`, `employee_code`, `full_name`, `email`, `employment_type`, `start_date`, and `contracted_hours` if given (F253) — every other field (Assignment edges, skill edges, graph history) untouched, because this is a field update on the same node, not a new one; create the `promoted_to` edge (GhostResource → Employee) once, never modified again (G03). **4c. Governing partition (F254).** `promoted_to` targets tier-split Employee and needs a `governingPartitions: { Employee: "operational" }` entry on its `packages/schema/src/registry/edges.ts` edge group — without it, `edgeRoleDecision` structurally refuses the write for every role, the same class of gap F208/F235 already fixed for `managed_by`/`scoped_to_entity`/`pattern_for`. Add the matching row to `VPS-A002`'s governing-partitions table.
5. **No audit call (F252).** Ghost promotion is an ordinary Tier 0 write, not a `VPS-F004`-audited event — the same shape F221/F225 already ruled for Entity transfer and Holiday confirmation. Do not call `appendAudit` anywhere in `ghostResource.create`/`.cancel`/`.promote`. The who-and-when record comes for free from `GhostResource`/Employee's own `updated_by`/`updated_at` and the `promoted_to` edge's `created_by`/`created_at` (G03) — no code needed beyond the ordinary write path every mutation already has.
6. **Wire Ghost rows into the Bench Forecast (`VRS-F005`'s own stated contract, F239).** In `packages/graph/src/queries/bench-forecast.ts`, remove the `node.record["employee_type"] !== "Ghost"` exclusion from the row-generation loop — Ghost Employee rows flow through the exact same computation as any other Employee row (G04's "no special case": same assignments, same bench-period derivation, same `Assignment`/`Project`/`Client` joins), because the row already carries `employee.record["employee_type"]`, which is all the UI needs to render the dashed/distinct treatment later — no new field on `BenchForecastRow` is needed. Leave `services/api/src/permission/bench-forecast-queries.ts`'s `authorizedCohort` exclusion (`summary.record["employee_type"] === "Ghost"` → `continue`) untouched — that filter feeds the real-employee `aggregateUtilization` figure, which must stay real-only; item 7 adds the separate Ghost figure G08 requires alongside it, never blended into it.
7. **Ghost contribution, reported separately (G08).** In `services/api/src/permission/bench-forecast-queries.ts`, add a Ghost-only capacity computation, structurally parallel to `authorizedCohort`/`utilization` but built over the Ghost employees `authorizedCohort` currently excludes: for each `Active` Ghost Employee in the same k-anonymity-controlled result, sum covered capacity (weighted by `billable_percentage`) over the window, divided by the same real cohort's total available capacity — not a separate Ghost-only denominator; this answers "how much higher would utilization read if these placeholders were real," the question G08's "reported separately alongside it" exists to answer, never "how utilized are the Ghosts themselves." `getBenchForecastAggregate` returns a new `ghostContribution` field alongside the existing `aggregateUtilization`, subject to the same disclosure-control suppression state — a suppressed cohort suppresses both figures together, never one without the other (`VPS-A004`'s own rule, F241's own precedent, applies identically here).
8. **Read-side queries.** `ghostResource.list(workspaceId, status?) -> GhostResource[]` — a plain permission-interceptor-gated read, no override row needed (item 9 proves the fallback).
9. **Permission verification — extend the existing override, split its test (F251).** In `packages/schema/src/policy/policy-table.ts`, change `MATRIX_OVERRIDES["GhostResource"].manager` from `READ_ANY()` to `FULL_ANY()`; leave `"team-member"` on `GhostResource` and every cell of `OpenRole`'s override exactly as Stage 13 left them. In `policy-table.test.ts`, remove `"GhostResource"` from the shared `it.each(["Assignment", "Project", "Client", "GhostResource", "OpenRole"])` test and give it its own dedicated test: `manager` → `"full"`, `"team-member"` → `"read"`, `owner`/`hr-admin` → `"full"`. The shared test keeps `Assignment`, `Project`, `Client` and `OpenRole` unchanged.
10. **Optimistic mutators.** `ghostResource.create` gets a full optimistic handler — both nodes are Tier 0, fully computable on-device offline (spec: "fully functional offline"; NFR: "Creation... fully functional offline") — write both nodes locally with the exact same field values `apply()` would produce (G01's atomic pair), the same shared-template discipline F224/F227 established. `ghostResource.cancel` and `ghostResource.linkOpenRole` get ordinary optimistic handlers (plain field/edge writes, no server-only resolution needed). `ghostResource.promote` gets **no** optimistic handler — mark `onlineOnly: true` in `defineMutation`, the same convention Stage 14 established for `rateCard.create`/`update`; a caller offline sees the mutation queue as pending, never as locally completed (G07, NFR).
11. **No UI.** Nothing under `apps/` changes — same discipline as Stage 8, 11, 12, 13 and 14. `apps/roster-web/src/lib/bench.ts`'s existing fixture-based Ghost rendering (badge, dashed styling, `ghostContribution` stat) is a useful reference for whichever later stage wires the real UI to this feature's server data, not something this stage touches.

**Done criteria:**
- `ghostResource.create` writes both an Employee (`employee_type: "Ghost"`) and a `GhostResource` node atomically, linked by `ghost_employee_id`, set once — proven by a test asserting both exist after one call and that a failure partway through (simulated) leaves neither committed (G01).
- The created Employee node has `employee_code`, `full_name`, `email` and `employment_type` exactly `null`, `contracted_hours` exactly `40`, and `job_title`/`start_date`/optional `seniority_level`/`billing_rate_default` matching the caller's input — proven by a test reading the node directly via `ghostEmployeeOperationalRecord`, not by the mutation merely not throwing (F253).
- `has_skill` edges from a Ghost's Employee node are written and traversed identically to a real employee's — proven by a test reusing an existing skill-edge read path unmodified against a Ghost employee id (G05).
- `ghostResource.linkOpenRole` closes any existing active `placeholder_for` edge before opening a new one — proven by a test that relinks twice and asserts exactly one active edge (G02).
- `ghostResource.cancel` and `ghostResource.promote` both check `expectedVersion` first — `stale-state` before anything else — proven by a test calling each with a stale version against a Ghost already changed by a concurrent call (F250/A003-T54).
- `ghostResource.promote` transitions `GhostResource.lifecycle_status` from `Active` to `Promoted` inside a serializable transaction, and updates the linked Employee's `employee_type`, `employee_code`, `full_name`, `email`, `employment_type`, `start_date` in the same transaction — proven by a test that every pre-existing Assignment and skill edge on that Employee node is unchanged (same edge ids, same content) after promotion (G06's "every Assignment edge, skill edge and piece of graph history is preserved untouched").
- `ghostResource.promote` rejects `duplicate-code`/`duplicate-email` exactly like `employee.create` does, via the same `requireUnique` helper, excluding the Ghost's own Employee node — proven by a test promoting a second Ghost with a colliding `employeeCode` or `email` (F253).
- Two genuinely concurrent `promote` calls against the same Ghost resolve to exactly one winner, the loser rejected outright (never merged or silently discarded) with a message naming who promoted it and when — proven by a test that issues both calls concurrently (not sequentially) and asserts on the serializable-retry-then-reject behavior, not just the sequential case (F250/G07).
- A failure injected partway through the promotion transaction leaves the Ghost `Active`, the Employee's fields unchanged, and no `promoted_to` edge created — proven by a test, not asserted (G06).
- `ghostResource.promote` is `onlineOnly: true` with no optimistic handler — proven by a test that the call is deferred/pending rather than locally completed when attempted offline (G07, NFR).
- `ghostResource.create` has a full optimistic handler writing both nodes locally with the same field values the server's `apply()` would produce — proven with the network cut (NFR: "fully functional offline").
- `packages/graph/src/queries/bench-forecast.ts` no longer excludes `employee_type === "Ghost"` from its row list — proven by a test asserting a Ghost employee's row appears in `getBenchForecast`'s output with the same shape (assignments, bench periods) a real employee's row has (F239's deferral condition now met).
- `services/api/.../bench-forecast-queries.ts`'s real-employee `aggregateUtilization` is unchanged by this stage — proven by a test that adding Ghosts to a workspace does not move the existing real-only figure (item 6/7's separation).
- `getBenchForecastAggregate` returns a new `ghostContribution` figure alongside `aggregateUtilization`, computed only from Ghost employees, over the real cohort's capacity denominator — proven by a test with a mixed real/Ghost cohort (G08).
- `ghostContribution` is suppressed together with `aggregateUtilization` under the same disclosure-control threshold, never reported alone when the other is suppressed — proven by a test at the k-anonymity boundary (`VPS-A004`).
- `GhostResource` resolves Owner/HR Admin/Manager → full, Team Member → read; `OpenRole` is unchanged from Stage 13 (Owner/HR Admin → full, Manager/Team Member → read) — proven by `GhostResource`'s own dedicated policy test, not the old shared assertion (F251).
- `ghostResource.promote` called with `existingEmployeeId` is rejected with a clear "not yet supported" error, never silently accepted or partially handled (F249).
- `"ghostResource.promote"` appears in `services/api/src/mutations/pipeline.ts`'s serializable-isolation list; `"ghostResource.cancel"` does not — grep the diff to confirm (F250).
- `pnpm verify` passes typecheck with real handlers for every new mutation — no stub, no-op, or placeholder.
- No file under `apps/` appears in the diff; the only change inside `MATRIX_OVERRIDES` is `GhostResource.manager` going from `READ_ANY()` to `FULL_ANY()` — no new row for either node type, no other cell touched (item 9) — grep the diff to confirm.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 15 report:** that `ghostResource.create` genuinely writes two nodes in one atomic transaction, not two separate calls a partial failure could split; that the Employee node's `employee_type` is exactly `"Ghost"` at creation and exactly `"Employee"` after promotion — grep the diff for both literal writes; that promotion's Assignment/skill-edge preservation is proven by asserting identity (same edge rows), not merely by absence of an error; that the promotion race test genuinely exercises two concurrent `promote` calls against the same Ghost (issued together, not sequenced one after the other), not two calls against two different Ghosts; that `"ghostResource.promote"` is genuinely on `pipeline.ts`'s serializable-isolation list and `"ghostResource.cancel"` is genuinely not (F250); that the rejection message on a second promote attempt actually names an actor and a time, not a generic "already promoted" string, and that a stale version caused by something other than a prior promotion (e.g. a concurrent `cancel`) still gets the ordinary `stale-state` rejection, not the actor-and-time one; that `ghostResource.promote` has no entry in `OPTIMISTIC_MUTATORS` — grep the diff, the same check Stage 14's F246 established; that `ghostResource.create`'s optimistic handler produces field-for-field the same record `apply()` does, not an approximation; that the Bench Forecast row-list change is a deletion of the existing exclusion condition, not a new parallel code path for Ghost rows (G04/G05's "no special case" is the point); that the real-employee `aggregateUtilization` figure has a test proving it is unaffected by Ghost presence, not merely unasserted; that `ghostContribution`'s denominator is the real cohort's capacity, not a Ghost-only denominator (re-read G08 and this brief's item 7 if the diff computes it differently); that disclosure-control suppression is proven to cover both figures together, not just `aggregateUtilization`; that a `promote` call with `existingEmployeeId` returns a clear rejection rather than being silently accepted (F249); and that no file under `apps/` appears in the diff.

### Stage 16 — Capacity Conflict Resolution (`VRS-F008`)

Depends on `VRS-F002` (Employee), `VRS-F004` (working days — overlap is measured in working days, not calendar days), `VRS-F005` (Assignment, Stage 13, merged `d9b6d74` — the 100% capacity constraint and its `ConflictError`, extended here to support a deliberate override), `VRS-F007` (Ghosts, Stage 15, merged `4081e9f` — subject to the identical constraint), and `VPS-A004` (Assignment's existing write permissions). Read `docs/Vulto_Specs/VRS-F008_Capacity_Conflict_Resolution.md` directly, including its now-corrected Depends-On, sweep prose, Technical Architecture and Security Considerations sections (F255–F258) — it is the source of truth this brief summarizes, not the reverse. Checked directly against the code before writing this brief: `Assignment` is a single-partition Tier 0 node (`packages/schema/src/registry/nodes.ts`, owner `VRS-F005`, not tier-split, so no governing-partition concern applies here unlike Stage 15); `assignmentCreate`'s `apply()` already writes `capacity_override_reason`, `capacity_override_by` and `capacity_override_at` as `null` on every ordinary create (`services/api/src/mutations/assignment.ts`) — pre-scaffolded for exactly this feature; `validateCapacity` (same file, called from both `assignment.create` and `assignment.update`) already throws `ConflictError` above 100%, but aggregates via a plain calendar-day iterator with no working-day awareness; `resolveWorkingDay` (`packages/schema/src/working-day.ts`, F237) and `resolvedDayOn` (`services/api/src/graph/working-days.ts`, G08) already exist and are the one established mechanism for working-day resolution, currently called only from `services/api/src/permission/bench-forecast-queries.ts`'s three read-query call sites; `services/api/src/permission/interceptor.ts`'s `rowScopeSatisfied` already resolves an employee's manager via the `managed_by` edge (`outgoing(tx, workspaceId, employeeId, "managed_by", asOf)`), though only as a private closure, not an exported helper; `assignment.update` deliberately carries no `expectedVersion` (confirmed in `packages/schema/src/mutations/assignment.ts`'s `defineMutation`, `stateTransition: false`), contrasted with `assignment.cancel`, which does check one — an intentional, already-approved distinction, not a gap. Eight gaps surfaced checking this feature directly against the code and the suite's own architecture documents before any product code was written, all closed the same day (full reasoning in `docs/Foundations_Findings.md`; `VRS-F008` and `VRS-F005` corrected to match):

- **F255** — the override gate assumes a Manager can already write Assignment, but `packages/schema/src/policy/policy-table.ts`'s `MATRIX_OVERRIDES` gives `Assignment.manager` only `READ_ANY()`, introduced without review by Stage 13's `f2d928d` and contradicting `VPS-A004`'s own Permission Matrix table, which specifies unscoped Full for that cell. Ruled: `Assignment.manager` is corrected to `FULL_ANY()`, matching `VPS-A004`'s table exactly — no other cell in that block changes.
- **F256** — `VRS-F008`'s Technical Architecture claims the conflict computation is "exactly the aggregation `VRS-F005`'s constraint already performs, reused rather than reimplemented," and G02 requires working-day-aware overlap, but `validateCapacity` aggregates plain calendar days with no working-day filter at all — reusing it unmodified would carry that gap straight into new code. Ruled, extending F237/G08's own precedent: `validateCapacity` gains a working-day filter through the existing `resolveWorkingDay`/`resolvedDayOn` mechanism at both its call sites; the new read query and sweep reuse the corrected function directly rather than reimplementing the sum.
- **F257** — the same shape F221/F225/F252 already ruled on: "Overrides are audited per `VPS-F004`" names no event that closed five-value taxonomy actually has for an ordinary successful Tier 0 write. Ruled on that precedent: no `appendAudit` call; the already-scaffolded `capacity_override_reason`/`_by`/`_at` fields are the record.
- **F258** — the same shape F248 already ruled on: the Depends-On section never names `VPS-A006`, the architecture document that is the actual authority for the sweep's "four-hourly cadence," and neither `services/jobs`/BullMQ nor `VRS-F012` (the sibling feature the cadence claims to match) exists anywhere in the repository yet. Ruled on F248's own precedent: the sweep ships now as the complete, callable read query G06 already specifies; its periodic BullMQ trigger is deferred to whichever stage builds `VPS-A006`'s job queue, named as a Partial forward dependency rather than silently assumed.
- **F259** — found resuming Stage 16 once Codex traced this brief into the existing server-authoritative mutation and device-cache boundaries, before any product code was written: `VRS-F008`'s own NFRs require `conflictCheck.evaluate` to return within 100ms from the local graph and say computation, panel and override all function fully offline, but this brief originally defined `conflictCheck.evaluate` as a new server query and marked `overrideAndProceed` `onlineOnly` with no optimistic handler — observably opposite behaviors. Ruled: the brief was wrong, not the spec. `assignmentCreate`'s own optimistic handler already carries the comment "capacity is deliberately server-only," and performs no local capacity check at all — the exact precedent for a local, best-effort `conflictCheck.evaluate` (built like `packages/graph/src/queries/bench-forecast.ts`'s own local query) alongside an unchanged, still-authoritative server-side `validateCapacity`; and for `overrideAndProceed` to get a genuine optimistic handler mirroring `assignmentCreate`'s own, using a locally-resolved `managed_by` check the same way the panel's own "the option does not render" behavior already requires client-side, re-verified authoritatively server-side on write.
- **F260** — found tracing F259's own corrected local-execution contracts into the device-cache APIs, before any product code was written: `conflictCheck.evaluate`'s `callerCanEdit` and `overrideAndProceed`'s local Manager/Owner gate both need caller identity and role facts that neither the query's input nor `MutatorContext` carried. Ruled: the Manager half needs no new capability — `OptimisticCache`'s existing `edgesFrom`/`getNode` point-access surface already answers it, the same edge direction `interceptor.ts` already reads server-side. The Owner half needs one small, precedented extension — `MutatorContext` gains `roles: readonly WorkspaceRole[]` (mirroring the server's own `CurrentWorkspaceSession` bundle), reusing the already-portable `deriveEffectiveRoles` (`@vulto/schema`) rather than inventing a second role-resolution mechanism; `conflictCheck.evaluate`'s input gains `callerUserId`/`callerRoles` as plain fields on its existing `input` object. **(F261 revised the Owner half's delivery mechanism — see below; the underlying conclusion is unchanged.)**
- **F261** — found tracing F260's own `InitPayload.roles` field to its two production `createGraphClient` callers, before any product code was written: neither `apps/roster-web`'s shell bootstrap (its Better Auth session exposes only `user.id`/`activeOrganizationId`) nor its sync harness (query params only) has a role value to supply, and Stage 16 forbids the `apps/` edit that would add one. Ruled: `InitPayload` never needed the field. `WorkspaceMembership`'s already-replicated `role` string (`membership-changes.ts`'s `canonicalRoles`, decoded with `parseWorkspaceRoles`) is reached via its `membership_of` edge to the caller's own `User` node, the identical shape `managed_by` already gives Manager — read with `OptimisticCache.edgesTo`/`getNode` (mutator) or `localNodes`/`localEdges` (query), no enumeration, no network call, no `apps/` file touched. Supersedes F260's `InitPayload.roles`/`MutatorContext.roles` design.
- **F262** — found resuming implementation once Codex reached the first real Postgres-backed `overrideAndProceed` integration test: `assignment_of`/`assigned_to` are mandatory edges on every Assignment write (item 6 reuses `assignment.create`'s complete path, item 4's ordinary create included), but `edgeRoleDecision` (`services/api/src/permission/interceptor.ts`) requires unscoped Full on both endpoint node types with no row context at all — deliberate architecture (FDN-92's "natural analogue" to the read path), never before exercised by a role holding anything less than unscoped Full on both endpoints. Manager's `Employee:operational` grant is row-scoped (`Full (direct reports)`, F255 leaves it untouched) and its `Project` grant is `READ_ANY()`, so neither edge could ever be authorized for a Manager, regardless of whether the target employee genuinely reports to them. Put directly to the founder — unlike F255–F261, no existing precedent decided it. Ruled: two additive mechanisms, neither widening an existing grant. `WriteTarget`'s edge variant gains optional endpoint node ids, threaded into `edgeRoleDecision` via the same `RowScopeContext` shape `decideRead` already uses for reads, so Manager's already-documented `Full (direct reports)` cell now correctly authorizes `assignment_of` for a genuine direct report. `packages/schema/src/registry/edges.ts` gains an opt-in `readSufficientEndpoints` field, styled on `governingPartitions`, declared only on `assigned_to`'s own registration (`{ Project: true }`) — Read, not Full, suffices on that one endpoint, mirroring `VPS-A004`'s own read-traversal rule; `Project`'s `manager` cell stays `READ_ANY()`, unchanged everywhere else. See item 7.

One scoping decision, not a finding: the spec's API contracts name `conflictResolution.adjustNew`, `.adjustExisting` and `.endExisting` alongside `.overrideAndProceed` and `.sweepOvercommitted`. Read against the existing mutation surface, the first three are not distinct operations — `adjustNew` is `assignment.create` with a lower `billable_percentage`, `adjustExisting` is `assignment.update` with a lower one, and `endExisting` is `assignment.update` with a new `end_date`, whose "requires the new end date to actually remove the overlap" clause is exactly what the corrected `validateCapacity` (F256) already enforces on any update. Registering three thin pass-through mutations that call the existing ones would duplicate rather than reuse — the project's own standing ethos (F237/G08's "one implementation" comment) — so none of the three gets its own registered mutation; a future UI stage's resolution panel calls `assignment.create`/`assignment.update` directly for these three cards, and only `overrideAndProceed` and `sweepOvercommitted` are genuinely new operations.

**Do:**

1. **`near_capacity_warning_threshold` on Workspace.** Add `near_capacity_warning_threshold: 90` to `services/api/src/graph/founding.ts`'s existing `base(input.workspaceId, "Workspace", { ... })` call, alongside `k_anonymity_minimum`/`k_anonymity_minimum_sensitive` — same pattern, same file. Add a small pure reader (`readNearCapacityThreshold`, modeled on `packages/schema/src/policy/disclosure-control.ts`'s `readKAnonymityThreshold`) so the default applies uniformly wherever the field is read, rather than each call site re-deriving it.
2. **`validateCapacity`'s working-day filter (F256).** In `services/api/src/mutations/assignment.ts`, extend `validateCapacity` so its date loop excludes non-working days for the employee before accumulating `billable_percentage`, using `resolvedDayOn` (`services/api/src/graph/working-days.ts`) with the mutation's own already-granted `tx`/`principal` in place of a query module's `readGuard`. Extract the corrected date-loop-plus-aggregation into an exported helper (e.g. `capacityTotalsFor(ctx, employeeId, startDate, endDate, excludeAssignmentId?)` returning the per-date working-day-filtered total and the overlapping Assignment list) so `validateCapacity` and `conflictResolution.sweepOvercommitted` (item 8) — both server-side — call one function rather than two independent sums. **(F259)** This helper is server-only, same as `validateCapacity` itself; it is not shared with `conflictCheck.evaluate` (item 4), which runs entirely on-device and has no access to `tx`/`resolvedDayOn` — it mirrors this same working-day-filtered aggregation independently, against the local cache, the same relationship `bench-forecast.ts`'s local query already has with its server-side counterpart (sharing only the pure `resolveWorkingDay` function, not a combined helper).
3. **`Assignment.manager: FULL_ANY()` (F255).** One-line change in `packages/schema/src/policy/policy-table.ts`'s `MATRIX_OVERRIDES`. No other cell in the `Assignment` block changes. Extend `policy-table.test.ts`'s existing `Assignment` assertions to cover the corrected `manager` cell.
4. **`conflictCheck.evaluate` — a portable local query (F259, corrected from a server query).** New `packages/graph/src/queries/conflict-check.ts`, modeled directly on `packages/graph/src/queries/bench-forecast.ts`'s own structure: reads `localNodes(database, ["Assignment", "Employee", "Project", "WorkingCalendar", "Holiday", "WorkingPattern", "WorkspaceMembership"])`/`localEdges(database, [..., "membership_of"])` from `./cache-data`, and calls the same pure `resolveWorkingDay` (`@vulto/schema`) `validateCapacity` uses server-side (F256) to filter non-working days — an independent aggregation, not a shared function with item 2's server-only `capacityTotalsFor`, the same relationship `bench-forecast.ts` already has with `bench-forecast-queries.ts`. `conflictCheck.evaluate(database, { employeeId, startDate, endDate, billablePercentage, excludeAssignmentId?, nearCapacityWarningThreshold, callerUserId }) -> { currentTotal, combinedTotal, wouldConflict, wouldWarn, suggestedFit, overlapWorkingDays, overlappingAssignments }` — `combinedTotal = currentTotal + billablePercentage`; `wouldConflict = combinedTotal > 100`; `wouldWarn = combinedTotal <= 100 && combinedTotal > nearCapacityWarningThreshold` (the caller supplies the workspace's `near_capacity_warning_threshold`, item 1, already locally replicated on the Workspace node), the two mutually exclusive per G03; `suggestedFit = 100 - currentTotal`; `overlappingAssignments` carries each overlapping Assignment's `assignmentId`, `projectName`, `billablePercentage`, `startDate`, `endDate`, and `callerCanEdit`. **`callerUserId` (F260, revised by F261) is the only caller-identity input needed** — the caller's own role is derived locally, not supplied: find the `membership_of` edge whose `toNodeId === callerUserId` in the `localEdges` array, read that `WorkspaceMembership` node's `role` field from `localNodes`, decode it with `parseWorkspaceRoles` (`@vulto/schema`) — the identical logic item 5's `resolveCallerRoles` helper applies over `OptimisticCache` for item 11's mutator, independently implemented against this function's own `localNodes`/`localEdges` arrays (the same local/server-independent-implementation relationship F259 already established for `capacityTotalsFor`). `callerCanEdit = deriveEffectiveRoles(callerRoles).includes("owner")`, or the overlapping Assignment's employee's outgoing `managed_by` edge resolves to an Employee whose `user_id === callerUserId` — the identical two-fact check item 11's optimistic handler makes, read here from `localNodes`/`localEdges` instead of `OptimisticCache` (G05: Ghosts participate identically, no special case). Entirely on-device, no server round-trip, best-effort — advisory only; `validateCapacity` remains the sole authority at write time.
5. **Local role resolution — no plumbing needed (F260, revised by F261).** F260 originally called for threading `roles` through `InitPayload`/`MutatorContext`; F261 found neither production `createGraphClient` caller (`apps/roster-web`'s shell bootstrap or its sync harness) has a role value to supply, and Stage 16 forbids the `apps/` edit that would require. Ruled instead: `WorkspaceMembership` (Tier 0, already replicated — F260) already carries the caller's own role as a `parseWorkspaceRoles`-decodable string (`services/api/src/graph/membership-changes.ts`'s `canonicalRoles`), reached via its existing `membership_of` edge to the caller's `User` node (`from_node_id: membershipId, to_node_id: userId`) — the same shape `managed_by` already has for Manager, just the reverse direction. Add a small shared helper, `resolveCallerRoles(cache: OptimisticCache, callerUserId: string): Promise<WorkspaceRole[]>` in `packages/graph/src/mutators/foundation.ts` (or a sibling module both item 4's query and item 11's mutator import): `cache.edgesTo(callerUserId, "membership_of")`'s first non-soft-deleted edge, `cache.getNode(edge.fromNodeId)`, `parseWorkspaceRoles(node.record.role as string)`. No change to `InitPayload`, `GraphClientOptions`, `MutatorContext`, `engine.ts`, `client.ts`, or either app caller — `OptimisticCache.edgesTo`/`getNode` already support this exactly, and the query path's `localNodes`/`localEdges` equivalent needs only `WorkspaceMembership`/`membership_of` added to its already-read type lists (item 4).
6. **`conflictResolution.overrideAndProceed` — the gated mutation.** New `packages/schema/src/mutations/conflict-resolution.ts` and `services/api/src/mutations/conflict-resolution.ts`. `overrideAndProceed(proposed: { employeeId, projectId, startDate, endDate, billablePercentage, rateCardId? }, reason: string) -> { assignmentId }`. Authorization gate before anything else: the caller must be the target employee's manager (resolved via `outgoing(tx, workspaceId, employeeId, "managed_by", asOf)` — the same graph call `interceptor.ts`'s `rowScopeSatisfied` already makes as a private closure; call the graph store function directly here rather than exporting that closure, since the mutation has its own `tx`/`principal` context already) or a workspace Owner — reject otherwise, the panel's own "the option does not render" behavior enforced server-side, not merely hidden client-side (`VRS-F008`'s own Security Considerations: "an application-layer check, not a new permission row"). On authorization passing, write the Assignment exactly as `assignment.create` does (reuse its internal write helper, not a second implementation) but skip `validateCapacity`'s throw for this one path, and additionally write `capacity_override_reason: reason`, `capacity_override_by: principal.userId`, `capacity_override_at: now()` — the three already-scaffolded fields (G04: this is the only path writing an Assignment above 100% combined; every other path fits or is rejected). **(F259)** This server-side check is authoritative regardless of what item 11's optimistic handler decided locally — a caller whose local `managed_by` read was stale (e.g. a recent reassignment not yet synced) is rejected here and rolled back client-side via the ordinary undo path, exactly like a capacity rejection on `assignment.create` already is.
7. **Edge-write authorization for `assignment_of`/`assigned_to` (F262).** `overrideAndProceed` (item 6) and ordinary `assignment.create` both build their Assignment through the single shared `buildAssignmentCreatePlan` (`services/api/src/mutations/assignment.ts`), whose `checks` already include `assignment_of`'s and `assigned_to`'s edge creates. As registered today, `edgeRoleDecision` (`services/api/src/permission/interceptor.ts`) requires unscoped Full on both endpoint node types with no row context at all (FDN-92's deliberate "natural analogue" to the read path) — Manager's `Employee:operational` grant is row-scoped (`Full (direct reports)`) and `Project`'s is `READ_ANY()`, so neither edge can ever be authorized for a Manager as things stand, regardless of whether the target employee actually reports to them. Two additive changes, neither widening an existing grant:
   - `WriteTarget`'s edge variant gains optional `readonly fromNodeId?: string | null` / `readonly toNodeId?: string | null`. `edgeRoleDecision` builds a `RowScopeContext` per endpoint when an id is supplied — the same shape `decideRead` already builds for reads — and passes it to `bestCell` instead of calling it with no row. `buildAssignmentCreatePlan`'s `edgeTarget` helper is extended with a `toNodeId` argument: `employee.nodeId` for `assignment_of`, `project.nodeId` for `assigned_to` (both already resolved `StoredNode`s before `checks` is built — nothing new to look up). Every other registered edge write supplies no id, so its behavior is unchanged. With real row context, Manager's unmodified `Full (direct reports)` cell on `Employee:operational` now correctly authorizes `assignment_of` when the target employee is genuinely that Manager's direct report, and correctly refuses it otherwise.
   - `packages/schema/src/registry/edges.ts`'s `EdgeGroupShape` gains an optional `readSufficientEndpoints?: Readonly<Partial<Record<NodeType, true>>>`, declared per edge type exactly as `governingPartitions` already is — absent everywhere except one new declaration, `assigned_to`'s own registration: `readSufficientEndpoints: { Project: true }`. `edgeRoleDecision` accepts a declared endpoint whose `bestCell` outcome is `"read"` as well as `"full"`. `Project`'s `manager` cell stays `READ_ANY()`, untouched — Manager still cannot write a Project's own fields anywhere else; only attaching an Assignment to one it can already read is now possible, mirroring `VPS-A004`'s own Graph traversal rule that a read needs only Read on both endpoints.

   Neither change touches `Assignment`'s own `FULL_ANY()` grant (F255) or `assignment_of`'s declared governing partition (F208: `Employee: "operational"`) — the FROM side of both edges is Assignment, whose scope is already `"any"` and therefore satisfied regardless of row context. Extend `policy-table.test.ts`/the edge-registry tests to cover the new field; extend `interceptor.test.ts` with a case asserting a Manager who is genuinely the target's direct manager can now create both edges, and one asserting an unrelated Manager still cannot.
8. **`conflictResolution.sweepOvercommitted` — the read-only safety net (F258, G06).** `sweepOvercommitted(workspaceId) -> { employeeId, combinedTotal, overlapWindow, hasRecordedOverride }[]`. Iterates Active employees, reuses item 2's `capacityTotalsFor` helper per employee, and returns every employee whose combined total exceeds 100 with `capacity_override_reason` null on every overlapping Assignment — `hasRecordedOverride` true if at least one carries a reason. Writes nothing (G06). Ships as a complete, directly callable, fully tested query now; the periodic four-hourly BullMQ trigger is out of scope, deferred to `VPS-A006`'s own not-yet-built job queue (F258) — not stubbed, not faked, simply not wired to a schedule that does not exist yet for any feature.
9. **No new mutations for `adjustNew`/`adjustExisting`/`endExisting`.** Per the scoping note above: these are `assignment.create`/`assignment.update` called with adjusted fields, not new operations. Nothing to build here beyond what item 2 already gives `assignment.update`'s existing capacity check for the `endExisting` case.
10. **No audit call (F257).** `overrideAndProceed` does not call `appendAudit`. The who/why/when is `capacity_override_reason`/`_by`/`_at` on the Assignment itself, visible to anyone who could already see the Assignment (`VRS-F008`'s own Security Considerations, corrected).
11. **Optimistic mutators (F259, corrected from `onlineOnly`; F260/F261, caller-identity mechanism).** `conflictCheck.evaluate` needs none — it is already local (item 4). `conflictResolution.overrideAndProceed` gets a genuine optimistic handler in `packages/graph/src/mutators/foundation.ts`, mirroring `assignmentCreate`'s own exactly: an authorization gate first, then write the Assignment locally with the caller-supplied fields plus `capacity_override_reason`/`_by`/`_at`, no local capacity check (matching `assignmentCreate`'s own "capacity is deliberately server-only" comment — this mutation is explicitly meant to exceed the ceiling anyway). The gate (F260/F261): `deriveEffectiveRoles(await resolveCallerRoles(context.cache, context.userId)).includes("owner")` (item 5's helper) — or `context.cache.edgesFrom(employeeId, "managed_by")`'s first edge's `toNodeId`, read via `context.cache.getNode(...)`, whose `record.user_id === context.userId` — otherwise throw `OptimisticRejection("not-authorized")` (a new value on the existing open-string `reason`, no type change) before any write. The server re-verifies the manager/Owner gate authoritatively inside the mutation (item 6) regardless of the local check, and rejects via the outbox's existing undo path on a stale or wrong local read — the same mechanism `assignmentCreate` already relies on for its own server-only capacity rejection, no new mechanism. `conflictResolution.sweepOvercommitted` needs none (a read, and NFR: "The sweep is best-effort and runs on next connectivity" describes the overcommitment list showing cached results while offline, not the sweep's own computation — an ordinary online-only read query already behaves this way with no special handling).
12. **No UI.** Nothing under `apps/` changes — the same discipline Stage 8, 11, 12, 13, 14 and 15 all followed. `VRS-F008`'s full Interface Specification (the resolution panel, the capacity strip, the overcommitment list) is reference material for whichever later stage wires the real UI to this feature's server data, not something this stage touches.

**Done criteria:**
- `validateCapacity` excludes non-working days from its aggregation — proven by a test with assignments overlapping only on a weekend or a public holiday, asserting no `ConflictError` where calendar-day aggregation would have thrown one (F256/G02).
- `Assignment.manager` resolves to `full`, matching `VPS-A004`'s table — proven by `policy-table.test.ts`'s extended assertion, not merely unasserted (F255).
- `conflictCheck.evaluate` returns `wouldConflict`/`wouldWarn` as mutually exclusive across the 100% and `near_capacity_warning_threshold` boundaries — proven by tests at both thresholds (G03).
- `conflictCheck.evaluate` (the local query, `packages/graph/src/queries/conflict-check.ts`) and `validateCapacity` (the server function) agree on the same total for the same inputs when run against identical data — proven by a test constructing matching local-cache and server-tx fixtures and asserting equal `overlapWorkingDays`/totals, since they are independent implementations sharing only the pure `resolveWorkingDay` function, not a combined helper (F259/G01, the "reused, not reimplemented" claim proven as fact at the shared-function level, not by literally calling one function from both runtimes).
- `conflictCheck.evaluate` runs with zero network calls — proven by a test run with the network cut, mirroring item 3 (Stage 13)'s own "client-side bench query... entirely from local cache rows" proof (F259).
- `overrideAndProceed` rejects a caller who is neither the target employee's manager nor an Owner, before any Assignment write occurs — proven by a test asserting no Assignment node is created on a rejected call, both via the server mutation directly and via a rejected optimistic write's rollback through the undo path (Security Considerations, application-layer gate; F259).
- `overrideAndProceed`'s optimistic handler genuinely resolves the caller's role via `resolveCallerRoles`/`membership_of` and the target employee's `managed_by` edge before attempting any local write — proven by a test asserting the handler throws `OptimisticRejection("not-authorized")` locally, with no local write attempted, when the caller is neither the target's manager nor an Owner, and succeeds locally for both an Owner caller and the target's actual manager (F260/F261).
- `overrideAndProceed` is the only path that writes an Assignment combining above 100% — proven by a test that `assignment.create`/`.update` still throw `ConflictError` above 100% while `overrideAndProceed` with the same inputs succeeds (G04).
- A Manager who is genuinely the target employee's manager can complete `overrideAndProceed` end-to-end (the Assignment node plus both required edges) through the real interceptor; a Manager who is not that employee's manager is refused with `reason: "role"` on the edge check, before validation or apply — proven by two Postgres-backed integration tests against the real interceptor, not a mocked one (F262).
- `assigned_to`'s Project endpoint is authorized only because `assigned_to`'s own registration declares `readSufficientEndpoints: { Project: true }` — grep the diff to confirm no other edge type in `packages/schema/src/registry/edges.ts` carries that declaration (F262).
- `overrideAndProceed` writes `capacity_override_reason`, `capacity_override_by` and `capacity_override_at` together on the created Assignment, all three or none — proven by a test reading the node directly.
- `overrideAndProceed` has a full optimistic handler in `packages/graph/src/mutators/foundation.ts`, mirroring `assignmentCreate`'s own shape exactly (no local capacity check, same field set plus the three override fields) — proven with the network cut, the same NFR proof Ghost creation used in Stage 15, not the `onlineOnly`/deferred-pending check Stage 14 and Stage 15 used for `rateCard.*`/`ghostResource.promote` (F259).
- `sweepOvercommitted` writes nothing — proven by a test asserting no node or edge is created or modified by a call that finds real overcommitment (G06).
- `sweepOvercommitted` returns every employee combined over 100% with no recorded override, and correctly reports `hasRecordedOverride: true` for one that has at least one overridden overlapping Assignment — proven by a test covering both cases.
- Ghost Resources participate identically in `conflictCheck.evaluate` and `overrideAndProceed` — proven by a test running the same assertions against a Ghost employee id (G05).
- `near_capacity_warning_threshold` defaults to 90 on a newly founded Workspace and is read through one shared helper, not re-derived at each call site — grep the diff to confirm (item 1).
- No file under `apps/` appears in the diff.
- `pnpm verify` passes typecheck with real handlers for every new mutation and query — no stub, no-op, or placeholder.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 16 report:** that the working-day filter genuinely changes `validateCapacity`'s behavior at a weekend/holiday boundary, not merely that the code compiles — read the specific test and its assertion, not just its name; that `sweepOvercommitted` (server-side) calls item 2's `capacityTotalsFor` helper, and that `conflictCheck.evaluate` (`packages/graph/src/queries/conflict-check.ts`, local) is a genuinely independent local implementation reading `localNodes`/`localEdges`, not a disguised server call — read the file's own imports, confirm it never touches `services/api` (F259); that `conflictCheck.evaluate` is proven to work with the network cut, the same proof standard Stage 13's local bench query already met; that `overrideAndProceed`'s manager/Owner gate is checked server-side before the write, independent of whatever the client's optimistic handler decided — read the mutation's own code path, not just a test name; that `overrideAndProceed`'s Assignment write reuses `assignment.create`'s existing internal helper rather than a second, parallel Assignment-construction implementation; that `overrideAndProceed` genuinely has an optimistic handler in `OPTIMISTIC_MUTATORS` (not `onlineOnly: true`) and that handler performs no local capacity check, matching `assignmentCreate`'s own precedent exactly (F259); that no `apps/` file or `InitPayload`/`MutatorContext` field was touched to resolve caller roles (F261) and that the optimistic handler's authorization gate genuinely reads the caller's `WorkspaceMembership` via `membership_of` plus `OptimisticCache.edgesFrom`/`getNode` — never a new enumeration method — before writing, rejecting with `OptimisticRejection("not-authorized")` when neither the Owner nor Manager check passes (F260/F261); that no `appendAudit` call was added anywhere in this stage (F257); that `Assignment.manager`'s policy-table change is the only cell touched in that block (F255) — grep the diff; that `sweepOvercommitted` is proven read-only by a test, not merely undocumented as a writer; that F262's row-context extension changes behavior only for edge writes that now supply a `fromNodeId`/`toNodeId` — confirm every pre-existing edge-write call site (`managed_by`, `pattern_for`, and the rest) still passes none and is therefore untouched, and that `readSufficientEndpoints` is declared on `assigned_to` alone, not applied broadly to relax other edge types' write requirements; and that no file under `apps/` appears in the diff.

---

### Stage 17 — Time Classification Taxonomy (`VRS-F009`)

Depends on `VRS-F002` (Employee), `VPS-A002` (the Pitch node registration and its field-level tier split, already in place). Read `docs/Vulto_Specs/VRS-F009_Time_Classification_Taxonomy.md` directly, including its now-corrected Staffing flow, Pitch creation and staffing subsection, API Contracts and Security Considerations sections (F264) — it is the source of truth this brief summarizes, not the reverse. Checked directly against the code before writing this brief: `Pitch` is already registered (`packages/schema/src/registry/nodes.ts`, owner `VRS-F009`, `fixedLifecycle("Active","Won","Lost","Converted")`, `splitProtection` into a Standard-class `identifying` partition and a Finance-restricted `commercial` partition) but nothing creates one, and no Employee↔Pitch relationship exists in `edges.ts`; `packages/schema/src/policy/policy-table.ts` carries no `MATRIX_OVERRIDES` entry for `Pitch` at all, so both partitions fall back to `DEFAULT_CLASS_MAPPING`, whose row-scoped cells cannot resolve for Pitch since `services/api/src/permission/interceptor.ts`'s `rowScopeSatisfied` only ever resolves a subject Employee for `Employee`/`BurnoutAlert` node types; `has_skill` (Employee↔Skill, owner `VPS-A002`, `governingPartitions: { Employee: "operational" }`) is the established model for a simple operational Employee-relationship edge; `edgeRoleDecision`'s row-context mechanism (F262) is confirmed generic — any edge-write call site that supplies `fromNodeId`/`toNodeId` gets correctly row-scoped authorization; `ghostResourceCreate` (`packages/schema/src/mutations/ghostResource.ts`) is the established model for a simple Tier-0 node-creating mutation; `packages/schema/src/registry/conversions.ts` already registers a `Pitch→Project` conversion (`originated_from`) with no owning feature, mirroring the already-registered, still-unimplemented `Candidate→Employee` conversion; `TimesheetEntry` (owner `VRS-F010`) is registered but has no creation mutation, so no `entryId` can exist until `VRS-F010` builds one.

One finding surfaced checking this feature directly against the code before writing the brief, closed the same day, traced to established precedent, no founder consultation needed — full reasoning in `docs/Foundations_Findings.md`; `VRS-F009` corrected to match:

- **F264** — the spec described selecting a Pitch, staffing employees onto one, and classifying time against it, but none of the underlying mechanism existed or was specified: no creation mutation, no staffing relationship, and no policy-table row for Pitch, plus an API Contracts section describing `timeEntry.classify*` as this feature's own live mutations against a node (`TimesheetEntry`) that cannot exist until `VRS-F010` builds it. Ruled: `pitch.create` and a new `staffed_on` (Employee→Pitch) edge modeled on `has_skill`, using F262's row-context mechanism to correctly scope Manager-initiated staffing to their own direct reports; a new `Pitch:identifying` policy-table row modeled on `GhostResource`'s own row (F251) — Owner/HR-admin/Manager unscoped Full (Manager deliberately unscoped, not row-scoped, to avoid a circularity on the very first staffing of a new pitch), Team Member explicitly `None` at the generic Pitch level since `pitch.listStaffedFor` is their only sanctioned path and is gated by their own already-working `Employee:operational` row-scope on the `employeeId` argument; the `timeEntry.classify*` signatures reframed as contract for `VRS-F010` to implement, not code this feature ships; the registered `Pitch→Project` conversion left explicitly out of scope, mirroring `Candidate→Employee`/`VRS-F028`.

**Do:**

1. **`staffed_on` edge registration.** `packages/schema/src/registry/edges.ts`: new `edgeGroup({ edgeType: "staffed_on", owner: "VRS-F009", pairs: [["Employee", "Pitch"]], governingPartitions: { Employee: "operational", Pitch: "identifying" } })`. Both endpoints are split nodes, so both need a governing partition (F265). Existence-only relationship, no intermediate node, no field payload beyond the standard edge envelope.
2. **`Pitch:identifying` policy-table row (F264).** `packages/schema/src/policy/policy-table.ts`'s `MATRIX_OVERRIDES`: add `"Pitch:identifying": { owner: FULL_ANY(), "hr-admin": FULL_ANY(), "finance-admin": READ_ANY(), manager: FULL_ANY(), "team-member": NONE_ANY() }`, styled on `GhostResource`'s own row. `Pitch:commercial` gets no entry — it stays on `DEFAULT_CLASS_MAPPING`'s Finance-restricted default, untouched. Extend `policy-table.test.ts` with `Pitch:identifying`'s own assertions, the same way `GhostResource` and `Employee:operational`/`:compensation` already have theirs split out.
3. **`packages/schema/src/pitch.ts`.** New file, modeled on `employee.ts`'s field-schema pattern. `pitchIdentifyingFieldsSchema`: `name` (string, required), `client_id` (UUID, nullable), `projected_start_date` (date, nullable), plus the standard node envelope fields (`pitch_id`, `workspace_id`, `lifecycle_status`) per the spec's Pitch Tier 0 schema section. No fields for the `commercial` partition — this feature never writes them.
4. **`packages/schema/src/mutations/pitch.ts`.** Three mutations, modeled directly on `ghostResource.ts`'s `defineMutation` shapes:
   - `pitchCreate = defineMutation({ name: "pitch.create", input: z.object({ name: pitchIdentifyingFieldsSchema.shape.name, client_id: ...optional, projected_start_date: ...optional }).strict(), tier: 0, onlineOnly: false, stateTransition: false })`.
   - `pitchStaffEmployee` / `pitchUnstaffEmployee = defineMutation({ name: "pitch.staffEmployee" / "pitch.unstaffEmployee", input: z.object({ pitch_id: uuidV4Schema, employee_id: uuidV4Schema }).strict(), tier: 0, onlineOnly: false, stateTransition: false })`.
5. **`services/api/src/mutations/pitch.ts`.** `pitchCreate.apply()`: writes a new Pitch node with `lifecycle_status: "Active"` and the identifying fields; the `commercial` partition's fields are left absent, to be written later by whichever application (`Vulto Sales`) first needs them — this feature's write path never touches that partition. `pitchStaffEmployee.apply()`/`pitchUnstaffEmployee.apply()`: create/remove the `staffed_on` edge with `fromNodeType: "Employee", fromNodeId: employeeId` and `toNodeType: "Pitch", toNodeId: pitchId` both supplied on the `WriteTarget`, so F262's row-context mechanism resolves Manager's existing `Employee:operational` `Full (direct-reports)` cell correctly — a Manager staffing their own direct report succeeds; staffing anyone else is refused with `reason: "role"` before any write, the identical shape `assignment_of` already proves for F262.
6. **`services/api/src/permission/pitch-queries.ts`.** `listStaffedFor(tx, principal, employeeId)`: first confirm the caller may read `employeeId`'s `Employee:operational` record — reuse the interceptor's own read-decision path (the same check `listEmployees`/an Employee-detail read already makes), not a new mechanism; return an empty result rather than throwing if the caller cannot (matching the spec's "no Pitch row at all" empty-state behavior for a denied read, not an error). If readable, traverse `employeeId`'s outgoing `staffed_on` edges to reach each staffed Pitch, read the `identifying` partition's Tier 0 fields directly (no interceptor read-decision needed per-Pitch — `pitch.listStaffedFor` is this feature's one deliberately bespoke access path, per the spec's own Security Considerations), and resolve `client_id` to a client name the same way an existing listing query resolves a related node's display name (grep for the established pattern rather than inventing a second one). Return `{ pitchId, name, clientName }[]` — `projected_start_date` and every `commercial`-partition field are never read here, let alone returned; a runtime shape assertion in the accompanying test should confirm the returned objects carry no stray keys.
7. **Transport wiring.** Register `pitch.create`, `pitch.staffEmployee`, and `pitch.unstaffEmployee` in the shared mutation registry and server pipeline; the existing `graph.applyMutations` router procedure is their only write transport. Add `pitch.listStaffedFor` as a read procedure in `services/api/src/router.ts`. This is the actual `ghostResource.*`/`conflictResolution.*` pattern (F266).
8. **The classification field schema fragment (not a TimesheetEntry mutation).** New `packages/schema/src/time-classification.ts`: `TIME_CATEGORIES = ["Billable", "NonBillable", "Pitch"] as const`, `INTERNAL_CATEGORIES = ["Meeting", "Training", "Admin", "InternalProject", "Other"] as const`, a `timeClassificationFieldsSchema` covering `time_category`/`internal_category` per G01, and a pure `validateTimeClassification({ time_category, internal_category, assignment_id, pitch_id }): void` enforcing the three write-time dependencies from the spec's own table (throws naming the reason on violation, never silently corrects). This module has no caller yet and writes nothing — it is inert until `VRS-F010` imports it into its own real `TimesheetEntry` mutations. Do not add `timeEntry.classify*` mutations, a `TimesheetEntry` creation mutation, or any router wiring for either — `VRS-F010` owns all of that (F264).
9. **No new mutations for the Pitch→Project conversion.** The already-registered `originated_from` conversion (`packages/schema/src/registry/conversions.ts`) stays unimplemented, exactly as `Candidate→Employee` already is — nothing to build here (F264).
10. **No UI.** Nothing under `apps/` changes, following Stage 8/11/12/13/14/15/16's own precedent. `VRS-F009`'s full Interface Specification (the category chips, the Pitch selector, the category indicator) is reference material for whichever later stage — most likely `VRS-F010` itself, since this feature owns no screen of its own — wires the real UI.

**Done criteria:**
- A Manager can create a Pitch and, in the same session, immediately staff a direct report onto it — proven by a test that never relies on any prior staffing existing, closing the circularity F264 identified against a row-scoped Manager design (Manager's `Pitch:identifying` grant is unscoped Full, so no chicken-and-egg).
- A Manager staffing an employee who is not their direct report is refused with `reason: "role"`, before any edge is written — proven against the real interceptor, the identical proof shape F262 already established for `assignment_of`.
- Owner and HR Admin can create a Pitch, and staff or unstaff any employee, unconditionally — proven by a test.
- Team Member cannot create a Pitch, cannot staff or unstaff anyone, and has no generic read on `Pitch:identifying` at all — proven by a test asserting refusal at each of the three mutations and confirming `policy-table.test.ts`'s new `Pitch:identifying` row resolves `none` for Team Member.
- `pitch.listStaffedFor(employeeId)` called by the employee for themselves, once staffed, returns that Pitch's `{ pitchId, name, clientName }` — proven by a test, with a runtime shape assertion that no `commercial`-partition field or `projected_start_date` is present on the returned object (G05's contract-level guarantee, not caller discipline).
- `pitch.listStaffedFor` called for an employee the caller may not read (an unrelated Team Member calling for a colleague they do not manage) returns empty rather than throwing or leaking the pitch's existence — proven by a test.
- A Manager calling `pitch.listStaffedFor` for their own direct report succeeds and returns that report's staffed pitches; calling it for an employee outside their reporting line returns empty — proven by a test, exercising the same `Employee:operational` row-scope the query reuses rather than a new mechanism.
- `Pitch:commercial` has no `MATRIX_OVERRIDES` entry and still resolves through `DEFAULT_CLASS_MAPPING`'s Finance-restricted default — grep the diff to confirm no new row was added for it.
- `staffed_on` is registered with `governingPartitions: { Employee: "operational", Pitch: "identifying" }` and no other declaration — the real interceptor test must prove staffing succeeds, since omitting Pitch's partition refuses everyone (F265).
- `packages/schema/src/time-classification.ts` has zero callers anywhere in `services/api` or `packages/graph` — grep the diff to confirm it is purely additive and inert this stage; `validateTimeClassification` is proven correct in isolation by unit tests covering all three dependency rules (Billable without `assignment_id` throws; NonBillable without `internal_category` throws; Pitch without `pitch_id` throws; each satisfied combination passes).
- No `TimesheetEntry` node, mutation, or router entry exists anywhere in the diff — grep to confirm; that remains `VRS-F010`'s to build.
- No file under `apps/` appears in the diff.
- `pnpm verify` passes typecheck with real handlers for every new mutation and query — no stub, no-op, or placeholder.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 17 report:** that `pitch.staffEmployee`/`unstaffEmployee` genuinely supply both `fromNodeId` and `toNodeId` on the edge target — read the mutation's own code path, not just a passing test name, and confirm a Manager-owns-direct-report case and a Manager-does-not case both exist against the real interceptor (F262's mechanism, reused, not reimplemented); that `Pitch:identifying`'s Manager cell is genuinely `FULL_ANY()`, not row-scoped — grep the policy-table diff directly, since a row-scoped cell here would silently reintroduce the circularity F264 ruled out; that `pitch.listStaffedFor` never calls a generic per-Pitch read-decision path — it should read the `identifying` partition directly once the `employeeId` gate passes, per the spec's own "the only path this feature uses to reach a Pitch" language, and the test proving no stray keys leak should assert on the actual returned object, not merely the TypeScript type; that `Pitch:commercial` was not given an override — grep for `Pitch:commercial` in the policy-table diff and confirm it's absent; that `time-classification.ts` is genuinely unreferenced outside its own file and test — grep the diff for its import; that no `TimesheetEntry` mutation, node-creation path, or router entry was added prematurely; and that no file under `apps/` appears in the diff.

---

### Stage 18 — Timesheet Speed-Run (`VRS-F010`)

Depends on `VRS-F002` (Employee — `contracted_hours`, `billability_target_override`), `VRS-F004` (working days and daily hours, via `resolveWorkingDay`), `VRS-F005` (Assignment edges populating the grid's rows), `VRS-F009` (the classification fields this schema incorporates directly), `VPS-A002` (TimesheetEntry and TimesheetAnomalyFlag registry entries, already in place). Read `docs/Vulto_Specs/VRS-F010_Timesheet_Speed-Run.md` directly, including its now-corrected Edges, Security Considerations, Detection rules, API contracts, Validation and Out of Scope sections (F267, F268, F269) — it is the source of truth this brief summarizes, not the reverse. Checked directly against the code before writing this brief: `TimesheetEntry` and `TimesheetAnomalyFlag` are already registered (`packages/schema/src/registry/nodes.ts`, owner `VRS-F010`; TimesheetEntry `fixedProtection("Standard", 0)`, TimesheetAnomalyFlag `fixedProtection("Manager-restricted", 2)`) and `packages/schema/src/policy/policy-table.ts` already carries both `MATRIX_OVERRIDES` rows, matching `VPS-A004`'s own table exactly — nothing to add there; `logged_against` (`TimesheetEntry→Assignment`/`TimesheetEntry→Pitch`, owner `VRS-F010`) is already registered in `edges.ts` but declares no `governingPartitions`, so every write touching the split `Pitch` endpoint would refuse for every role (F267); `services/api/src/permission/interceptor.ts`'s `rowScopeSatisfied` only resolves a row's subject Employee for `Employee` and `BurnoutAlert` node types, so neither TimesheetEntry's nor TimesheetAnomalyFlag's row-scoped policy cells can resolve today (F268); `resolveWorkingDay`/`addIsoDays`/`isoDatesInclusive` (`packages/schema/src/working-day.ts`, F237's own precedent) are directly reusable for the grid's columns, `expectedHours`, and the `HoursExceedExpected` rule's expected-hours comparison; `MutationRejection("invalid-args")` paired with the optimistic mutator's `GraphValidationError` (`assignment.ts`/`calendar.ts`/`employee.ts`) is the established validation-error convention, replacing the spec's own descriptive `TimesheetValidationError` name; `services/api/src/mutations/pipeline.ts` runs each named mutation in its own single transaction (optionally serializable), so `timesheet.submitWeek` gets G07's required atomicity for free as one mutation call, not by composing several `saveCell`-style writes; `filterReadable` (`listEmployees`/`sweepOvercommitted`'s own precedent, F263) is the established pattern for scoping a workspace-wide listing query to what the caller's row-scoped grant actually allows, directly applicable to `timesheetAnomaly.listActive`'s Manager-direct-reports scoping; and no creation mutation exists anywhere for either node type — this feature is the first to touch them.

Two findings surfaced checking this feature directly against the code before writing the brief, both closed the same day, both traced to established precedent, no founder consultation needed — full reasoning in `docs/Foundations_Findings.md`; `VRS-F010` corrected to match:

- **F267** — `logged_against` declared no `governingPartitions` on either pair. `Pitch` is a split node exactly like it was for `staffed_on`/F265, so every `logged_against` write touching the Pitch endpoint would refuse for every role, identical mechanism, identical mistake. Ruled: `logged_against` declares `governingPartitions: { Pitch: "identifying" }`; `Assignment` is unsplit and needs no entry.
- **F268** — `TimesheetEntry`'s and `TimesheetAnomalyFlag`'s row-scoped policy cells (Team Member `Full (own)` on TimesheetEntry, Manager `Read`/`Full (direct reports)` on both) cannot resolve today: `rowScopeSatisfied` only finds a row's subject Employee for `Employee` or `BurnoutAlert`; neither node type is in that list, so every row-scoped check against either falls to `else return false`, refusing even an employee their own timesheet. Ruled, corrected by F269 below: `TimesheetEntry` needs a new resolution branch reading its own direct `employee_id` field via the interceptor's already-imported `getNode` — G04's own reason ("a Pitch entry has no Assignment to traverse through at all") rules out an edge here, and `TimesheetEntry` is `fixedProtection("Standard", 0)`, so the field genuinely is in the plain record. `TimesheetAnomalyFlag` needs no new mechanism at all: it already carries a registered `triggered_by` edge to Employee, so the existing `BurnoutAlert` branch already reaches it (F269).
- **F269** — Codex, resuming Stage 18, correctly declined to implement F268's `TimesheetAnomalyFlag` branch as originally ruled (reading `employee_id` off `getNode`'s plain `record`) and stopped to report it, rather than shipping a check that fails closed for every caller: `TimesheetAnomalyFlag` is `fixedProtection("Manager-restricted", 2)`, and Tier 1/2 node content lives encrypted in `graph_protected_fragments`, never in the plain `graph_nodes.record` that `getNode` reads. Ruled: `TimesheetAnomalyFlag` already carries a registered `triggered_by` edge to Employee — same `edgeGroup` as `BurnoutAlert`, same Tier 2 protection — so `rowScopeSatisfied`'s existing `BurnoutAlert` branch extends to cover it unchanged; edges aren't protected content. `timesheetAnomaly.evaluate` must write that `triggered_by` edge when it creates a flag.
- **F270** — Resuming Stage 18 after F269, Codex correctly stopped again rather than invent or impersonate an authorized writer: the Team Member who submits their own week has `NONE` on `TimesheetAnomalyFlag` (deliberately — the spec requires the flagged employee never see their own flags), so `timesheetAnomaly.evaluate` cannot run as that submitter's own principal, and no existing mechanism supplies another one — `SystemPrincipal`s get zero policy roles in `authorizeWrite` today, and the three operations that do exist for them bypass the interceptor entirely, writing infrastructure tables no graph node lives in. Put to the founder as a genuine fork (no precedent forces a single answer) and ruled: a new system principal, `timesheet-anomaly-evaluate`, is added to the existing closed table (`principal-policy.ts`), permitted exactly one new operation, `timesheet-anomaly.create-flag`; `authorizeWrite` gains a distinct Gate 1 path for system principals, checked against that whitelist rather than the role matrix. `timesheetAnomaly.evaluate` constructs this system principal — never the submitter's own — for the flag-and-edge write only; every other Stage 18 operation keeps running under the calling member's own principal.
- **F271** — Resuming Stage 18 after F270, Codex correctly stopped a third time rather than skip G09/G09b's enforcement: `timesheetAnomaly.evaluate` must inspect prior `TimesheetAnomalyFlag` content (`employee_id`, `week_start_date`, `flag_reason`, `cleared_at`, `clearance_outcome`) before deciding whether to create a flag, update one's `detail`, or skip a cleared week, but F270's ruling authorized only a write operation for `timesheet-anomaly-evaluate` — the audited decrypt path, `readProtected`/`authorizeRead`, still gives system principals zero policy roles for reads. Traced directly to F270's own freshly-established mechanism — the read-side seam (`decisionRoles`'s "its policy rows list operations" comment) predates both rulings, unwired until now — and ruled the same day, no founder consultation needed: `timesheet-anomaly-evaluate` gains a second closed-table operation, `timesheet-anomaly.read-flags`; `decideRead` gains the read-side counterpart to `authorizeWrite`'s Gate 1, checked against the same whitelist. `timesheetAnomaly.evaluate` queries prior flags through `readProtected` under the same system principal it already constructs for the write.
- **F272** — Resuming Stage 18 after F271, Codex correctly stopped a fourth time rather than invent an unruled provenance value: `created_by`/`updated_by` (node) and `created_by` (edge) require a genuine `user_id UUID` in every schema variant `TimesheetAnomalyFlag` and its `triggered_by` edge fall under, and `SystemPrincipal` has no User UUID to supply — attributing the write to the submitting Team Member is false and contradicts F270 (they're deliberately denied read access to the record); no existing system operation has ever touched `graph_nodes`/`graph_edges`, so there was no precedent to trace to. Put to the founder as a genuine fork and reasoned through directly with him: each named system principal is given its own reserved, non-membership `User` identity per workspace — lazily and deterministically provisioned (the same derived-ID pattern F233/F247 already established), self-attributed, invisible to every people-facing query because none of them reach `User` except through `WorkspaceMembership`, which this identity never receives. A closed `SYSTEM_PRINCIPAL_DISPLAY_NAMES` table pairs each principal with a plain human label for later UI use — `timesheet-anomaly-evaluate`'s is "Automatic Review." `VPS-A002`'s universal conventions corrected to match, since this governs every future system-authored write, not only this one.
- **F273** — Resuming Stage 18 after F272, Codex traced the submission and week-query contracts before writing any mutation code and stopped a fifth time: Do item 5 requires `submitWeek` not to error on a zero-entry week, but the only specified persisted submission state is `TimesheetEntry.lifecycle_status`/`submitted_at`, both per-entry — when there are no entries, the transaction changes nothing, so `getWeek` and `hrCompliance.listSubmissionStatus` cannot later tell a correctly-submitted empty week apart from one nobody has opened, defeating the entire purpose of the compliance view. Codex checked `graph_mutations` directly and confirmed it unsuited (an idempotency log keyed by a hash, never replicated, not a queryable business-status store), and correctly declined the two workarounds that would have papered over this rather than fixed it (a synthetic zero-hour entry; a bespoke status table outside the graph model, bypassing the interceptor). Traced directly to `PolicyAcknowledgment`'s own registered precedent — a real, already-registered node type whose entire purpose is recording that an Employee took a confirming action with nothing richer to say about it — no founder consultation needed: a new Tier 0 `TimesheetWeekSubmission` node, created only for the zero-entry case, removed on `unlockWeek`, resolved as a fallback only when no `TimesheetEntry` rows exist.
- **F274** — Resuming Stage 18 after implementing the F270–F273 infrastructure (`3cda786`), Codex traced the first ordinary Team Member `saveCell`/zero-entry-marker create before writing it and stopped a sixth time: `TimesheetEntry`/`TimesheetWeekSubmission` both give Team Member `Full (own only)`, but the pipeline authorizes every check before `apply()` writes anything, and `rowScopeSatisfied` resolves "own" by reading the row's stored `employee_id` — on a create, no such row exists in the database yet, so the read comes back empty and the scope check fails closed. Every node type ever created before this was created only by a role holding unscoped Full, so this gap had never been exercised; `VPS-A004`'s own "self-service pattern" already names six node types (including this one) meant to work exactly this way, but none had been built until now. Codex correctly ruled out trusting an unverified claimed subject and ruled out moving authorization after the write — both would have broken invariants every other stage already relies on — and stopped rather than invent either. Put to the founder as a genuine fork (no node type had ever needed this before) and ruled: a mutation's own already-validated args declare the new row's subject through a new `WriteChange.declaredSubjectEmployeeId` field, kept distinct from Gate 3's existing `subjectEmployeeId`; `rowScopeSatisfied` compares it against the caller's own identity exactly as it compares a stored row's field once one exists. Because nothing enforces that a future mutation's declared subject and its actual write stay in sync, the founder asked for the stronger version: the pipeline independently re-reads the row immediately after `apply()` and refuses the whole transaction if it does not match what was authorized.
- **F275** — Resuming Stage 18 after F274's mechanism was implemented, Codex found that saving a Billable or Pitch cell still failed for an ordinary Team Member, independently of F274's fix: `services/api/src/permission/interceptor.ts`'s `edgeRoleDecision` requires `Full` on an edge's endpoint unless that edge's own registration declares `readSufficientEndpoints` for the endpoint's node type, and `logged_against` (TimesheetEntry to Assignment or Pitch) declared none for either. This split into two independent halves. The Assignment half traced directly to `assigned_to`'s own registered precedent (F262, Stage 16) — an edge write already declaring `readSufficientEndpoints: { Project: true }` for the identical shape of problem, a writer holding only Read on an endpoint that should still count as sufficient — no founder consultation needed. The Pitch half is a genuine fork: Team Member's outcome there is unconditionally `NONE`, a deliberate F264–F267 restriction (pitch details can be commercially sensitive before a deal is won), not an oversight, so no `readSufficientEndpoints` declaration could touch it — closing it needed an actual access-widening decision. Codex correctly named three materially different security choices with no existing rule picking one (a blanket Team Member Pitch grant; a staffing-scoped grant; a mutation-local exception bypassing the interceptor) and stopped rather than choose one unilaterally. Put to the founder, who chose the staffing-scoped grant and, on being asked directly whether it was the most robust long-term choice, asked for a reusable, declarative mechanism rather than a Pitch-specific hand-coded check — the identical shape of grant `VPS-A004`'s own "Three recurring override patterns" section already named for InterviewRound/FeedbackEntry/OnboardingTask but had never implemented.
- **F276** — Resuming Stage 18 after F275's mechanism was implemented (verified 5/5 against the real interceptor: Assignment access, Pitch denial, staffing, unstaffing, restaffing), Codex traced `saveCell`'s own required `week_start_date` derivation before writing it and stopped a ninth time: `VRS-F004`'s `working_week` schema permits any arrangement of seven booleans, not only a contiguous block — a genuine seven-day working week has no rest gap to anchor a "first working day" on, and a split pattern (Sunday/Tuesday/Thursday/Friday, say) has several candidates. `resolveWorkingDay` also folds holidays into `is_working`, so even an ordinary week's key could have silently shifted depending on where a holiday fell. No `week_start_day` or equivalent field existed anywhere. Codex correctly declined to invent a hidden Monday default or a longest-gap heuristic and removed the incomplete `timesheet` mutation-schema draft rather than leave a stub. Put to the founder as a genuine fork — no existing concept in this codebase ever defined which day begins a week — and, on the founder's own second-pass robustness check of his first recommendation, refined further: a rest-gap heuristic was rejected as *more* exposed to drift than a calendar-level anchor (an ordinary `pattern.set` edit is far more frequent than a rare, Owner/HR-Admin-only `calendar.update`), and restricting permitted schedules was rejected outright as breaking `VRS-F004`'s own explicitly-supported split-pattern employees. Ruled: a new required `week_start_day` field on `WorkingCalendar`, versioned identically to `working_week`, decouples the week boundary from `is_working`, holidays and `WorkingPattern` entirely — a fixed calendar-level fact resolved by pure date arithmetic. One boundary case (a mid-week calendar edit or entity re-scoping splitting a week across two anchor versions) is named explicitly as an accepted, inherent limitation rather than guarded in code, since guarding it would require `VRS-F004`'s own mutations to read `VRS-F010`'s `TimesheetEntry` table — the identical cross-feature coupling `VRS-F004`'s own G08 already forbids in the other direction.

Also settled while briefing, none rising to a numbered finding — established conventions and scope boundaries applied here for the first time:

- `overtime_flag_threshold` is a hardcoded constant this stage; no per-workspace configuration mechanism exists yet. `VPS-F005` (Workspace Configuration Console), unbuilt and positioned later in MVP order, will own making it configurable.
- `hrCompliance.sendReminder` is out of scope entirely this stage — it requires `VPS-F003` (Notification and Alert Center), unbuilt. Build `hrCompliance.listSubmissionStatus` only; do not stub, no-op, or partially implement `sendReminder`.
- `timesheetAnomaly.clear`'s `ApprovedOvertime` outcome records the outcome, note, `cleared_at`/`cleared_by`, and clears the flag — it does not call `VRS-F018`'s unbuilt `overtime.approve`. The spec's own Out of Scope section already defers TOIL accrual and formal overtime approval to that feature; this stage owns only the flag and its clearance outcome.
- Anomaly evaluation runs as a separate step immediately after `submitWeek`'s transaction commits, not inside that transaction — G10's own "never a gate" language rules out coupling flag evaluation to the write it must never delay.

**Do:**

1. **`logged_against` governing partition (F267).** `packages/schema/src/registry/edges.ts`: add `governingPartitions: { Pitch: "identifying" }` to the existing `logged_against` `edgeGroup`. `Assignment` is unsplit and needs no entry. Also add `readSufficientEndpoints: { Assignment: true }` to the same `edgeGroup` (F275): identical in shape to `assigned_to`'s own `readSufficientEndpoints: { Project: true }` from F262, letting a Team Member's existing `READ_ANY()` on Assignment count as sufficient for this edge write without widening anyone's Assignment visibility.

   **1b. `WorkingCalendar.week_start_day` (F276).** `packages/schema/src/mutations/calendar.ts`: add a required `week_start_day: z.int().min(1).max(7)` field to `workingWeekSchema`'s sibling calendar-record shape and to `calendarUpdate`'s input (alongside `working_week`/`daily_hours`); extend `initialWorkingWeekFor(jurisdiction)` to also return `week_start_day` — `1` (Monday) for `PK`, `Global`, `IN`, `SG`, `UK`, `US`; `7` (Sunday) for `AE`/`SA` — matching the weekday each template's own `working_week` already begins on, so every existing calendar's behavior is unchanged. `calendarUpdate.apply()`: carry `week_start_day` into the new superseding `WorkingCalendar` version's own record exactly as `working_week`/`standard_daily_hours` already are (no default derivation — an admin editing a calendar supplies it explicitly). `packages/schema/src/working-day.ts`: add `resolveWeekStartDate(date: string, weekStartDay: number): string`, a pure function alongside `resolveWorkingDay`/`addIsoDays`/`isoDatesInclusive` (F237's own "one portable implementation" precedent) that walks backward from `date` to the most recent date whose ISO weekday equals `weekStartDay` (at most six days back) — consulting no `is_working`, holiday or pattern state at all. `services/api/src/mutations/entity.ts`'s optimistic handler: carry `week_start_day` into the locally-computed `WorkingCalendar` object it derives from the same shared `initialWorkingWeekFor` template logic, so an offline-created calendar does not diverge from the server-created one on this field — extend F233's existing online/offline parity test to cover it.
2. **`rowScopeSatisfied` resolution branches (F268, corrected by F269).** `services/api/src/permission/interceptor.ts`: extend the existing `else if (row.nodeType === "BurnoutAlert")` condition to `else if (row.nodeType === "BurnoutAlert" || row.nodeType === "TimesheetAnomalyFlag")` — no other change to that branch's body, it already follows the registered `triggered_by` edge the same way for both node types. Add a separate `else if (row.nodeType === "TimesheetEntry")` branch: read the row with the already-imported `getNode(tx, principal.workspaceId, nodeId)`, and set `subjectEmployeeId` from `node.record.employee_id` when it is a string; otherwise fall through to `return false`, matching the existing branches' fail-closed posture. No change to the `own`/`direct-reports`/`own-plus-team` evaluation below this point.
   **2b. System-principal write authority (F270).** `packages/schema/src/policy/principal-policy.ts`: add `"timesheet-anomaly-evaluate"` to `SYSTEM_PRINCIPAL_NAMES`, add `"timesheet-anomaly.create-flag"` to `SYSTEM_OPERATIONS`, and set `SYSTEM_PRINCIPAL_OPERATIONS["timesheet-anomaly-evaluate"] = ["timesheet-anomaly.create-flag"]`. `services/api/src/permission/interceptor.ts`, `authorizeWrite`: give `principal.kind === "system"` its own Gate 1 path — checked against `isSystemOperationPermitted` for the node/edge being written, never against `bestCell`'s role matrix (a system principal still never receives a `PolicyRole`). Leave Gate 2 and Gate 3 untouched. `timesheetAnomaly.evaluate` passes `{ kind: "system", name: "timesheet-anomaly-evaluate", workspaceId }` for the flag-and-edge write only; every other Stage 18 mutation keeps the calling member's own principal.
   **2c. System-principal read authority (F271).** `packages/schema/src/policy/principal-policy.ts`: add `"timesheet-anomaly.read-flags"` to `SYSTEM_OPERATIONS`; extend `SYSTEM_PRINCIPAL_OPERATIONS["timesheet-anomaly-evaluate"]` to `["timesheet-anomaly.create-flag", "timesheet-anomaly.read-flags"]`. `services/api/src/permission/interceptor.ts`, `decideRead`: give `principal.kind === "system"` the read-side counterpart to Gate 1 above — checked against `isSystemOperationPermitted(principal.name, "timesheet-anomaly.read-flags")`, never against `bestCell` with an empty role set. Widen `ReadDecision`'s `full`/`read` variant to `role: PolicyRole | null`, mirroring `WriteDecision`'s own shape — a system principal still never receives a `PolicyRole`. No change to `readProtected` itself; it calls `authorizeRead` per fragment already and will honor the new grant unmodified. `timesheetAnomaly.evaluate` queries prior `TimesheetAnomalyFlag` rows for the employee/week/reason being evaluated through `readProtected`, under the same system principal item 2b already has it construct for the write.
   **2d. System-principal provenance identity (F272).** `packages/schema/src/policy/principal-policy.ts`: add a closed `SYSTEM_PRINCIPAL_DISPLAY_NAMES: Readonly<Record<SystemPrincipalName, string>>` alongside the existing tables — `"timesheet-anomaly-evaluate": "Automatic Review"`. New helper (naming Codex's own call — a `system-actor.ts` alongside `founding.ts` is a reasonable home): given a `(workspaceId, SystemPrincipalName)` pair, derive a deterministic `node_id` the same way F233/F247's own derived-ID precedent does, and get-or-create a reserved `User` node under it — universal fields only, no linked Employee, no `WorkspaceMembership` edge, `created_by`/`updated_by` self-referential — via a race-safe `INSERT ... ON CONFLICT DO NOTHING` (or the store layer's existing equivalent). `packages/schema/src/mutations/shared.ts`'s `Provenance`/`stampNewNode`/`stampNewEdge` are unchanged; the resolution step belongs where `provenance(ctx)` is built for the anomaly mutation — when the acting principal is the `timesheet-anomaly-evaluate` `SystemPrincipal`, `Provenance.userId` is this reserved identity's UUID (get-or-created in the same transaction as the flag/edge write itself), not a submitter's own `userId`. This identity is never exposed as a `MemberPrincipal` and never gains node-type grants of its own — it exists solely to satisfy `created_by`/`updated_by`'s existing `user_id UUID` requirement honestly.
   **2e. `TimesheetWeekSubmission` registration (F273).** `packages/schema/src/registry/nodes.ts`: register `TimesheetWeekSubmission` (owner `VRS-F010`, `fixedProtection("Standard", 0)` — the same tier as `TimesheetEntry` and `PolicyAcknowledgment`). `packages/schema/src/timesheet.ts`: `timesheetWeekSubmissionFieldsSchema` — `employee_id` (UUID, required, a direct field per G04, not an edge, since this node is Tier 0 exactly like `TimesheetEntry`), `week_start_date` (date), `submitted_at` (timestamp), plus the standard node envelope. `packages/schema/src/policy/policy-table.ts`: give `TimesheetWeekSubmission` the identical `MATRIX_OVERRIDES` row `TimesheetEntry` already has (owner/HR Admin `FULL_ANY()`, Finance-admin `READ_ANY()`, Manager `cell("read", "direct-reports", ...)`, Team Member `cell("full", "own", ...)`) — knowing a week was formally submitted is no more sensitive than seeing the entries themselves. `services/api/src/permission/interceptor.ts`: extend `rowScopeSatisfied`'s `TimesheetEntry` branch's condition to `row.nodeType === "TimesheetEntry" || row.nodeType === "TimesheetWeekSubmission"`, reading `employee_id` off the row the identical way — no new resolution mechanism. `docs/Vulto_Specs/VPS-A002_Master_Graph_Schema_Definition.md`'s node type registry table gains a `TimesheetWeekSubmission` row matching `TimesheetEntry`'s and `TimesheetAnomalyFlag`'s own format.
   **2f. Row-scope creation authorization (F274).** `services/api/src/mutations/types.ts`: `WriteChange` gains `declaredSubjectEmployeeId?: string | null` — distinct from the existing `subjectEmployeeId` (Gate 3's reader-set field; leave it untouched) — set by a mutation's own plan-builder from its already-Zod-validated args, never a raw client claim. `services/api/src/permission/interceptor.ts`: extract `rowScopeSatisfied`'s existing per-node-type row-reading resolution (the Employee / BurnoutAlert-TimesheetAnomalyFlag / TimesheetEntry-TimesheetWeekSubmission branches F268/F269/F273 already established) into its own reusable function, e.g. `resolveStoredSubjectEmployeeId(tx, workspaceId, nodeType, nodeId, asOf, context)`, called by `rowScopeSatisfied` exactly as before whenever a row already exists. Add a new branch: when the check is for a create (`authorizeWrite`'s Gate 1 passes this down explicitly as its own flag, never inferred from `nodeId` being null — this codebase's own convention, per `graph.createNode`, already pre-generates a real id before a row is inserted) and the node type is `TimesheetEntry` or `TimesheetWeekSubmission`, resolve the subject from the declared value instead of reading a row that does not exist yet; every other node type still fails closed, matching the existing conservative default. Thread this through `authorizeWrite`'s Gate 1 node branch (pass `change.declaredSubjectEmployeeId` and whether `change.operation === "create"` into the row context) and through `edgeRoleDecision` (add a `change: WriteChange` parameter; the identical declared-subject treatment applies per endpoint, so `logged_against`'s TimesheetEntry side authorizes correctly when the entry and its edge are created in the same mutation). `services/api/src/mutations/pipeline.ts`: immediately after `plan.apply()` returns and before the mutation's outcome is recorded, for every check whose `change.operation === "create"` and `change.declaredSubjectEmployeeId` is non-null, re-read the row `apply()` actually wrote using the same `resolveStoredSubjectEmployeeId` and confirm it matches what was declared and authorized; a mismatch throws `MutationRejection("subject-mismatch")`, rolling back the whole transaction rather than partially committing. This is the create-time half of the row-scope mechanism `VPS-A004`'s self-service pattern already names for six node types; `TimesheetEntry`/`TimesheetWeekSubmission` are the first to build it, and every later self-service node type (LeaveRequest, Expense, DevelopmentGoal, TrainingRecord, Invoice) reuses it unchanged, never reinvents it.

   **2g. Participant-scoped edge grant for Pitch (F275).** New file `packages/schema/src/registry/participant-grants.ts`: define
   ```ts
   export interface ParticipantGrant {
     readonly viaEdgeType: EdgeType;
     readonly fromNodeType: NodeType;
     readonly outcome: "read" | "full";
   }
   export const PARTICIPANT_GRANTS: Readonly<Partial<Record<NodeType, ParticipantGrant>>> = {
     Pitch: { viaEdgeType: "staffed_on", fromNodeType: "Employee", outcome: "read" },
   };
   ```
   `services/api/src/permission/interceptor.ts`: add `participantGrantSatisfied(tx, principal, nodeType, nodeId, asOf, context)` — for a member principal only, resolve the caller's own Employee via the existing `resolveEmployeeForUser` helper (`rowScopeSatisfied` already uses it), look up `PARTICIPANT_GRANTS[nodeType]`, and if present, check for a currently-active outgoing edge of `viaEdgeType` from that Employee to `nodeId` via the existing `outgoing(tx, workspaceId, employeeId, viaEdgeType, asOf)` (its own `activeAt(at)` filtering already resolves only currently-active staffing, no new lifecycle logic needed). Wire this into `edgeRoleDecision` only — never into `decideRead`/`bestCell` generally, since general staffed-Team-Member Pitch read access is already handled by `pitch.listStaffedFor` (Stage 17/F264–F267) and widening that general path is not this ruling's question — as an independent additional path to `sufficient`, alongside (never replacing) the existing role-cell and `readSufficientEndpoints` paths: `sufficient = outcome === "full" || (outcome === "read" && readSufficientEndpoints[nodeType] === true) || participantGrantSatisfied(...)`. This registry is deliberately reusable: when InterviewRound, FeedbackEntry, or OnboardingTask are eventually built, each adds its own entry to `PARTICIPANT_GRANTS` and wires whichever consumer (`decideRead` and/or `edgeRoleDecision`) it actually needs, reusing this same table rather than inventing a parallel mechanism — this is the founder's own explicit request for the more robust, declarative version over a Pitch-specific hand-coded check.
3. **`packages/schema/src/timesheet.ts`.** New file, modeled on `employee.ts`'s field-schema pattern, incorporating `VRS-F009`'s `time-classification.ts` directly (`timeClassificationFieldsSchema`, `validateTimeClassification`) rather than layering it on. `timesheetEntryFieldsSchema`: `employee_id` (UUID, required — a direct field, per G04), `assignment_id` (UUID, nullable), `pitch_id` (UUID, nullable), the classification fields, `date` (date), `hours` (decimal, min 0, max 24), `week_start_date` (date), `notes` (text, nullable), `lifecycle_status` (enum: Draft, Submitted), `submitted_at` (timestamp, nullable), plus the standard node envelope. `timesheetAnomalyFlagFieldsSchema`: `employee_id`, `week_start_date`, `flag_reason` (enum per spec), `detail` (text), `lifecycle_status` (enum: Active), `cleared_at`/`cleared_by`/`clearance_outcome` (enum: Corrected, AcceptedAsNormal, ApprovedOvertime)/`clearance_note` — all nullable until clearance, plus the standard envelope.
4. **`packages/schema/src/mutations/timesheet.ts`.** Four mutations modeled directly on `assignment.ts`'s `defineMutation` shapes:
   - `timesheetSaveCell = defineMutation({ name: "timesheet.saveCell", input: z.object({ employee_id: uuidV4Schema, date: isoDate, row_context: z.discriminatedUnion(...) /* assignment_id | 'non-billable' | pitch_id */, hours: z.number().min(0).max(24), category fields... }).strict(), tier: 0, onlineOnly: false, stateTransition: false })`. Validate the 24-hour daily cap and `validateTimeClassification`'s three write-time dependencies before the write reaches the store, rejecting with `MutationRejection("invalid-args")` naming the day and total on violation — never silently correcting.
   - `timesheetSubmitWeek = defineMutation({ name: "timesheet.submitWeek", input: z.object({ employee_id: uuidV4Schema, week_start_date: isoDate }).strict(), tier: 0, onlineOnly: false, stateTransition: true })`. One mutation call, one transaction (`pipeline.ts`'s own per-mutation transaction gives G07's atomicity for free) — do not decompose this into N separate `saveCell`-style writes.
   - `timesheetUnlockWeek`, same input shape, transitions Submitted back to Draft.
5. **`services/api/src/mutations/timesheet.ts`.** `saveCell.apply()`: create-or-update exactly one TimesheetEntry per `(employee_id, date, row_context)` (G01), writing `logged_against` to Assignment or Pitch per category, none for NonBillable. Before writing, resolve `week_start_date` (item 1b, F276): the employee's Entity via the existing `resolveForEmployee(tx, workspaceId, args.employee_id, asOf: args.date)`, that Entity's calendar version in force as of the same date via the existing `resolveCalendarForEntity`, then `resolveWeekStartDate(args.date, calendar.record.week_start_day)`; store the result once on the row — never recomputed on a later read, the same stability F230 already established for a historical working week. Its own create check — and the `logged_against` edge's, when one is written — declares `change.declaredSubjectEmployeeId: args.employee_id` (item 2f, F274): the identical field `apply()` writes into the new row's own `employee_id`, read once from the parsed args, never recomputed a second way that could drift from it. The `logged_against` edge write itself now succeeds for an ordinary staffed Team Member through two independent mechanisms (item 2g, F275): `readSufficientEndpoints: { Assignment: true }` for a Billable cell, and `participantGrantSatisfied` resolving their own current `staffed_on` edge to the target Pitch for a Pitch cell; an unstaffed Team Member's Pitch cell write is still refused. `submitWeek.apply()`: within its own transaction, read every Draft entry for `(employee_id, week_start_date)` — where the caller-supplied `date` is itself first resolved to `week_start_date` via item 1b's identical mechanism, never trusted as a raw client-supplied key — and transition all to Submitted, setting `submitted_at`; zero entries is not an error — an empty week can genuinely be submitted, per Do item 5's own requirement that a person between assignments can confirm they have nothing to log. When that read comes back with zero rows, create exactly one `TimesheetWeekSubmission` row (item 2e, F273) for `(employee_id, week_start_date)` in the same transaction — the only persisted record that this empty week was actually submitted, as opposed to never opened. That create's own check likewise declares `change.declaredSubjectEmployeeId: args.employee_id` (item 2f, F274). The ordinary non-empty case creates no such row; `TimesheetEntry.lifecycle_status` already carries status correctly whenever entries exist. `unlockWeek.apply()`: the reverse transition, gated the same way any other write is — TimesheetEntry is not immutable at the permission layer once Submitted, only interface-gated.
6. **`services/api/src/permission/timesheet-queries.ts`.** `getWeek(tx, principal, employeeId, weekStartDate?)`: when `weekStartDate` is omitted, derive it for "today" through item 1b's own `resolveWeekStartDate` mechanism — never a hardcoded Monday, per F276 — then resolve the employee's working days for the target week via `resolveWorkingDay`/`isoDatesInclusive` (F237/`VRS-F004`'s own precedent, never `contracted_hours` divided by a weekday count — G06); rows from the employee's active Assignment edges plus a standing Non-Billable row and, where a `staffed_on` edge exists, a Pitch row (reuse `pitch.listStaffedFor`'s own Tier-0-only read path, `VRS-F009`'s precedent — never expose `commercial`-partition fields here); read existing TimesheetEntry rows for the week via the row-scoped read this stage's item 2 makes resolvable. `hrCompliance.listSubmissionStatus(tx, principal, workspaceId, weekStartDate)`: list each employee's Draft/Submitted/Not-Started status for the week — HR-Admin-scoped per the policy table. Both resolve status by checking that employee/week's `TimesheetEntry` rows first (unchanged — status is Draft or Submitted per the existing per-entry fields whenever any entry exists) and, only when none exist, falling back to whether a live (non-soft-deleted) `TimesheetWeekSubmission` marker exists for `(employee_id, week_start_date)` (item 2e, F273): present means Submitted, absent means Not-Started. `getWeek`'s own two-value contract folds Not-Started into Draft for a single employee's own view — an untouched grid looks identical either way; `hrCompliance.listSubmissionStatus`'s three-value contract reports Not-Started distinctly, which is the entire reason this marker exists. `unlockWeek.apply()` soft-deletes the live marker when reversing a zero-entry week's submission, mirroring the reverse transition already specified for entries.
7. **Anomaly detection — pure functions plus mutations.** New `packages/schema/src/timesheet-anomaly.ts`: three pure, independently unit-testable functions — `detectPostEndDateAssignment`, `detectZeroVarianceWeek`, `detectHoursExceedExpected(entries, expectedWeeklyHours, threshold = 1.3)` — matching the spec's Detection rules section exactly, including `HoursExceedExpected`'s "no Pitch entries in the week" exclusion and `overtime_flag_threshold`'s hardcoded 1.3 default (see the brief's own settled-while-briefing note above). `services/api/src/mutations/timesheet-anomaly.ts` / `permission/timesheet-anomaly-queries.ts`: `timesheetAnomaly.evaluate(employeeId, weekStartDate)` runs the three pure functions against that week's just-submitted entries and creates/updates TimesheetAnomalyFlag rows (G09: at most one Active uncleared flag per employee/week/reason, a repeat trigger updates `detail`; G09b: an `ApprovedOvertime`-cleared `HoursExceedExpected` flag is never re-raised) by first reading that employee/week/reason's prior flags through `readProtected` under the read grant item 2c authorizes (F271), then writing the new flag's `triggered_by` edge to the flagged Employee in the same transaction (F269 — this is what `rowScopeSatisfied`'s shared `BurnoutAlert`/`TimesheetAnomalyFlag` branch traverses; without it every row-scoped check against the flag fails closed). The flag-and-edge write runs under a new `SystemPrincipal` (`{ kind: "system", name: "timesheet-anomaly-evaluate", workspaceId }`), never the submitting Team Member's own principal, and its `created_by`/`updated_by`/edge `created_by` resolve to that principal's own reserved `User` identity (item 2d, F272) via the same get-or-create used everywhere else that identity is needed — never the submitter's UUID, never a fabricated one — that Team Member has `NONE` on `TimesheetAnomalyFlag`, deliberately, so `timesheetAnomaly.evaluate` cannot construct the flag under its caller's own authority (F270). Everything else in this item — `listActive`, `clear` — still runs under the calling member's own principal, unchanged — called as a separate step immediately after `submitWeek`'s transaction commits (not inside it, per the settled-while-briefing note above), and its own errors never surface as a `submitWeek` failure. `timesheetAnomaly.listActive(workspaceId)`: scope through `filterReadable` the same way `sweepOvercommitted`/`listEmployees` already do (F263's own precedent), so a Manager sees only direct reports' flags. `timesheetAnomaly.clear(flagId, outcome, note?)`: requires `outcome`, sets `cleared_at`/`cleared_by`/`clearance_note`; an `ApprovedOvertime` outcome records itself as such and exempts that week from future `HoursExceedExpected` re-evaluation (G09b) but does not call `VRS-F018`'s unbuilt `overtime.approve` — no `toilDaysAccrued` is computed or returned this stage (see the brief's own settled-while-briefing note above).
8. **No UI.** Nothing under `apps/` changes, following Stage 8/11/12/13/14/15/16/17's own precedent. The full Interface Specification (the grid, mobile day view, compliance and anomaly-review screens) is reference material for whichever later stage wires the real UI.

**Done criteria:**
- `logged_against` is registered with `governingPartitions: { Pitch: "identifying" }` and no other declaration on the Pitch pair — a real interceptor test must prove a Pitch-categorized `saveCell` succeeds, since omitting the partition refuses everyone (F267).
- `rowScopeSatisfied`'s branches are proven against the real interceptor: an employee reading or writing their own TimesheetEntry succeeds; an unrelated Team Member reading another employee's TimesheetEntry is refused; a Manager reading/clearing a direct report's TimesheetAnomalyFlag succeeds; a Manager doing the same for a non-report is refused (F268, corrected by F269).
- `timesheet.saveCell` rejects a 25-hour single-date total with `MutationRejection("invalid-args")` before any write reaches the store, naming the day and the total — proven by a test — and the optimistic mutator surfaces this as `GraphValidationError`.
- `timesheet.submitWeek` transitions every Draft entry for `(employee_id, week_start_date)` atomically — proven by a test that a failure partway through (an injected constraint violation, mirroring Stage 15's promotion-transaction proof style) leaves every entry still Draft, no partial submission (G07).
- `getWeek`'s columns and `fd`/`hd` resolution both route through `resolveWorkingDay`, not `contracted_hours` divided by a weekday count — proven by a test against a six-day week, a compressed four-day pattern, and a workspace half-day (G02, G06).
- `week_start_date` is the employee's first working day, not a hardcoded Monday — proven by a test against a Sunday-to-Thursday workspace (G05).
- `HoursExceedExpected` compares against `resolveWorkingDay`'s expected hours for that week, not `contracted_hours` — proven by the spec's own four-day-pattern acceptance criterion (34 logged against 32 expected × 1.3, no flag).
- A week containing a Pitch entry never triggers `HoursExceedExpected`, regardless of total hours — proven by a test.
- A cleared flag with `outcome: "ApprovedOvertime"` is never re-flagged by a later `HoursExceedExpected` evaluation against the same week — proven by a test that exercises `timesheetAnomaly.evaluate`'s own prior-flag read (F271), not a mock — and `timesheetAnomaly.clear` makes no call to any `VRS-F018` mutation (grep the diff).
- `timesheetAnomaly.evaluate` runs after, not inside, `submitWeek`'s transaction — proven by a test that an evaluation failure (simulate one) does not roll back or fail the submission.
- `timesheetAnomaly.evaluate`'s prior-flag lookup goes through `readProtected` under the `timesheet-anomaly-evaluate` system principal's `timesheet-anomaly.read-flags` grant, never a direct decrypt or the submitting Team Member's own principal — proven by a test that a Team Member with `NONE` on `TimesheetAnomalyFlag` still gets correct G09/G09b enforcement on their own submission (F271).
- The created `TimesheetAnomalyFlag` and its `triggered_by` edge carry the `timesheet-anomaly-evaluate` principal's own reserved `User` identity in `created_by`/`updated_by`, never the submitting Team Member's UUID and never a value with no row behind it — proven by a test that resolves `created_by` back to a real, gettable `User` node with no `WorkspaceMembership` edge (F272). A second evaluation for the same workspace reuses the same identity rather than creating a second one — proven by a test asserting the same UUID across two separate evaluations.
- Submitting a zero-entry week creates exactly one `TimesheetWeekSubmission` row, and both `getWeek` and `hrCompliance.listSubmissionStatus` report that week as Submitted afterward, not Draft or Not-Started; a genuinely never-opened week reports Not-Started in `hrCompliance.listSubmissionStatus`; and a week with at least one entry never creates a `TimesheetWeekSubmission` row at all — proven by tests covering all three cases. `unlockWeek` on a zero-entry submitted week soft-deletes the marker, and the week reports Not-Started again afterward — proven by a test (F273).
- An ordinary Team Member can save their own first `TimesheetEntry` cell and submit their own first genuinely empty week — proven end-to-end against the real interceptor, not a privileged Owner/HR Admin fixture. A Team Member's create attempt carrying a *different* employee's id in `employee_id` is refused, not silently corrected — proven by a test. A mutation's post-write re-check genuinely re-reads the written row rather than trusting the declared value a second time — proven by a test that corrupts what `apply()` writes (a deliberately mismatched `employee_id`) and asserts the whole transaction rolls back with `subject-mismatch`, not a partial write (F274).
- An ordinary Team Member currently staffed on a Pitch can save a Pitch-category `TimesheetEntry` cell referencing it, and an ordinary Team Member can save a Billable cell referencing any Assignment they can already read — both proven end-to-end against the real interceptor. A Team Member who is *not* currently staffed on a given Pitch is refused when saving a cell against it, proven by a test; staffing that Team Member on the Pitch and re-attempting the identical save succeeds, and unstaffing them again (Stage 17's `pitch.unstaffEmployee`) makes the identical save fail again — proven by a test exercising `outgoing()`'s own active-edge semantics, not merely a static staffed/unstaffed fixture (F275).
- `week_start_date` genuinely resolves through `resolveWeekStartDate`/`resolveCalendarForEntity`, never through `resolveWorkingDay`, `is_working` or holiday state — proven by a test using a genuinely seven-day `working_week` and a separate test using a split pattern (for example Sunday/Tuesday/Thursday/Friday), both producing a stable, correct key; a holiday landing on what would otherwise be the anchor day is proven not to change `week_start_date` for that week. A Sunday-to-Thursday (`week_start_day: 7`) calendar and a Monday-to-Friday (`week_start_day: 1`) calendar in the same workspace each group their own entries correctly and independently — proven by a test, not asserted (F276).
- `timesheetAnomaly.listActive` scopes through `filterReadable` exactly as `sweepOvercommitted` does — a Manager sees only direct reports' flags, an unrelated Team Member sees none — proven by a test.
- `TimesheetAnomalyFlag` is never readable by the flagged employee themselves — proven by a test, matching BurnoutAlert's own precedent.
- `hrCompliance.sendReminder` does not exist anywhere in the diff — grep to confirm; only `hrCompliance.listSubmissionStatus` is built.
- No call to `overtime.approve` or any other `VRS-F018` mutation exists anywhere in the diff — grep to confirm.
- No file under `apps/` appears in the diff.
- `pnpm verify` passes typecheck with real handlers for every new mutation and query — no stub, no-op, or placeholder.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 18 report:** that `logged_against`'s `governingPartitions` genuinely includes `Pitch: "identifying"` — grep the edges-registry diff directly, since its absence structurally refuses every Pitch-categorized entry regardless of role (F267); that `rowScopeSatisfied`'s `TimesheetEntry` branch reads `employee_id` from the row's own stored field via `getNode` (never an edge — none exists for a Pitch-categorized entry), and that its `TimesheetAnomalyFlag` handling instead extends the existing `BurnoutAlert` branch to traverse the registered `triggered_by` edge, never a field read off a Tier 2 record — read the interceptor diff's own code path, not just a passing test name (F268, corrected by F269); that `timesheetAnomaly.evaluate` writes the new flag's `triggered_by` edge to the flagged Employee in the same transaction as the flag itself, not as an afterthought (F269); that `timesheetAnomaly.evaluate`'s prior-flag read calls `readProtected` under the `timesheet-anomaly-evaluate` system principal's new `timesheet-anomaly.read-flags` grant — read the query code's own principal construction, not just a passing G09/G09b test (F271); that the flag and its edge genuinely resolve `created_by`/`updated_by` to the reserved system-principal `User`'s own UUID — read the provenance resolution's own code path, not just a passing test — and that this identity has no `WorkspaceMembership` edge and no linked Employee (F272); that `submitWeek` creates a `TimesheetWeekSubmission` row only on the zero-entry path, never alongside real entries — read the mutation's own branching, not just a passing test — and that `getWeek`/`hrCompliance.listSubmissionStatus` check `TimesheetEntry` rows first and fall back to the marker only when none exist, rather than checking the marker unconditionally (F273); that `saveCell`'s and `submitWeek`'s create checks genuinely declare `declaredSubjectEmployeeId` from the parsed args' own `employee_id`, not a separately recomputed value — read the plan-builder's own code, not just a passing test — and that the pipeline's post-`apply()` re-check is real (a deliberately mismatched write is rejected with `subject-mismatch` and rolled back whole, proven by a test, not merely asserted in prose) (F274); that `logged_against`'s registration genuinely carries `readSufficientEndpoints: { Assignment: true }` and that `PARTICIPANT_GRANTS`/`participantGrantSatisfied` are wired only into `edgeRoleDecision`, not into `decideRead`/`bestCell` — read the interceptor's own code, not just a passing test — and that a Team Member's Pitch grant genuinely tracks live `staffed_on` state rather than a cached or one-time check (F275); that `WorkingCalendar.week_start_day` is genuinely carried through `initialWorkingWeekFor`, `calendarUpdate.apply()`, and `entity.create`'s optimistic handler alike — grep for all three, since a divergence between the online and offline template would silently reintroduce F233's own parity gap for this one field — and that `resolveWeekStartDate` is a pure function taking no calendar/holiday/pattern state beyond the single `weekStartDay` integer, read directly, not inferred from a passing test alone (F276); that `timesheet.submitWeek` is genuinely one mutation performing every entry's transition inside its own single transaction, not a loop of separate `saveCell`-style calls that could partially fail (G07); that `getWeek` and the shortcut resolution both call `resolveWorkingDay`, confirmed by grep, never `contracted_hours / weekday-count` arithmetic anywhere in the diff (G02, G06); that `HoursExceedExpected`'s pure function takes `expectedWeeklyHours` as a parameter rather than recomputing `contracted_hours`-based hours internally, and that its Pitch-week exclusion is exercised by a real test, not merely present in the code; that `timesheetAnomaly.evaluate` is called as a step after `submitWeek`'s transaction commits — read the calling code's own sequencing, not just a test name — and that nothing in `submitWeek`'s own transaction depends on evaluation succeeding (G10); that `timesheetAnomaly.listActive` calls `filterReadable` on the same node type/scope shape `sweepOvercommitted` already established (F263's own precedent), not a new, parallel scoping mechanism; that `timesheetAnomaly.clear`'s `ApprovedOvertime` path makes no call into any `VRS-F018` file or mutation — grep the diff; that `hrCompliance.sendReminder` is absent from the diff entirely, not stubbed; and that no file under `apps/` appears in the diff.

---

### Stage 19 — Billable vs Non-Billable Pulse (`VRS-F011`)

Server-side only, following every prior stage's own precedent — no file under `apps/` changes; the Interface Specification is reference material for whichever later stage wires the real UI.

**Findings F277 and F278, both closed before this brief was written, no founder consultation needed for either:**

- **F277** — `VRS-F011`'s own spec still described `week_start_date` as "the first working day of the week" and `expected_hours` as coming from a `workingDays.count(employeeId, weekStart, weekEnd)` function, both written before Stage 18 existed. Neither matches what Stage 18 actually built: `week_start_date` resolves through `WorkingCalendar.week_start_day` (F276) via `weekStartForEmployee`, and `getWeek` (`services/api/src/permission/timesheet-queries.ts`) already computes exactly the "expected hours for this employee this week" figure this feature needs, via `hoursOn` summed across the week. Corrected to name the real mechanisms; `utilizationSnapshot.compute`'s own expected-hours figure must reuse this identical computation, never a second, independently-written one.
- **F278** — `VRS-F011`'s Security Considerations claimed `UtilizationSnapshot` "follows Employee's Standard, Tier 0 visibility... a deliberate reuse of an existing rule," but neither of Employee's two Tier 0 policy rows matches that shape alone: `Employee:operational`'s Team Member cell is `Read (own + team)`, not "own" alone; `Employee:compensation`'s Manager cell is `Restricted`, not "direct reports." Ruled: the new row is composed from each existing row's matching cell rather than copying either wholesale.

**Three more findings, surfaced by Codex tracing this brief into the existing interceptor before writing any product code, all closed before implementation resumed:**

- **F279** — `resolveStoredSubjectEmployeeId`'s stored-field branch (`services/api/src/permission/interceptor.ts`) covered `TimesheetEntry`/`TimesheetWeekSubmission` only; `UtilizationSnapshot` fell through to `null`, leaving F278's own Team Member "own only" grant with no subject to resolve against and refusing every such read. Ruled directly on F268's own precedent: `UtilizationSnapshot` added to the same branch.
- **F280** — `utilizationSnapshot.compute`'s reactive write has no authorized writer: F278's own Team Member row is `Read (own only)`, never `Full`, so the ordinary caller whose edit triggers the write cannot write the resulting snapshot. Ruled directly on `VRS-F010`'s own `timesheetAnomaly.evaluate` system-principal precedent (F270–F272): a new system principal, `utilization-snapshot-compute`, authorized for a new write operation, `utilization-snapshot.compute`.
- **F281** — escalated. `getAgencyAggregate` must return the identical agency-wide figure for every role (this feature's own spec, corrected in its Security Considerations section below), but the only existing cohort mechanism, `listEmployees`, runs under the caller's own role-scoped principal — a Team Member's own cohort cannot produce the agency-wide number their own dashboard is specified to show, and no precedent existed for a role-independent read. Resolved by founder-delegated decision: `utilization-snapshot-compute` gains a second operation, `utilization-snapshot.read-cohort`, run uniformly for every caller regardless of role; `perEmployee` stays a fully separate, ordinarily-scoped, role-gated read. Both new operations are declared in one new closed table, `SYSTEM_OPERATION_TARGETS`, rather than a third and fourth hardcoded interceptor branch (`VPS-A004`'s new A004-T25). See F279–F281's own detail sections in `Foundations_Findings.md` for the full reasoning.

1. **`UtilizationSnapshot` policy-table row (F278).** `packages/schema/src/policy/policy-table.ts`'s `MATRIX_OVERRIDES`: add `UtilizationSnapshot: { owner: FULL_ANY(), "hr-admin": FULL_ANY(), "finance-admin": READ_ANY(), manager: cell("full", "direct-reports", "Full (direct reports)"), "team-member": cell("read", "own", "Read (own only)") }` — `Employee:operational`'s own Owner/HR-Admin/Finance-Admin/Manager cells, `Employee:compensation`'s own Team Member cell. `UtilizationSnapshot` is already registered in `packages/schema/src/registry/nodes.ts` (`owner: "VRS-F011"`, `fixedProtection("Standard", 0)`) — no registry change needed, only this policy row. Extend `policy-table.test.ts` with `UtilizationSnapshot`'s own assertions, the same way `Pitch:identifying` and `Employee:operational`/`:compensation` already have theirs split out.
2. **`packages/schema/src/utilization-snapshot.ts`.** New file, modeled on `timesheet.ts`'s field-schema pattern. `utilizationSnapshotFieldsSchema`: the full field set from the spec's Technical Architecture section (`snapshot_id`, `workspace_id`, `employee_id`, `week_start_date`, `expected_hours`, `billable_hours`, `non_billable_hours`, `pitch_hours`, `logged_hours`, `utilization_rate`, `billable_share_of_logged`, `logging_completeness`, `non_billable_breakdown` — a JSON map keyed by `VRS-F009`'s internal categories, `target_utilization` nullable decimal, `computed_at`), plus the standard node envelope. A pure `calculateUtilization({ expectedHours, billableHours, nonBillableHours, pitchHours })` function alongside it: `utilization_rate = expectedHours === 0 ? 0 : (billableHours / expectedHours) * 100`; `logged_hours = billableHours + nonBillableHours` (pitch excluded, per G02); `billable_share_of_logged = loggedHours === 0 ? 0 : (billableHours / loggedHours) * 100`; `logging_completeness = expectedHours === 0 ? 0 : (loggedHours / expectedHours) * 100` — every division guarded at zero per G08, matching `detectHoursExceedExpected`'s own zero-guard style.
3. **`resolveStoredSubjectEmployeeId` gains a `UtilizationSnapshot` branch (F279).** `services/api/src/permission/interceptor.ts`: add `"UtilizationSnapshot"` alongside `"TimesheetEntry" || "TimesheetWeekSubmission"` in the stored-`employee_id` branch — identical shape, same field, F268's own precedent recurring a fourth time. Without this, item 1's own Team Member "own only" grant has no subject to resolve against and every such read is refused.
4. **New system principal, `utilization-snapshot-compute`, and the `SYSTEM_OPERATION_TARGETS` registry (F280, F281).** `packages/schema/src/policy/principal-policy.ts`: add `"utilization-snapshot-compute"` to `SYSTEM_PRINCIPAL_NAMES` with a display name ("Utilization Snapshot"); add `"utilization-snapshot.compute"` and `"utilization-snapshot.read-cohort"` to `SYSTEM_OPERATIONS`, both granted to this principal in `SYSTEM_PRINCIPAL_OPERATIONS`. Add the new closed table:
   ```ts
   export const SYSTEM_OPERATION_TARGETS: Readonly<
     Partial<Record<SystemOperation, readonly { nodeType: NodeType; partitionKey: string }[]>>
   > = {
     "timesheet-anomaly.create-flag": [{ nodeType: "TimesheetAnomalyFlag", partitionKey: "record" }],
     "timesheet-anomaly.read-flags": [{ nodeType: "TimesheetAnomalyFlag", partitionKey: "record" }],
     "utilization-snapshot.compute": [{ nodeType: "UtilizationSnapshot", partitionKey: "record" }],
     "utilization-snapshot.read-cohort": [
       { nodeType: "Employee", partitionKey: "operational" },
       { nodeType: "UtilizationSnapshot", partitionKey: "record" },
     ],
   };
   ```
   `services/api/src/permission/interceptor.ts`'s `decideRead` and `authorizeWrite` Gate 1 system-principal branches — both currently hardcoded to the single `timesheet-anomaly.*` operation and target — are rewritten to look up the caller's `target` against this table for the requested operation, rather than growing a third and fourth hardcoded clause. `authorizeWrite`'s separate `triggered_by` edge-shape check for `timesheet-anomaly.create-flag` is untouched — leave it as its own explicit clause, not folded into this table (A004-T25 states this explicitly). Extend the interceptor's own test suite with `UtilizationSnapshot`'s two new operations the same way `TimesheetAnomalyFlag`'s are already covered.
5. **`utilizationSnapshot.compute` mutation (`packages/schema/src/mutations/utilization-snapshot.ts` / `services/api/src/mutations/utilization-snapshot.ts`).** `defineMutation({ name: "utilizationSnapshot.compute", input: z.object({ employee_id: uuidV4Schema, week_start_date: isoDate }).strict(), tier: 0, onlineOnly: false, stateTransition: false })`, executed under the `utilization-snapshot-compute` system principal (F280) — never the triggering caller's own, which item 1's Team Member row cannot write under. `apply()`: read the employee's `TimesheetEntry` rows **directly by `employee_id` and `week_start_date`** (G02) — the identical query shape `weekRows` already uses in `timesheet-queries.ts`, never a traversal through `assignment_of`/`assigned_to`, since that path cannot reach a Pitch-categorized entry at all. Route `Billable`/`NonBillable` rows into the standard calculation, `Pitch` rows into `pitch_hours` exclusively (never either side of `utilization_rate`, per G02/G04 and the spec's own repeated warning). Resolve `expected_hours` via `getWeek`'s own mechanism (F277) — call `hoursOn` for each date in the week via `isoDatesInclusive(weekStart, addIsoDays(weekStart, 6))`, or extract `getWeek`'s per-day loop into a shared helper both call, whichever avoids the duplication more directly; either way, the two figures must be structurally unable to diverge. `target_utilization`: the employee's own override field if `VRS-F002`'s Employee record carries one (grep for the actual field name), else `0.75` — a hardcoded constant, not read from `Workspace.record`, matching `VRS-F010`'s own `overtime_flag_threshold` precedent (`billability_target` is a registered `VPS-A002` key with no console yet built to edit it; see the corrected spec's Decisions Recorded section). **G01**: query for an existing `UtilizationSnapshot` with this exact `(employee_id, week_start_date)` pair first — update it if found, create only if not; never a second row for the same pair.
6. **Reactive trigger — `saveCell` gains its own `afterCommit` hook.** `services/api/src/mutations/timesheet.ts`'s `saveCell` currently has no `afterCommit` at all (only `submitWeek`/`unlockWeek`'s shared `action === "submit"` branch does, calling `timesheetAnomalyEvaluate`). Add one, following the identical G10 shape already proven there: non-gating, wrapped so a thrown error inside it cannot roll back or fail the already-committed write, calling `utilizationSnapshot.compute(ctx.principal.workspaceId, ctx.args.employee_id, weekStart)` under the system principal (item 4/5) — reuse the same `weekStart` the mutation already resolved for `week_start_date`, never a second resolution. Extend the existing `submitWeek`/`unlockWeek` hook to call it too, since transitioning entries from Draft to Submitted does not itself change any hour figure but a snapshot computed mid-week (before submission) should still reflect the final state once submitted — call order within the hook doesn't matter, both are idempotent recomputations of the same pair. A week with zero entries ever written gets no snapshot at all — this is intentional, not a gap: the spec's own Empty state ("*No time logged this week*, not a zero percentage presented as a result") is the read endpoint's responsibility, not a reason to synthesize a zero-valued snapshot nobody wrote to.
7. **`utilizationSnapshot.getIndividual` and `utilizationDashboard.getAgencyAggregate` — two separately-authorized reads, not one (F281) (`services/api/src/permission/utilization-queries.ts`).** `getIndividual(tx, principal, employeeId, weekStartDate?)`: confirm the caller may read `employeeId`'s `UtilizationSnapshot` row via the ordinary interceptor read-decision under the caller's own principal (the row's own `employee_id` field, F279's subject-resolution fix), return the stored snapshot for the resolved week (default: `weekStartForEmployee` for today, per F276's own mechanism — never a hardcoded Monday) or `null` if none exists. `getAgencyAggregate(tx, principal, workspaceId, weekStartDate?, filters?)` builds two things under two different principals, never one: the aggregate figure (`aggregateUtilization`, `aggregateLoggingCompleteness`, `weekOverWeekDelta`, `cohortSize`) is built under the `utilization-snapshot-compute` system principal's `utilization-snapshot.read-cohort` operation — read `Employee:operational` for cohort membership (`bench-forecast-queries.ts`'s own precedent for excluding Ghost Resources, G05, never a new filtering mechanism, plus a new `contracted_hours === 0` exclusion — no existing precedent, since no prior feature needed it) and `UtilizationSnapshot:record` to sum each cohort member's already-stored snapshot — run identically for every caller regardless of role, since this figure must be the same number for everyone. `perEmployee` is a fully separate read, executed under the caller's own ordinary principal via `listEmployees` plus row-scoped `UtilizationSnapshot` reads, gated by role (item 8). Pass the aggregate figure through `applyDisclosureControl` (`packages/schema/src/policy/disclosure-control.ts`) exactly as `bench-forecast-queries.ts` already does for its own filtered/unfiltered pair (G07) — do not write a second suppression mechanism. Name the aggregating function distinctly from `bench-forecast-queries.ts`'s own module-private `utilizationTotals` — that function computes a different, calendar/assignment-based capacity figure for bench cost, not this feature's `TimesheetEntry`-based one, and the identical name risks a future reader assuming reuse where none exists.
8. **The per-employee comparison is gated by role, separately from disclosure control and from the system-principal aggregate read.** Per the spec's own System States table, a Team Member's response carries their own bar and the agency aggregate but never `perEmployee` — this is a role check (`principal.role === "team-member"` omits the field, or the endpoint refuses a dedicated ranked-list request from one), not something `applyDisclosureControl`'s cohort-size suppression handles on its own, and it must never share a code path with item 7's system-principal aggregate step; the checks compose, they don't substitute for each other.
9. **Router wiring.** `utilizationSnapshot.compute` is never called directly by a client — it only ever runs from `saveCell`/`submitWeek`'s own `afterCommit` hook, the identical shape `timesheetAnomaly.evaluate` already has; do not add a router procedure for it. Add `utilizationSnapshot.getIndividual` and `utilizationDashboard.getAgencyAggregate` as read procedures in `services/api/src/router.ts`.
10. **No UI, no `WorkspaceConfigurationConsole`/`VPS-F005` work.** Nothing under `apps/` changes; `billability_target` stays the hardcoded `0.75` from item 5, not written into `services/api/src/graph/founding.ts`'s `Workspace` defaults — matching `overtime_flag_threshold`'s own already-settled precedent, for the identical reason (no console exists yet to read or edit either).

**Done criteria:**
- Recomputing the same `(employee_id, week_start_date)` pair twice updates the existing `UtilizationSnapshot` row rather than creating a second one — proven by a test (G01).
- An entry logged against a Pitch is correctly counted into `pitch_hours` and excluded from both `utilization_rate`'s numerator and its denominator — proven by a test reproducing the spec's own acceptance criterion (20 billable + 15 pitch over 40 expected yields 50.0, not diluted) (G02, G04).
- `expected_hours` for a given employee and week is proven, by a test, to equal `getWeek`'s own `expectedWeeklyHours` for the identical employee and week — not merely both individually correct, but structurally the same number (F277).
- `utilization_rate` divides by `expected_hours`, never `logged_hours` — proven by the spec's own acceptance criterion (4 billable of 40 expected yields 10.0, not 100.0), with `logging_completeness` separately correct at 10.0 for the same case (G04).
- Every ratio resolves to `0`, never throws, when its denominator is `0` — proven by a test with `expected_hours: 0` (G08).
- The agency aggregate excludes Ghost Resources and employees with `contracted_hours` of `0` — proven by a test with both present in the cohort (G05).
- A cohort filtered below `k_anonymity_minimum` returns suppressed, reusing `applyDisclosureControl` — proven by a test exercising the same mechanism `bench-forecast-queries.ts` already has coverage for, not a newly-written suppression check (G07).
- A Team Member's `getAgencyAggregate` response carries the aggregate and their own figure but no `perEmployee` field; a Manager's carries `perEmployee` scoped to their own direct reports only — proven by a test against the real interceptor and the new `UtilizationSnapshot` policy row (F278).
- A Team Member can read their own `UtilizationSnapshot` row — proven by a test against the real interceptor exercising F279's fixed `resolveStoredSubjectEmployeeId` branch, not merely a unit test of the function in isolation.
- `utilizationSnapshot.compute` genuinely runs under the `utilization-snapshot-compute` system principal, not the triggering caller's own — proven by a test that a Team Member's own `saveCell` edit still produces a written snapshot despite their row's own policy grant being Read only (F280).
- The agency aggregate figure (`aggregateUtilization`, `aggregateLoggingCompleteness`, `weekOverWeekDelta`, `cohortSize`) is proven, by a test, to be byte-identical for an Owner, a Manager and a Team Member calling `getAgencyAggregate` in the same state — never a smaller or team-scoped number for any role — while each caller's own `perEmployee` field differs correctly by role (F281).
- `saveCell` genuinely gained a non-gating `afterCommit` hook — a deliberately-thrown error inside `utilizationSnapshot.compute` must not fail or roll back the already-committed `TimesheetEntry` write, proven by a test mirroring Stage 18's own G10 proof exactly.
- No `Workspace` record anywhere carries a `billability_target` key — grep the diff to confirm `founding.ts` is untouched by this stage.
- No file under `apps/` appears in the diff.
- `pnpm verify` passes typecheck with real handlers for every new mutation and query — no stub, no-op, or placeholder.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 19 report:** that `utilization_rate`'s formula divides by `expected_hours`, read directly off the calculation function's own code, not inferred from a passing test name (G04); that `pitch_hours` is genuinely routed outside both `utilization_rate`'s numerator and denominator — grep the formula, not just the acceptance-criterion test; that `expected_hours` is computed via `hoursOn`/`isoDatesInclusive` (or a helper shared with `getWeek`), never a second, independently-written traversal that could silently diverge from the Speed-Run grid's own numbers (F277); that `UtilizationSnapshot`'s policy-table row matches F278's composed shape exactly — Team Member `Read (own only)`, not `Employee:operational`'s own-plus-team cell copied wholesale — read the policy-table diff directly; that `resolveStoredSubjectEmployeeId`'s new `UtilizationSnapshot` branch reads the same stored `employee_id` field the other three branches do, not a new resolution path (F279); that `utilizationSnapshot.compute` is registered under, and actually executes as, the `utilization-snapshot-compute` system principal — read the mutation's own principal-substitution code directly, don't infer it from a passing test (F280); that `SYSTEM_OPERATION_TARGETS` is the sole authority both `decideRead` and `authorizeWrite` consult for every system-principal node-targeting operation, with no operation still reachable through a leftover hardcoded clause (A004-T25); that `getAgencyAggregate`'s aggregate figure and its `perEmployee` field come from two genuinely separate code paths under two different principals — read the function's own code to confirm the aggregate step never varies by caller role, rather than trusting a same-output test alone (F281); that the agency aggregate's Ghost/`contracted_hours`-zero exclusion and its `applyDisclosureControl` call are both present and are the only two filters applied, in that order, before any figure is computed; that the per-employee comparison's role gate is a separate check from disclosure control and from the system-principal aggregate step, not folded into the same function such that a large-enough cohort accidentally exposes it to Team Member; that `saveCell`'s new `afterCommit` hook is proven non-gating by an actual injected-failure test, the same proof shape G10 already required in Stage 18, not merely present in the code; that `utilizationSnapshot.compute` has no router entry of its own — grep `router.ts` to confirm it is reachable only through the two mutations' own `afterCommit` hooks; that `billability_target` is a hardcoded constant in the compute mutation's own code, never a `founding.ts` `Workspace` field; and that no file under `apps/` appears in the diff.

### Stage 20 — Revenue Gap Alert (`VRS-F012`)

Server-side only, following every prior stage's own precedent — no file under `apps/` changes; the Interface Specification is reference material for whichever later stage wires the real UI. `VPS-F003` (Notification and Alert Center) is itself unbuilt, an already-documented forward dependency (`VRS-F012`'s own Depends-On section) — this stage builds the alert's own schema, engine and API only, not an Inbox surface to place a card into.

**Findings F282, F283 and F284, all closed before this brief was written, no founder consultation needed for any of them:**

- **F282** — `VRS-F012`'s own spec still described the engine running inside a Web Worker "consumer of the materialized index," computing and syncing "entirely from the local graph," and alerts "written offline sync within 1 second of reconnection" — the local-first architecture the project dropped project-wide before Stage 7, never corrected for this spec until now. Corrected throughout to the server-authoritative shape every built reactive engine already uses.
- **F283** — G02 requires reusing `VRS-F005`'s bench computation "including... the Pitch exclusion," and names a `benchForecast.computeBench(employeeId)` function. Neither claim held: no such function exists (`bench-forecast-queries.ts` exports only a cohort-wide aggregate and a compensation-based cost query), and the private `cohortEmployee` helper that actually computes bench days has never excluded Pitch time — built at Stage 13, before Pitch time tracking existed, and never revisited. Ruled: `VRS-F005`'s own module gains the Pitch exclusion and a new exported single-employee `computeBenchStatus`, which this feature calls.
- **F284** — the spec's Security Considerations and System States table both claimed cost visibility "follows `VRS-F005`'s rule" (compensation-gated), directly contradicting the same document's own decision, two headings above, that cost derives from billing rate and "never reads a salary," and the already-registered flat-Tier-0 `RevenueGapAlert` node. Ruled: the stale rule removed; cost fields are genuinely Tier 0, visible to anyone who can read the alert at all.

`docs/Vulto_Specs/VRS-F012_Revenue_Gap_Alert.md` is corrected to match all three. See F282–F284's own detail sections in `docs/Foundations_Findings.md` for the full reasoning.

1. **`VRS-F005`'s own bench computation gains the Pitch exclusion, and a single-employee export (F283).** `services/api/src/permission/bench-forecast-queries.ts`: extend `cohortEmployee`'s `benchDays` loop with a third exclusion alongside the calendar/`resolvedDayOn` and `assignmentCovers` checks already there — skip a date if the employee has a `TimesheetEntry` with `time_category: "Pitch"` on it. Read `TimesheetEntry` the same trusted, internal way `utilizationSnapshotCompute` already reads it for its own derived computation (a plain `getNodes` filter, no per-row `authorizeRead` — this feeds a derived Tier 0 figure, it never exposes a raw entry to any caller). Export a new function, `computeBenchStatus(tx, workspaceId, employeeId, asOfDate, guard)`, built on the identical exclusion logic `cohortEmployee` uses (never a second, parallel implementation) — it should either call a shared extraction of that logic or wrap a single-employee cohort call and read back that one employee's `benchDays`. Return `{ benchDays: readonly string[]; benchStartDate: string | null }` — `benchStartDate` is `null` when `benchDays` is empty, otherwise the earliest date in a contiguous run ending at `asOfDate` (per G03/G04, "the `end_date` of the most recently ended Assignment" — derive this from the Assignment record directly per item 3 below, not from `benchDays`' own first element, since a gap year ago followed by a fresh Assignment and a new bench period must not report the old gap's start date).
2. **`RevenueGapAlert` policy-table row and node registration.** `RevenueGapAlert` is already registered in `packages/schema/src/registry/nodes.ts` (`owner: "VRS-F012"`, `fixedProtection("Standard", 0)`) — no registry change. `packages/schema/src/policy/policy-table.ts`'s `MATRIX_OVERRIDES`: add `RevenueGapAlert: { owner: FULL_ANY(), "hr-admin": FULL_ANY(), "finance-admin": READ_ANY(), manager: FULL_ANY(), "team-member": READ_ANY() }` — Tier 0, "visible workspace-wide" per the spec's own Security Considerations, with write access (dismiss) extended to Manager alongside Owner/HR-Admin, matching `GhostResource`'s own precedent (F251) for a workspace-wide-visible node a Manager must also be able to act on. Extend `policy-table.test.ts` with `RevenueGapAlert`'s own assertions.
3. **`packages/schema/src/revenue-gap-alert.ts`.** New file, modeled on `utilization-snapshot.ts`'s pattern. Field schema per the spec's Technical Architecture section (`alert_id`, `workspace_id`, `employee_id`, `bench_start_date`, `bench_days`, `daily_cost`, `accumulated_cost`, `severity` enum `Low`/`Medium`/`High`, `lifecycle_status` enum `Active`/`Resolved`, `escalated_at`/`dismissed_at`/`resolved_at` nullable timestamps, `resolved_by` nullable, `resolved_by_assignment_id` nullable UUID, plus the universal node envelope). A pure `resolveDailyCost(mostRecentEndedAssignment, employee)` function per G04: the assignment's own `effective_billing_rate` (already stored on the Assignment record, per `VRS-F006`) × 8, else `employee.billing_rate_default`, else `0` — never an error. A pure `severityFor(benchDays, threshold)` function per the spec's escalation rule: `Low` at the configured threshold (5 working days, hardcoded per `overtime_flag_threshold`'s own already-settled precedent — no `Workspace` console exists to configure it), `Medium` at 2×, `High` at 3×.
4. **New system principal, `revenue-gap-alert-evaluate`.** `packages/schema/src/policy/principal-policy.ts`: add `"revenue-gap-alert-evaluate"` to `SYSTEM_PRINCIPAL_NAMES` with a display name ("Revenue Gap Alert"); add `"revenue-gap-alert.write"` to `SYSTEM_OPERATIONS`, granted to this principal; add `"revenue-gap-alert.write": [{ nodeType: "RevenueGapAlert", partitionKey: "record" }]` to the existing `SYSTEM_OPERATION_TARGETS` table (A004-T25 — this is the table's job precisely so a fourth operation doesn't need a fourth hardcoded interceptor clause). `revenueGapAlert.evaluate`'s own write runs under this principal, on `VRS-F010`/`F011`'s own established reasoning: the ordinary caller whose Assignment change triggered the evaluation (often a Manager creating an Assignment for someone else, or the affected employee themselves in no case here) has no reliable write grant shape for a node the spec requires visible-but-Manager-writable workspace-wide, and a system-authored derived write should never ride on the triggering caller's own incidental grant regardless.
5. **`revenueGapAlert.evaluate` mutation (`packages/schema/src/mutations/revenue-gap-alert.ts` / `services/api/src/mutations/revenue-gap-alert.ts`).** `defineMutation({ name: "revenueGapAlert.evaluate", input: z.object({ employee_id: uuidV4Schema }).strict(), tier: 0, onlineOnly: false, stateTransition: false })`, internal only, executed under `revenue-gap-alert-evaluate` (item 4) — never a router entry (item 9). `apply()`: confirm `employee_type === "Employee"` (G09 — Ghost Resources are out of scope, `listEmployees`'s own `employee_type` filter precedent); call `computeBenchStatus` (item 1) for today; find the existing Active `RevenueGapAlert` for this employee, if any (G05 — query by `employee_id` and `lifecycle_status: "Active"`, never more than one). If `benchDays.length === 0`: if an Active alert exists, resolve it — `lifecycle_status: "Resolved"`, `resolved_at: now`, `resolved_by: "system"`, `resolved_by_assignment_id` set to the Assignment whose `start_date <= today` that ended the bench period (the employee's current covering Assignment, read via the same `assignmentsFor`-style query `bench-forecast-queries.ts` already has) — else do nothing (no alert to write for a never-benched employee, per G01's own "0 elsewhere" precedent). If `benchDays.length` is below the 5-working-day threshold: do nothing (no alert yet). Otherwise: resolve `daily_cost` (item 3) from the most recently ended Assignment (`end_date` immediately before `bench_start_date`, ordered descending — a plain `getNodes` filter/sort, no new traversal), compute `accumulated_cost = daily_cost * benchDays.length`, resolve `severity` (item 3); if an Active alert already exists, update it in place (G05 — same node ID, set `escalated_at: now` only if severity increased from its stored value, never on every recompute); if none exists, create one and write the `triggered_by` edge from the new `RevenueGapAlert` to the Employee (G06, `TimesheetAnomalyFlag`'s own precedent for this exact edge shape — created once, never re-created on later updates to the same alert).
6. **Reactive triggers — `afterCommit` hooks on the Assignment mutations.** `services/api/src/mutations/assignment.ts` currently has no `afterCommit` hook on any of its six mutations. Add one, following the identical non-gating G10 shape `saveCell`/`submitWeek` already prove (Stage 18/19), to `assignmentCreate`, `assignmentUpdate` (only when `start_date`/`end_date`/`employee_id` actually changed) and `assignmentCancel` — each calls `revenueGapAlert.evaluate(ctx.principal.workspaceId, employeeId)` for the affected employee under the system principal (item 4/5). This is the "any Assignment change... immediate re-evaluation" trigger the spec requires (NFR: within 30 seconds — an async, non-blocking `afterCommit` call comfortably meets this).
7. **Manually-callable sweep, `revenueGapAlert.sweep` (idempotency, G08).** `services/api/src/permission/revenue-gap-alert-queries.ts` (or folded into the mutation file): a sweep function that lists every Active, non-Ghost employee (`listEmployees`'s own `employee_type` filter, item 5's own reuse) and calls `revenueGapAlert.evaluate` for each. `VPS-A006`'s job scheduler does not exist yet — this stage exposes the sweep as a manually-callable, authenticated procedure exactly the way `conflictResolution.sweepOvercommitted` already is (Stage 16's own precedent), not an actual four-hourly cron; real scheduling is deferred to whichever stage builds `VPS-A006`'s scheduler, and this dependency is already correctly named in the spec's own Depends-On section. Idempotency (G08) follows directly from item 5's own "update in place, never a second node" logic — a repeated sweep against unchanged state re-evaluates each employee, finds the same Active alert (or none), and writes nothing new.
8. **`revenueGapAlert.dismiss` mutation.** `defineMutation({ name: "revenueGapAlert.dismiss", input: z.object({ alert_id: uuidV4Schema }).strict(), tier: 0, onlineOnly: false, stateTransition: false })`, an ordinary client mutation (not system-principal — the caller's own Owner/HR-Admin/Manager write grant from item 2 authorizes it directly). `apply()`: `dismissed_at: now` only — `lifecycle_status` is explicitly left unchanged per G07 ("Only a new Assignment resolves an alert"). Refuse (not silently no-op) a dismiss on an already-Resolved alert.
9. **`revenueGapAlert.listActive` query and router wiring.** `services/api/src/permission/revenue-gap-alert-queries.ts`: `listActive(tx, principal, workspaceId)` — an ordinary caller-scoped read via the item 2 policy row (no system principal involved; ordinary Tier 0 workspace-wide visibility, ranked as the caller is authorized to see, which for every role in item 2's row means every Active alert), sorted by `accumulated_cost` descending per the spec's own explicit ordering decision. `services/api/src/router.ts`: add `revenueGapAlert.dismiss` (mutation) and `revenueGapAlert.listActive` (query) as router procedures. `revenueGapAlert.evaluate` and `revenueGapAlert.sweep` are internal-only — `evaluate` reachable solely through the three `afterCommit` hooks (item 6), `sweep` exposed as a callable procedure per item 7's own "testability" requirement, the identical shape `conflictResolution.sweepOvercommitted` already has in the router.
10. **No UI, no `VPS-F003` Inbox wiring, no `VPS-A006` scheduler.** Nothing under `apps/` changes. The Bench Forecast row badge and the Inbox card are both out of scope — `revenueGapAlert.listActive` is the query surface whichever later stage builds them against.

**Done criteria:**
- An employee whose last Assignment ended five working days ago, with no new Assignment and no logged Pitch time, gets an Active alert at Low severity with `bench_days: 5` — proven by a test reproducing the spec's own acceptance criterion exactly (G01, G03).
- The same employee's most recent Assignment carrying an `effective_billing_rate` of 15/hour yields `daily_cost: 120`, not `billing_rate_default`, even where one is set on the Employee record — proven by a test (G04).
- An employee who has logged Pitch time every working day this week and holds no Assignment gets zero `benchDays` from `computeBenchStatus` and no alert is written — proven by a real test against `VRS-F005`'s corrected `cohortEmployee`/`computeBenchStatus`, not a mock (F283, the spec's own acceptance criterion).
- A UAE entity's Friday/Saturday weekend is correctly excluded from a fortnight's `bench_days` count — proven by a test reusing `VRS-F004`'s own working-calendar fixtures, the same way `bench-forecast.integration.test.ts` already does (G03).
- An Active alert at Low severity with 5 bench days, re-evaluated once bench days reach 10, updates in place to Medium with `escalated_at` set — no second `RevenueGapAlert` node exists afterward, proven by a test asserting node-ID identity across both evaluations (G05).
- Creating an Assignment starting at or before today for an employee with an Active alert resolves it — `lifecycle_status: "Resolved"`, `resolved_at` and `resolved_by_assignment_id` both set to the real new Assignment's own ID — proven by a real reactive test exercising `assignmentCreate`'s new `afterCommit` hook end-to-end, not a direct call to `evaluate` (item 6, the spec's own acceptance criterion).
- Dismissing an Active alert sets `dismissed_at` and leaves `lifecycle_status` as `Active` — proven by a test that a subsequent re-evaluation at the next threshold still fires (G07, the spec's own acceptance criterion).
- Running the sweep twice against unchanged state produces no second alert and no changed `updated_at` on the existing one — proven by a test (G08).
- An employee with no Assignment history and no `billing_rate_default` gets `daily_cost: 0` and `accumulated_cost: 0`, written and surfaced normally, no error — proven by a test (the spec's own acceptance criterion).
- `revenueGapAlert.evaluate` genuinely runs under the `revenue-gap-alert-evaluate` system principal, not the triggering caller's own — proven by a test that a Manager with no direct write grant on `RevenueGapAlert` still triggers a written alert via `assignmentCancel`'s `afterCommit` hook (item 4/5/6).
- A caller with only Team Member's `READ_ANY()` grant (item 2) can list every Active alert workspace-wide via `listActive`, but a `dismiss` call from that same caller is refused — proven by a test against the real interceptor and the new policy row.
- Ghost Resources are never evaluated and never produce an alert, regardless of bench duration — proven by a test (G09, the spec's own explicit scope boundary).
- No `apps/` file appears in the diff; `pnpm verify` passes typecheck with real handlers for every new mutation and query, no stub or placeholder.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 20 report:** that `cohortEmployee`'s Pitch exclusion and `computeBenchStatus` share one implementation, not two independently-written ones that could silently disagree (F283) — read the diff directly, don't infer from a passing test name; that `daily_cost` resolves from the most recently ended Assignment's own stored `effective_billing_rate`, never a fresh rate-card lookup or `Employee.billing_rate_default` used ahead of it (G04); that only one Active `RevenueGapAlert` node ever exists per employee across repeated evaluations — read the create/update branch directly to confirm it queries for an existing Active row before ever constructing a new node ID (G05); that `revenueGapAlert.evaluate` is registered under, and actually executes as, the `revenue-gap-alert-evaluate` system principal — read the mutation's own principal-substitution code, don't infer it from a passing test (item 4/5); that `SYSTEM_OPERATION_TARGETS` gained exactly one new entry for `revenue-gap-alert.write` and no interceptor branch was hardcoded a fifth time (A004-T25); that resolution genuinely records the real triggering Assignment's own ID in `resolved_by_assignment_id`, not a placeholder or the alert's own `employee_id`; that dismissal leaves `lifecycle_status` as `Active`, confirmed by reading the mutation's field list directly, not merely the acceptance-criterion test's assertions; that the sweep is a callable procedure, not an actual cron — grep for any scheduling library or cron syntax and confirm none was added, since `VPS-A006`'s scheduler is still unbuilt; that Ghost Resources are excluded at the very first step of every evaluation path (reactive, sweep, and any read), not merely filtered out of the sweep's own list; and that no file under `apps/` appears in the diff.

---

### Stage 21 — Skill-to-Project Matcher (`VRS-F013`)

Server-side only, following every prior stage's own precedent — no file under `apps/` changes; the Interface Specification is reference material for whichever later stage wires the real UI. `VPS-F002` (Local-First Search) is itself unbuilt, an already-documented forward dependency (`VRS-F013`'s own Blocks section) — this stage builds the ad-hoc engine itself, not the `Cmd+K` trigger that will eventually call it, matching `VRS-F012`'s own Inbox deferral at Stage 20.

**Findings F285, F286 and F287, all closed before this brief was written, no founder consultation needed for any of them:**

- **F285** — `requires_skill` (Project/OpenRole/Pitch/CareerMilestone/Requisition to Skill) declared no `governingPartitions` at all, though Pitch and Requisition are both tier-split — the sixth recurrence of `staffed_on`/`logged_against`'s own class of gap (F265, F267). Fixed directly in the registry: `governingPartitions: { Pitch: "identifying", Requisition: "identifying" }`, already committed ahead of this brief.
- **F286** — `VRS-F013`'s own spec still described persistent match results as "Entirely local," the ad-hoc mode budgeted at "10ms of the third character typed," and an explicit offline acceptance criterion and NFR — the local-first architecture the project dropped project-wide before Stage 7, never corrected for this foundational spec until now. Corrected throughout to the server-authoritative shape every built reactive engine already uses.
- **F287** — `VRS-F002`'s own API contract has specified `employee.attachSkill(employeeId, skillId, proficiencyLevel)` since that document was written, with G03 fully specifying its replace-on-duplicate semantics, but no such mutation exists anywhere in the codebase — `has_skill` can currently only be written through Ghost Resource creation. Ruled: built in `VRS-F002`'s own module this stage, matching its already-specified contract exactly.

`docs/Vulto_Specs/VRS-F013_Skill-to-Project_Matcher.md` is corrected to match F286 and F287 (F285 touches only the executable registry, no spec text to correct). See F285–F287's own detail sections in `docs/Foundations_Findings.md` for the full reasoning.

1. **`employee.attachSkill` (F287), in `VRS-F002`'s own module.** `packages/schema/src/mutations/employee.ts`: new `defineMutation({ name: "employee.attachSkill", input: z.object({ employee_id: uuidV4Schema, skill_id: uuidV4Schema, proficiency_level: <the ordering enum from item 2> }).strict(), tier: 0, onlineOnly: false, stateTransition: false })`, added to `EMPLOYEE_MUTATIONS`. `services/api/src/mutations/employee.ts`: `apply()` finds an existing active `has_skill` edge for this `(employee_id, skill_id)` pair; if one exists, update its `proficiency_level` field in place per G03 ("replaces... rather than creating a second edge"); if none exists, create it.

   **F289's store-level prerequisite, ruled the same day, no founder consultation needed.** `has_skill` already declares `governingPartitions: { Employee: "operational" }` (Tier 0, F136/FDN-92), but `services/api/src/graph/store.ts::insertEdge`'s `protectedEndpoint` check (`hasProtectedPartition`) tests only whether Employee has *any* non-Tier-0 partition anywhere — it does, `compensation` — never which partition the edge itself governs, so `metadata` is unconditionally stripped to `{}` on every Employee-touching edge today, `has_skill` included. Fix `insertEdge` itself: it already calls `assertRegisteredRelationship` and receives the edge's own registration back (currently discarded); use it. For each endpoint, call `getProtectionPartitions(nodeType)`; if more than one partition exists, look up `registration.governingPartitions[nodeType]` and fail closed — treat as protected — when it is undeclared, mirroring `edgeRoleDecision`'s own `declared === undefined` branch in `services/api/src/permission/interceptor.ts` exactly; otherwise resolve that partition's tier via `resolveRegisteredProtectionTier` (`packages/schema/src/registry/protection.ts`) and strip metadata only when the tier is non-zero. Add a parallel generic Tier-0 mutation, `graph.updateEdgeMetadata`, in `packages/schema/src/mutations/foundation.ts` alongside `graph.createEdge`/`graph.closeEdge` (`{ edge_id: uuidV4Schema, metadata: jsonObject }`, tier 0), with a `services/api/src/graph/store.ts` primitive applying the identical corrected check, for `has_skill`'s in-place replace-on-duplicate write — not bespoke per-mutation SQL. Update `store.integration.test.ts`'s `"stores no edge metadata when an endpoint has a protected partition"` test: it currently asserts on `managed_by` (Employee ↔ Employee, `governingPartitions: { Employee: "operational" }`), the exact case this fix corrects — metadata must now be *preserved* there. Replace it with two tests: metadata preserved when the governing partition resolves Tier 0 (`has_skill` or `managed_by`), and metadata genuinely stripped when a tier-split endpoint's governing partition is undeclared or resolves non-zero — proving the protection itself still holds, not merely relaxed. `verified`/`verified_by`/`verified_at` are never caller-supplied: when the caller's own `employee_id` (via `resolveStoredSubjectEmployeeId`'s existing self-resolution) equals the target, the edge is self-reported (`verified: false`); when the caller holds independent write authority on the target Employee's `operational` partition (i.e., is not merely attaching to their own record), it is `verified: true` with `verified_by`/`verified_at` stamped to the caller and now. `employee.attachCertification`/`holds_certification` is explicitly out of scope (F287) — do not build it. `has_skill` is added to item 13's `FEATURE_OWNED_EDGE_TYPES` set (F291): generic `graph.createEdge`/`graph.updateEdgeMetadata` must refuse it, so this named mutation is the only write path.
2. **Skill's schema and the proficiency ordering.** New file `packages/schema/src/skill-matcher.ts`: the proficiency ordering as a closed enum, `Beginner`, `Intermediate`, `Senior`, `Expert`, in that order, exported once for every consumer (this feature, and eventually `VRS-F014`) to import — G02, no second ordering defined anywhere. A pure `proficiencyMeets(held, required)` comparator per G04 ("meets or exceeds"). Skill itself needs no dedicated create/deprecate mutation: it is already registered Standard/Tier 0, non-split, and not in `FEATURE_LIFECYCLE_NODE_TYPES`, so it rides the existing generic `graph.createNode` (create) and `graph.transitionLifecycle` (Active → Deprecated, G10) mutations directly, exactly as every other non-feature-owned Standard node type already does.
3. **Policy rows.** `packages/schema/src/policy/policy-table.ts`'s `MATRIX_OVERRIDES`: both `Skill` and `SkillGap` need explicit rows, because the `Standard` class default's qualifiers ("direct reports", "own + team") have no employee subject to scope against on either node type — the same reason `RevenueGapAlert`, `GhostResource` and `OpenRole` already needed overrides rather than inheriting the class default. `Skill: { owner: FULL_ANY(), "hr-admin": FULL_ANY(), "finance-admin": READ_ANY(), manager: FULL_ANY(), "team-member": READ_ANY() }` — Manager included in `FULL_ANY()` since a project manager attaching a requirement, or an HR Admin attaching a profile skill, may need to create a not-yet-registered Skill inline from the picker, matching `GhostResource`'s own F251 precedent for extending Manager write access to a workspace-visible node their own flow requires. `SkillGap: { owner: FULL_ANY(), "hr-admin": FULL_ANY(), "finance-admin": READ_ANY(), manager: READ_ANY(), "team-member": READ_ANY() }` — "visible to every member" per the spec's own Security Considerations, all writes routed through the system principal (item 9), no ordinary role needs `Full`. Extend `policy-table.test.ts` with both nodes' own assertions.
4. **`requires_skill` on Project — `project.attachSkillRequirement`.** New file `services/api/src/mutations/skill-matcher.ts`. `defineMutation({ name: "project.attachSkillRequirement", input: z.object({ project_id: uuidV4Schema, skill_id: uuidV4Schema, proficiency_level_required: <the ordering enum> }).strict(), tier: 0, onlineOnly: false, stateTransition: false })`. `apply()`: replace-on-duplicate per G03, the identical shape item 1 uses for `has_skill` (at most one active `requires_skill` edge per project/skill pair). This pairing is Project ↔ Skill only — neither node is tier-split, so no `governingPartitions` entry is needed for this write path (F285 only concerned the Pitch/Requisition pairings of the same edge type, which this stage does not exercise). Manager is this mutation's own intended caller and holds only `READ_ANY()` on Project, not Full — `requires_skill`'s registration now declares `readSufficientEndpoints: { Project: true }` (F288, closed by `assigned_to`'s own F262 precedent), so a Manager who can read the Project can attach the requirement without a broader Project write grant. After commit, evaluate this one requirement for a match (item 8) and write or resolve its `SkillGap` immediately — never leave a newly-attached or newly-changed requirement showing a stale gap state until the next unrelated trigger. `requires_skill` is added to item 13's `FEATURE_OWNED_EDGE_TYPES` set (F291) — generic `graph.createEdge`/`graph.updateEdgeMetadata` now refuse the edge type entirely, every pairing, not only Project/Skill; this mutation is the only write path for the Project/Skill pair, and the still-unowned Pitch/OpenRole/CareerMilestone/Requisition pairings currently have no write path at all until the feature that owns each one builds its own attach mutation — an accepted, explicitly recorded forward dependency, not a regression, since nothing in the product writes those pairings today.
5. **Project gains `start_date`.** No feature has ever defined Project's full schema — it exists today only as the minimal bootstrap `VRS-F005` created at Stage 13 (`lifecycle_status`, `name`). `VRS-F013`'s own G07 needs "working days to project start" for `SkillGap.severity`, and no field carries that yet. Add `start_date` (date, nullable — a Project in early Pitch stage may have none) via the existing generic `graph.updateNodeFields`/node shape, no new named mutation. When `start_date` is null, severity resolves to `Low` — the most lenient reading, never an error, matching this project's own consistent "missing input degrades gracefully" convention (`VRS-F012`'s own zero-cost fallback).
6. **`SkillGap` schema and severity.** In `packages/schema/src/skill-matcher.ts` alongside item 2: the full `SkillGap` field schema per the spec's Technical Architecture (`skill_gap_id`, `workspace_id`, `project_id`, `skill_id`, `proficiency_level_required`, `identified_at`, `severity` enum `Low`/`Medium`/`High`/`Critical`, `lifecycle_status` enum `Active`/`Resolved`, `resolved_at`/`resolved_by` nullable, plus the universal envelope). A pure `severityFor(workingDaysToStart)` function per G07's own thresholds: `>60` Low, `30–60` Medium, `14–30` High, `<14` Critical (and `null` start date → Low, item 5).

   **F290's calendar-resolution ruling, put to the founder and decided: resolve from Assignments, else the workspace's sole Entity, else Low.** `resolvedDayOn`/`countWorkingDays` (`services/api/src/graph/working-days.ts`) are hard-anchored to a specific Employee — personal `WorkingPattern`, `location`, all of it — and a Project has no structural link to any calendar; picking an arbitrary Employee to borrow one from would apply a stranger's personal overrides to the Project's severity and yield nondeterministic results across otherwise-identical gaps, exactly what the founder ruled against. New `resolveProjectCalendarEntity(tx, workspaceId, projectId, asOf)` in `services/api/src/graph/entity-resolution.ts`: read the Project's incoming `assigned_to` edges (Assignment → Project), take the not-soft-deleted one with the earliest edge `created_at` (first person staffed, deterministic), resolve its Employee via `assignment_of` and then `resolveEntityAssignment(tx, workspaceId, employeeId, asOf)`; if that yields an Entity, return it. If the Project has no Assignment, or resolution fails, query `getNodes(tx, workspaceId, { nodeType: "Entity", lifecycleStatus: "Active" })`; every workspace today has at most one real Entity (multi-entity is `VRS-F064`, unbuilt) — if exactly one exists, return it. Otherwise return `null`. New `countWorkingDaysForEntity(tx, workspaceId, entityId, from, to, mayRead)` alongside `countWorkingDays` in `working-days.ts`: the identical loop and the same shared `resolveWorkingDay` pure function from `@vulto/schema`, reusing `resolveCalendarForEntity`/the file's own `holidaysFor` helper, but with `pattern` always `null` and `location` always `null` — no Employee to have either. G08's "one implementation" rule stays intact: only the input-gathering wrapper gains an Employee-less variant, `resolveWorkingDay` itself is not touched. `skillGap.evaluate` (item 8) calls `resolveProjectCalendarEntity`; `null` → severity `Low`, exactly like the null-`start_date` case (item 5) — now confined to the genuinely ambiguous residual case (no Assignment and zero or multiple Entities), not the common path. Record this resolution order as an explicit forward dependency on `VRS-F064`: once multi-entity workspaces are real, the "exactly one Active Entity" fallback will need a real Project→Entity link, not solved here.
7. **The match query engine, shared by both modes.** New file `services/api/src/permission/skill-matcher-queries.ts`. A single internal function, `matchCandidates(tx, workspaceId, skillId, proficiencyRequired, availabilityWindowDays, guard)`, used by both `getMatchResults` and `adHocSearch` (G05's "never a second implementation" applies to the matching logic itself, not only the bench call): reads every Employee and Ghost with a qualifying `has_skill` edge (item 2's `proficiencyMeets`) via `filterReadable` (workspace-readable operational data per the spec's own Decisions Recorded), then resolves availability per employee using the same Assignment-coverage primitives `cohortEmployee`/`computeBenchStatus` already use (`assignmentCovers`, the Assignment query helper in `bench-forecast-queries.ts`) — not `computeBenchStatus` itself, which answers only "is this person on bench today," not "when do they become free." A candidate with no covering Assignment today is `availabilityDate: null` ("Available now"); a candidate covered by an Assignment whose `end_date` falls within `availabilityWindowDays` (default 30) is `availabilityDate: end_date + 1`; a candidate covered by an Assignment extending beyond the window is excluded entirely — G05's "filtered... within a configurable window" is a filter, not merely a display value. A Ghost's `availabilityDate` is its own projected start per `VRS-F007`'s own Ghost shape (G09). Results rank by availability date (nulls first), then proficiency descending, real matches before Ghost matches at equal availability per the spec's own Interface Specification. This workspace-wide candidate read depends on item 14's `Employee:operational` read/write split (F292) — without it, `filterReadable` silently drops every candidate outside the caller's own direct-reports/team row scope.
8. **`project.getMatchResults` and `skillGap` reactive evaluation.** `project.getMatchResults(projectId, availabilityWindowDays?)`: an ordinary caller-scoped read via item 7, iterating every `requires_skill` edge on the project. `skillGap.evaluate(projectId, skillId)` (internal, mirroring `revenueGapAlert.evaluate`'s own shape): calls item 7's matcher; zero matches with an existing Active `SkillGap` leaves it Active (G06, no re-creation); zero matches with none existing creates one via the `gap_for` edge (already registered, `SkillGap → Skill`) plus the `requires_skill` edge's own project/skill pair, severity from item 6; one or more matches with an existing Active `SkillGap` resolves it (`lifecycle_status: "Resolved"`, `resolved_at: now`, `resolved_by: "system"`); one or more matches with none existing does nothing. Runs under a new system principal (item 9), called from item 4's `afterCommit` (requirement side) and item 10's `afterCommit` (candidate side, G08).
9. **New system principal, `skill-gap-evaluate`, and its two operations (F293).** `packages/schema/src/policy/principal-policy.ts`: add `"skill-gap-evaluate"` to `SYSTEM_PRINCIPAL_NAMES`; add `"skill-gap.write"` and `"skill-gap.read-cohort"` to `SYSTEM_OPERATIONS`, both granted to this principal. `SYSTEM_OPERATION_TARGETS` (A004-T25's own table) gains two entries: `"skill-gap.write": [{ nodeType: "SkillGap", partitionKey: "record" }]` — `F280`/`F283`'s own precedent: the ordinary caller triggering evaluation (a project manager attaching a requirement, or anyone attaching a skill to someone else's profile) has no reliable write grant shape for a node visible workspace-wide but not caller-owned, and a system-authored derived write should never ride the triggering caller's own incidental grant — and `"skill-gap.read-cohort": [{ nodeType: "Employee", partitionKey: "operational" }]` (F293, `utilization-snapshot.read-cohort`'s own precedent, F280/F281): a system principal holds no `PolicyRole` at all, so `decideRead`'s system branch checks only the operation's own closed target list, never a policy cell — without this, `skillGap.evaluate` calling item 7's `matchCandidates` under this principal could not read a single candidate Employee, and every reactive evaluation would silently produce zero matches, indistinguishable in its output from a genuine capability gap. No target entry is needed for Skill or Assignment: item 7's Assignment-coverage reads reuse `bench-forecast-queries.ts`'s own trusted, unauthenticated derived-day pattern (`assignmentsFor`'s direct `getNodes` call, never `filterReadable`), and `Skill` carries no row-scoped cell for any role (item 3's `MATRIX_OVERRIDES` row grants every role at least `READ_ANY()`) — and in any case matching compares required proficiency against the `has_skill` edge's own metadata (F289), never Skill node content.
10. **Reactive trigger on `has_skill` (G08).** `employee.attachSkill`'s own `afterCommit` hook (item 1): find every Active `SkillGap` for this `skill_id` where `proficiency_level` now qualifies (item 2's `proficiencyMeets`), and re-run item 8's evaluation for each affected project/skill pair — evaluated per write, never on a schedule, per G08's own explicit language.
11. **`skillMatcher.adHocSearch` and `skillGap.listActive`.** `skillMatcher.adHocSearch(query)`: item 7's matcher run without a `skill_id`/proficiency filter, instead resolving `query` against `Skill.name` first (a simple case-insensitive match, no fuzzy search — that is `VPS-F002`'s own eventual job, not this feature's), then item 7's candidate resolution. No persistence, no `SkillGap`, per the spec's own explicit line. `skillGap.listActive(workspaceId)`: an ordinary caller-scoped read via item 3's `SkillGap` policy row, sorted by severity then project start date per the spec's own Skill gaps table ordering.
12. **Router wiring, no UI.** `services/api/src/router.ts`: add `project.attachSkillRequirement`, `project.getMatchResults`, `skillMatcher.adHocSearch`, `skillGap.listActive` as router procedures. `skillGap.evaluate` (item 8) is internal-only, reachable solely through the two `afterCommit` hooks (items 4, 10), the identical shape `revenueGapAlert.evaluate` already has. Nothing under `apps/` changes; the `Cmd+K` trigger and the Project view's Skill Requirements section are both out of scope, per `VPS-F002` being unbuilt (this stage's own opening note).

13. **Generic edge-write guard for feature-owned edge types (F291), `services/api/src/mutations/foundation.ts`.** `createEdgeMutation` and `updateEdgeMetadataMutation` currently have `validate: nothing` — the exact node-level bypass `FEATURE_LIFECYCLE_NODE_TYPES` was built to close (`graph.createNode`/`updateNodeFields`/`softDeleteNode`/`transitionLifecycle` all already reject a listed node type with `MutationRejection("requires-feature-mutation")`) has no edge-side counterpart, so an authorized caller could reach `has_skill`/`requires_skill` through `graph.applyMutations([{ name: "graph.createEdge" | "graph.updateEdgeMetadata", ... }])` directly — bypassing `employee.attachSkill`'s server-derived `verified`/`verified_by`/`verified_at` (self-attestation, the exact thing that field exists to prevent) and both mutations' G03 at-most-one-active-edge replace-on-duplicate rule entirely. New `FEATURE_OWNED_EDGE_TYPES: ReadonlySet<string>` in `packages/schema/src/mutations/employee.ts` alongside `FEATURE_LIFECYCLE_NODE_TYPES` (same file, same pattern, same export surface via `packages/schema/src/mutations/index.ts`): `new Set(["has_skill", "requires_skill"])`. `createEdgeMutation.validate()` and `updateEdgeMetadataMutation.validate()` (both currently `nothing`) gain the identical check the node mutations already use — `if (FEATURE_OWNED_EDGE_TYPES.has(edgeType)) throw new MutationRejection("requires-feature-mutation")` — reusing the same rejection reason, not inventing a new one. Add the same check to `closeEdgeMutation` for symmetry, even though this stage never closes a `has_skill`/`requires_skill` edge. The block is edge-type-wide, not pair-scoped — `requires_skill`'s four still-unowned pairings (Pitch, OpenRole, CareerMilestone, Requisition) are blocked from generic writes too, matching `FEATURE_LIFECYCLE_NODE_TYPES`'s own type-wide granularity rather than inventing per-pair tracking. Extend `mutations.test.ts` with `FEATURE_OWNED_EDGE_TYPES.has("has_skill")` and `.has("requires_skill")` assertions, mirroring the existing `FEATURE_LIFECYCLE_NODE_TYPES` ones exactly.

14. **Read/write split for policy cells, and `Employee:operational`'s workspace-wide read (F292).** `VRS-F013`'s Decisions Recorded already settles the product question — employee operational data is readable by every workspace member — but the executable `Employee:operational` row (`packages/schema/src/policy/policy-table.ts`) still carries `manager: cell("full", "direct-reports", ...)` and `"team-member": cell("read", "own-plus-team", ...)`, and `PolicyCell` holds exactly one `(outcome, scope)` pair per role/partition: `bestCell`'s row-scope check resolves an out-of-scope row straight to `none`, and `filterReadable` drops a `none` row entirely — so item 7's `matchCandidates` would silently return incomplete results for any Manager or Team Member. Not new to the matcher: `listEmployees` (`services/api/src/permission/employee-queries.ts`), which the plain employee directory and Bench Forecast's own cohort (`bench-forecast-queries.ts::authorizedCohort`) both already call, hits the identical drop today — the spec's own cited justification ("`VRS-F005` already renders every active employee as a row to every viewer") does not actually hold under the current row; this is a second, previously undiscovered instance of the same gap, not a working precedent for it. Ruled by the founder (genuine fork — no existing mechanism combines a wider read ceiling with a narrower write ceiling in one cell; decided in favor of the general fix over a matcher-only carve-out, since it also correctly closes the identical gap in Bench Forecast and the plain directory): `PolicyCell` gains an optional `readScope?: PolicyScope`, defaulting to the cell's own `scope`, consulted only when the caller's narrow `scope` check has already failed and even then ceilinged at outcome `"read"` — never promoted to `"full"`, and never bypassing `Restricted`/subject-exclusion or any other existing check. `bestCell` (`services/api/src/permission/interceptor.ts`) gains an `operation: "read" | "write"` parameter defaulting to `"write"`, so its two write-adjacent call sites — `edgeRoleDecision` and the node-write gate — are unchanged, byte for byte; only `decideRead`'s own call site passes `"read"`, so `filterReadable` and every one of its callers (the directory, Bench Forecast, the matcher) benefit uniformly, with zero effect on write authorization. `Employee:operational`'s `manager` and `team-member` cells both gain `readScope: "any"` — manager's literal `Full (direct reports)` write ceiling is untouched; team-member's outcome was already `read`, so the split costs it nothing beyond the wider scope. Extend `policy-table.test.ts` and the matrix-coverage fidelity checks to cover the new field, and add a dedicated `bestCell`/`decideRead` test proving a Manager now reads (not writes) an Employee outside their direct reports, while `authorizeWrite`/`edgeRoleDecision` against that same non-report Employee is unaffected — proving the split, not just a widening.

15. **Narrow-scope admission for three older derived queries (F294, F295).** F292's `readScope` correctly widens `Employee:operational`'s own directory-purpose read, but three queries built for earlier, unrelated features used that same generic Employee decision as their sole admission check for a *different* disclosure, and each has its own governing spec with its own scope, untouched by F292: `services/api/src/permission/pitch-queries.ts::listStaffedFor` (`VRS-F009`/F264: Team Member `None` at the generic Pitch level, sanctioned only via this proxy on `Employee:operational`'s row-scope), `services/api/src/permission/conflict-resolution-queries.ts::sweepOvercommitted` (`VRS-F008`/F263: explicitly direct-reports/own-plus-team), and `services/api/src/permission/timesheet-queries.ts::listSubmissionStatus` (`VRS-F010`/F273: `TimesheetEntry`/`TimesheetWeekSubmission`'s own row, Manager direct-reports, Team Member **own only** — narrower than `Employee:operational` has ever been, a pre-existing gap independent of F292 that F295 caught). Add a new exported primitive to `services/api/src/permission/interceptor.ts` — resolving whether a candidate Employee falls within the caller's role-scoped reach for a *named* node type/partition's own policy cell, using `bestCell`'s existing narrow-tier resolution (the `operation` value that ignores `readScope`, already what `edgeRoleDecision`/the node-write gate use), without requiring an actual stored row of that other node type to exist. `listStaffedFor` and `sweepOvercommitted` call it against `Employee:operational` specifically (their own specs define exactly that scope); `listSubmissionStatus` calls it against `TimesheetEntry:record` (or `TimesheetWeekSubmission:record` — F273 registered them identically) instead of `Employee:operational`'s. This restores all three queries' own governing boundaries with zero effect on F292's own directory-read fix for the matcher, the plain directory, or Bench Forecast. Separately, update the stale pre-F292 audience and matrix-fidelity test expectations `pnpm verify:full` surfaced — confirmed as F292's own already-decided widening propagating correctly through `audience-recompute`'s design (which derives a device's own replicated cache from `decideRead`'s outcome directly), not a new question.

**Done criteria:**
- A project with two requirements and three/one matching people respectively returns the correct ranked cards for each, real people before Ghost matches at equal availability — proven by a test against `VRS-F007`'s own Ghost fixtures (item 7, the spec's own acceptance criterion).
- Attaching a requirement nobody qualifies for writes a `SkillGap` with the correct skill, proficiency and severity computed from working days to `Project.start_date` — proven by a test reusing `VRS-F004`'s own working-calendar fixtures (item 4, 6, 8, the spec's own acceptance criterion).
- Attaching a `has_skill` edge that newly qualifies an Employee for an Active `SkillGap`'s requirement resolves that gap within the same transaction chain, `resolved_at`/`resolved_by: "system"` set — proven by a real reactive test exercising `employee.attachSkill`'s new `afterCommit` hook end-to-end, not a direct call to `skillGap.evaluate` (item 10, G08, the spec's own acceptance criterion).
- A duplicate `employee.attachSkill` call on the same employee/skill pair updates the existing edge's `proficiency_level` in place — no second edge exists afterward, proven by a test asserting edge-ID identity across both calls (item 1, G03).
- Self-attaching a skill produces `verified: false`; an HR Admin attaching a skill to a different Employee produces `verified: true` with `verified_by`/`verified_at` set — proven by a test against the real interceptor (item 1).
- A candidate covered by an Assignment ending within the requested `availabilityWindowDays` appears with the correct `availabilityDate`; one covered beyond the window is excluded entirely — proven by a test against real Assignment fixtures (item 7, G05).
- The ad-hoc search returns matching people with no `SkillGap` written and no edge written, whether or not a match is found — proven by a test (item 11, the spec's own acceptance criterion).
- A Manager with no direct write grant on `SkillGap` still triggers a written gap via `project.attachSkillRequirement`'s `afterCommit` hook — proven by a test that the write genuinely runs under `skill-gap-evaluate`, not the Manager's own principal (item 8/9).
- `requires_skill`'s `governingPartitions` fix (F285) is exercised by at least one write touching Pitch or Requisition, proving the interceptor's own partition resolution no longer refuses it. F291's guard (item 13) correctly closes the *generic* `graph.createEdge` path for `requires_skill` entirely, so this proof is no longer a real write through that path — an `edgeRoleDecision`/`insertEdge` level test, in the same style `store.integration.test.ts` already uses to test `insertEdge` directly, exercising a synthetic Pitch-paired `requires_skill` write and asserting it is authorized, is sufficient and correct.
- The `FEATURE_OWNED_EDGE_TYPES` guard (item 13, F291) is proven both ways: a direct `graph.applyMutations` call attempting `graph.createEdge`/`graph.updateEdgeMetadata` on `has_skill` or `requires_skill` is rejected with `requires-feature-mutation`, and `employee.attachSkill`/`project.attachSkillRequirement` themselves still succeed — proving the guard blocks the bypass without blocking the legitimate named path.
- A Manager reads (does not merely avoid erroring on) a qualifying candidate Employee outside their own direct reports through item 7's matcher; the same non-report Employee stays unwritable by that Manager through the ordinary node-write gate — proven by a test exercising both directions against the same row (item 14, F292).
- `skillGap.evaluate`, run under the `skill-gap-evaluate` system principal, genuinely reads a real qualifying candidate and resolves an Active `SkillGap` — proven by a test, since a silently-empty cohort read and a genuine capability gap produce the same visible output (item 9, F293).
- An unrelated Team Member calling `listStaffedFor` for another Employee learns nothing — no Pitch name, no client name, indistinguishable from a nonexistent Employee — while `sweepOvercommitted` still returns only a Manager's direct reports or a Team Member's own team, both proven by the pre-existing tests passing again, not merely not erroring (item 15, F294).
- `listSubmissionStatus` returns a Team Member only their own compliance status, never a teammate's — proven by a new test, since none previously existed for a non-Owner principal (item 15, F295).
- No `apps/` file appears in the diff; `pnpm verify` passes typecheck with real handlers for every new mutation and query, no stub or placeholder.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI.

**What to check in the Stage 21 report:** that `employee.attachSkill`'s replace-on-duplicate genuinely updates the existing edge rather than closing and recreating one, and that `verified` is derived from the caller/target relationship, never accepted as a caller-supplied boolean (item 1, F287); that the match engine (item 7) is a single shared implementation called by both `getMatchResults` and `adHocSearch`, not two independently-written traversals that could silently disagree — read the diff directly; that availability resolution reuses `bench-forecast-queries.ts`'s own Assignment-coverage primitives rather than a parallel query, and that the window genuinely excludes candidates beyond it rather than merely labeling them (G05); that `SkillGap` evaluation runs under the `skill-gap-evaluate` system principal on both trigger paths (item 4 and item 10), not the triggering caller's own — read the mutation's own principal-substitution code, don't infer it from a passing test; that `SYSTEM_OPERATION_TARGETS` gained exactly two new entries — `skill-gap.write` and `skill-gap.read-cohort` (F293) — and no interceptor branch was hardcoded a sixth time (A004-T25); that a Manager or Team Member genuinely reads a qualifying candidate Employee outside their own direct-reports/team scope through the matcher, proven by a real test under both roles, while that same Employee stays unwritable by the same caller through the ordinary write gate and `edgeRoleDecision` (F292, item 14's `readScope` split); that `skillGap.evaluate` under the `skill-gap-evaluate` system principal genuinely reads real candidates rather than silently seeing an empty, authorization-emptied cohort (F293); that `requires_skill`'s `governingPartitions` fix is real (F285) — confirm a Pitch- or Requisition-attached requirement write actually succeeds under real roles, not only the Project-Skill pairing this stage's own flow exercises; that `employee.attachCertification`/`holds_certification` was not built (F287's explicit scope boundary); that `listStaffedFor`, `sweepOvercommitted` and `listSubmissionStatus` each authorize against their own named node type's policy cell (`Employee:operational`, `Employee:operational`, `TimesheetEntry:record` respectively), not a single shared check, and that `listSubmissionStatus`'s new test genuinely distinguishes a Team Member's own status from a teammate's, not merely a stranger's (item 15, F294/F295); and that no file under `apps/` appears in the diff.

---

### Stage 22 — Skill Matrix (`VRS-F014`)

Entirely local, following the spec's own API contract and G03 — no file under `apps/` or `services/api/` changes; the Interface Specification is reference material for whichever later stage wires the real UI, the same deferral `VRS-F013`'s own Interface Specification already received at Stage 21.

**Finding F296, closed before this brief was written, no founder consultation needed:**

- **F296** — `VRS-F013`'s own `certified` field has been hardcoded `false` since Stage 21 — no metadata slot exists on `has_skill` for it, `holds_certification` is registered but unwritable, and `VRS-F041` (the only feature that will ever derive it) is unbuilt and far down the MVP order. `VRS-F014`'s own spec repeated the identical unqualified claim ("a small filled dot in the corner marks certified") with no caveat. Ruled: both specs corrected with a one-line forward-dependency note; this stage carries `certified: false` forward unchanged, matching Stage 21's own precedent exactly.

`docs/Vulto_Specs/VRS-F013_Skill-to-Project_Matcher.md` and `docs/Vulto_Specs/VRS-F014_Skill_Matrix.md` are both corrected to match. See F296's own detail section in `docs/Foundations_Findings.md` for the full reasoning.

1. **`getSkillMatrix`, the grid query.** New file `packages/graph/src/queries/skill-matrix.ts`, following `bench-forecast.ts`'s own established shape exactly — a plain async function taking the device's `SyncDatabase` and reading `localNodes`/`localEdges` directly, no interceptor call, no server round trip (G03: the device's own already-synced cache is the sole source of truth for what this caller may see; F292's Stage 21 `readScope` widening already reaches the client through the existing sync/audience mechanism unchanged by this stage). Fetch `["Employee", "Skill"]` nodes and `["has_skill"]` edges (add `["holds_certification"]` too, for forward-compatibility with F296's eventual resolution, even though no such edge exists in practice yet). For every Active Employee (real or Ghost, `employee_type` read off the record exactly as `VRS-F013`'s own `matchCandidates` already does — G06, no new Ghost-detection logic), and every Active-or-Deprecated Skill (a Deprecated Skill's column still renders, G04), build one cell per `has_skill` edge: `proficiencyLevel` and `verified` read off the edge's own metadata, `certified: false` unconditionally (F296). Apply `filters?: { category?, minProficiency?, verifiedOnly? }` (the spec's own contract) to the cell set *before* computing coverage and depth, never after (G05 — a figure must never describe a population other than the one on screen); reuse `proficiencyMeets`/`PROFICIENCY_LEVELS` from `packages/schema/src/skill-matcher.ts` unchanged for the `minProficiency` comparison (G02, no second ordering). `coverage` is the filtered cell count per skill; `depth` restricts that count to `Senior`/`Expert` (the spec's own two-figure distinction — compute both from the same filtered set, not depth from an unfiltered superset). Return shape matches the spec's own contract exactly: `{ employees: [{ employeeId, isGhost, name }], skills: [{ skillId, name, category, coverage, depth, isDeprecated }], cells: [{ employeeId, skillId, proficiencyLevel, verified, certified }] }`.
2. **`getSkillHolders`, the single-skill view.** Same file. `skillMatrix.forSkill(skillId)`'s own contract: `{ holders: [{ employeeId, proficiencyLevel, verified, certified, availabilityStatus }] }`, ranked by proficiency descending (the spec's own "fastest answer to who could cover this" framing). `availabilityStatus` reuses `getBenchForecast`'s own employee-assignment-coverage logic in the same file (or a small shared helper factored out of it, if that is cleaner than duplicating the coverage loop — either is acceptable, but there must be exactly one implementation of "is this employee covered by an Assignment today," not a second one written for this function) — never a parallel bench computation, matching G03 of `VRS-F005` and this stage's own "read-only context, never a query or forecast" framing (Out of Scope).
3. **Category grouping, computed from the returned `skills` array.** No separate query — the spec's own "a category collapses to its aggregate coverage across every skill within it" is a client-side reduction over item 1's own `skills[].category`/`coverage`/`depth` fields, not a third exported function. State this explicitly in the module's own comments so a later UI stage does not invent a duplicate server-side aggregation.
4. **Tests.** New file `packages/graph/src/queries/skill-matrix.test.ts`, following `bench-forecast.test.ts`'s own precedent exactly: `SqliteCache`/`openTestDatabase`, no Postgres, no `services/api` involvement. Cover: the spec's own coverage/depth acceptance criterion (twelve holders, three Senior-or-above → header reads `12`/`3`); a verified-only + minimum-proficiency filter narrowing both the cell set and the recomputed coverage/depth together, proving G05 against the *filtered* population, not the full one; a Deprecated Skill's existing cells remaining visible with `isDeprecated: true`, its coverage/depth still computed from its (now frozen) holder set; a Ghost Resource appearing as a row via the identical `employee_type` check, with its own `has_skill` edges contributing to coverage exactly like a real Employee's; `certified: false` on every cell, matching F296; and `forSkill`'s ranking and `availabilityStatus`, reusing the same Assignment fixtures `bench-forecast.test.ts` already establishes.

**Done criteria:**
- Twelve holders of a skill, three at Senior or above, produce `coverage: 12, depth: 3` on that skill's entry (item 1, the spec's own Feature Acceptance Criterion).
- A verified-only, Senior-or-above filter changes both the returned `cells` and the `coverage`/`depth` figures together — proven by a test where the filtered and unfiltered figures genuinely differ, not merely a smaller `cells` array with stale header numbers (item 1, G05).
- A Deprecated Skill's column still appears with `isDeprecated: true` and its existing cells intact, never dropped (item 1, G04).
- A Ghost Resource appears as a row through the identical `employee_type: "Ghost"` check `VRS-F013`'s matcher already uses — no new or duplicated Ghost-detection logic anywhere in the diff (item 1, G06).
- `certified` is `false` on every cell returned by both `getSkillMatrix` and `getSkillHolders`, matching F296 — no interim certification mechanism invented.
- `forSkill`'s `availabilityStatus` reuses `getBenchForecast`'s own coverage logic (or a helper factored out of it) rather than a second, independently-written Assignment-coverage traversal (item 2).
- The proficiency ordering used for `minProficiency` filtering and `forSkill`'s ranking is imported from `packages/schema/src/skill-matcher.ts` unchanged (item 1, 2, G02).
- No file under `apps/` or `services/api/` appears in the diff; no new policy row, no new mutation, no new router procedure — this stage's only new files are under `packages/graph/src/queries/`.

**Gates:** the standard four (`pnpm install --frozen-lockfile`, `pnpm stack:up`, `pnpm verify`, `pnpm verify:full`). No browser suite is required — this stage has no UI. `pnpm stack:up` and the Postgres-backed half of `verify:full` will pass trivially untouched, since nothing in this stage's diff reaches `services/api/` — do not treat an empty diff there as a gap to fill.

**What to check in the Stage 22 report:** that `getSkillMatrix`/`getSkillHolders` read only from the local `SyncDatabase` — grep the diff for any `fetch`/tRPC client call or new `services/api` file, and confirm there is none; that coverage and depth are recomputed from the *filtered* cell set on every call, not cached or computed once from the unfiltered population (G01, G05) — read the filter test directly, don't infer it from the function signature; that the Ghost-row check is byte-identical in spirit to `VRS-F013`'s own `matchCandidates` (`employee_type === "Ghost"` on an `Employee`-typed node), not a new check against the separately-registered `GhostResource` node type, which carries no `has_skill` edges of its own; that `availabilityStatus` in `forSkill` is proven, by a real test, to reuse `getBenchForecast`'s own coverage determination rather than silently duplicating it with different edge cases; that `certified` reads `false` everywhere per F296, with no metadata field added to `has_skill` that F296's own ruling explicitly deferred to `VRS-F041`; and that no file under `apps/` or `services/api/` appears in the diff.

---

