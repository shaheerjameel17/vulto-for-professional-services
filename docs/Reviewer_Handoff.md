# Reviewer Handoff — state for the founder's review chat

**Purpose.** This file lets a fresh Claude chat (the *reviewer*, not Claude Code) resume reviewing the server-authoritative rebuild with no re-explanation. Keep it current: the reviewer updates it at each stage boundary.
**Last updated:** 25 September 2026 (Stage 23 merged). Stage 23 (`VPS-F002`, Local-First Search) reported complete by Codex (`95115d5`) after five stops (F301–F305, each ruled the same day) and independently verified directly against the actual diff before merge, not the completion report alone — no findings; merged `--no-ff` as `b50ab85`, `RST-51` Done. The review is in section 24. F305 (the sync browser suite has been red on `main` since Stage 11) stays OPEN, its repair tracked as `FDN-126`; Stage 23's browser gate was a recorded-baseline comparison and the report shows the identical nine failures on clean `main` and on the branch. Before that (fifth stop): Codex finished the Stage 23 implementation on the F304 correction (`pnpm verify` and `pnpm verify:full` green, 319 API tests passed, search p95 0.84 ms, the new `cache_search` assertions passing in the real browser) and stopped on the last gate: the full `pnpm test:sync-browser` ended 18 passed, 9 failed. The failures are not Stage 23's: the suite's `seedEntities` and `newEntity` create an `Entity` through `graph.createNode`, which has returned `requires-feature-mutation` since Stage 11 (`82c0b31`, 22 September), so the suite has been red on `main` for eleven stages and the A003-T56 no-protected-data-on-device test in particular has not run green in that time. The reviewer's own F301 gate ("the full suite passes") was unsatisfiable and had been written without running the suite on `main`. Ruled the same day as an OPEN finding (F305): repair deferred to its own Linear issue; Stage 23's gate becomes a baseline comparison against `main` plus a real-browser sentinel check for `cache_search` in the passing role-removal test. The founder should check the slow-lane `sync-browser` job's history on `main`; this shell cannot. `codex/stage-23-local-first-search` (`9f50931`) resumes without a restart. Before that (fourth stop): Codex built the partial Stage 23 implementation (preserved at `90ba633`, focused tests 24/24), ran `pnpm verify`, and it failed at `arch:check`: the test re-export F302 authorized (`nodeRecord`/`insertNode` from `graph/store` in `services/api/src/test/sync-browser-support.ts`) violates A003-T52's `graph/store` import boundary, which permits only the graph, permission, mutations, protected, audience and jobs folders. The brief was wrong a fourth time — the reviewer ruled that exception in without running the repository's own gate against the file it named. Ruled the same day (F304): `addNode` in `services/api/src/permission/test-support.ts` (an allowed folder; the file the gate itself treats as test support) gains an optional `fields` parameter, `sync-browser-support.ts` is restored to `main`'s version, `seedProtected` calls `addNode(..., { ... })`, and no gate changes; this reverses F302's "do not change `addNode`" clause. `codex/stage-23-local-first-search` resumes on its preserved partial work, no restart. Before that (third stop): Codex resumed Stage 23 on F302, traced the new label guard against the real Ghost writer before writing product code, and stopped a third time, on F303: the guard (label from `full_name` only) excluded every real Ghost Employee, whose `full_name` is deliberately null with the role title in `job_title` (`ghostEmployeeOperationalRecord`, `VRS-F007`), while the brief requires finding Ghosts by role title. The brief was wrong a third time. Ruled the same day: the registry's `labelField` becomes an ordered `labelFields` list (`Employee`: `full_name`, then `job_title`), the label is the first non-empty text, Ghost tests use `ghostEmployeeOperationalRecord`, and the module-private `operationalShape` is exported (or a key list derived from it) for the registry's Tier 0 validation. This time the reviewer also audited the brief's remaining field assumptions against the code in the same pass. `codex/stage-23-local-first-search` (`11684a6`) resumes without a restart. Before that: Codex resumed Stage 23 on F301 and stopped a second time before product code, on F302: Do item 8 (added for F301) required a seeded Employee's `full_name` in the existing browser suite, but `seedProtected`'s Employee is created by `addNode` with universal fields only — the fixture does not exist, and the reviewer wrote item 8 without reading `helpers.ts`. Tracing it also exposed a real design hazard: nameless Employee rows already reach devices in that suite, and a trigger inserting `label` (`NOT NULL`) from an absent `full_name` would abort `putNode` and break replication of that row. Ruled the same day: triggers index a node only when its registered label field is a non-empty string; `seedProtected` gets a named Employee via one re-export line in `services/api/src/test/sync-browser-support.ts` (the single, test-only exception to Stage 23's `services/api/` boundary); the assertions move to the existing role-removal test. Brief, ledger and section 24 corrected; `codex/stage-23-local-first-search` (`7f87ae6`) resumes without a restart. Before that: Codex traced the Stage 23 brief against `package.json` and the slow-lane workflow before writing product code and stopped on F301: the brief's claim that `verify:full` already runs a device-store browser suite was false (`verify:full` is `pnpm verify && pnpm verify:preflight && pnpm --filter @vulto/api test`; the browser suite is the separate `pnpm test:sync-browser`, run by the slow lane's own `sync-browser` job). The brief, not the code, was wrong — the reviewer had written that sentence without reading `package.json`. Ruled the same day: `pnpm test:sync-browser` is an additional required gate for Stage 23, with one added assertion in the existing `sync.spec.ts`; brief and section 24 corrected. `codex/stage-23-local-first-search` (`fa51a3c`, no product code) resumes without a restart. Linear connected later on 25 September and the outstanding housekeeping was executed (see `Reviewer_Role.md`). Before that, Stage 22 (`VRS-F014`, Skill Matrix) is merged as `cd17db0` with no findings, and `VPS-F002` (Local-First Search) is briefed as Stage 23 — `VRS-001`'s Feature Register confirms it as the correct next MVP-order feature after `VRS-F014` (`VRS-F015` is permanently vacant, promoted to the suite). Checking it directly against the code before writing the brief surfaced and closed F297 through F300 the same day: F297 (the spec's SQLite FTS5 index cannot be built — the pinned `wa-sqlite@1.0.0` has no FTS3/4/5 module in either shipped build, verified by running `CREATE VIRTUAL TABLE`; a genuine architectural fork, decided by the founder in favor of a trigger-maintained derived `cache_search` table, no dependency change), F298 (`search.query` cannot return the server-side `adHocSearch`'s result locally — closed on F286's/F199's precedent), F299 (only `Employee`, `Skill`, `Project` and `Client` can be registered now; `Document`, `Policy`, `Deliverable` and `search.queryProtected` are forward dependencies — closed on F296's/F248's precedent) and F300 (the interceptor-filtering language and command availability have no local mechanism — closed on Stage 22's own G03 precedent) — see section 24 and `docs/Foundations_Findings.md`. `docs/Vulto_Specs/VPS-F002_Local-First_Search.md` corrected to match. Branch `codex/stage-23-local-first-search` about to start; `RST-51` to be filed. The findings ledger's running recount sentence, stale since F281, was reconciled the same day (242 numbered rows through F296, now 246 through F300). Linear was checked again for tools with two `ToolSearch` queries (`"Linear"`, `"linear issue"`) and is still not connected. Before that, Stage 18 (`VRS-F010`, Timesheet Speed-Run) reported complete by Codex (`e0bb426`, all four gates passed, `RST-46` moved to In Review) and independently verified directly against the finished code before merge, not the completion report alone: every one of F274's (post-write re-check, adversarial rollback test), F275's (`PARTICIPANT_GRANTS`/`participantGrantSatisfied` wiring), F270–F272's (system-principal write/read authority and provenance) and F276's (`week_start_day`/`resolveWeekStartDate`, multi-jurisdiction stability, the holiday-on-anchor case) rulings read directly against the code and proven by genuine real-PostgreSQL tests — all 11 tests in `timesheet.integration.test.ts` read line by line, no `.only`/`.skip` found anywhere in the diff, no `apps/` changes. Merged `--no-ff` as `1389d18`. `RST-46` Done. `VRS-001`'s Feature Register confirms `VRS-F011` (Billable vs Non-Billable Pulse) as the correct next MVP-order feature — checking it directly against the code before writing its Stage 19 brief surfaced and closed two more findings the same day, no founder consultation needed either time: F277 (the spec's own `week_start_date`/`expected_hours` language predated Stage 18's real mechanisms — `weekStartForEmployee` and `getWeek`'s own `expectedWeeklyHours`, which `utilizationSnapshot.compute` must reuse rather than reimplement) and F278 (`UtilizationSnapshot`'s claimed policy-row "reuse" matched neither existing `Employee` row on its own, and is composed instead from each row's matching cell) — see section 20 and `docs/Foundations_Findings.md`. `docs/Vulto_Specs/VRS-F011_Billable_vs_Non-Billable_Pulse.md` corrected to match. Start a new branch `codex/stage-19-billable-non-billable-pulse` from the current `main`. `RST-47` filed and moved to In Progress. Codex then traced that brief into the existing interceptor before writing any product code and stopped, reporting three more findings the same day (`docs/stage-reports/STAGE-19_Billable_vs_Non-Billable_Pulse.md`, `164b55e`): F279 (`resolveStoredSubjectEmployeeId` had no branch for `UtilizationSnapshot`, leaving F278's own Team Member grant with no subject to resolve against), F280 (no authorized principal exists for `utilizationSnapshot.compute`'s reactive write, F278's Team Member row being Read only) and F281 (a role-filtered cohort cannot produce the role-independent agency aggregate this feature's own spec requires) — the first two closed directly on existing precedent, the third escalated as a genuine architectural fork and resolved by founder-delegated decision after a robustness challenge corrected the reviewer's first-pass recommendation (which had relied on a factually backwards analogy to the Bench Forecast's deliberately team-scoped Manager aggregate, and had first proposed an interceptor-bypassing read rather than reusing existing system-principal machinery). All three ruled and documented the same day — see section 20. `codex/stage-19-billable-non-billable-pulse` resumed on the ruled design; `RST-47` back to In Progress. Stage 19 (`VRS-F011`, Billable vs Non-Billable Pulse) reported complete by Codex (`0631c91`, all four gates passed, `verify:full` at 301 API tests passed / 2 skipped, `RST-47` moved to In Review) and independently verified directly against the finished code before merge, not the completion report alone: F279's `resolveStoredSubjectEmployeeId` `UtilizationSnapshot` branch, F280's `utilization-snapshot-compute` system-principal write path (never the triggering caller's own), and F281's role-independent `agencyFiguresUnderSystemPrincipal` cohort-and-sum path (run uniformly for every caller, with `perEmployee` and `getIndividual` as fully separate ordinary-principal paths) all read directly against the actual diff, not asserted from the report; the new `SYSTEM_OPERATION_TARGETS` registry confirmed as the sole authority for both `decideRead`'s and `authorizeWrite`'s system-principal node-target branches, with the `triggered_by` edge-shape clause for `timesheet-anomaly.create-flag` left separate exactly as A004-T25 requires; `calculateUtilization` confirmed to divide by `expected_hours`, never logged hours, with `pitch_hours` routed outside both the numerator and denominator; `expectedWeek` confirmed genuinely shared between `getWeek` and the snapshot computation, not reimplemented; the real interceptor and timesheet integration tests read in full, proving a Team Member's own read and a denied cross-employee read (F279), a Team Member's own `saveCell` still producing a written snapshot despite a Read-only row grant (F280), and Owner, Manager and Team Member all receiving byte-identical aggregate fields while `perEmployee` correctly varies by role and a department filter correctly triggers suppression (F281); no `apps/` file touched, confirmed by direct diff. Merged `--no-ff` as `0e7e4d9`. `RST-47` moved to Done. `VRS-001`'s Feature Register confirms `VRS-F012` (Revenue Gap Alert) as the correct next MVP-order feature. Checking it directly against the code before writing its brief surfaced and closed three more findings the same day, no founder consultation needed for any: F282 (the spec's own local-first Web-Worker/offline-sync language, never corrected since the architecture pivot), F283 (`VRS-F005`'s bench computation has never actually implemented the Pitch exclusion this spec assumes it already has, and names a `benchForecast.computeBench` function that does not exist) and F284 (the spec's own compensation-gated cost-visibility bullet contradicts its own billing-rate-only cost-source decision and `RevenueGapAlert`'s already-registered flat-Tier-0 node). `docs/Vulto_Specs/VRS-F012_Revenue_Gap_Alert.md` corrected to match. Branch `codex/stage-20-revenue-gap-alert` started from `main`; `RST-48` filed and moved to In Progress. Stage 20 reported complete by Codex (`1a7071b`, all four gates passed, `verify:full` at 308 tests passed / 2 skipped, `RST-48` moved to In Review) and independently verified directly against the finished code before merge: `bench-forecast-queries.ts`'s new shared `benchDaysForWindow` helper (used by both `cohortEmployee` and the new `computeBenchStatus` export) genuinely excludes Pitch days, proven by a real reactive test saving Pitch cells on every working day and observing zero bench days and no alert (F283); `revenueGapAlert.evaluate` genuinely runs under the new `revenue-gap-alert-evaluate` system principal, proven by a Manager's `assignment.cancel` producing an alert stamped with the system actor's own ID, never the Manager's; `SYSTEM_OPERATION_TARGETS` gained exactly one new entry and the `triggered_by` edge clause was cleanly generalized to a second edge-shaped system write rather than hardcoded a fifth time (an extension this brief hadn't explicitly specified, resolved correctly on direct A004-T25 precedent); resolution, escalation, dismissal, sweep idempotency and Ghost exclusion (G05–G09) were each read against a genuine Postgres-backed test, not the report's prose alone; no `apps/` file touched. Merged `--no-ff` as `c2583a5`. `RST-48` moved to Done. The merge itself hit a stale `.git/index.lock` from an interrupted first attempt (unrelated to Codex's own build) — cleared with the founder's delete permission, working tree reset to `main`'s committed state, and the merge re-run cleanly the second time. Codex's own push of `codex/stage-20-revenue-gap-alert` was refused by its environment as an unauthorized repository export; the reviewer's own linked-computer shell has no GitHub credentials either (confirmed by `git push --dry-run`). Resolved from the founder's own machine the same day: `main` pushed to `origin` (`bf20ea5..2697e79`), and the fully-merged `codex/stage-20-revenue-gap-alert` pushed for the record and deleted locally — confirmed in sync by a direct re-check of this shell's own `git status` against `origin/main`. See section 21. Before that, checking `VRS-F010` (Timesheet Speed-Run) directly against the code before writing its Stage 18 brief surfaced and closed two more findings the same day, no founder consultation needed either time: F267 (`logged_against` needed `Pitch:identifying` declared as a governing partition, the identical omission F265 found in `staffed_on`) and F268 (`rowScopeSatisfied` needed a third subject-resolution branch for `TimesheetEntry`/`TimesheetAnomalyFlag`, the identical structural gap F264 found for Pitch). `docs/Vulto_Specs/VRS-F010_Timesheet_Speed-Run.md` corrected to match, along with four scoping decisions settled on established precedent (the `overtime_flag_threshold` hardcoded-pending-`VPS-F005` note, `hrCompliance.sendReminder`'s full deferral to `VPS-F003`, the `ApprovedOvertime`/`VRS-F018` boundary, and the post-transaction anomaly-evaluation timing). `RST-46` filed and moved to In Progress; `codex/stage-18-timesheet-speed-run` about to start. See section 19. Before that, Codex closed the one verification gap Stage 17's review had disclosed rather than glossed over: `pnpm verify:full` re-run in its own correctly-provisioned environment (Node `v24.18.0`, live PostgreSQL) at `8a91a01`, reporting the same 284 API tests passed / 2 skipped the Stage 17 report originally claimed — genuine terminal output, independently confirmed, not re-asserted from the report. Before that, Stage 17 (`VRS-F009`, Time Classification Taxonomy) is merged (`65a8578`, `--no-ff`) — reviewed directly against the finished code, not the completion report alone: every code diff read line by line against the brief's Do items, the real PostgreSQL `pitch.integration.test.ts` suite read in full (Manager staffs own direct report and is refused an outsider on both staff and unstaff; Owner/HR Admin staff and unstaff anyone; Team Member refused every write and the generic `Pitch:identifying` read; the selector returns exactly `{ pitchId, name, clientName }` with no existence leak), `time-classification.ts` independently confirmed to have zero callers anywhere outside its own file and test, no `apps/` or `TimesheetEntry` file touched confirmed by direct diff and repo grep, and `ghostResource.create`'s own router absence independently checked to confirm F266's citation. Codex's build surfaced and closed two further findings the same day — F265 (`staffed_on` needed `Pitch:identifying` declared as a governing partition alongside `Employee:operational`, or the interceptor refused every staffing write) and F266 (Pitch's writes belong in the shared `graph.applyMutations` pipeline, not a dedicated router entry, correcting the brief's own `ghostResource.*` citation) — both verified correct against the code and closed with no founder consultation needed. One process note: this is the first stage where the builder recorded its own findings directly into `docs/Foundations_Findings.md` and the other reviewer-authoritative documents, rather than surfacing them as open findings for the reviewer to rule and record; both rulings checked out, but the founder should decide whether that becomes standing practice. The findings ledger's table rows and master recount were out of sync with F265/F266's detail sections (a recurring drift this file has hit before) and were corrected the same day: **212 numbered rows, F54–F266 with one gap (F198): 198 closed, eight open (unchanged), and six recorded facts or limitations (unchanged).** `RST-45` moved to Done. See section 18. Before that, Stage 16 was fully closed: Codex fixed F263 (`56581cf`) exactly as ruled — `sweepOvercommitted` filters its Employee list through `filterReadable`, plus real-Postgres coverage for an unrelated outsider and a scoped-manager, both independently re-verified directly against the code. `RST-44` is Done. See section 16/17. Before that, Stage 16 was reported complete by Codex, but independent review of the finished implementation before merge found F263: `conflictResolution.sweepOvercommitted` read every Active Employee and Assignment in the workspace directly and returned every overcommitted employee to any authenticated workspace member, with no `filterReadable` call and no role check — the one workspace-wide node-listing query in the codebase to skip the pattern `listEmployees`/`listGhostResources` already established for the identical `Employee` node type and a sibling one. Traced directly to that existing pattern and closed the same day, no founder consultation needed: `sweepOvercommitted` gains the identical `filterReadable` call those two queries already make, scoping its results to what each caller's `Employee:operational` grant actually allows. `RST-44` stays In Review pending the fix; `codex/stage-16-capacity-conflict-resolution` resumes on the existing branch, no restart. See section 16. Before that, Stage 16 stopped a fourth time resuming implementation on F262: the first real Postgres-backed `overrideAndProceed` integration test found that `edgeRoleDecision` requires unscoped Full on both endpoints of every edge write, with no row context at all — deliberate FDN-92 architecture, never before exercised by a role holding less than unscoped Full on both endpoints — so Manager's row-scoped `Employee:operational` grant and `Project`'s `READ_ANY()` grant could never authorize `assignment_of`/`assigned_to`. Unlike F255–F261, no existing precedent decided this one; put directly to the founder. Ruled the same day: `edgeRoleDecision` gains real per-endpoint row context (reused from `decideRead`'s own pattern), making Manager's already-documented `Full (direct reports)` cell resolve correctly for `assignment_of`; a new opt-in `readSufficientEndpoints` registry field, declared only on `assigned_to`, lets that one edge accept Read on `Project` — unchanged, still `READ_ANY()` for Manager — mirroring `VPS-A004`'s own read-traversal rule. `RST-44` back to In Progress, `codex/stage-16-capacity-conflict-resolution` resumed on the corrected brief, no restart. See section 16. Before that, Stage 16 stopped a third time resuming on F261: tracing F260's own `InitPayload.roles` field to its two production `createGraphClient` callers found neither the shell bootstrap nor the sync harness (both under `apps/`, off-limits this stage) has a role value to supply. Ruled the same day: `InitPayload` never needed the field — `WorkspaceMembership`'s already-replicated `role` string, reached via its `membership_of` edge the same way `managed_by` already reaches Manager, replaces F260's delivery mechanism entirely, with no `apps/` file touched. `RST-44` back to In Progress, `codex/stage-16-capacity-conflict-resolution` resumed on the corrected brief, no restart. See section 16. Before that, Stage 16 stopped a second time resuming on F260: tracing F259's own corrected local-execution contracts into the device-cache APIs exposed that neither `conflictCheck.evaluate`'s input nor `MutatorContext` carried the caller identity and role facts the local Manager/Owner checks need. Ruled the same day: the Manager check needs no new capability (`OptimisticCache`'s existing `edgesFrom`/`getNode` already answers it); the Owner check needs `MutatorContext` extended with a `roles` field mirroring the server's own `CurrentWorkspaceSession` bundle, reusing `deriveEffectiveRoles` rather than a second mechanism (this delivery mechanism was itself superseded by F261, above). `RST-44` back to In Progress, `codex/stage-16-capacity-conflict-resolution` resumed on the corrected brief, no restart. See section 16. Before that, Stage 16 stopped once resuming on F259: the brief's server-only `conflictCheck.evaluate`/`onlineOnly overrideAndProceed` design contradicted the spec's own local/offline NFRs. Ruled the same day: `conflictCheck.evaluate` corrected to a portable local query on `bench-forecast.ts`'s own precedent; `overrideAndProceed` corrected to a genuine optimistic mutator on `assignmentCreate`'s own precedent (whose comment already establishes capacity as deliberately server-only, unchanged). `RST-44` back to In Progress, `codex/stage-16-capacity-conflict-resolution` resumed on the corrected brief, no restart. See section 16. Before that, `VRS-001`'s Feature Register confirmed `VRS-F008` (Capacity Conflict Resolution) as the correct next MVP-order feature; checking it directly against the code and against `VPS-A006`'s own architecture decisions before writing the brief surfaced and closed F255 (`Assignment.manager`'s policy contradicting `VPS-A004`'s own table — a real, unreviewed regression from Stage 13's `f2d928d`), F256 (`validateCapacity`'s calendar-day aggregation contradicting G02 and the "reused, not reimplemented" claim, extended on F237/G08's own precedent), F257 (the "audited per `VPS-F004`" claim, no home in the closed taxonomy, on F221/F225/F252's own precedent) and F258 (the Depends-On omission of `VPS-A006`'s unbuilt job-scheduling infrastructure, on F248's own Partial-forward-dependency precedent). `RST-44` filed, `codex/stage-16-capacity-conflict-resolution` about to start. See section 16/17. Before that, Stage 15 (`VRS-F007`, Ghost Resources) was reviewed directly against the finished code, not the completion report alone: the F254 fix confirmed as a clean one-line `governingPartitions` declaration touching nothing else; zero `appendAudit` calls anywhere in the Ghost mutation module (F252); `requireUnique` genuinely reused, not reinvented (F253); the promotion transaction read in full matches every ruling exactly (version check first, actor/time rejection on re-promotion, `existing_employee_id` deferral, serializable isolation, one `promoted_to` edge write); concurrency and rollback proven with genuine `Promise.all` races and an injected constraint violation against real PostgreSQL, not mocks. Approved and merged `--no-ff` as `4081e9f`. `RST-43` moved to Done. See section 15/16. Before that, Stage 15 reached real implementation (`dae1d01`, preserved) and its first real permission-interceptor integration test exposed F254: `promoted_to` (GhostResource → Employee) declared no governing partition on tier-split Employee, so the interceptor structurally refused every promotion regardless of role — the identical class of gap F208 and F235 already fixed. Ruled the same day on that precedent: `promoted_to` is declared `{ Employee: "operational" }` in both the live edge registry and `VPS-A002`'s governing-partitions table. `RST-43` back to In Progress, `codex/stage-15-ghost-resources` resumed on `dae1d01` with no restart. See section 15. Before that, 23 September 2026: Stage 15 stopped a fourth time resuming on F253 (`employeeOperationalRecord` requires `employee_code`/`full_name`/`email`/`employment_type`, none of which Ghost creation supplies, and promotion never supplied `employee_code` either) — ruled the same day: a Ghost-only `ghostEmployeeCreateFieldsSchema`/`ghostEmployeeOperationalRecord` pair nulls exactly those four fields (the real-Employee path is untouched), `contracted_hours` defaults to 40 like real creation, and promotion's contract gains a required `employeeCode` plus optional `contractedHours`, validated with `employee.create`'s own existing `requireUnique` helper. `RST-43` back to In Progress, `codex/stage-15-ghost-resources` resumed on the corrected brief. See section 15. Before that, Stage 15 stopped a third time resuming on F252 (Do item 5 required a successful-promotion `VPS-F004` audit entry, but Ghost promotion is Tier 0 and `VPS-F004` only logs successful Tier 1/3 access) — ruled the same day on F221/F225's own precedent: no taxonomy extension, no `appendAudit` call; `GhostResource`/Employee's own `updated_by`/`updated_at` and the `promoted_to` edge's `created_by`/`created_at` (G03) already give a permanent promotion record. `RST-43` back to In Progress, `codex/stage-15-ghost-resources` resumed on the corrected brief. See section 15. Before that, Stage 15 stopped a second time resuming on F251 (this brief's own claim that `GhostResource`/`OpenRole` have zero `MATRIX_OVERRIDES` entries was wrong — Stage 13 had already added and tested explicit overrides for both) — ruled the same day: the overrides stay, `GhostResource`'s `manager` cell alone is extended to `FULL_ANY()` so `VRS-F007`'s Manager-create flow is reachable, `team-member` and `OpenRole` are unchanged, the resulting Manager write access to promote/cancel is accepted as this codebase's node-type-scoped permission model rather than a new gap, and the shared policy test is split so `GhostResource` gets its own assertions. `RST-43` back to In Progress, `codex/stage-15-ghost-resources` (`dcdfc88`) resumed. See section 15. Before that, Stage 15 stopped once resuming on F250 (`ghostResource.cancel`/`promote` missing `expectedVersion`, and promotion's transition guard alone not concurrency-safe under `pipeline.ts`'s default isolation) — ruled the same day: both mutations gain `expectedVersion`, `promote` joins the serializable-isolation list, and a version mismatch returns G07's actor-and-time rejection specifically when caused by a prior promotion, the ordinary `stale-state` rejection otherwise. `RST-43` back to In Progress, `codex/stage-15-ghost-resources` (`355a08f`) resumed. See section 15. Before that, `VRS-F007` (Ghost Resources) is briefed as Stage 15 — checking it directly against the code before writing the brief surfaced and closed F248 (the undocumented forward dependency on `VRS-F028`'s unbuilt `OpenRole`) and F249 (the promotion API's underspecified `existingEmployeeId` branch), both founder-delegated, same day. `RST-43` filed, `codex/stage-15-ghost-resources` about to start — see section 15. Before that, `origin/main` is pushed and in sync with local `main` (`111d32d`) — the push-authorization gap that had built up across the F245–F247 stop points and the merge is now closed. Repo hygiene: 20 fully-merged remote branches and their local counterparts deleted (including `codex/stage-11/13/14-*`, `stage-1` through `stage-10`, and the `fdn-*` staging branches), plus 11 stray local `worktree-agent-*` refs with no live worktree behind them. Five genuinely unmerged branches were deliberately kept — `archive/local-first-e2e` (the pre-pivot local-first/E2E architecture, archived on purpose, not stale), `fdn-68-audit-wip`, and the three early `stage-9-*` attempts superseded by `stage-9-shell-bootstrap-v3`/`stage-10-reconnect-shell` — confirmed present and untouched by direct check, not assumed from the report. Before that, after Stage 14 (`VRS-F006`, Rate Card Engine) was reviewed directly against the finished code and approved with no further findings, and merged `--no-ff` as `04f2a7a`. `RST-42` moves to Done. The review confirmed every one of F243–F247's rulings against real code and real Postgres-backed tests, and caught one genuine, previously dormant bug in `services/api/src/permission/reader-set.ts` that Codex's own fix corrected and proved safe (Finance-restricted's `team-member` own-scope cell was unconditionally poisoning reader-set resolution for any fixed-class node with no subject; RateCard is simply the first such node any stage has built). Before that, Stage 14 was briefed and then resumed twice more — checking directly against the code before writing the brief surfaced and closed F243 (Assignment's registered but unwritten `governed_by` edge to RateCard) and F244 (`VRS-F006`'s stale local-first sync/retention-window language), both the same day; Codex then traced the corrected brief into the mutation registry before writing any code and stopped again, surfacing F245 (`rateCard.update`'s missing `currency`/`name` handling and stale-state contract) and F246 (card-bearing `assignment.create`'s undefined optimistic behavior), both ruled the same day on direct precedent (F228, F224/F241); resuming a second time, reaching the RateCardLine lookup itself, exposed F247 (RateCardLine had no traversable or indexed path from its RateCard), closed the same day by extending F233's own deterministic-ID precedent — `RST-42` filed and `codex/stage-14-rate-card-engine` resumed again. Before that, Stage 13 (`VRS-F005`, The Bench Forecast) was reviewed directly against the finished code — F237's `resolveWorkingDay`, F238's disclosure control and k-anonymity floor, F240's version-check-first `assignment.cancel`, and F242's `matchesBenchForecastFilters`/employee-ID-free `getAggregate` all independently re-verified against the actual implementation, not the completion report alone; one test-coverage gap found (HR Admin never exercised against the Contextual Intelligence Panel's protected fields) and closed at `9f553ef` before merge — approved, and merged `--no-ff` as `d9b6d74`. `RST-41` moved to Done. Stage 12 (`VRS-F004`, Working Calendar and Working Patterns) was reviewed directly against the finished code — F225 through F235 (eleven findings, four stop points) all closed, every founder-ruled contract point independently re-verified against the actual implementation and a real Postgres-backed test proving `pattern_for`'s fix — approved with no further findings, and merged `--no-ff` as `0249d5c`. `RST-40` moved to Done. Stage 7 merged as `dec4eb3`; Stage 8 merged as `5897b7b`, reviewed and ruled. Stage 9 (the real shell bootstrap, redefined from F216) stopped five times before it finished, correctly every time (F216 through F220, all ruled — see section 8), was then reviewed directly against the finished code and approved, and merged to `main` as `5a0e9ee`. Stage 10 (the Reconnect shell state, F214's design) built cleanly on the first attempt, was approved for merge with no further findings, and merged as `570b15d`; the two stray worktrees and the abandoned Stage 9 report left over from earlier reviewer sessions were also cleaned up, confirmed clean by direct check — see section 10. Stage 11 (`VRS-F003`) stopped twice while briefing and starting — F221 through F224, all ruled the same day each was found — was then reviewed directly against the finished code, approved with no further findings, and merged as `96d53eb` — see section 11. Stage 12 (`VRS-F004`) stopped four times before it finished — F225–F226 while briefing, F227–F231 and F232–F234 while tracing the corrected contracts, and F235 (a missing `governingPartitions` declaration on `pattern_for`) found by the first real Postgres-backed `pattern.set` test — all eleven findings ruled the same day each was raised, was then reviewed directly against the finished code and approved, and merged as `0249d5c` — see section 12. Stage 13 (`VRS-F005`, The Bench Forecast) was briefed — three findings surfaced reading the spec directly before writing the brief (F236: the spec's local-first language wrongly includes Tier 2/compensation data F199's architecture never caches on device; F237: the working-day index F226 deferred to this stage is resolved without building one, via a shared pure function; F238: the k-anonymity disclosure-control mechanism this feature's own acceptance criteria require had never been built by any stage, now built for real as this feature's first caller) — all ruled the same day, folded into the brief, `RST-41` filed in Backlog. Codex then started the branch and stopped four times more tracing it into the code: F239 (the Pitch-day exclusion's unbuilt `VRS-F009`/`VRS-F010` dependency, `189cf6c`), F240 (`assignment.cancel`'s missing state-transition version, `67852a9`), F241 (the bench aggregate locality contradiction with `VPS-A004`'s server-only disclosure-control rule, `bfc6714`) and F242 (the undefined `filters` schema shared between the local query and the new server aggregate, `1a45be1`) — all ruled the same day each was raised, each on direct precedent already established (F236/F237/F238's partial-forward-dependency pattern for F239; F223/F228's version-first pattern for F240; `VPS-A004`'s own "Where enforcement runs" text for F241; the same shared-pure-function pattern F237/F238 already established, for F242) — then built cleanly on the corrected contracts, was reviewed directly against the finished code and approved after one test-coverage gap was found and closed before merge (HR Admin's access to the Contextual Intelligence Panel's protected fields, `9f553ef`), and merged `--no-ff` as `d9b6d74` — see section 13. Two different builders have worked this project now — Claude Code through Stage 8, OpenAI Codex from Stage 9 — the loop is agent-agnostic; see section 1.

---

## 1. Roles and working method

- **Shaheer (founder)** is not an engineer. He relays between two agents: a builder (Claude Code or, as of Stage 9, OpenAI Codex — the loop does not care which, as long as it follows this document's conventions) builds; the reviewer (you) reviews and rules.
- **Loop:** the builder finishes a stage, then writes `docs/stage-reports/STAGE-N_*.md` and stops. Shaheer pastes its summary to the reviewer. The reviewer reads the report **and the code directly** (via the linked-computer shell, repo mounted at `$HOME/mnt/vulto-for-professional-services`). The reviewer gives a verdict, rulings on open findings, and one fenced block of paste-ready text for the builder. The builder applies the fixes, merges with `--no-ff` when CI is green, and waits for "Stage N go". A builder that hits a genuine missing-design gap mid-stage should raise a numbered finding and stop (BLOCKED status), the way Codex did for F216 — that is success, not failure, and the reviewer's job is to verify the gap is real (as F216's was) before ruling on it, not to wave it through or scold the builder for stopping.
- **Always read code, not just the report.** Direct review caught issues every report missed:
  - membership changes not reaching the graph (Stage 2/3);
  - generic mutations able to write protected fields (Stage 4);
  - the key provider defaulting to the dev key (Stage 5);
  - a tenant-crossing outbox drain, protected-read fallback on refusal, and fake-success erasure (Stage 6).
- **Ruling style:** fail closed, tenant-isolated, no silent data loss, no "merge on red", no recorded exceptions where a clean fix exists. A spec disagreement is always a numbered finding, never only a micro-decision.
- **Brief amendments:** added to `docs/Claude_Code_Build_Prompt.md` → "Amendments", on `main`.
  - Method: `git worktree add .wt-<name> main` inside the repo, edit, commit with the attribution trailer, then `git worktree remove`.
  - The linked-computer shell **cannot push** (no GitHub credentials). Claude Code pushes `main`, so the paste text must tell it to.
- **Delete permission** in the repo folder is for git housekeeping only (stale lock files, worktrees).

## 2. Founder preferences

- Addressed as "Sir". Calm, precise, dry. Direct verdicts; push back on weak ideas.
- Plain explanations: say what and why, and avoid jargon.
- Structured and concise. American English.
- The priority is building the app. FDN-110 (pentest and assurance) is deferred until before the first real-data pilot.

## 3. Direction (settled — do not reopen)

- **Dropped:** local-first, CRDT/Loro, the Rust sync engine, Tier 1 E2E envelopes and key recovery. The code is archived on branch `archive/local-first-e2e`.
- **Kept deferred:** Tier 3 as an isolated E2E module.
- **Stack:**
  - Postgres (Drizzle) is the single source of truth. `services/api` (Fastify + tRPC) is the only writer. Better Auth handles identity.
  - The graph is still nodes and edges; only its storage moved to Postgres.
  - The read path is Electric 1.8.1, Apache-2.0, pinned by digest (host port 5133). The client library is `@electric-sql/client` 1.5.28, exact. PowerSync was rejected as paid or ambiguous.
  - Exactly two shapes (nodes, edges), filtered by the `sync_node_audience` / `sync_edge_audience` tables. The materializer is the only writer of those tables. Devices reach Electric only through the authenticated shape proxy.
  - Server-side field encryption: AES-256-GCM, KMS root → per-workspace KEK → DEKs. Erasure destroys DEKs. `protected.read` runs authorize → audit → decrypt.
  - Device: SharedWorker plus wa-sqlite cache (Tier 0 only) and an outbox. Tier 1/2 values live in worker memory only.
- **Trust promises:** VPS-A008 (eight promises).
- **`CLAUDE.md` security rules** (non-negotiable):
  - Tier 1/2 data never reaches device storage.
  - Decryption happens only in the audited path.
  - Tier 3 plaintext never reaches a server.
  - No `localStorage` or `sessionStorage`.
  - The graph is written only through named mutations.
  - The audience is written only by the materializer.
  - No new shapes.

## 4. Where everything is

| What | Where |
|---|---|
| Staged brief + all rulings | `docs/Claude_Code_Build_Prompt.md` (Part 3 stages; "Amendments" = every ruling to date) |
| Stage reports | `docs/stage-reports/STAGE-1…7_*.md` |
| Findings (F54–F220, one gap at F198) | `docs/Foundations_Findings.md` (the table is authoritative; the count paragraph is recounted programmatically) |
| Specs | `docs/Vulto_Specs/` — A001–A008, F001/F004/F007, VPS-002/003, VRS-001 |
| Agent rules | `CLAUDE.md` |
| Linear | Team FDN. Stage 6 = FDN-99/100/101 (Done). Stage 7 = FDN-103, FDN-54 (Done). Stage 8 = FDN-104 (priority slice only), RST-33. FDN-104's remaining specs correct just-in-time per later feature. Deferred: FDN-110 (pentest) |

## 5. Stage status

| Stage | Result | Merge |
|---|---|---|
| 1 Alignment | done | pushed |
| 2 Graph store | done (F204, F205) | merged |
| 3 Interceptor + audit | done (F206) | merged |
| 4 Mutations | done (F208) | merged |
| 5 Protected data | done (F209, no default key provider) | `8837556` |
| 6 Sync | done, two review rounds (F210–F212) | `961830d` |
| **7 Retire + harden CI** | **approved 22 September (F210 verified by direct read; F213 ruled — keep the column)** | **`dec4eb3`** (merged `--no-ff`, 22 September) |
| **8 Spec sweep (priority slice) + RST-33** | **reviewed 22 September (F130 closure confirmed; F215 confirmed; F214 ruled — design accepted, build deferred to Stage 9)** | **`5897b7b`** (merged `--no-ff`, 22 September) |
| **9 The real shell bootstrap** | **done 22 September** (blocked five times first, all ruled F216–F220; completion report at `08e2873` verified directly against code, no further findings) | **`5a0e9ee`** (merged `--no-ff`, 22 September) |
| **10 The Reconnect shell state** | **done 22 September** (completion report at `cf59ed5` verified directly against code, no further findings) | **`570b15d`** (merged `--no-ff`, 22 September) |
| **11 Multi-Entity and Jurisdiction Foundation (VRS-F003)** | **done 22 September** (completion report at `fb4ef35` verified directly against code, no further findings) | **`96d53eb`** (merged `--no-ff`, 22 September) |
| **12 Working Calendar and Working Patterns (VRS-F004)** | **done 23 September** (stopped four times — F225–F226, F227–F231, F232–F234, F235 — all ruled the same day each was raised; completion report at `d09d952` verified directly against code, no further findings) | **`0249d5c`** (merged `--no-ff`, 23 September) |
| **13 The Bench Forecast (VRS-F005)** | **done 23 September** (F236–F238 ruled briefing; F239 ruled at `189cf6c`; F240 ruled at `67852a9`; F241 ruled at `bfc6714`; F242 ruled at `1a45be1`; completion report at `ab2708b` verified directly against code, one test-coverage gap found and closed at `9f553ef`, no further findings) | **`d9b6d74`** (merged `--no-ff`, 23 September) |
| **14 Rate Card Engine (VRS-F006)** | **merged, 23 September** (`04f2a7a`) | F243–F247 ruled (three stop points); reviewed directly against the finished code, one dormant `reader-set.ts` bug caught and fixed; `RST-42` Done |
| **15 Ghost Resources (VRS-F007)** | **merged, 24 September** (`4081e9f`) | F248–F254 ruled (five stop points); reviewed directly against the finished code — the F254 registry fix, zero `appendAudit` calls, `requireUnique` reuse, the full promotion transaction and its real-PostgreSQL concurrency/rollback tests all independently re-verified; `RST-43` Done |
| **16 Capacity Conflict Resolution (VRS-F008)** | **merged `e705aad`; blocked four times (F259, F260, F261, F262) plus one post-completion review finding (F263), fixed same day every time** | F255–F263 ruled; `RST-44` Done |
| **17 Time Classification Taxonomy (VRS-F009)** | **merged `65a8578`; F265 and F266 found and closed the same day building against the brief, no founder consultation needed either time** | F264–F266 ruled; `RST-45` Done |
| **18 Timesheet Speed-Run (VRS-F010)** | **merged 25 September** (nine stop points, F267–F276, all ruled the same day each was raised — see section 19 for full reasoning; Codex's completion report independently verified by reading all 11 tests in `timesheet.integration.test.ts` line by line against the real PostgreSQL integration suite, confirming every ruling proven by genuine test coverage, not asserted in prose — no discrepancies found) | F267–F276 ruled; merged `--no-ff` as `1389d18`; `RST-46` Done |
| **19 Billable vs Non-Billable Pulse (VRS-F011)** | **merged 25 September** (F277, F278 ruled briefing; Codex then traced the brief into the interceptor before writing product code and stopped on F279, F280 and F281, all ruled the same day — F281 escalated as a genuine architectural fork, resolved after a robustness challenge corrected the reviewer's first-pass recommendation; completion report at `0631c91` independently verified directly against the actual diff and both integration test suites against every ruling, not asserted from the report — no discrepancies found; see section 20) | F277–F281 ruled; merged `--no-ff` as `0e7e4d9`; `RST-47` Done |
| **20 Revenue Gap Alert (VRS-F012)** | **merged 25 September** (F282–F284 ruled briefing; completion report at `1a7071b` independently verified directly against the actual diff and both integration test suites — no discrepancies found; see section 21) | F282–F284 ruled; merged `--no-ff` as `c2583a5`; `RST-48` Done |
| **21 Skill-to-Project Matcher (VRS-F013)** | **merged 25 September** (nine stop points, F285–F295, all ruled the same day each was raised — see section 22 for full reasoning; Codex's completion report at `15d8aaf` (319 API tests passed, 2 skipped) independently verified directly against the diff, not asserted from the report — no discrepancies found) | F285–F295 ruled; merged `--no-ff` as `414b8df`; `RST-49` Done |
| **22 Skill Matrix (VRS-F014)** | **merged 25 September** (F296 ruled the same day the stage was briefed; no findings on completion review — see section 23 for full reasoning; independently verified directly against the diff, including a genuinely-run typecheck and test suite, not asserted from Codex's report; merged `--no-ff` as `cd17db0`; `RST-50` Done) | F296 ruled; merged `--no-ff` as `cd17db0`; `RST-50` Done |
| **23 Local-First Search (VPS-F002)** | **merged 25 September** (five stops, F301–F305, each ruled the same day — see section 24; F305, the sync browser suite red on `main` since Stage 11, stays open with its repair filed as `FDN-126`; completion report at `95115d5` independently verified directly against the actual diff, `tsc --noEmit`, unit suites, `arch:check`, format and lint genuinely run in this shell, not asserted from the report — no findings) | F297–F305 ruled (F305 open); merged `--no-ff` as `b50ab85`; `RST-51` Done |

**Stage 7 facts to remember:**
- Branch `stage-7-retire` at `d749c3c`. Green slow lane: https://github.com/shaheerjameel17/vulto-for-professional-services/actions/runs/35637057091. `main` was green at `961830d`/`4d91705` before branching.
- CI Postgres image (unchanged since Stage 6): `ghcr.io/shaheerjameel17/vulto-for-professional-services/ci-postgres@sha256:eec77295062736b9aa8468fe3f370b5a9e89931e4887827ead61d1c0e5792cfe`.
- **After the merge:** the `ci-image` workflow republishes without Rust (the Dockerfile no longer installs it). Read the new digest from that run and repin `CI_IMAGE` in `.github/workflows/ci-image-ref.env` before trusting a fresh CI run's timing, per the report's section 10.
- `device.revoke` intentionally does not gate `protected.read` or any tRPC mutation — only the shape proxy checks `device_workspace_revocation`. Confirmed against `VPS-F001`'s own contract (workspace-scoped cache/sync trust only). Not a gap; no code follows from it. See Amendments dated 22 September for the full reasoning.
- Rulings made in the Stage 7 review: see Amendments dated 22 September (the merge verdict, the device-revoke scope confirmation, F213).

## 6. Stage 7 review — closed, 22 September 2026

Every checklist item from the Stage 7 brief was verified by direct code read, not just the report (F210's migration and its test, F212's CI job list, the accessibility gate, the done-criteria grep, the adversarial suite, the findings recount reproduced programmatically, `CLAUDE.md`/`Bootstrap.md`/`CONTRIBUTING.md`, the file/line diff stat). Full reasoning is in the Amendments entry dated 22 September. Merge completed cleanly (verified: the net diff from the merge commit to the final head touches only the three intended files — a stray 602-file commit from the reviewer's own worktree cleanup was caught and fully reverted the same day, confirmed by diff, not by trusting the report).

## 7. Stage 8 review — closed, 22 September 2026

Every checklist item from the Stage 8 brief (below) was checked against the code, not just the report: `rowScopeSatisfied`/`decideRead`/`resolveReaderSet` read in full and cross-checked against the policy table and the temporal edge-filtering in `graph/store.ts`; the `Employee` registry entry confirmed byte-for-byte unchanged via `git show`; `FEATURE_LIFECYCLE_NODE_TYPES`'s symmetric client/server refusal confirmed; `employeeLinkUser`'s bidirectional uniqueness read in full; the four F130 subject-exclusion tests read in full, including the sentinel-string end-to-end proof; the `transitionStatus` status-table and stale-state test read in full; the import-equivalence test read in full; the `VPS-D004` F214 marker checked against the actual section text; the findings recount independently reproduced by parsing every `| F<n> |` row (161 rows, F54–F215, one gap at F198, 146 closed, 9 open — exact match). The mutation-testing pass/fail counts were not re-run (no `pnpm` on the linked machine in this session) but are consistent with the test bodies read. Full ruling in the Amendments entry dated 22 September (Stage 8 review). Three outcomes: **F130 closure confirmed**, no action; **F215's three sub-decisions confirmed**, plus a small follow-up (define "team" in `VPS-A004`'s own matrix, since it names "own + team" but never defines "team"); **F214 ruled** — the proposed single `Reconnect` state is accepted, with both tradeoffs decided as recommended (a missing session goes to sign-in, not `Reconnect`; nothing is erased on a plain `401`, safe only because the device cache is Tier 0 only) — but the actual UI and wiring are **not** built in this pass; that was briefed as Stage 9. `VPS-D004` and `VPS-A004` got their text corrected in that pass; the merge, the `Foundations_Findings.md` updates and the Linear status were Claude Code's to apply per the ruling.

## 8. Stage 9 review — F216, then F217, then F218, then F219, then F220, all ruled

**First attempt (`stage-9-reconnect-shell`), F216.** OpenAI Codex, a fresh session, stopped correctly before writing any UI. Verified directly: `apps/roster-web`'s entire shell is fixture-only — `Shell.tsx` hardcodes `workspaceName` and `syncStatus` as literals; `createGraphClient` is mounted nowhere but `/sync-harness`; `useSession` appears only on `/auth-ready` and `/devices`, outside the shell; `SyncEngine.#handleRevoked` stops every source before any retry could reuse it. This is `FDN-69` (the shell/workspace bootstrap), never built, sitting in Backlog since before Stage 1. F216 ruled: Better Auth's `organization()` plugin and `session.activeOrganizationId` already exist server-side; the gap is that `auth-client.ts` never added `organizationClient()` client-side. Redefined Stage 9 as the minimal real bootstrap and renumbered the original Reconnect brief Stage 10.

**Second attempt (`stage-9-shell-bootstrap`), F217.** Codex, another fresh session, traced the corrected brief before writing code and stopped again, correctly: F216's ruling assumed `session.activeOrganizationId` was already populated somewhere. Verified directly: `createWorkspace` → `confirmWorkspaceAdmission` never touches it; the only production write anywhere is the revocation path's clear-to-null. Offline, each `(workspace, user)` cache has no marker of which was last active, and the device-identity database is deliberately, test-enforced narrow. F216's ruling should have checked this and didn't — that gap was this review's, not either builder's. F217 ruled: `confirmWorkspaceAdmission` now sets the active organization when the confirmed membership is the person's sole active one (never auto-switching someone already active elsewhere); a null active organization with exactly one active membership resolves to it; more than one, online or off, is never silently picked — it's the same undesigned workspace-switching gap already on the follow-up list. The Stage 9 section in Part 3 was updated in place with both fixes rather than left as two amendments to reconcile later.

**Third attempt (`stage-9-shell-bootstrap-v2`), F218.** Codex, a third fresh session, traced the twice-corrected brief before writing code and stopped a third time, correctly: the F217-updated point 1 ("no data after `isPending` clears" unconditionally means sign in) and point 4 (a cold offline boot mounts from cache, no network call) both fire on a cold offline load, since `useSession()` also resolves to "no data" once it gives up trying to reach the server. Verified directly against the pinned client (`session-atom.mjs`, Better Auth 1.6.29): a request that reached the server — a genuine no-session answer or any HTTP-level failure — sets `data: null` with `error` either `null` or a `BetterFetchError` (always carries a numeric `.status`); a request that never reached the server sets `data: null` with whatever the fetch layer itself threw, never a `BetterFetchError`, no numeric `.status`. That distinction was already sitting in the client, unused. F218 ruled: point 1 fires only on a confirmed answer (`error` null or with a numeric `.status`); an `error` without one is indeterminate and falls through to point 4 instead. Point 4 gained the one clause its wording lacked — when point 1 couldn't resolve at all, there is no known user to filter cached workspaces by, so enumerate every cache on the device unfiltered, same one-cache-mounts rule. Grounded in a split the sync engine already makes for the identical reason (`engine.ts`'s `"network"` vs `"unauthenticated"` failure kinds) — no new mechanism invented.

**Fourth attempt (`stage-9-shell-bootstrap-v3`, FDN-117), F219 — different in kind.** Codex implemented the F216/F217/F218-corrected brief and, for the first time, actually exercised it in a real browser rather than tracing it before writing code. That's exactly what surfaced F219: point 3's `organizationClient()` design calls Better Auth's `organization.list()`, and `services/api/src/auth/http.ts` returns 404 for every `/api/auth/organization/*` route — reads included, by deliberate design (the route's own comment cites FDN-85/FDN-86's "no second write path" boundary). Verified directly: `trpc.ts`'s `createContext` also confirms there is no existing workspace-independent authenticated read anywhere in the API — every `protectedProcedure` needs `session.activeOrganizationId` already resolved, which is exactly the value this fallback exists to produce. F219 ruled: no organization route opens; one new narrow route, `POST /workspace/list-active-memberships`, session-gated with no workspace scope (mirroring the existing `/devices/retire`), returns only the caller's active/confirmed memberships via the same predicate already used twice elsewhere in the codebase. **Unlike the first three findings, this one does not restart the branch** — the rest of `stage-9-shell-bootstrap-v3`'s preliminary work (the F218 split, offline fallback, graph client mount, refusal-signal wiring, sync-client changes) was read directly and is sound; only the one blocked call needs replacing. `VPS-F001` got one bullet added recording the boundary this finding made explicit.

**Fifth attempt — still `stage-9-shell-bootstrap-v3`, F220.** After F219's fix landed (its route works, server test 45/45 passing), the same previously-failing browser test still failed: the workspace name never rendered. F219's route correctly named the sole active workspace, but nothing made that name the server's actual `session.activeOrganizationId` — `/devices/register` still 403'd the workspace header, `trpc.ts`'s context builder still returned no principal. Verified directly: `confirmWorkspaceAdmission` (F217's own fix) is the *only* production write to that field for the ordinary case, and it fires exactly once, at confirmation. It cannot reach a session created afterward — a fresh sign-in on a new device or browser, for someone already an active confirmed member, gets a brand-new session row at the column's `null` default, permanently stuck, until now. This was not a rare edge case; it was the ordinary state of every fresh sign-in for an already-confirmed single-workspace person, undiscovered until this stage actually exercised one in a browser. F220 ruled: a second new route, `POST /workspace/activate-sole-membership`, re-derives sole active membership independently (trusts nothing the client asserts) and performs the identical write `confirmWorkspaceAdmission` already does, reachable now from any session, not only the confirming one — and only ever from `null`, never overwriting an already-set active organization, so it stays consistent with F217's no-silent-switching rule. Same as F219, no restart: the branch's preliminary work, F219's route included, stays exactly as it is.

**Pattern worth naming.** Five stops in a row on the same stage is not the loop failing — it's exactly what "read the code, not the report" and "no recorded exceptions where a clean fix exists" are for. Every finding was real, every builder stopped instead of inventing a design locally, and every ruling was grounded in code or a pattern that already existed (Better Auth's org plugin; the `revokeWorkspaceAdmission` membership-check pattern; the sync engine's own network-vs-auth failure split; `requireCurrentWorkspaceSession`'s own membership predicate; `confirmWorkspaceAdmission`'s own session write, now reused a second time). F218 came from wording *this review itself* introduced in the F217 fix; F219 and F220 were both only reachable by actually running the code in a browser, not by re-reading a brief harder — a reminder that "trace before writing code" catches design gaps, but some gaps (especially ones about what a server write actually reaches) only show up once something real is called end to end. If a sixth attempt stops again, treat it the same way: verify before ruling, don't assume the brief is complete just because rounds have passed.

**Completion review, 22 September 2026 — approved for merge.** The finished branch (`08e2873`, FDN-117 In Review) was read directly, not just the report. Both new routes in `services/api/src/auth/http.ts` match the F219/F220 rulings exactly and exceed them — `activate-sole-membership` runs inside a `db.transaction` with a `.for("update")` row lock on the session plus a belt-and-suspenders `isNull(session.activeOrganizationId)` in the final `UPDATE`'s `WHERE` clause, on top of the required re-derivation and read-before-write check. `auth-client.ts` has `organizationClient()` fully removed, back to `[passkeyClient()]`. `shell-bootstrap.tsx`'s full boot sequence was read in full: `hasServerAnswer()` gates both new calls the same way it already gated the F218 sign-in split, and the offline fallback still runs on a genuine network-level failure. In `packages/graph/src/sync-client`, `engine.ts`'s new `#refusal` field is set to `"unauthorized"` from every failure path that can reach it (restart, upload, protected read) and to `"access-revoked"` in the existing stop path, cleared on the next successful connection; `host.ts` tears down a stopped `access-revoked` session on its next `init` instead of leaving Stage 10 to retry inside a dead engine, exactly as the report's micro-decisions claim; `client.ts`'s new `listCachedWorkspaces` and `host.ts`'s new `discoverCachedWorkspaces` correctly reuse the existing `cacheDatabaseName`/`isCacheDatabaseName`/`listIdbDatabases` helpers and the existing `session_hint` table rather than inventing new storage. `Shell.tsx` and `(shell)/layout.tsx`'s final diffs replace the `workspaceName`/`syncStatus` fixtures with real state and expose `state.refusal` via a `data-testid="shell-refusal"` node for tests; `entityName` is removed cleanly everywhere (no dangling reference anywhere in `apps/roster-web`). All eight browser tests in `shell-bootstrap.spec.ts` were read in full: each is a genuine end-to-end run against a real database and, for the offline cases, a real `NetworkSwitch.cut()` TCP-level failure, not a mock — including the two tests directly proving F220's fix (a later sign-in's brand-new session ends up with the same `activeOrganizationId` as every other session that user holds; two active memberships with no selection renders the unresolved holding copy, not a guess). `git diff --stat main stage-9-shell-bootstrap-v3` outside `docs/` touches exactly 13 files, no fixture screen among them. The findings recount was independently reproduced by parsing every `| F<n> |` row of this branch's own `docs/Foundations_Findings.md` (not just diffed against `main`): 166 rows, F54–F220, one gap (F198), 152 closed, 8 open, 6 recorded — an exact match. No discrepancy found anywhere against the report's claims. **Verdict: approved for merge, no further findings.** Codex: `--no-ff` merge `stage-9-shell-bootstrap-v3` into `main`, push both, move `FDN-117` to Done, update this document's Stage status table with the merge commit, then proceed straight into Stage 10 (already briefed below, section 10) on the merged `main`.

## 9. Stage 9 — the real shell bootstrap (briefed, F216 through F220, approved for merge)

Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3 (updated in place for F220 — read it directly, don't reconstruct it from this summary or from the Amendments entries). Scope: `POST /workspace/list-active-memberships` (F219, session-gated, no workspace scope) replacing the `organizationClient()` design that route's finding ruled out; `POST /workspace/activate-sole-membership` (F220, same shape) actually setting `session.activeOrganizationId` for the resolved workspace before the graph client is constructed — independently re-derived server-side, only ever written from `null`; `confirmWorkspaceAdmission` also still sets it server-side for a person's sole active membership at confirmation time (F217); a sign-in path gated on a *confirmed* no-session answer (`error` null or carrying a numeric `.status`), not merely "no data" (F218); a real `createGraphClient` mount, replacing `Shell.tsx`'s fixture props; offline cold start from a single `session_hint` cache, enumerated without a user filter when no session ever resolved (more than one cached workspace is unresolved, not a silent pick); a `401`/`access-revoked` signal exposed at the shell boundary (not rendered — that's Stage 10). No fixture screen is touched. `FDN-114` (Stage 10), `FDN-115` and `FDN-116` (the two abandoned earlier Stage 9 attempts) all stay as-is. **`FDN-117` and branch `stage-9-shell-bootstrap-v3` continue** — neither F219 nor F220 restarted the branch: its preliminary code is sound apart from the calls these two findings add and replace, so both fixes land on the same branch.

**What to check in the Stage 9 report:** that the membership-list route excludes pending and revoked memberships (test both, not just the happy path) and that no `/api/auth/organization/*` call survives anywhere in the diff; that the activation route re-derives sole membership independently rather than trusting the caller, and never overwrites an already-set `activeOrganizationId`; that the full end-to-end path was proven in a real browser — device registration, the shape proxy, and the rendered workspace name all working together, not just the two new routes' own unit/integration tests; that the `confirmWorkspaceAdmission` change is genuinely what closes F217's gap; that a second-workspace creation/join by someone already active elsewhere does not switch them (test, not assertion); that no screen outside the shell's own bootstrap changed; that offline boot was tested for both the single-cache and multi-cache cases, not just reasoned about; that the sign-in-vs-offline split was tested with an actual network-level failure, not a mocked 401 or a mocked empty response standing in for offline; that the refusal-signal shape is documented well enough for Stage 10 to consume without re-deriving it; and that "no session" truly never triggers a workspace call.

## 10. Stage 10 — the Reconnect shell state (briefed, reviewed, approved for merge)

F214's design is ruled (Stage 8 review) and unchanged in substance. Brief: build the single `Reconnect` state in `apps/roster-web` consuming Stage 9's exposed refusal signal (trigger only on `401`/`UNAUTHORIZED` or `access-revoked`, never network failure/timeout/5xx; full-bleed, one **Retry** that re-runs Stage 9's bootstrap, not a literal call re-issue; a missing session routes to sign-in, already Stage 9's job); require a browser test proving cold-start and mid-session render identically and that Retry actually recovers. `FDN-114` is its Linear issue.

**Completion review, 22 September 2026 — approved for merge.** `stage-10-reconnect-shell` at `cf59ed5` (FDN-114, In Review), branched cleanly from the merged Stage 9 `main` (`298afbb`), was read directly against the code, not just the report. `packages/ui/src/ReconnectState.tsx` renders VPS-D004's copy byte-for-byte (\"Reconnect to continue. Vulto needs to verify your session before opening your workspace.\"), full-bleed and centered, no illustration, one primary Retry — confirmed against the spec text itself. `Shell.tsx` gates the entire shell on `state.refusal` (an early return replaces the whole tree with `ReconnectState`, not a partial overlay) and disables `useShortcuts` while refused; the non-refused render path is otherwise untouched. `shell-bootstrap.tsx`'s Retry correctly re-runs the full Stage 9 bootstrap rather than re-issuing a single call, exactly as this document's Stage 9 section already specified — and handles a real race the report's micro-decisions describe accurately: a `refused`/`retryPhase` ref pair keeps an already-refused shell mounted through an unrelated Better Auth session auto-refresh (which would otherwise re-trigger the boot effect and wrongly redirect to sign-in, erasing VPS-D004's cold-start/mid-session distinction), and sequences Retry's own session refetch ahead of the next bootstrap attempt so the two can't race and strand the shell on its \"Opening your workspace…\" holding copy. `host.ts`'s one new branch was read against `engine.ts` directly: an `\"unauthenticated\"` failure sets `#refusal` but schedules no restart timer (unlike the `\"network\"` branch), so a session with no fresh answer never self-heals; the new `session.engine.notifyOnline()` call on re-init is exactly what un-sticks it, and is correctly distinct from the existing `access-revoked` branch's full session teardown-and-recreate. `engine.test.ts`'s new assertions confirm `#refusal` is set on `unauthenticated` and never on a `server`-classified failure (5xx/429 map to `\"server\"`, a timeout to `\"network\"`, per `api.ts`), matching the done-criterion that only `401`/`access-revoked` ever trigger Reconnect. All eight tests in `reconnect.spec.ts` were read in full: real DB/session manipulation, a real `NetworkSwitch.cut()` for the genuine-outage case, real 503/429/timeout session-refresh responses proving an already-open shell stays open, and a literal `outerHTML` byte-for-byte comparison between the mid-session and a cold-reloaded Reconnect render — the done-criterion proven, not asserted. The diff touches exactly 8 files (`ReconnectState.tsx`, its `ui` export, `Shell.tsx`, `shell-bootstrap.tsx`, `host.ts`, `engine.test.ts`, `reconnect.spec.ts`, the stage report); no fixture screen, `Offline.tsx`, spec doc or findings doc changed — correctly, since no finding or spec correction was needed. **Verdict: approved for merge, no further findings.** Codex: `--no-ff` merge `stage-10-reconnect-shell` into `main`, push both, move `FDN-114` to Done, update this document's Stage status table with the merge commit.

## 11. Stage 11 — Multi-Entity and Jurisdiction Foundation (approved for merge)

`VRS-001`'s Feature Register settles the MVP-order question the prior version of section 12 (then section 11) left open: `VRS-F003` sits at position three, directly after `VRS-F002` (RST-33, built in Stage 8) and before `VRS-F004` (Working Calendar). Checked directly against the code before writing this brief: `Entity`, `scoped_to_entity`, and Entity's `OWNERSHIP_REGISTRY` row are already fully registered in `packages/schema/src/registry/{nodes,edges,ownership}.ts` — nothing schema-level is missing — but zero mutations, zero permission-matrix row, and zero bootstrap path exist anywhere; `VRS-F002`'s own `employeeCreate` mutation already hard-requires a valid `entity_id`, so a freshly created workspace cannot create its first Employee today, either.

Four genuine spec gaps surfaced confirming and starting this brief, and all four are ruled the same day, founder-delegated; full reasoning is in `docs/Foundations_Findings.md`, and `VRS-F003`'s own text is corrected to match all four:

- **F221** — `VRS-F003`'s Security Considerations claimed Entity transfer (`employee.setEntity`) is a `VPS-F004`-audited event. `VPS-F004`'s `event_type` column is a closed, access-focused enum (five values, all denial/access/erasure-related); an ordinary, permitted Owner/HR-Admin write of Tier 0 data fits none of them. Ruled: `scoped_to_entity`'s own edge history (`created_by`, `created_at`, `effective_from`, `effective_to`, already required by G02/G03) is the record of every transfer, its actor and its timing — the Silent Audit Log's taxonomy is not extended for it.
- **F222** — G04 requires every workspace to always have at least one Entity, "created automatically during `VPS-F006`'s setup wizard" — but `VPS-F006` is not built and no code path creates a workspace's first Entity at all. Ruled: `services/api/src/graph/founding.ts`'s `writeFoundingRecords` gains a sixth founding record — a default Entity named after the workspace, `jurisdiction: "Global"`, `default_currency: "USD"` — written in the same transaction as the other five, editable afterward via `entity.update`.
- **F223** — `entity.deactivate`'s field-and-version contract was undefined: `VRS-F003` gave Entity both `is_active` and `lifecycle_status: Active | Dissolved` without saying which one deactivation changes, and no version argument to satisfy `VPS-A003` A003-T54. Found by Codex tracing the brief before writing any code (`RST-39`) — Stage 11 correctly stopped there. Ruled: `is_active` is dropped (a `featureOwnedLifecycle`-convention leftover; Entity is `fixedLifecycle`, like `VRS-F004`'s WorkingCalendar/Holiday/WorkingPattern, which carry `lifecycle_status` alone); deactivation transitions `lifecycle_status` to `Dissolved`; `Entity` joins `FEATURE_LIFECYCLE_NODE_TYPES`, and `entity.deactivate(entityId, expectedVersion)` checks the version first (`stale-state`) then G04/G05, exactly as `employee.transitionStatus` already does.
- **F224** — the four new mutations live in the shared `packages/schema/src/mutations/foundation.ts` registry `packages/graph` also consumes; its `OPTIMISTIC_MUTATORS` type requires a client handler for every registered name, and `employee.setEntity`'s handler needs to find the device's currently-open `scoped_to_entity` edge, which the builder read as a second traversal G06 forbids outside `entity.resolveForEmployee`. Found implementing the brief (`RST-39`) — Stage 11 correctly stopped a second time. Ruled: G06 protects only *temporal* (`asOf`-based) resolution; the mechanical, non-temporal "find the open edge" lookup is a different question, already precedented by `org.moveEmployee`'s existing optimistic mutator for `managed_by`. `entity.create`/`entity.update` get ordinary node-write optimistic handlers; `employee.setEntity`'s handler closes the open edge and reopens a new one via the shared `moveEmployeeEdgeId`; `entity.deactivate`'s handler checks only `expected_version`, leaving G04/G05's workspace-wide counts to the server and the sync outbox's existing rejection/undo path.

Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, new Stage 11 section — read it directly, don't reconstruct it from this summary. Server-side only, following Stage 8's own precedent exactly: no file under `apps/` changes. The Entities screen (`VPS-F005`'s host) and the conditional entity selector on the Employee profile both wait for the fixture-migration work already on the follow-up list (section 12) rather than being built piecemeal ahead of it. `RST-39` is its Linear issue (confirm the number before filing, in case another issue landed first).

**What to check in the Stage 11 report:** that `entity.create`/`update`/`deactivate` and `employee.setEntity` each go through the same checks/validate/apply mutation pipeline every other mutation does; that the two distinct deactivation refusals (G04 — sole remaining Active Entity, even with zero employees scoped to it — and G05 — an Active employee remains scoped) are each independently tested; that `entity.resolveForEmployee` is the only code path that ever traverses `scoped_to_entity` directly (G06) — grep the diff to confirm nothing else does; that the founding-record bootstrap commits or rolls back with the other five founding records, never as a separate follow-up write; that the new `MATRIX_OVERRIDES["Entity"]` row is tested against a Team Member and a Manager specifically, not just Owner/HR Admin, and that a test existed which failed before the override and passes after; that `entity.deactivate` rejects a stale `expected_version` before it ever reaches G04/G05 (F223); that nothing anywhere reads or writes a field called `is_active` on Entity (F223 dropped it); that no file under `apps/` appears in the diff; that all four Stage 11 mutations have real optimistic handlers in `packages/graph/src/mutators/foundation.ts`, none of them stubs (F224); that `employee.setEntity`'s handler only looks up the open `scoped_to_entity` edge and takes no `asOf` argument; and that `entity.deactivate`'s optimistic handler contains no G04/G05 count logic, relying on the outbox's rejection path instead.

**Completion review, 22 September 2026 — approved for merge.** `codex/stage-11-multi-entity-jurisdiction` at `fb4ef35` (RST-39, In Review) was read directly against the code, not just the report. `packages/graph/src/mutators/foundation.ts`'s four new optimistic handlers match the F224 ruling exactly: `entity.create`/`entity.update` are ordinary node writes; `employee.setEntity` finds only the currently-open `scoped_to_entity` edge (`effectiveTo === null`, no `asOf`), closes it, and opens a new one with `moveEmployeeEdgeId(mutationId)` — the same id the server independently derives; `entity.deactivate` checks only `expected_version` and carries no G04/G05 logic, with a comment recording why. The three generic optimistic mutators (`createNode`, `updateNodeFields`, `softDeleteNode`) now also refuse any `FEATURE_LIFECYCLE_NODE_TYPES` member, closing the same bypass server-side in `services/api/src/mutations/foundation.ts` — a defense-in-depth addition beyond what the ruling required, and a correct one. `services/api/src/mutations/entity.ts`'s `entity.deactivate` validates in the ruled order — stale version, then G04's active-entity count, then G05's active-employee count — and now runs under `serializable` isolation alongside `org.moveEmployee`, which the two workspace-wide aggregate checks warrant. `founding.ts` writes the sixth founding record (default Entity, `Global`/`USD`) inside the same transaction as the other five. `policy-table.ts`'s `MATRIX_OVERRIDES.Entity` grants Owner/HR Admin Full and Finance Admin/Manager/Team Member Read, exactly the workspace-configuration pattern. The full test suite was read, not sampled: `foundation.test.ts`'s new `setEntity` test proves the historical edge is left untouched while only the open edge closes; `engine.test.ts`'s rewritten outbox test proves a server-refused `entity.deactivate` (`last-active-entity`) reverts the device's optimistic `Dissolved` write back to `Active` through the existing undo path; `entity.integration.test.ts`'s seven tests cover the interceptor, the full permission matrix by role, the version-before-business-rules ordering, both G04 and G05 refusals independently (including the zero-employee G04 edge case), a successful deactivation blocking the generic lifecycle path, and the UK→Pakistan transfer preserving history across 1 March. No conflict markers, no stub handler, no stray `is_active` reference, and no file under `apps/` anywhere in the diff. **Verdict: approved for merge, no further findings.** Codex: `--no-ff` merge `codex/stage-11-multi-entity-jurisdiction` into `main`, push both, move `RST-39` to Done, update this document's Stage status table with the merge commit.

## 12. Stage 12 — Working Calendar and Working Patterns (VRS-F004)

`VRS-001`'s Feature Register confirms `VRS-F004` as the correct next MVP-order feature after `VRS-F003` (Stage 11, merged `96d53eb`) — it blocks all bench, leave and payroll arithmetic. Checked directly against the code before writing this brief: `WorkingCalendar`, `Holiday`, `WorkingPattern`, their ownership rows and their edges (`Entity`→`WorkingCalendar`, `WorkingPattern`→`Employee`, `Holiday`→`WorkingCalendar`, `WorkingCalendar`→`WorkingCalendar` for `supersedes`) are already fully registered in `packages/schema/src/registry/{nodes,edges,ownership}.ts` — nothing schema-level is missing — but no mutation, no permission-matrix row, and no `workingDays.*` API exists anywhere.

Two findings surfaced confirming the brief, both ruled the same day (full reasoning in `docs/Foundations_Findings.md`; `VRS-F004` corrected to match):

- **F225** — Holiday (and `ReducedHoursPeriod`) confirmation is not actually `VPS-F004`-audited; its closed, access-focused `event_type` taxonomy has no category for an ordinary business write, the exact gap F221 already found for Entity transfer. Each node's own `confirmed_at`/`confirmed_by` fields are the record instead.
- **F226** — the spec's G06/G07 materialized working-day index (client-side, Worker-rebuilt, sized against `VRS-F005`'s Bench Forecast render budget) predates the server-authoritative pivot and has no caller yet — `VRS-F005` is not built. Stage 12 computes `workingDays.*` directly against the graph, unindexed; the index itself is deferred in full to whichever stage builds `VRS-F005`.

Tracing the corrected brief before writing any code, Codex stopped a second time on five more contract gaps (`fcfe213`, `RST-40` moved to Blocked) — all ruled the same day, founder-delegated, full reasoning in `docs/Foundations_Findings.md`; `VRS-F004` corrected to match on all five, and this round also corrects F226's own overreach:

- **F227** — no initial working week was named for `Global`, `IN` or `SG`, though every Entity requires an Active `WorkingCalendar` at creation. Ruled: all three default to Monday-Friday, `standard_daily_hours: 8`, written atomically with the Entity by a new shared `createInitialCalendar` helper.
- **F228** — the six new mutations carried no caller base version for A003-T54. Ruled, on F223's own reading (a `lifecycle_status` transition only): `calendar.update`, `holiday.cancel`, `pattern.set` (when superseding) and `pattern.clear` require `expectedVersion`; `holiday.add` and `holiday.confirm` do not.
- **F229** — the stated `pattern.set` overlap check rejected the very replacement it was meant to allow. Ruled: `WorkingPattern` reuses Stage 11's `closeAt` shared-boundary convention; the only real guard is that a new `effective_from` may not precede the pattern it supersedes.
- **F230** — neither the spec nor the brief said whether holidays carry forward across a superseding `calendar.update`. Ruled: still-Active holidays are copied onto the new version; the old version's own holidays stay frozen for historical reads.
- **F231, correcting F226** — F226 wrongly said Stage 12 proves G07's PayRun/LeaveRequest/Contract exemptions directly; none of those three features exists yet. Ruled: Stage 12 proves only G07's live-view default; the three exemptions are recorded as binding rules for whichever stage builds each consuming feature.

Resuming Stage 12 on those rulings, Codex traced the corrected brief into the code and spec before writing any and stopped a third time on three more gaps (`bc21a52`, `RST-40` moved to Blocked again) — all ruled the same day, founder-delegated, full reasoning in `docs/Foundations_Findings.md`; `VRS-F004` corrected to match:

- **F232** — `VRS-F004`'s own setup-flow prose named AE's initial template Monday-to-Friday, contradicting the feature's own acceptance criterion that a Friday is excluded as non-working for an AE entity. Ruled: AE's template is corrected to Sunday-to-Thursday, matching Saudi Arabia's own template; the acceptance criterion was right all along.
- **F233** — F227 made `entity.create` write a calendar and ownership edge atomically server-side, but its existing optimistic mutator was never extended to match, and no portable deterministic ID or shared template logic existed for the client half. Ruled: the optimistic handler now creates the calendar and edge too, from two new deterministic ID derivations and one shared, portable template function both halves call.
- **F234** — `ReducedHoursPeriod` is inline WorkingCalendar data the required Ramadan test depends on, but no named mutation could write one. Ruled: it's edited exclusively through a new optional `reducedHoursPeriods` parameter on `calendar.update`, following the same copy-forward-or-replace shape F230 gave Holiday.

Resuming on those rulings, Codex built the calendar/pattern mutation module for real and reached the first Postgres-backed `pattern.set` integration test, which failed and correctly stopped the stage a fourth time (`e4a9599`, `RST-40` Blocked again, real partial implementation preserved at `7e64708` — schema and graph tests passing, API typechecking clean):

- **F235** — `pattern_for` connects `WorkingPattern` to split-protection `Employee` with no `governingPartitions` declaration in the edge registry, so F136's conservative rule refuses the edge for every role. The exact same class of gap F208 found for `managed_by`/`scoped_to_entity`. Ruled: `governingPartitions: { Employee: "operational" }` declared on `pattern_for`, recorded in `VPS-A002`'s table — which working pattern applies to an employee is operational scheduling data, not compensation.

Unlike the two prior rounds, the branch is **not** restarted this time — it carries real, tested implementation that must be kept. `docs/Foundations_Findings.md`, `VPS-A002`, `docs/Claude_Code_Build_Prompt.md` and this document are reviewer-authoritative and are brought onto the branch from `main` as-is; only `docs/Foundations_Findings.md` conflicts with the branch's own F235-open edit, resolved by taking `main`'s version.

Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, new Stage 12 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here.

**What to check in the Stage 12 report:** that `calendar.update`/`pattern.set`/`pattern.clear` all supersede rather than edit in place (G01, G04); that a newly created Entity has an Active `WorkingCalendar` with no manual step, `Global`/`IN`/`SG` on Monday-Friday and AE on the corrected Sunday-Thursday default (G02, F227, F232); that `entity.create`'s optimistic handler creates a matching calendar and edge offline, proven by a parity test (F233); that an omitted weekday inherits from the entity calendar at read time rather than defaulting to zero at write time (G03); that `pattern.set`'s overlap guard allows a same-day replacement and refuses a backdated one (F229); that a regional holiday is proven against employees in two different locations, not asserted (G05); that `calendar.update`, `holiday.cancel`, `pattern.set` (when superseding) and `pattern.clear` each check `expectedVersion` first, and `holiday.add`/`holiday.confirm` take none (F228); that `calendar.update` copies forward the prior version's still-Active holidays, frozen correctly for historical reads (F230); that `calendar.update`'s `reducedHoursPeriods` parameter is tested both omitted and provided, with no separate reduced-hours mutation anywhere (F234); that `pattern_for` carries its `governingPartitions: { Employee: "operational" }` declaration and `pattern.set`'s edge write is proven against the real interceptor, not mocked (F235); that `holiday.confirm` writes no `VPS-F004` `AuditEntry` anywhere in the diff (F225); that no materialized index, cache table or precomputed row appears anywhere (F226) — grep the diff for anything resembling one; that `workingDays.*` is the only code path in the diff that reads `WorkingCalendar`, `Holiday` or `WorkingPattern` to decide whether a day is a working day (G08); that the report does not claim to prove the PayRun/LeaveRequest/Contract exemptions, only the live-view default (F231); that no file under `apps/` appears in the diff; and that `holiday.importForJurisdiction` is not implemented this stage.

## 13. Stage 13 — The Bench Forecast (VRS-F005)

`VRS-001`'s Feature Register confirms `VRS-F005` as the correct next MVP-order feature after `VRS-F004` (Stage 12, merged `0249d5c`) — it is the product's signature screen and the reason the working-day resolution order Stage 12 built exists at all. Checked directly against the code before writing this brief: `Assignment`, `Project`, `Client`, `GhostResource`, `OpenRole`, `BurnoutAlert`, `FlightRiskSignal` and `WellnessTriggerEvent` are already fully registered in `packages/schema/src/registry/{nodes,edges}.ts` (`assignment_of` already carries `governingPartitions: { Employee: "operational" }`, F136's rule already satisfied) — nothing schema-level is missing — but no mutation, no permission-matrix row, no portable working-day function, and no aggregate disclosure-control mechanism exist anywhere.

Reading `VRS-F005` directly, three genuine architecture gaps surfaced before any brief was written, all ruled the same day, founder-delegated — full reasoning in `docs/Foundations_Findings.md`; `VRS-F005` (and `VRS-F004` for F237) corrected to match:

- **F236** — the spec states three times that the Contextual Intelligence Panel is "entirely local," each time immediately followed by a description of the same traversal surfacing BurnoutAlert and FlightRiskSignal — both registered Tier 2. `benchForecast.get` claims "no network round-trip" while the same section's bench cost is explicitly compensation-derived, and `Employee`'s compensation half is registered Tier 1. The offline acceptance criterion and NFR both claim "no read degradation." Per F199's rewritten architecture, only Tier 0 replicates to the device; Tier 1 and Tier 2 are fetched on demand, never cached. None of this is true for BurnoutAlert, FlightRiskSignal or the bench cost figure. Ruled: the timeline's bars/dates/bench-day counts and the Panel's Tier 0 fields (skills, open roles, references) stay genuinely local and need no change — `Assignment`, `Project`, `Client`, `GhostResource`, `OpenRole` and `effective_billing_rate` are all Tier 0. The Panel's Tier 2 fields become an on-demand server fetch issued when the Panel opens, held in memory only. Bench cost becomes a separate, batched, on-demand call (`benchForecast.getCost`) that never blocks the local render. Offline claims are rewritten to cover Tier 0 reads only.
- **F237** — `VRS-F004`'s G06/G07 deferred a per-employee-per-date materialized working-day index "in full to whichever stage builds `VRS-F005`" (F226/F231), but the index as specified — "materialized in the SQLite-WASM index... rebuilt in the worker" — assumes the retired CRDT-era client materialization pipeline, not the current one. Checked directly: `WorkingCalendar`, `Holiday` and `WorkingPattern` are all Tier 0, already fully present in the device's local Electric-replicated cache with no additional design needed; `wa-sqlite` is retained per F199/F201, now "fed by replication rather than by CRDT materialization." Ruled: no separate index is built. The resolution-order logic Stage 12 already implemented server-side becomes one portable, pure, no-I/O function in `packages/schema` (the same pattern F233 already established for `initialWorkingWeekFor`), called unchanged by `services/api`'s existing `workingDays.*` and by a new client-side query evaluating it directly against the local Tier 0 cache. At 150 employees × 90 cells (13,500 plain-function evaluations over already-local rows, not a graph traversal through a sync protocol), this meets the 200ms/50ms budgets without precomputation — proven at Stage 13's real numbers rather than assumed; a future finding is the right response if profiling says otherwise, not building the original index speculatively now.
- **F238** — `VRS-F005`'s own Security Considerations and four of its ten acceptance criteria require `VPS-A004`'s k-anonymity disclosure-control mechanism (suppression below threshold; the unfiltered aggregate returned when a filter's reduction is smaller than the threshold; a differencing-attempt audit trail). A repository-wide search found no implementation anywhere, by any stage, and the two threshold settings are documented as living in `VPS-F005_Workspace_Configuration_Console`, which is not built. Unlike `VRS-F006`/`VRS-F007` (explicitly non-blocking forward dependencies), this cannot be deferred — it is this feature's own acceptance criteria, not a forward-looking allowance. Ruled: `k_anonymity_minimum` (default 5) and `k_anonymity_minimum_sensitive` (default 8) are added directly to `Workspace`'s schema now, floor of 3 enforced, no settings UI — the Console still owns editing them later, the same "data now, UI later" split F222/F227 already used. One shared, reusable disclosure-control function is built for real in `packages/schema`, general enough that a later feature (`VRS-F048`, `VRS-F058`, `VRS-F059`, `VRS-F060`, `VRS-F071`, `VRS-F072`, `VRS-F011`) calls it rather than reimplementing it, per `VPS-A004`'s own "every aggregate... calls this mechanism" intent. The differencing-attempt audit trail is explicitly deferred as a recorded, tracked limitation (`VPS-F004`'s `AuditEntry.event_type` has no category for an ordinary aggregate read) — not silently dropped, a named follow-up for whichever stage next extends that taxonomy.

Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, Stage 13 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side only, following Stage 8/11/12's own precedent: no file under `apps/` changes. Ghost rows, rate-card resolution and (per F239) Pitch-day exclusion all stay out of scope, each per the spec's own explicit non-blocking allowance for `VRS-F006`/`VRS-F007`/`VRS-F009`/`VRS-F010`. `RST-41` is its Linear issue.

**F239, found tracing the brief into the query layer before implementation, 23 September 2026.** `VRS-F005`'s bench-time formula and one acceptance criterion require excluding days carrying Pitch-categorized time, but `TimesheetEntry`, `Pitch` and the `logged_against` edge connecting them — owned by `VRS-F009` (MVP order F18) and `VRS-F010` (F08), both after `VRS-F005` (F03) — have no mutation anywhere in the repository, only registry entries. Codex correctly stopped rather than guessing (`codex/stage-13-bench-forecast` at `189cf6c`, `RST-41` moved to Blocked). Ruled the same day: the same partial-forward-dependency treatment the spec already gives `VRS-F006`'s rate resolution and `VRS-F007`'s Ghost rows, simply never extended to `VRS-F009`'s Pitch taxonomy. `VRS-F005`'s Partial forward dependencies line, G02, the bench computation prose, the Pitch acceptance criterion, the Out of Scope list and the Decisions Recorded note are all corrected; Stage 13's own query contract needed no change — it already omitted `TimesheetEntry` correctly.

**F240, found resuming Stage 13 on F239's ruling, before any product code was written, 23 September 2026.** `assignment.cancel(assignmentId)` transitions Assignment from `Active` to `Canceled` but carries no caller base version, violating `VPS-A003` A003-T54 exactly as F223 established for `entity.deactivate` and F228 established for `holiday.cancel`/`calendar.update`/`pattern.set`/`pattern.clear`. Codex correctly stopped a second time rather than guessing (`codex/stage-13-bench-forecast` at `67852a9`, `RST-41` moved to Blocked again). Ruled the same day, on F223/F228's own precedent: `assignment.cancel(assignmentId, expectedVersion)`, version checked first (`stale-state`), then the transition; the optimistic handler mirrors `holidayCancel`'s exact shape. `assignment.create`/`update` are unaffected. `docs/Foundations_Findings.md`, `VRS-F005`, `docs/Claude_Code_Build_Prompt.md` and this document are reviewer-authoritative and are brought onto the branch from `main` as-is; only `docs/Foundations_Findings.md` conflicts with the branch's own F240-open edit, resolved by taking `main`'s version — the same resolution Stage 12 used for F235 and Stage 13 already used once for F239.

**F241, found resuming Stage 13 a third time on F240's ruling, before any product code was written, 23 September 2026.** `VRS-F005`'s `benchForecast.get` contract claims `aggregateUtilization`/`cohortSize` resolve "entirely local... no network round-trip," while Stage 13 Do item 4 already computes the same aggregate server-side through the new disclosure-control function, returning it from "`benchForecast.get`'s aggregate portion" — two reviewer-authoritative documents naming the same return value as both local and server-computed, with no endpoint named for the server side. Codex correctly stopped a third time rather than guessing (`codex/stage-13-bench-forecast` at `bfc6714`, `RST-41` moved to Blocked again). Ruled the same day: `VPS-A004`'s own "Where enforcement runs" section prohibits "a client-side visibility rule" as a second answer to who may read what, and names the Bench Forecast's filtered utilization specifically as the reason its disclosure-control mechanism exists; one of its three differencing-protection rules requires a server-side Silent Audit Log write no device can perform. The contradiction resolves toward the server: a new `benchForecast.getAggregate(workspaceId, window, filters?)` endpoint is added, gated by the permission interceptor and `applyDisclosureControl`, issued after the local render and never blocking it — the same non-blocking shape F236 already gave `benchForecast.getCost`. `benchForecast.get` narrows to `rows` only. The spec's 50ms filter budget, offline acceptance criterion, NFR list, G06 and Security Considerations are all corrected to treat local filter-matching and server-side aggregate disclosure as two separate operations rather than one local claim. `docs/Foundations_Findings.md`, `VRS-F005`, `docs/Claude_Code_Build_Prompt.md` and this document are reviewer-authoritative and are brought onto the branch from `main` as-is; only `docs/Foundations_Findings.md` conflicts with the branch's own F241-open edit, resolved by taking `main`'s version — the same resolution Stage 12 used for F235 and Stage 13 has now used twice, for F239 and F240.

**F242, found resuming Stage 13 a fourth time on F241's ruling, before any product code was written, 23 September 2026.** `benchForecast.getAggregate(..., filters?)` and Stage 13's local row query both take a `filters` argument, but `VRS-F005`'s Filtering section names skill, seniority, department, entity and availability window in prose only — no field names, value types, AND/OR combination rules, or availability-window meaning are defined anywhere, so the local rows and the server aggregate have no guarantee of deriving the same cohort. Codex correctly stopped a fourth time rather than guessing (`codex/stage-13-bench-forecast` at `1a45be1`, `RST-41` moved to Blocked again). Ruled the same day: passing the local query's matched IDs to `getAggregate` is not an available fix, since F241 already ruled the client cannot be a second authority for who is visible. A single `BenchForecastFilters` type (AND across its five fields, OR within a field) and one portable `matchesBenchForecastFilters` function in `packages/schema` — alongside `resolveWorkingDay` (F237) and `applyDisclosureControl` (F238) — are called identically by the local query and `benchForecast.getAggregate`; availability reuses G02's own bench-day derivation rather than a second definition; `getAggregate` derives both the filtered and unfiltered disclosure-control cohorts itself, from the caller's own authorized base cohort, never from client-supplied IDs. `docs/Foundations_Findings.md`, `VRS-F005`, `docs/Claude_Code_Build_Prompt.md` and this document are reviewer-authoritative and are brought onto the branch from `main` as-is; only `docs/Foundations_Findings.md` conflicts with the branch's own F242-open edit, resolved by taking `main`'s version — the same resolution Stage 12 used for F235 and Stage 13 has now used four times, for F239, F240 and F241.

**What to check in the Stage 13 report:** that `assignment.create`/`update`/`cancel` go through the same checks/validate/apply pipeline every other mutation does; that the 100% capacity constraint is tested against a genuinely overlapping date range naming the current total and the attempted addition; that `resolveWorkingDay` is a single shared implementation called identically by `services/api`'s existing `workingDays.*` and the new client-side bench query — grep the diff for a second copy of the resolution order; that the client-side bench query is proven offline with the network cut, not merely coded to look local; that `benchForecast.getCost` and `benchForecast.getAggregate` are both separate calls that never block `benchForecast.get`'s local render; that `benchForecast.getCost` returns `null` (never a `billing_rate_default` substitute) for an unauthorized caller; that `benchForecast.getAggregate` is gated by the permission interceptor and never resolves offline (F241); that `matchesBenchForecastFilters` is one implementation called by both the local query and `benchForecast.getAggregate`, and that `getAggregate` never accepts a client-supplied employee-ID list as its cohort (F242); that `applyDisclosureControl` is proven against both of `VPS-A004`'s own worked examples (four-person suppression at the default threshold, and the twelve-to-eleven differencing-protection case) as two independent tests; that the two new `Workspace` k-anonymity fields enforce the floor of 3, not just document it; that the Contextual Intelligence Panel's Tier 2 fetch is proven against the real interceptor with a Manager who does and does not manage the subject employee; that `WellnessTriggerEvent` appears nowhere in the diff's query code; that the five new `MATRIX_OVERRIDES` rows (`Assignment`, `Project`, `Client`, `GhostResource`, `OpenRole`) each have a failing-before/passing-after test against a Team Member and a Manager specifically; that `assignment.create`'s optimistic handler carries no capacity-constraint logic, relying on the outbox's rejection path instead; that no file under `apps/` appears in the diff; and that neither a `GhostResource` write path nor a rate-card lookup is implemented this stage.

**Stage 13 review — closed, 23 September 2026.** Reviewed directly against the finished code on `codex/stage-13-bench-forecast` (tip `ab2708b`, later `9f553ef`), not merely the completion report's word. Confirmed: `resolveWorkingDay` (F237) is one pure implementation in `packages/schema/src/working-day.ts`, called unchanged by `services/api`'s `working-days.ts` (renamed `resolvedDayOn`, delegating rather than reimplementing) and by the new local bench query — no second copy of the resolution order anywhere in the diff. `assignment.cancel` checks `expected_version` before the lifecycle-status transition, both server-side and in the optimistic handler (F240); optimistic `assignment.create` carries no capacity logic, matching the F224 precedent. `matchesBenchForecastFilters` (F242) is one implementation in `packages/schema/src/bench-forecast.ts`, imported unchanged by the local query and `services/api/src/permission/bench-forecast-queries.ts`; `benchForecast.getAggregate`'s input schema carries no employee-ID field — the server derives both disclosure-control cohorts itself via `listEmployees` under the caller's principal. `applyDisclosureControl`/`readKAnonymityThreshold` (F238) enforce the floor of 3, and a real Postgres-backed integration test proves both the suppression path and the differencing-protection substitution against genuine workspace data, alongside the 5/8 defaults. The capacity-conflict rejection reason names both the current total and the attempted addition on a genuinely overlapping range. `getCost` returns `null` to an unauthorized caller and correctly splits a calendar-year-boundary window across two different working-days-in-year denominators. The five new/changed `MATRIX_OVERRIDES` rows (`Project`, `Client`, `GhostResource`, `OpenRole`, `Assignment.manager`) each have direct unit coverage against both Manager and Team Member. One gap found: the Contextual Intelligence Panel's protected-field integration test exercised Manager, an unrelated Manager and Owner, but never HR Admin — a missing assertion, not a policy defect (`FlightRiskSignal.hr-admin: FULL_ANY()` was already correct and untouched by this stage). Not a spec ambiguity worth a founder ruling; sent back as a one-line fix rather than a formal finding, closed at `9f553ef`, reverified clean. Approved with no further findings and merged `--no-ff` as `d9b6d74`. `RST-41` moved to Done.

## 14. Stage 14 — Rate Card Engine (VRS-F006)

`VRS-001`'s Feature Register confirms `VRS-F006` as the correct next MVP-order feature after `VRS-F005` (Stage 13, merged `d9b6d74`) — bench cost and margin arithmetic cannot be trusted with one flat rate per person, and this is the bootstrap that fixes that before Accounts exists. Checked directly against the code before writing this brief: `RateCard` and `RateCardLine` are already fully registered in `packages/schema/src/registry/nodes.ts` (Finance-restricted, Tier 1), the `governed_by` (Assignment → RateCard) and `supersedes` (RateCard → RateCard) edges are already registered in `edges.ts`, and `RateCard` already carries Cross-Suite ownership alongside `Invoice` — nothing schema-level is missing — but no mutation, no rate-resolution logic, and no edge write path exist anywhere.

Five findings surfaced across briefing and implementation, all ruled the same day each was raised, founder-delegated — full reasoning in `docs/Foundations_Findings.md`; `VRS-F006` corrected to match:

- **F243** — `VPS-A002`'s edge registry declares `governed_by: Assignment → RateCard`, owned by this feature and backed by real registry code, but `VRS-F006`'s own contracts described resolution purely against Assignment's scalar `rate_card_id` field, with no mutation ever writing the registered edge — leaving `rateCard.usageCount` and the version-history panel's active-assignment count with no traversal path, which A002-T03 already forbids outright as an unmaterialized FK join (the same class of gap FDN-75 closed once before, for `assigned_to`). Ruled: Assignment keeps `rate_card_id` as its own resolution field, unchanged from Stage 13; `assignment.setRateCard`/`assignment.create` additionally write the registered `governed_by` edge, mirroring `assignment_of`/`assigned_to`'s existing dual scalar-plus-edge shape, managed manually — close the prior active edge, open the new one — the same way `governed_by_calendar` is already managed by `calendar.ts`, since `governed_by` carries no `historyPolicy`.
- **F244** — `VRS-F006`'s NFRs, System States and Depends-On line described RateCard/RateCardLine as syncing to Owner/Finance Admin/HR Admin devices and aging out of a Tier 1 window — `VPS-A003` says Tier 1 is memory-only, fetched on demand, never persisted to any device, and withdraws the retention window entirely. The identical class of stale local-first language F236 already corrected once in `VRS-F005`, simply never re-swept in this document. Ruled: corrected to fetch-on-demand phrasing throughout; "Aged out" marked Not applicable, the same answer `VRS-F005` already gives its own Tier 2 Panel content.
- **F245** — Codex traced the corrected brief into the mutation registry before writing any code and found `rateCard.update`'s own signature carries no `currency`/`name` parameter despite the brief calling `currency` caller-supplied on update, and that marking the prior card `is_active: false` on supersession carried no `expectedVersion` despite `VPS-A003` A003-T54 requiring one for every state transition. Ruled: `rateCard.update(rateCardId, expectedVersion, lines)`, version checked first — `stale-state` before anything else — on `calendar.update`'s own precedent (F228); `name`/`currency` are never accepted by `update` and copy forward from the prior card unchanged; the prior card's `is_active: false` transition is a permitted lifecycle-only exception to G02, the same shape Stage 12's `WorkingPattern` supersession already uses.
- **F246** — found in the same trace: card-bearing `assignment.create` was made responsible for a `governed_by` edge write and a Tier 1 `RateCardLine` read, but Stage 13's existing optimistic handler for it was never revisited, despite RateCard/RateCardLine being uncached Tier 1 data (F244). Ruled: the `governed_by` edge is written exclusively in each mutation's server-side `apply()` step, never optimistically — nothing on-device ever reads the edge (RateCard/RateCardLine are never cached, the local Bench Forecast query needs no rate data, and `rateCard.usageCount`/version-history are server-only reads per F243), so there is no local consumer to serve and no reason to risk a speculative write. `assignment.create`'s optimistic handler is unchanged from Stage 13: it already writes `rate_card_id` as a plain scalar and always defaults `effective_billing_rate` to `billing_rate_default` locally, corrected once the server confirms — the same "deliberately server-only, no client precompute" discipline F224 established for capacity and F241 established for disclosure control. No conditional `onlineOnly` mechanism is needed.
- **F247** — found resuming Stage 14 a second time, once Codex reached the RateCardLine lookup itself: `rate_card_id`/`seniority_level` are protected Tier 1 fields with no edge and no index, but `rateCard.getPreview` and `resolveAssignmentRate` both need a point lookup by `(rateCardId, seniority)`, and `readProtected` only accepts node ids — never a predicate over decrypted content. The only alternative was a workspace-wide decrypt-and-filter scan, exactly what A002-T03 already forbids (the same rule F243 was closed on for Assignment-side `rate_card_id`). Ruled: `RateCardLine.line_id` is generated deterministically from `(rate_card_id, seniority_level)` — the same `deterministicUuid(seed)` scheme `services/api/src/auth/membership-edge-ids.ts` already uses for `membership_of`/`membership_in`, and F233's `initialCalendarId` already uses for `WorkingCalendar` — so both callers compute the id they need instead of searching for it. No edge, no index, no new mechanism; G05 ("at most one line per seniority") becomes automatic, since a duplicate seniority always computes the identical id and overwrites the same fragment.

Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, new Stage 14 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side only, following Stage 8/11/12/13's own precedent: no file under `apps/` changes. `RST-42` is its Linear issue.

**What to check in the Stage 14 report:** that `rateCard.update` creates a genuinely new versioned node linked by `supersedes`, never editing the prior version in place (G02); that `rateCard.update` rejects a stale `expectedVersion` before creating that new node, not after (F245); that `rateCard.create` accepts `name`/`currency` while `update` never does, with `update`'s new version carrying both forward from the prior card unchanged (F245); that `RateCardLine.line_id` is computed with `deterministicUuid`, not a random id (F247); that a duplicate `seniority_level` within one write replaces rather than doubling a `RateCardLine`, by construction of the id scheme (G05, F247); that `rateCard.getPreview` and `resolveAssignmentRate` both reach a RateCardLine via a single computed-id `readProtected` call, never a loop or an enumeration over a card's lines (F247); that `resolveAssignmentRate` is one implementation called by all four sites that can trigger it (`create`, `setRateCard`, `setRateOverride`, `clearRateOverride`); that `effective_billing_rate` is written atomically with its triggering mutation, never recomputed retroactively when a card is later superseded (G04); that the `governed_by` edge write closes the prior active edge before opening a new one, proven with a caller that changes cards twice, not once (F243); that the edge write appears only inside each mutation's server-side `apply()`, never in `OPTIMISTIC_MUTATORS` (F246); that `assignment.create`'s optimistic-handler diff is empty or near-empty against its Stage 13 form (F246); that `rateCard.usageCount` and the version-history count genuinely traverse `incoming(..., "governed_by")` rather than scanning Assignment rows (F243); that RateCard/RateCardLine resolve through the existing Finance-restricted default with zero new `MATRIX_OVERRIDES` rows; that `rateCard.create`/`update` are `onlineOnly: true` with no optimistic mutator (F244); that `assignment.setRateOverride`'s optimistic handler always resolves locally (rule 1 needs no RateCardLine) while `setRateCard`'s and `clearRateOverride`'s leave `effective_billing_rate` untouched, never guessed, whenever the true resolution needs a RateCardLine the device cannot read; and that no file under `apps/` appears in the diff.

**Reviewed directly against the finished code, approved, merged `--no-ff` as `04f2a7a`.** Every item above was independently checked against real Postgres-backed integration tests, not the completion report alone: the stale-`expected_version` rejection creates no third card version; the prior card's protected content (name, currency) is byte-unchanged after an update and only `is_active` flips; a duplicate seniority within one `create` collapses to exactly one `RateCardLine` node; `getPreview` for a seniority with no line returns `null`; two consecutive `assignment.setRateCard` calls leave exactly one active `governed_by` edge and two closed ones, and `rateCard.usageCount` counts only the open ones; an existing Assignment's `rate_card_id` and `effective_billing_rate` are untouched by an unrelated `rateCard.update`; `MATRIX_OVERRIDES["RateCard"]`/`["RateCardLine"]` are directly asserted `undefined` against the live table, with a full owner/hr/finance/manager/team `authorizeRead` matrix over both node types; `assignment.create`'s optimistic handler diff is empty against Stage 13's; the three new Assignment optimistic handlers are tested including the case where clearing an override with a card still set correctly leaves `effective_billing_rate` unchanged rather than guessing; `rateCard.create`/`update` have no `OPTIMISTIC_MUTATORS` entry and `applyOptimistic` returns a no-op rather than rejecting for an `onlineOnly` mutation with no handler; and no file under `apps/` appears in the diff. One additional, out-of-brief fix was reviewed and accepted: `services/api/src/permission/reader-set.ts` had a dormant bug where a Finance-restricted `team-member` "own"-scope cell with no resolvable subject made the *entire* reader-set resolution unconditionally unresolvable, rather than contributing zero readers — never previously exercised because RateCard is the first Finance-restricted fixed-class node any stage has actually built mutations and reads for. The fix is scoped correctly: it relaxes resolution only for node types with no registered subject concept at all (not `Employee`, not any type with a registered subject exclusion), and the pre-existing test proving `Employee`'s fail-closed behavior with a null subject still passes unchanged. `RST-42` moved to Done.

## 15. Stage 15 — Ghost Resources (VRS-F007)

`VRS-001`'s Feature Register confirms `VRS-F007` as the correct next MVP-order feature after `VRS-F006` (Stage 14, merged `04f2a7a`) — named capacity placeholders that appear in the Bench Forecast as fully formed rows before a real person is hired. Checked directly against the code before writing this brief: `GhostResource` and `OpenRole` are already fully registered in `packages/schema/src/registry/nodes.ts` (both Tier 0), the `placeholder_for` and `promoted_to` edges are already registered in `edges.ts`, `GhostResource → Employee, Promoted` is already the registered conversion in `conversions.ts` (F88's prior ruling), `employee_type: z.enum(["Employee", "Ghost"])` is already live on Employee in `packages/schema/src/employee.ts`, and `GhostResource`/`OpenRole` already carry explicit Stage 13 `MATRIX_OVERRIDES` rows — nothing schema-level is missing. But no mutation, no read query, and no promotion transaction exist anywhere.

Seven findings surfaced across briefing and implementation, all ruled the same day each was raised, founder-delegated — full reasoning in `docs/Foundations_Findings.md`; `VRS-F007` corrected to match:

- **F248** — `VRS-F007`'s Depends-On section omitted `VRS-F028` (Recruitment Pipeline), although the optional `openRoleId` on `ghostResource.create`, `ghostResource.linkOpenRole` and the registered `placeholder_for` edge all target `OpenRole` — a node type with no mutation anywhere, owned by a much-later MVP feature. Ruled: the same F239 treatment — a Partial forward dependencies line added; the mechanism is built now regardless, simply unreachable until `VRS-F028` lands.
- **F249** — `ghostResource.promote`'s contract named a second, unspecified path (`{ existingEmployeeId }`, linking to a separate, already-existing Employee node) with no acceptance criterion, G-spec or edge-reconciliation mechanics defined anywhere for it. Ruled: Stage 15 builds the fully-specified path only (`{ fullName, email, employmentType, startDate }`, filling the Ghost's own linked Employee node in place); `existingEmployeeId` is deferred and rejected with a clear "not yet supported" error rather than a half-built cross-node merge invented on no real grounding.
- **F250** — found resuming Stage 15 once Codex traced the corrected brief into the mutation registry, before any product code was written: `ghostResource.cancel`/`promote` transition `GhostResource.lifecycle_status` but carried no `expectedVersion` (`VPS-A003` A003-T54), and the brief's "the transition guard gives G07's exclusivity for free" claim only holds for sequential requests — `services/api/src/mutations/pipeline.ts` uses default transaction isolation unless a mutation is explicitly placed on its serializable list, so two concurrent `promote` calls could both read `Active` and proceed. Ruled: both mutations gain `expectedVersion` (F228/F245's own precedent); `ghostResource.promote` (a genuinely multi-row transaction) joins the serializable list, `ghostResource.cancel` (single-row) does not; a version mismatch on `promote` returns G07's actor-and-time rejection when the cause is an existing promotion, the ordinary `stale-state` rejection otherwise.
- **F251** — found resuming Stage 15 a second time, before any product code was written: the brief claimed `GhostResource`/`OpenRole` had zero `MATRIX_OVERRIDES` entries and resolved through the `Standard` default, but Stage 13 (`f2d928d`) had already added and tested explicit overrides for both, and the default's relationship-scoped cells can't resolve a subject for either node type — deleting the overrides would drop Manager/Team Member to `none`, not read. The existing override and the brief's own Done criteria also both left Manager read-only, contradicting `VRS-F007`'s own Manager-create flow, and `authorizeWrite` requires a literal `full` outcome with no per-mutation gate anywhere in this codebase. Ruled: the Stage 13 overrides stay; `GhostResource`'s `manager` cell alone is extended to `FULL_ANY()`; `team-member` and `OpenRole` are unchanged; the resulting Manager write access to promote/cancel is accepted as this codebase's node-type-scoped permission model, matching `VRS-F007`'s own Security Considerations; the shared policy test is split so `GhostResource` gets its own dedicated assertions.
- **F252** — found resuming Stage 15 a third time: the brief's Do item 5 required a successful-promotion `VPS-F004` audit entry, but `VPS-F004` logs successful access only at Tier 1/3, GhostResource and Employee are both Tier 0, and the closed five-value event taxonomy has no ordinary successful-operation category — the identical gap F221 (Entity transfer) and F225 (Holiday confirmation) already closed. Ruled on that precedent: no taxonomy extension, no `appendAudit` call anywhere in Stage 15; `GhostResource`/Employee's own `updated_by`/`updated_at` and the `promoted_to` edge's `created_by`/`created_at` (created once, never modified, G03) — all mandatory per `VPS-A002` — already record who promoted the Ghost and when.
- **F253** — found resuming Stage 15 a fourth time: `employeeOperationalRecord`'s required-field input (`employee_code`, `full_name`, `email`, `employment_type`) can't be satisfied by `ghostResource.create`'s parameters, and promotion never supplied `employee_code` either, so no valid Ghost or promoted-real Employee record could be built. Ruled: a second, Ghost-only `ghostEmployeeCreateFieldsSchema`/`ghostEmployeeOperationalRecord` pair nulls exactly those four fields, leaving the real-Employee schema and constructor untouched; `contracted_hours` defaults to 40 like real creation already does; promotion's contract gains a required `employeeCode` and optional `contractedHours`, validated with `employee.create`'s own existing `requireUnique` helper rather than a new mechanism.
- **F254** — found once real implementation (`dae1d01`, preserved) reached its first real permission-interceptor integration test: `promoted_to` (GhostResource → Employee) declares no `governingPartitions` entry, and Employee is tier-split, so the interceptor structurally refuses every promotion with `reason: "role"` regardless of caller role — the identical class of gap F208 found for `managed_by`/`scoped_to_entity` and F235 found for `pattern_for`. Ruled on that precedent: `promoted_to` is declared `{ Employee: "operational" }` in `packages/schema/src/registry/edges.ts` and `VPS-A002`'s governing-partitions table, matching the same reasoning already used for six other Employee-endpoint edges — promotion writes only Tier 0 identity fields, never compensation.

Also recorded, not a new finding: `VRS-F005`'s own text already states "`VRS-F007` supplies Ghost rows; until it exists, none are rendered" (F239's closing text) — Stage 13's Bench Forecast row query correctly implements that deferral today with an `employee_type !== "Ghost"` exclusion. Now that `VRS-F007` exists, the brief removes that exclusion, per `VRS-F005`'s own stated contract.

Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, new Stage 15 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side only, following Stage 8/11/12/13/14's own precedent: no file under `apps/` changes. `RST-43` is its Linear issue.

**What to check in the Stage 15 report:** that `ghostResource.create` writes both nodes atomically, with no partial-commit path (G01); that `ghostResource.cancel`/`promote` both check `expectedVersion` first (F250/A003-T54); that `"ghostResource.promote"` is genuinely on `pipeline.ts`'s serializable-isolation list and `"ghostResource.cancel"` is genuinely not (F250); that the promotion race test issues two genuinely concurrent calls, not two sequential ones, and that a stale version caused by something other than a prior promotion still gets the ordinary `stale-state` rejection rather than the actor-and-time one; that Assignment/skill-edge preservation across promotion is proven by identity, not merely by absence of an error (G06); that `ghostResource.promote` has no `OPTIMISTIC_MUTATORS` entry (mirrors F246's check); that the Bench Forecast change is a deletion of the existing `Ghost` exclusion, not a new parallel row-generation path (G04/G05); that the real-employee `aggregateUtilization` figure is proven unaffected by Ghost presence; that `ghostContribution`'s denominator is the real cohort's capacity, not a Ghost-only one (G08); that disclosure-control suppression covers both figures together; that `MATRIX_OVERRIDES["GhostResource"].manager` is `FULL_ANY()` while `"team-member"` and every `OpenRole` cell are unchanged from Stage 13, proven by `GhostResource`'s own dedicated policy test rather than the old shared assertion (F251); that no `appendAudit` call appears anywhere in `ghostResource.create`/`.cancel`/`.promote` — grep the diff (F252); that a created Ghost's Employee node has `employee_code`/`full_name`/`email`/`employment_type` exactly `null` and `contracted_hours` exactly `40`, and that promotion rejects `duplicate-code`/`duplicate-email` via the same `requireUnique` helper `employee.create` uses (F253); that `promoted_to` carries `governingPartitions: { Employee: "operational" }` in `packages/schema/src/registry/edges.ts` and that the real integration test actually exercises the interceptor rather than mocking it (F254); and that no file under `apps/` appears in the diff.

---

## 16. Stage 16 — Capacity Conflict Resolution (VRS-F008)

`VRS-001`'s Feature Register confirms `VRS-F008` as the correct next MVP-order feature after `VRS-F007` (Stage 15, merged `4081e9f`) — the resolution flow that fires when `VRS-F005`'s hard 100% constraint rejects a write, plus a deliberate, logged override and a periodic safety-net sweep. Checked directly against the code and against the suite's own architecture documents before writing this brief: `Assignment` is single-partition Tier 0, not tier-split, so no governing-partition concern applies; `assignmentCreate`'s `apply()` already writes `capacity_override_reason`/`_by`/`_at` as `null` on every create — pre-scaffolded for exactly this feature; `validateCapacity` already throws `ConflictError` above 100% but aggregates via a plain calendar-day iterator; `resolveWorkingDay`/`resolvedDayOn` (F237/G08) already exist as the one established working-day mechanism; `assignment.update` deliberately carries no `expectedVersion` — an intentional, already-approved design, not a gap, contrasted with `assignment.cancel`, which does check one.

Eight findings surfaced checking this feature directly against the code and against `VPS-A006` (the suite's own job-scheduling architecture document) before, during and after product implementation, all closed the same day — seven founder-delegated, one (F263) traced directly to existing precedent in the codebase's own permission layer — full reasoning in `docs/Foundations_Findings.md`; `VRS-F008` and `VRS-F005` corrected to match:

- **F255** — the override gate assumes a Manager can already write Assignment, but `packages/schema/src/policy/policy-table.ts`'s `MATRIX_OVERRIDES` gives `Assignment.manager` only `READ_ANY()`, introduced without review by Stage 13's `f2d928d` and contradicting `VPS-A004`'s own Permission Matrix table (`| Assignment | Full | Full | Read | Full | Read |`), which specifies unscoped Full. Ruled: `Assignment.manager` corrected to `FULL_ANY()`, matching `VPS-A004`'s table exactly — a one-line code fix, no other cell touched.
- **F256** — `VRS-F008`'s Technical Architecture claims the conflict computation is "exactly the aggregation `VRS-F005`'s constraint already performs, reused rather than reimplemented," and G02 requires working-day-aware overlap, but the existing `validateCapacity` aggregates plain calendar days with no working-day filter — reusing it unmodified would carry that gap straight into new code. Ruled, extending F237/G08's own precedent: `validateCapacity` gains a working-day filter through the existing `resolveWorkingDay`/`resolvedDayOn` mechanism; the new read query and sweep reuse the corrected function directly rather than reimplementing the sum, restoring "reused, not reimplemented" as fact.
- **F257** — the identical shape F221/F225/F252 already ruled on: "Overrides are audited per `VPS-F004`" names no event the closed five-value taxonomy actually has for an ordinary successful Tier 0 write. Ruled on that precedent: no `appendAudit` call; the already-scaffolded `capacity_override_reason`/`_by`/`_at` fields are the record.
- **F258** — the identical shape F248 already ruled on: the Depends-On section never names `VPS-A006`, the architecture document that is the actual authority for the sweep's "four-hourly cadence," and neither `services/jobs`/BullMQ nor `VRS-F012` (the sibling feature the cadence claims to match) exists anywhere in the repository — `VRS-F012` itself sits four stages later in MVP order and is not yet built either. Ruled on F248's own precedent: the sweep ships now as the complete, callable read query G06 already specifies (it writes nothing); its periodic BullMQ trigger is deferred to whichever stage builds `VPS-A006`'s job queue, named as a Partial forward dependency rather than silently assumed buildable.
- **F259** — found resuming Stage 16 once Codex traced the corrected brief into the existing server-authoritative mutation and device-cache boundaries, before any product code was written: `VRS-F008`'s own NFRs require `conflictCheck.evaluate` to return within 100ms from the local graph and say computation, panel and override all function fully offline, but this brief's original Do items 4 and 9 defined `conflictCheck.evaluate` as a server query and marked `overrideAndProceed` `onlineOnly` with no optimistic handler — observably opposite behaviors. Ruled: the brief, not the spec, was wrong. `services/api/src/mutations/assignment.ts`'s own `assignmentCreate` optimistic handler already carries the comment "capacity is deliberately server-only," and performs no local capacity check at all — precedent for a local, best-effort `conflictCheck.evaluate` (built like `packages/graph/src/queries/bench-forecast.ts`'s own local query, sharing only the pure `resolveWorkingDay` function with the unchanged, still-authoritative server-side `validateCapacity`) and for `overrideAndProceed` to get a genuine optimistic handler mirroring `assignmentCreate`'s own, using a locally-resolved `managed_by` check the panel's own "the option does not render" behavior already needs client-side, re-verified authoritatively server-side on write.
- **F260** — found tracing F259's own corrected local-execution contracts into the device-cache APIs, before any product code was written: neither `conflictCheck.evaluate`'s input nor `MutatorContext` carried the caller identity and role facts `callerCanEdit` and the local Manager/Owner gate need. Ruled: the Manager half needed no new capability — `OptimisticCache`'s existing `edgesFrom`/`getNode` point-access surface already answers it, the same edge direction `interceptor.ts` reads server-side. The Owner half needed one small, precedented extension — `MutatorContext` gains `roles: readonly WorkspaceRole[]`, mirroring the server's own `CurrentWorkspaceSession` bundle, reusing the already-portable `deriveEffectiveRoles` (`@vulto/schema`) rather than a second mechanism. **(F261 revised this delivery mechanism — see below; the underlying conclusion stands.)**
- **F261** — found tracing F260's own `InitPayload.roles` field to its two production `createGraphClient` callers, before any product code was written: neither the shell bootstrap nor the sync harness (both under `apps/`, off-limits this stage) has a role value to supply. Ruled: `InitPayload` never needed the field — `WorkspaceMembership`'s already-replicated `role` string (decoded with `parseWorkspaceRoles`) is reached via its `membership_of` edge to the caller's own `User` node, the identical shape `managed_by` already gives Manager, read with `OptimisticCache.edgesTo`/`getNode` (mutator) or `localNodes`/`localEdges` (query) — no enumeration, no network call, no `apps/` file touched. Supersedes F260's `InitPayload.roles`/`MutatorContext.roles` design.
- **F262** — found resuming implementation once Codex reached the first real Postgres-backed `overrideAndProceed` integration test: `assignment_of`/`assigned_to` are mandatory edges on every Assignment write, but `edgeRoleDecision` (`services/api/src/permission/interceptor.ts`) requires unscoped Full on both endpoint node types with no row context at all — deliberate FDN-92 architecture, never before exercised by a role holding anything less than unscoped Full on both endpoints. Manager's `Employee:operational` grant is row-scoped (`Full (direct reports)`) and its `Project` grant is `READ_ANY()`, so neither edge could ever be authorized for a Manager, regardless of whether the target employee genuinely reports to them. Unlike F255–F261, no existing precedent decided this one — put directly to the founder. Ruled: two additive mechanisms, neither widening an existing grant. `edgeRoleDecision` gains real per-endpoint row context (`WriteTarget`'s edge variant gains optional endpoint node ids, threaded through the same `RowScopeContext` shape `decideRead` already uses for reads), making Manager's already-documented `Full (direct reports)` cell resolve correctly for `assignment_of`. A new opt-in `readSufficientEndpoints` registry field, styled on `governingPartitions`, is declared only on `assigned_to`'s own registration (`{ Project: true }`) — Read, not Full, suffices on that one endpoint, mirroring `VPS-A004`'s own read-traversal rule; `Project`'s `manager` cell stays `READ_ANY()`, unchanged everywhere else.
- **F263** — found reviewing the finished implementation directly against every prior ruling, before merge: `sweepOvercommitted` (`services/api/src/permission/conflict-resolution-queries.ts`) reads every Active Employee/Assignment in the workspace and returns every overcommitted employee to any authenticated workspace member — no `filterReadable` call, no role check, in either the query or its `router.ts` wiring. `services/api/src/permission/employee-queries.ts`'s `listEmployees` (the identical `Employee` node type) and `ghost-resource-queries.ts`'s `listGhostResources` both already pipe their rows through `filterReadable` before returning them; `sweepOvercommitted` is the only workspace-wide listing query in the codebase that skips it, exposing `Employee:operational` data — row-scoped for Manager (`Full (direct reports)`) and Team Member (`Read (own + team)`) — to every workspace member regardless of role. Unlike F262, this traced directly to existing precedent — no founder consultation needed. Ruled: `sweepOvercommitted` filters its `employees` array through `filterReadable(tx, principal, employees, context)`, the identical call `listEmployees` already makes, before computing overcommitment — no new mechanism, no policy-table change.

Also recorded, not a new finding: the spec's API contracts name `conflictResolution.adjustNew`/`.adjustExisting`/`.endExisting` alongside `.overrideAndProceed`/`.sweepOvercommitted`, but the first three are not distinct operations once read against the existing mutation surface — each is `assignment.create`/`.update` called with adjusted fields, and `endExisting`'s "requires the new end date to actually remove the overlap" clause is exactly what the corrected `validateCapacity` (F256) already enforces on any update. None of the three gets its own registered mutation, per the project's own "one implementation" ethos (F237/G08); a future UI stage's resolution panel calls the existing mutations directly.

Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, new Stage 16 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side only, following Stage 8/11/12/13/14/15's own precedent: no file under `apps/` changes. `RST-44` is its Linear issue.

**What to check in the Stage 16 report:** that the working-day filter genuinely changes `validateCapacity`'s behavior at a weekend/holiday boundary, not merely that the code compiles (F256/G02); that `conflictCheck.evaluate` (`packages/graph/src/queries/conflict-check.ts`, local) is a genuinely independent local implementation reading `localNodes`/`localEdges`, never touching `services/api`, proven to work with the network cut (F259); that `sweepOvercommitted` calls item 2's server-only `capacityTotalsFor` helper; that `Assignment.manager`'s policy-table change is the only cell touched in that block (F255); that `overrideAndProceed`'s manager/Owner gate is checked server-side before the write regardless of what the client's optimistic handler decided, proven by a test asserting no Assignment is created (or an optimistic one is rolled back) on a rejected call; that `overrideAndProceed` is the only path writing an Assignment combining above 100% (G04); that `overrideAndProceed` reuses `assignment.create`'s existing internal write helper rather than a second, parallel implementation; that no `appendAudit` call was added anywhere in this stage (F257); that `overrideAndProceed` genuinely has an optimistic handler in `OPTIMISTIC_MUTATORS` mirroring `assignmentCreate`'s own (no local capacity check), not `onlineOnly: true` (F259); that no `apps/` file or `InitPayload`/`MutatorContext` field was touched to resolve caller roles, and that the optimistic handler's local gate reads the caller's `WorkspaceMembership` via `membership_of` plus `OptimisticCache.edgesFrom`/`getNode` — no new enumeration capability anywhere — rejecting with `OptimisticRejection("not-authorized")` before any write when neither the Owner nor Manager check passes (F260/F261); that a Manager who genuinely is the target employee's manager can complete `overrideAndProceed` end-to-end through the real interceptor (Assignment node plus both edges), and an unrelated Manager is refused with `reason: "role"` on the edge check, proven against the real interceptor, not a mock; that `readSufficientEndpoints` is declared on `assigned_to` alone — grep the registry to confirm no other edge type carries it (F262); that `sweepOvercommitted` is proven read-only by a test (G06), not merely undocumented as a writer; that Ghost Resources participate identically in both new operations (G05); that `sweepOvercommitted` filters its Employee list through `filterReadable` before computing overcommitment, proven by a test run as the fixture's existing `outsider` (team-member) principal that omits employees the outsider cannot read, and by a Manager-scoped test that returns only that Manager's own direct reports among the overcommitted set (F263); and that no file under `apps/` appears in the diff.

---

## 17. After Stage 16

- Stage 11 (`VRS-F003`, Multi-Entity and Jurisdiction Foundation) is merged (`96d53eb`), reviewed and approved with no further findings. Stage 12 (`VRS-F004`, Working Calendar and Working Patterns) is merged (`0249d5c`), reviewed directly against the finished code and approved with no further findings — completion report at `d09d952`. `RST-40` is Done. Stage 13 (`VRS-F005`, The Bench Forecast) is merged (`d9b6d74`), reviewed directly against the finished code and approved after one test-coverage gap was found and closed (`9f553ef`) — completion report at `ab2708b`, F236–F238 ruled briefing, F239 ruled at `189cf6c`, F240 ruled at `67852a9`, F241 ruled at `bfc6714`, F242 ruled at `1a45be1`. `RST-41` is Done. Stage 14 (`VRS-F006`, Rate Card Engine) is merged (`04f2a7a`), server-side only — F243–F247 ruled across briefing and three implementation stop points (section 14), reviewed directly against the finished code and approved with no further findings, one dormant `reader-set.ts` bug independently verified safe and accepted. `RST-42` is Done. Stage 15 (`VRS-F007`, Ghost Resources) is merged (`4081e9f`) — F248 and F249 ruled checking directly against the code before writing the brief, F250 ruled resuming after implementation stopped on the missing `expectedVersion`/concurrency contract, F251 ruled resuming a second time after the brief's own permission claims were found to contradict Stage 13's already-approved policy overrides, F252 ruled resuming a third time after the brief's required promotion audit event was found to have no home in `VPS-F004`'s closed taxonomy (resolved on F221/F225's own precedent), F253 ruled resuming a fourth time after Ghost creation and promotion were found unable to satisfy Employee's required fields (resolved with a Ghost-only record constructor and an extended promotion contract), F254 ruled after real implementation's first integration test found `promoted_to` had no governing Employee partition (resolved on F208/F235's own precedent, applied as a one-line registry declaration) — reviewed directly against the finished code and approved with no further findings, completion report at `docs/stage-reports/STAGE-15_Ghost_Resources.md`. `RST-43` is Done. Stage 16 (`VRS-F008`, Capacity Conflict Resolution) is merged (`e705aad`) — F255 through F262 ruled across briefing and four implementation stop points (section 16), reviewed directly against the finished code, not the completion report alone: every file matched its ruling exactly except one. F263 (`sweepOvercommitted` returning every overcommitted employee workspace-wide with no `filterReadable` call) was found in that review, traced directly to `listEmployees`/`listGhostResources`'s own precedent with no founder consultation needed, and fixed the same day (`56581cf`) — the fix and its new outsider/direct-manager test coverage independently re-verified directly against the code before merge. `RST-44` is Done.
- Open follow-ups:
  - KEK rotation (FDN-96 follow-up list);
  - k-anonymity enforcement when analytics features arrive;
  - F125 (backdated `managed_by`, decided in VRS-F037);
  - workspace switching (the client is fail-closed until it is designed — related to but not solved by Stage 9's single-workspace bootstrap; a user with more than one `activeOrganizationId` option is still unhandled);
  - FDN-110 before the first real-data pilot;
  - F213 (`member.projection_state`), scoped into the FDN-104 spec sweep per the 22 September ruling — not yet revisited, FDN-104's remaining specs are still just-in-time;
  - Migrating the remaining fixture screens (Home, People, Timesheets, Foundations) off fixture data — explicitly out of Stage 9's scope, not yet briefed as its own stage.

---

## 18. Stage 17 — Time Classification Taxonomy (VRS-F009)

`VRS-001`'s Feature Register confirms `VRS-F009` as the correct next MVP-order feature after `VRS-F008` (Stage 16, merged `e705aad`) — the classification taxonomy for logged time (Billable/NonBillable/Pitch), and the Pitch node's field-level tier split that the taxonomy's Pitch category depends on. Checked directly against the code before writing this brief: `Pitch` is already registered (`packages/schema/src/registry/nodes.ts`, owner `VRS-F009`, `splitProtection` into a Standard-class `identifying` partition and a Finance-restricted `commercial` partition) but nothing creates one, and no Employee↔Pitch relationship exists anywhere; `packages/schema/src/policy/policy-table.ts` carries no `MATRIX_OVERRIDES` entry for `Pitch` at all; `has_skill` (Employee↔Skill, owner `VPS-A002`) is the established model for a simple operational Employee-relationship edge; F262's row-context mechanism is confirmed generic, reusable here without modification; `TimesheetEntry` (owner `VRS-F010`) is registered but has no creation mutation, so no `entryId` can exist until `VRS-F010` builds one.

One finding surfaced checking this feature directly against the code before writing the brief, closed the same day, traced directly to existing precedent, no founder consultation needed:

- **F264** — the spec described selecting a Pitch, staffing employees onto one, and classifying time against it, but none of the underlying mechanism existed or was specified: no creation mutation, no staffing relationship, and no policy-table row for Pitch, plus an API Contracts section describing `timeEntry.classify*` as this feature's own live mutations against a node that cannot exist until `VRS-F010` builds it. Ruled: `pitch.create` and a new `staffed_on` (Employee→Pitch) edge modeled on `has_skill`, using F262's row-context mechanism so Manager-initiated staffing is correctly scoped to their own direct reports; a new `Pitch:identifying` policy-table row modeled on `GhostResource`'s own row (F251) — Owner/HR-admin/Manager unscoped Full (Manager deliberately unscoped, not row-scoped, to avoid a circularity on the very first staffing of a new pitch), Team Member explicitly `None` at the generic Pitch level since `pitch.listStaffedFor` is their only sanctioned path and is gated by their own already-working `Employee:operational` row-scope on the `employeeId` argument; the `timeEntry.classify*` signatures reframed as contract for `VRS-F010` to implement, not code this feature ships; the registered `Pitch→Project` conversion left explicitly out of scope, mirroring `Candidate→Employee`/`VRS-F028`.

Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, new Stage 17 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side only, following Stage 8/11/12/13/14/15/16's own precedent: no file under `apps/` changes. `RST-45` is its Linear issue, filed and moved to In Progress.

**What to check in the Stage 17 report:** that `pitch.staffEmployee`/`unstaffEmployee` genuinely supply both `fromNodeId` and `toNodeId` on the edge target, proven against the real interceptor with both a Manager-owns-direct-report case and a Manager-does-not case; that `Pitch:identifying`'s Manager cell is genuinely `FULL_ANY()`, not row-scoped — a row-scoped cell here would silently reintroduce the circularity F264 ruled out; that `pitch.listStaffedFor` never calls a generic per-Pitch read-decision path, and that its no-stray-keys test asserts on the actual returned object; that `Pitch:commercial` was not given an override; that `time-classification.ts` is genuinely unreferenced outside its own file and test this stage; that no `TimesheetEntry` mutation, node-creation path, or router entry was added prematurely; and that no file under `apps/` appears in the diff.

---

**Reviewed and merged, 24 September 2026.** Checked directly against the finished code before merge, not the completion report alone. Every changed file in the diff read line by line: `packages/schema/src/registry/edges.ts` (`staffed_on` declares both `Employee:operational` and `Pitch:identifying`), `packages/schema/src/policy/policy-table.ts` (`Pitch:identifying` matches `GhostResource`'s own shape exactly, `Pitch:commercial` untouched), `packages/schema/src/pitch.ts` and `mutations/pitch.ts` (the identifying-fields schema and three `defineMutation` shapes), `services/api/src/mutations/pitch.ts` (the server handlers correctly supply both `fromNodeId`/`toNodeId` on the `staffed_on` write target and the `identifying`-partition target on Pitch itself), `services/api/src/permission/pitch-queries.ts` (`listStaffedFor` gates on the caller's own `Employee:operational` read decision, then reads the Pitch's `identifying` fields directly — no generic per-Pitch read path), and `services/api/src/router.ts`/`mutations/pipeline.ts` (the three writes register only in `pipeline.ts`'s `IMPLEMENTATIONS` map, matching `ghostResource.create`'s own precedent — independently confirmed by reading that precedent directly, not taking F266's claim on faith).

The real PostgreSQL integration suite (`services/api/src/permission/pitch.integration.test.ts`) was read in full: it exercises `applyMutation` against the actual interceptor, not a mock, and proves every Done-criteria case — a Manager creating and immediately staffing their own direct report, the same Manager refused with `reason: "role"` staffing or unstaffing an outsider, Owner and HR Admin staffing and unstaffing anyone, Team Member refused every write and resolving to `none` on a generic `Pitch:identifying` read, and the selector returning exactly `{ pitchId, name, clientName }` (asserted by key, not just by type) with no existence leak to an unrelated caller. `packages/schema/src/time-classification.ts` was independently grepped and confirmed to have zero callers anywhere outside its own file and test. `git diff --stat` against `main` was checked directly to confirm no file under `apps/` and no `TimesheetEntry` file appears anywhere in the change.

Two findings surfaced during the build itself, both closed the same day with no founder consultation needed, both independently verified correct against the code before this review accepted them:

- **F265** — `staffed_on`'s original F264 registration declared only `Employee:operational`; `Pitch` is a split node, and `edgeRoleDecision` applies F136's conservative default to a split endpoint with no declared partition, so the first implementation refused every staffing write regardless of role. Closed by declaring `Pitch:identifying` alongside `Employee:operational` — no new mechanism, proven by the real interceptor test.
- **F266** — the Stage 17 brief instructed wiring the three Pitch writes directly into `router.ts`, citing `ghostResource.*` as precedent; that citation was wrong — `ghostResource.create` itself has no router entry, only its reads do. Closed by registering the three writes in the shared `graph.applyMutations` pipeline instead, matching the actual established pattern. Independently re-verified by reading `ghostResource.create`'s own wiring directly rather than accepting the finding's own citation.

One process note, not a technical finding: this is the first stage where Codex recorded F265 and F266 directly into `docs/Foundations_Findings.md`, `docs/Claude_Code_Build_Prompt.md`, and the owning spec documents itself, rather than surfacing them as open findings for the reviewer to rule and record — a departure from every prior stage's division of labor. Both rulings were independently checked and are correct, and the reviewer's own exclusive editing of these documents was not itself compromised by anything incorrect being written. But the separation exists so a wrong self-ruling gets caught before it becomes doctrine, and this stage is the first test of whether that separation held only by luck. Flagged for the founder to decide whether the builder self-recording findings into the reviewer's own authoritative documents becomes standing practice or gets reverted to escalate-and-wait, going forward. **Ruled by the founder, 24 September 2026: reverted to escalate-and-wait, standing practice for every future stage.** Codex reports build-time findings in the stage report and Linear; it does not edit `docs/Foundations_Findings.md`, `docs/Claude_Code_Build_Prompt.md`, or any `docs/Vulto_Specs/*.md` file directly. The reviewer rules and records, same as every stage before this one.

The findings ledger itself had drifted the same way it has before (see the file's own recurring note on this): F265 and F266 had full detail sections but no table rows, and the master recount paragraph still ended at F264. Corrected the same day — both table rows added, both occurrences of the running recount tail extended through F266.

One verification gap, disclosed rather than glossed over: `pnpm verify:full` was not independently re-run this review. The device shell available for this review has no live PostgreSQL reachable and a Node/native-binding mismatch against this repo's pinned toolchain (Node 22 present, `>=24.18.0 <25` required; `vitest`'s `rolldown` binding failed to load) that a full reinstall was not undertaken to fix mid-review. The report's own gate results (`pnpm verify:full`: 284 API tests passed, 2 skipped) are taken on the report's word for the live run itself; every test's actual assertions were read directly and independently judged sound, which is the substantive form of verification, but the pass/fail of the live run was not reproduced by this review.

Merged `--no-ff` as `65a8578`, no further findings beyond F265/F266 (both already closed by Codex and independently confirmed here). `RST-45` moved to Done. `main` has not been pushed to `origin` — this reviewing environment has no push credentials; push remains Codex's to do from its own credentialed environment, same as every prior stage.

**Follow-up, 24 September 2026.** Codex closed the one disclosed verification gap: `pnpm verify:full` re-run in its own correctly-provisioned environment (Node `v24.18.0`, live PostgreSQL) at `8a91a01`, reporting the same 284 API tests passed / 2 skipped the Stage 17 report originally claimed — genuine terminal output, independently confirmed rather than re-asserted from the report. Codex also confirmed the founder's escalate-and-wait ruling above as understood and acted on going forward.

## 19. Stage 18 — Timesheet Speed-Run (VRS-F010)

`VRS-001`'s Feature Register confirms `VRS-F010` as the correct next MVP-order feature after `VRS-F009` (Stage 17, merged `65a8578`) — the keyboard-first weekly timesheet grid, TimesheetEntry's complete schema (incorporating `VRS-F009`'s classification fields directly), and TimesheetAnomalyFlag's exception-based detection. Checked directly against the code before writing this brief: `TimesheetEntry` and `TimesheetAnomalyFlag` are already registered (`packages/schema/src/registry/nodes.ts`, owner `VRS-F010`) with their `MATRIX_OVERRIDES` policy-table rows already correctly present, matching `VPS-A004`'s own table; `resolveWorkingDay` (`packages/schema/src/working-day.ts`, F237's own precedent) is directly reusable for the grid's column/expectedHours resolution and the `HoursExceedExpected` rule; the established `MutationRejection("invalid-args")`/`GraphValidationError` convention (`assignment.ts`/`calendar.ts`/`employee.ts`) replaces the spec's own invented `TimesheetValidationError` name; and no creation mutation exists anywhere for either node type — this feature is the first to touch them.

Two findings surfaced checking this feature directly against the code before writing the brief, both closed the same day, both traced directly to existing precedent, no founder consultation needed:

- **F267** — `logged_against` (`TimesheetEntry→Assignment`/`TimesheetEntry→Pitch`, registered by `VRS-F010` in `edges.ts`) declared no `governingPartitions` at all. `Pitch` is a split node exactly like it was for `staffed_on`/F265, so every `logged_against` write touching the Pitch endpoint would refuse for every role — identical mechanism, identical mistake, now caught before any code is written. Ruled: `logged_against` declares `governingPartitions: { Pitch: "identifying" }`; `Assignment` is unsplit and needs none.
- **F268** — `TimesheetEntry`'s and `TimesheetAnomalyFlag`'s row-scoped policy cells (Team Member `Full (own)` on TimesheetEntry, Manager `Read`/`Full (direct reports)` on both) cannot resolve today: `rowScopeSatisfied` only finds a row's subject Employee for `Employee` (the node itself) or `BurnoutAlert` (via its `triggered_by` edge); neither `TimesheetEntry` nor `TimesheetAnomalyFlag` is in that list, so every row-scoped check against either falls to `else return false`, refusing even an employee their own timesheet. Ruled, corrected by F269 below: `TimesheetEntry`'s `employee_id` (G04's own direct field, deliberately not an edge — a Pitch entry has no Assignment to traverse through at all) is read via a new branch calling the interceptor's existing `getNode`. `TimesheetAnomalyFlag` needs no new mechanism: it already carries a registered `triggered_by` edge to Employee, so the existing `BurnoutAlert` branch already reaches it.
- **F269** — Codex, resuming Stage 18, correctly declined to implement F268's `TimesheetAnomalyFlag` branch as originally ruled and stopped to report it rather than ship a check that fails closed for every caller. `TimesheetAnomalyFlag` is `fixedProtection("Manager-restricted", 2)`; Tier 1/2 node content is stored encrypted in `graph_protected_fragments`, never in the plain `graph_nodes.record` `getNode` reads, so `node.record.employee_id` would have been `undefined` for every flag. Ruled, traced directly to `BurnoutAlert`'s own precedent — same `triggered_by` edge group, same Tier 2 protection, already registered in `edges.ts`: `rowScopeSatisfied`'s existing `BurnoutAlert` branch extends to `TimesheetAnomalyFlag` unchanged, since edges are not protected content. `timesheetAnomaly.evaluate` must write that `triggered_by` edge when it creates a flag. No founder consultation needed for either ruling.
- **F270** — Resuming Stage 18 after F269, Codex stopped a third time rather than invent or impersonate a writer: `TimesheetAnomalyFlag` is deliberately `NONE` for Team Member (the spec requires the flagged employee never see their own flag), so `timesheetAnomaly.evaluate` cannot run as the submitting Team Member's own principal, and no existing principal kind supplies another one — `SystemPrincipal`s get zero policy roles in `authorizeWrite`, and the three system operations that do exist bypass the interceptor entirely, writing infrastructure tables outside the graph-node model. This is a genuine architectural fork — no precedent forces a single answer — put directly to the founder. Ruled: a new system principal, `timesheet-anomaly-evaluate`, added to the existing closed table, permitted exactly one new operation, `timesheet-anomaly.create-flag`; `authorizeWrite` gains a distinct Gate 1 path for system principals, checked against that whitelist rather than the role matrix — a system principal still never receives a `PolicyRole`. `timesheetAnomaly.evaluate` constructs this principal for the flag-and-edge write only; every other Stage 18 operation keeps the calling member's own principal.
- **F271** — Resuming Stage 18 after F270, Codex stopped a fourth time: G09 (at most one Active uncleared flag per employee/week/reason) and G09b (an `ApprovedOvertime`-cleared flag never reappears) both require reading prior `TimesheetAnomalyFlag` content — `employee_id`, `week_start_date`, `flag_reason`, `cleared_at`, `clearance_outcome` — before deciding whether to create, update, or skip a flag. F270 authorized `timesheet-anomaly-evaluate` to write a flag; it granted no read, and the audited decrypt path (`readProtected`/`authorizeRead`) still gives system principals zero policy roles. Unlike F270, this is not a genuine fork: the read-side seam (`decisionRoles`'s own "its policy rows list operations" comment) predates both rulings, unwired until now, and the scope — one principal, one node type, one partition — is already fixed by F270 itself. Traced directly to F270's own mechanism, ruled the same day, no founder consultation needed: `timesheet-anomaly-evaluate` gains a second closed-table operation, `timesheet-anomaly.read-flags`; `decideRead` gains the read-side counterpart to `authorizeWrite`'s Gate 1, checked against the same whitelist rather than `bestCell`'s empty role set for system principals; `ReadDecision`'s `role` field widens to `PolicyRole | null`, mirroring `WriteDecision`'s own existing shape. `timesheetAnomaly.evaluate` queries prior flags through `readProtected` under the same system principal it already constructs for the write.
- **F272** — Resuming Stage 18 after F271, Codex stopped a fifth time: `created_by`/`updated_by` (node) and `created_by` (edge) require a genuine `user_id UUID` in every schema shape `TimesheetAnomalyFlag` and its `triggered_by` edge fall under, and `SystemPrincipal` has no User UUID. Attributing the write to the submitting Team Member is false and contradicts F270 directly (they're deliberately denied read access to the record they'd be recorded as authoring); no existing system operation has ever written `graph_nodes`/`graph_edges`, so nothing settles this by precedent. This is a second genuine fork — put directly to the founder, reasoned through together rather than traced to an existing pattern, since the decision governs every future system-authored write, not only this one (`BurnoutAlert`, `FlightRiskSignal`, `RevenueGapAlert` all need it next). Ruled: each named system principal gets its own reserved, non-membership `User` identity per workspace, lazily and deterministically provisioned (F233/F247's own derived-ID pattern), self-attributed, invisible to every people-facing query since none of them reach `User` except through `WorkspaceMembership`, which this identity never receives. A closed `SYSTEM_PRINCIPAL_DISPLAY_NAMES` table gives each principal a plain human label for later interface use — `timesheet-anomaly-evaluate`'s is "Automatic Review," never the internal principal name. `VPS-A002`'s universal conventions corrected to state this explicitly.
- **F273** — Resuming Stage 18 after F272, Codex stopped a sixth time: Do item 5 requires `submitWeek` not to error on a zero-entry week, but the only specified persisted submission state is `TimesheetEntry.lifecycle_status`/`submitted_at`, both per-entry — with no entries, the transaction changes nothing, so `getWeek`/`hrCompliance.listSubmissionStatus` cannot later distinguish a correctly-submitted empty week from Not-Started, defeating the compliance view's entire purpose. Codex checked `graph_mutations` directly and confirmed it unsuited (an idempotency log keyed by a hash, never replicated, not a queryable business-status store), and declined both a synthetic zero-hour entry and a bespoke status table outside the graph model rather than invent one unilaterally. This traces directly to `PolicyAcknowledgment`'s own registered precedent — a real, already-registered node type recording that an Employee took a confirming action with nothing richer to say about it, structurally identical in kind to what this needs — no founder consultation needed. Ruled: a new Tier 0 `TimesheetWeekSubmission` node (owner `VRS-F010`, same tier as `TimesheetEntry` and `PolicyAcknowledgment`), created only when `submitWeek` finds zero Draft entries, removed by `unlockWeek` for the same case, and resolved by `getWeek`/`hrCompliance.listSubmissionStatus` only as a fallback when no `TimesheetEntry` rows exist for that employee/week.
- **F274** — Resuming Stage 18 after implementing the F270–F273 infrastructure (`3cda786`), Codex traced the first ordinary Team Member `saveCell`/zero-entry-marker create before writing it and stopped a seventh time: `TimesheetEntry`/`TimesheetWeekSubmission` give Team Member `Full (own only)`, but the pipeline authorizes every check before `apply()` writes anything, and "own" is normally judged by reading the row's stored `employee_id` — on a create, that row does not exist yet, so the read comes back empty and the check fails closed. Every node type ever created before this was created only by a role holding unscoped Full, so this specific gap had never been exercised, even though `VPS-A004`'s own "self-service pattern" already names six node types meant to work exactly this way. Codex correctly ruled out trusting an unverified claimed subject and ruled out authorizing after the write — both break invariants the rest of the system already relies on — and stopped rather than invent either. This is a third genuine fork — put directly to the founder, reasoned through together rather than traced to an existing pattern, since no node type had ever needed row-scoped creation authorization before and the decision governs every future self-service node type. Ruled: a mutation's own already-validated args declare the new row's subject through a new `WriteChange.declaredSubjectEmployeeId` field (kept distinct from Gate 3's existing `subjectEmployeeId`); the row-scope check compares it against the caller's own identity exactly as it compares a stored row's field once one exists. Because nothing enforces that a future mutation's declared subject and its actual write stay in sync on their own, the founder asked for the stronger version too: the pipeline independently re-reads the row immediately after `apply()` and rejects the whole transaction, unwritten, if it does not match what was authorized.
- **F275** — Resuming Stage 18 after F274's mechanism was implemented, Codex found that saving a Billable or Pitch cell still failed for an ordinary Team Member, independently of F274's fix: `edgeRoleDecision` requires `Full` on an edge's endpoint unless that edge's own registration declares `readSufficientEndpoints` for the endpoint's node type, and `logged_against` declared none for either. This split into two halves. The Assignment half traced directly to `assigned_to`'s own registered precedent (F262) — the identical shape of problem, a writer holding only Read on an endpoint that should still count as sufficient — no founder consultation needed. The Pitch half is a genuine fork: Team Member's outcome on `Pitch:identifying` is unconditionally `NONE`, a deliberate F264–F267 restriction, not an oversight, so no `readSufficientEndpoints` declaration could touch it; closing it needed an actual access-widening decision. Codex correctly named three materially different security choices with no existing rule picking one (a blanket Team Member Pitch grant; a staffing-scoped grant; a mutation-local exception bypassing the interceptor) and stopped rather than choose unilaterally. Put directly to the founder, who chose the staffing-scoped grant and, asked a second time whether the recommendation was the most robust long-term choice, asked for a reusable, declarative mechanism over a Pitch-specific hand-coded check — the identical shape of grant `VPS-A004`'s own "Three recurring override patterns" section already named for InterviewRound/FeedbackEntry/OnboardingTask but had never implemented. Ruled: `logged_against` gains `readSufficientEndpoints: { Assignment: true }`; a new closed registry, `PARTICIPANT_GRANTS`, and a new resolver, `participantGrantSatisfied`, wired only into `edgeRoleDecision`, resolve a staffed Team Member's currently-active `staffed_on` edge to the target Pitch, reusing `outgoing()`'s existing active-edge semantics rather than inventing new lifecycle logic.
- **F276** — Resuming Stage 18 after F275's mechanism was implemented (verified 5/5 against the real interceptor), Codex traced `saveCell`'s own required `week_start_date` derivation before writing it and stopped a ninth time: `VRS-F004`'s `working_week` schema permits any arrangement of seven booleans, not only a contiguous block — a genuine seven-day working week has no rest gap to anchor a "first working day" on, and a split pattern has several candidates. `resolveWorkingDay` also folds holidays into `is_working`, so even an ordinary week's key could have silently shifted depending on where a holiday fell. No `week_start_day` or equivalent field existed anywhere in the schema. Codex correctly declined to invent a hidden Monday default or a longest-gap heuristic and removed the incomplete mutation-schema draft rather than leave a stub. Put directly to the founder as a genuine fork — no existing concept in this codebase ever defined which day begins a week — and, on a second robustness pass of his own first recommendation, refined further: a rest-gap heuristic was rejected as *more* exposed to drift than a calendar-level anchor, since an ordinary `pattern.set` edit is far more frequent than a rare, Owner/HR-Admin-only `calendar.update`; restricting permitted schedules was rejected outright, since it would break the split-pattern employees `VRS-F004` already explicitly supports. Ruled: a new required `week_start_day` field on `WorkingCalendar`, versioned identically to `working_week`, decouples the week boundary from `is_working`, holidays and `WorkingPattern` entirely — resolved by pure date arithmetic, immune to both failure modes by construction. One boundary case is named explicitly as an accepted, inherent limitation rather than guarded in code: a mid-week calendar edit or entity re-scoping can legitimately split one real week's entries across two `week_start_date` values, since guarding it would require `VRS-F004`'s own mutations to read `VRS-F010`'s `TimesheetEntry` table, the identical cross-feature coupling `VRS-F004`'s own G08 already forbids in the other direction.

Also settled while briefing, none rising to a numbered finding — established conventions and scope boundaries applied here for the first time:

- `overtime_flag_threshold` is a hardcoded constant this stage; no per-workspace configuration mechanism exists yet. `VPS-F005` (Workspace Configuration Console), unbuilt and positioned later in MVP order, will own making it configurable.
- `hrCompliance.sendReminder` is out of scope entirely this stage — it requires `VPS-F003` (Notification and Alert Center), unbuilt. The compliance view lists submission status only; the reminder action in the interface spec is deferred until that delivery mechanism exists.
- `timesheetAnomaly.clear`'s `ApprovedOvertime` outcome records the outcome and clears the flag but does not call `VRS-F018`'s unbuilt `overtime.approve` — the spec's own Out of Scope section already correctly defers TOIL accrual and formal overtime approval to that feature; this stage owns only the flag and its clearance outcome.
- Anomaly evaluation runs as a separate step immediately after `submitWeek`'s transaction commits, not inside that transaction — G10's own "never a gate" language rules out coupling flag evaluation to the write it must never delay.
- Validation error naming aligned to the established `MutationRejection("invalid-args")`/`GraphValidationError` convention; `TimesheetValidationError` in the spec was descriptive prose, not a class to invent.

`docs/Vulto_Specs/VRS-F010_Timesheet_Speed-Run.md` corrected to match all of the above; `docs/Vulto_Specs/VPS-A002_Master_Graph_Schema_Definition.md`'s universal conventions corrected for F272 (platform-wide) and its node type registry extended for F273 (feature-scoped, `TimesheetWeekSubmission`); `docs/Vulto_Specs/VPS-A004_Graph_Permission_Layer.md`'s self-service pattern and technical specifications corrected for F274 (platform-wide — every future self-service node type inherits this mechanism), and its participant-scoped grants note corrected for F275 to record a real, implemented mechanism rather than documented intent, Pitch as its first instance; `docs/Vulto_Specs/VRS-F004_Working_Calendar_and_Working_Patterns.md`'s WorkingCalendar field listing and Decisions Recorded section corrected for F276 (feature-scoped, `week_start_day`). Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, new Stage 18 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side only, following every prior stage's own precedent: no file under `apps/` changes. `RST-46` is its Linear issue; the branch is `codex/stage-18-timesheet-speed-run` at `d98c5d1` (F267's fix and the F268/F269-corrected interceptor branches are committed at `2cd5b03`; the F270–F273 infrastructure is committed at `3cda786`; F274's mechanism is committed at `a6e4fe5`; F275's mechanism is committed at `d98c5d1`; F276 is ruled but not yet implemented — no mutation code has been written for it), ready to resume on the `WorkingCalendar.week_start_day` field and the new `resolveWeekStartDate` mechanism above before any of the actual mutations are built.

**What to check in the Stage 18 report:** that `logged_against`'s `governingPartitions` genuinely includes `Pitch: "identifying"` (F267); that `rowScopeSatisfied`'s `TimesheetEntry` branch reads `employee_id` from the row's own stored field via `getNode` (no edge exists for a Pitch-categorized entry), and that its `TimesheetAnomalyFlag` handling extends the existing `BurnoutAlert` branch to traverse the registered `triggered_by` edge rather than reading a field off a Tier 2 record — read the interceptor diff's own code path, not just a passing test name (F268, corrected by F269); that `timesheetAnomaly.evaluate` writes the new flag's `triggered_by` edge to the flagged Employee in the same transaction as the flag itself (F269); that `timesheetAnomaly.evaluate`'s prior-flag read calls `readProtected` under the `timesheet-anomaly-evaluate` system principal's `timesheet-anomaly.read-flags` grant, never a direct decrypt or the submitting Team Member's own principal (F271); that the flag and its edge genuinely resolve `created_by`/`updated_by` to the reserved system-principal `User`'s own UUID, with no `WorkspaceMembership` edge and no linked Employee (F272); that `submitWeek` creates a `TimesheetWeekSubmission` row only on the zero-entry path, and that `getWeek`/`hrCompliance.listSubmissionStatus` check `TimesheetEntry` rows first, falling back to the marker only when none exist (F273); that `saveCell`'s and `submitWeek`'s create checks declare `declaredSubjectEmployeeId` from the parsed args' own `employee_id`, not a separately recomputed value, and that the pipeline's post-`apply()` re-check genuinely re-reads the written row and rejects a deliberate mismatch whole, proven by a test, not merely present in the code (F274); that `logged_against`'s registration genuinely carries `readSufficientEndpoints: { Assignment: true }` (F275); that `PARTICIPANT_GRANTS`/`participantGrantSatisfied` are wired only into `edgeRoleDecision`, never `decideRead`/`bestCell` — read the interceptor's own code path, not just a passing test — and that a Team Member's Pitch grant genuinely tracks live `staffed_on` state, proven by a test that stages, unstaffs, and re-stages the same Team Member (F275); that `WorkingCalendar.week_start_day` is genuinely carried through `initialWorkingWeekFor`, `calendarUpdate.apply()`, and `entity.create`'s optimistic handler alike, not only the server path — a divergence there would silently reintroduce F233's own parity gap for this one field — and that `resolveWeekStartDate` is proven against a genuinely seven-day `working_week` and a split pattern, with a holiday landing on the anchor day proven not to move `week_start_date` (F276); that `timesheet.submitWeek` is genuinely one mutation performing every entry's transition inside its own single transaction, not a loop of separate `saveCell`-style calls that could partially fail (G07); that `getWeek` and the shortcut resolution both call `resolveWorkingDay`, never `contracted_hours` divided by a weekday count (G02, G06); that `HoursExceedExpected`'s Pitch-week exclusion is exercised by a real test; that `timesheetAnomaly.evaluate` is called as a step after `submitWeek`'s transaction commits, not inside it (G10); that `timesheetAnomaly.listActive` calls `filterReadable` on the same shape `sweepOvercommitted` already established (F263's own precedent); that `timesheetAnomaly.clear`'s `ApprovedOvertime` path makes no call into any `VRS-F018` file or mutation; that `hrCompliance.sendReminder` is absent from the diff entirely, not stubbed; and that no file under `apps/` appears in the diff.

## 20. Stage 19 — Billable vs Non-Billable Pulse (VRS-F011)

`VRS-001`'s Feature Register confirms `VRS-F011` as the correct next MVP-order feature after `VRS-F010` (Stage 18, merged `--no-ff` as `1389d18`) — the live utilization engine, computing the fraction of available working hours that generated revenue, surfaced as an individual pulse bar and an agency dashboard. Checked directly against the code before writing this brief: `UtilizationSnapshot` is already registered (`packages/schema/src/registry/nodes.ts`, owner `VRS-F011`, `fixedProtection("Standard", 0)`) with no policy-table row yet; `bench-forecast-queries.ts` already has a directly-reusable `applyDisclosureControl` call (`packages/schema/src/policy/disclosure-control.ts`, F238's own mechanism) and a `listEmployees(..., employeeType: "Employee")` call excluding Ghost Resources (no new filtering mechanism needed for G05); `billability_target` is a registered `VPS-A002` configuration key (default `0.75`) with no `founding.ts` `Workspace` default and no console to edit it yet, the identical position `overtime_flag_threshold` was already settled in at Stage 18's own briefing.

Two findings surfaced checking this feature directly against the code before writing the brief, both closed the same day, both traced directly to existing precedent, no founder consultation needed:

- **F277** — The spec's own `UtilizationSnapshot` schema comment for `week_start_date` still read "the first working day of the week per VRS-F004," the identical pre-`week_start_day` language F276 had already corrected in `VRS-F010`'s own field comment — written before F276 existed and never revisited. Its "Expected hours" section separately cited `workingDays.count(employeeId, weekStart, weekEnd)`, a function existing under neither that name nor that day-count semantics anywhere in the code. Stage 18's own `getWeek` (`services/api/src/permission/timesheet-queries.ts`) already computes exactly this figure — `expectedWeeklyHours`, via `hoursOn` summed across the week resolved by `weekStartForEmployee`. Ruled: corrected to name the real mechanisms; `utilizationSnapshot.compute`'s own expected-hours figure must reuse this identical computation, never a second, independently-written traversal — two separately-implemented "expected hours" computations for the same employee and week is precisely the silent-disagreement risk the spec's own Related Notes section already names for the Bench Forecast's figure.
- **F278** — The spec's Security Considerations claimed `UtilizationSnapshot` "follows Employee's Standard, Tier 0 visibility... a deliberate reuse of an existing rule," but neither of Employee's two Tier 0 policy rows matches that shape alone: `Employee:operational`'s Team Member cell is `Read (own + team)`, not "own" alone — reusing it wholesale would let any Team Member see a colleague's individual utilization on that colleague's own profile, contradicting the spec's own explicit "their own" restriction (restated in its System States table). `Employee:compensation`'s Manager cell is `Restricted — Visible to Finance Admin`, not "direct reports" — reusing it wholesale would hide a Manager's own reports' utilization from them entirely. Ruled: `UtilizationSnapshot`'s own row is composed from each existing row's matching cell instead — Owner/HR-Admin `FULL_ANY()` and Manager `Full (direct reports)` from `Employee:operational`'s own shape; Team Member `Read (own only)` from `Employee:compensation`'s own shape, the closer analog for an individually-sensitive per-person figure; Finance Admin `READ_ANY()`, `Employee:operational`'s own Tier 0 default for a role the spec does not otherwise restrict.

Codex then traced this brief into the existing interceptor before writing any product code, and stopped three times more the same day, reporting all three in `docs/stage-reports/STAGE-19_Billable_vs_Non-Billable_Pulse.md` (`164b55e`) before any implementation, gates, or `apps/` changes:

- **F279** — `resolveStoredSubjectEmployeeId`'s stored-field branch (`services/api/src/permission/interceptor.ts`) covered `TimesheetEntry`/`TimesheetWeekSubmission` only; `UtilizationSnapshot` fell through to `null`, leaving F278's own Team Member "own only" grant with no subject to resolve against — every such read was refused, denying a Team Member their own pulse bar entirely. Ruled directly on F268's own precedent, no founder consultation needed: `UtilizationSnapshot` added to the same branch.
- **F280** — `utilizationSnapshot.compute`'s reactive write, triggered from `saveCell`/`submitWeek`/`unlockWeek`'s `afterCommit` hooks, has no authorized writer: F278's own Team Member row is `Read (own only)`, never `Full`, so the ordinary caller whose edit triggered the write cannot write the resulting snapshot. Ruled directly on `VRS-F010`'s own `timesheetAnomaly.evaluate` system-principal precedent (F270–F272), no founder consultation needed: a new system principal, `utilization-snapshot-compute`, authorized for a new operation, `utilization-snapshot.compute`.
- **F281** — escalated as a genuine architectural fork. `getAgencyAggregate` must return the identical agency-wide figure for every role (this feature's own spec is explicit the aggregate figure itself is the same single number for Owner, HR Admin, Finance Admin, Manager and Team Member alike — only `perEmployee` is role-scoped), but the only existing cohort mechanism, `listEmployees`, runs under the caller's own role-scoped principal, and no precedent existed for a read that must return the same result regardless of who asks. Put to a robustness challenge, a first-pass recommendation (a dedicated bypass function reading `Employee` rows directly, outside the interceptor, relying on `applyDisclosureControl` alone) was found wrong on two counts: it wrongly analogized to the Bench Forecast, whose own spec states its Manager aggregate is deliberately team-scoped — the opposite of what this feature needs — and it would have violated `VRS-F005`'s own stated principle that no query may construct permission logic of its own outside the interceptor. Resolved instead by reusing F280's own system-principal machinery: `utilization-snapshot-compute` gains a second operation, `utilization-snapshot.read-cohort`, reading `Employee:operational` (cohort membership, Ghost/`contracted_hours`-zero exclusion) and `UtilizationSnapshot:record` (summing each cohort member's stored figure) uniformly for every caller regardless of role; `perEmployee` stays a fully separate, ordinarily-scoped, role-gated read. Both new operations are declared in one new closed table, `SYSTEM_OPERATION_TARGETS` (`VPS-A004`'s new A004-T25), rather than growing a third and fourth hardcoded interceptor branch.

Also settled while briefing, none rising to a numbered finding:

- `billability_target` stays a hardcoded `0.75` constant this stage, on `overtime_flag_threshold`'s own already-settled Stage 18 precedent — a registered configuration key with no console yet built to edit it gains nothing from being written into `Workspace`'s own record ahead of that console's existence.
- `saveCell` (`services/api/src/mutations/timesheet.ts`) currently has no `afterCommit` hook at all — only `submitWeek`/`unlockWeek`'s shared branch does, for `timesheetAnomaly.evaluate`. This feature's own "reactive on any entry write" contract requires adding one to `saveCell` too, following G10's identical non-gating shape.
- The per-employee comparison list's role restriction (Team Member never sees it) is a role check, separate from `applyDisclosureControl`'s cohort-size suppression — the two compose, neither substitutes for the other.
- `bench-forecast-queries.ts` has a module-private function already named `utilizationTotals`, computing an unrelated calendar/assignment-based capacity figure for bench cost, not a `TimesheetEntry`-based one. This feature's own aggregating function must be named distinctly to avoid a future reader assuming reuse where none exists.

`docs/Vulto_Specs/VRS-F011_Billable_vs_Non-Billable_Pulse.md` corrected to match: the `week_start_date`/`expected_hours` language (F277), the Security Considerations policy-row description (F278), the `resolveStoredSubjectEmployeeId` fix (F279), the system-principal write for `utilizationSnapshot.compute` (F280), and the two-tier read model for `getAgencyAggregate` (F281) — API contracts and Security Considerations both updated, with Decisions Recorded entries covering all five findings plus the `billability_target` precedent note. `docs/Vulto_Specs/VPS-A004_Graph_Permission_Layer.md` gains A004-T25, the closed `SYSTEM_OPERATION_TARGETS` registry a system-principal node-targeting operation must be declared in. Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, Stage 19 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side only, following every prior stage's own precedent: no file under `apps/` changes. `RST-47` is its Linear issue; the branch is `codex/stage-19-billable-non-billable-pulse`, resumed on `main` carrying the F277–F281 corrections and Stage 18's merge. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**What to check in the Stage 19 report:** that `utilization_rate`'s formula divides by `expected_hours`, read directly off the calculation function's own code (G04); that `pitch_hours` is genuinely routed outside both the numerator and denominator — grep the formula, not just the acceptance-criterion test; that `expected_hours` is computed via `hoursOn`/`isoDatesInclusive` or a helper shared with `getWeek`, never a second, independently-written traversal (F277); that `UtilizationSnapshot`'s policy-table row matches F278's composed shape exactly — Team Member `Read (own only)`, not `Employee:operational`'s own-plus-team cell copied wholesale; that `resolveStoredSubjectEmployeeId`'s new `UtilizationSnapshot` branch is present and a Team Member can genuinely read their own snapshot (F279); that `utilizationSnapshot.compute` actually runs under the `utilization-snapshot-compute` system principal, not the triggering caller's own (F280); that `SYSTEM_OPERATION_TARGETS` is the sole authority for every system-principal node-targeting operation, with no leftover hardcoded clause (A004-T25); that `getAgencyAggregate`'s aggregate figure is proven identical for every role while `perEmployee` correctly varies by role, built from two genuinely separate code paths (F281); that the agency aggregate's Ghost/`contracted_hours`-zero exclusion and its `applyDisclosureControl` call are both present, and that the per-employee comparison's role gate is a separate check from disclosure control and from the system-principal aggregate step; that `saveCell`'s new `afterCommit` hook is proven non-gating by an actual injected-failure test, the same proof shape G10 already required in Stage 18; that `utilizationSnapshot.compute` has no router entry of its own — reachable only through the mutations' own `afterCommit` hooks; that `billability_target` is a hardcoded constant, never a `founding.ts` `Workspace` field; and that no file under `apps/` appears in the diff.

**Independently verified and merged, 25 September.** Codex reported Stage 19 complete at `0631c91` (all four gates passed; `verify:full` — 23 files, 301 tests passed, 2 skipped; `RST-47` moved to In Review). Verified directly against the code before merge, not the completion report's prose: the `SYSTEM_OPERATION_TARGETS` table in `packages/schema/src/policy/principal-policy.ts` matches A004-T25 exactly, and `interceptor.ts`'s `decideRead` and `authorizeWrite` both replaced their hardcoded single-operation checks with the new `systemNodeTargetPermitted` helper against it, while `authorizeWrite`'s `triggered_by` edge-shape clause for `timesheet-anomaly.create-flag` stayed a separate, untouched branch exactly as A004-T25 requires; `services/api/src/mutations/utilization-snapshot.ts` genuinely calls `authorizeWrite` under the `utilization-snapshot-compute` system principal with `systemOperation: "utilization-snapshot.compute"`, never the triggering caller's own principal; `services/api/src/permission/utilization-queries.ts`'s `agencyFiguresUnderSystemPrincipal` runs `listEmployees`/`filterReadable` under that same system principal with `utilization-snapshot.read-cohort` unconditionally for every caller, excluding Ghost Resources and zero-`contracted_hours` employees before any figure is computed, while `comparisonForCaller` (`perEmployee`) and `getIndividual` are fully separate functions under the caller's own ordinary principal; the real Postgres-backed tests in `interceptor.integration.test.ts` and `timesheet.integration.test.ts` were read in full and proven, not merely present — including the new cohort test showing Owner, Manager and Team Member calling `getAgencyAggregate` in the same state all receive identical `aggregateUtilization`/`aggregateLoggingCompleteness`/`weekOverWeekDelta`/`cohortSize` fields while `perEmployee` correctly differs (5 rows for Owner, 1 for Manager, absent for Team Member) and a department filter narrowing the cohort to 1 correctly triggers `applyDisclosureControl` suppression; `calculateUtilization`'s unit tests confirmed (4/40 = 10.0, 20 billable + 15 pitch /40 = 50.0 with pitch excluded from both sides, all zero-denominator guards); `getWeek` confirmed refactored onto the new shared `services/api/src/graph/expected-week.ts` helper rather than a second traversal; the pipeline's own pre-existing `afterCommit` try/catch (G10) confirmed to make the new reactive hook non-gating, and the injected-failure test proven against that real mechanism; `git diff --stat` against the merge base confirmed zero `apps/` files touched. This device's own restricted shell has no Docker and could not re-run `pnpm verify:full`'s live-Postgres suite itself, so verification here is the direct code and test read above, not a fresh gate run — consistent with the report's own gate output. Merged `--no-ff` as `0e7e4d9`. `RST-47` moved to Done. `VRS-001`'s Feature Register names `VRS-F012` (Revenue Gap Alert) as the next MVP-order feature; checked directly against the finished code before its brief was written, per standing practice, surfacing F282–F284 — see section 21. Stage 20 was independently verified and merged `--no-ff` as `c2583a5` (F283's shared `benchDaysForWindow` Pitch exclusion, the `revenue-gap-alert-evaluate` system-principal write path, and the `SYSTEM_OPERATION_TARGETS`/`triggered_by` edge-clause generalization all confirmed directly against real Postgres-backed tests; a stale `.git/index.lock` from an interrupted first merge attempt was cleared with no data loss). `RST-48` moved to Done. `main` was then pushed to `origin` from the founder's own machine (`bf20ea5..2697e79..f31b959`), closing the push blocker Codex's environment had refused as an unauthorized repository export and this reviewer's own linked-computer shell independently confirmed it also lacks credentials for. `VRS-001`'s Feature Register confirms `VRS-F013` (Skill-to-Project Matcher) as the correct next MVP-order feature. Checking it directly against the code before writing its brief surfaced and closed F285, F286 and F287 the same day, no founder consultation needed for any: F285 (`requires_skill` declares no `governingPartitions` for either of its two tier-split endpoints, Pitch and Requisition, the sixth recurrence of F208/F235/F254/F265/F267's own class), F286 (stale local-first architecture language throughout the spec, never corrected since Stage 7's pivot) and F287 (`VRS-F002`'s own already-specified `employee.attachSkill` mutation was never actually implemented). `docs/Vulto_Specs/VRS-F013_Skill-to-Project_Matcher.md` and `packages/schema/src/registry/edges.ts` corrected to match. Branch `codex/stage-21-skill-to-project-matcher` started from `main`; `RST-49` filed and moved to In Progress. Codex then resumed implementation and stopped once on F288 (Manager's `READ_ANY()` grant on Project left `requires_skill` unwritable by the brief's own intended caller), traced directly to F262's own precedent and ruled the same day, no founder consultation needed: `readSufficientEndpoints: { Project: true }` added to `requires_skill`'s registration. Codex resumed again and stopped a second time on F289 (`insertEdge`'s edge-metadata protection check tests whether Employee has *any* non-Tier-0 partition anywhere, never which partition an edge actually governs, so `has_skill`'s already-Tier-0-declared `Employee:operational` endpoint still has its metadata silently stripped), traced directly to `edgeRoleDecision`'s own existing partition-resolution and fail-closed pattern and ruled the same day, no founder consultation needed. Codex implemented that fix (`0a62803`, 42 tests passing) and stopped a third time on two findings together: F290 (`SkillGap.severity` has no calendar to resolve for a Project with no Assignment, the exact case that creates a gap) — a genuine architectural fork, no precedent to trace to, put directly to the founder and decided the same day: resolve from Assignments, else the workspace's sole Entity, else `Low` — and F291 (the generic `graph.createEdge`/`graph.updateEdgeMetadata` mutations enforce none of `has_skill`/`requires_skill`'s own business rules, letting a caller bypass verification derivation or the duplicate-edge rule entirely), traced directly to `FEATURE_LIFECYCLE_NODE_TYPES`'s own existing node-level precedent and closed the same day, no founder consultation needed. Codex implemented both fixes (`e4d15e2`) and stopped a fourth time on two findings together: F292 (item 7's `matchCandidates` reads candidates via `filterReadable`, which silently drops every qualifying Employee outside the caller's own direct-reports/team scope under `Employee:operational`'s current policy row — not new to the matcher, `listEmployees`/Bench Forecast's own cohort hits the identical drop today, so the spec's own “`VRS-F005` already renders every employee” justification does not actually hold) — a genuine architectural fork, no precedent for a policy cell holding a wider read ceiling than its write ceiling, put directly to the founder and decided the same day: a general `readScope` split on `PolicyCell`, consulted only by the read path, over a matcher-only carve-out — and F293 (the new `skill-gap-evaluate` system principal had no read grant for its own match cohort, so every reactive evaluation would silently see zero candidates), traced directly to `utilization-snapshot.read-cohort`'s own precedent (F280/F281) and closed the same day, no founder consultation needed. Codex implemented both fixes and, building the matcher through to a full `pnpm verify:full` run, reported it a fifth time on F294 (F292's now-wide `Employee:operational` read silently widened `pitch-queries.ts::listStaffedFor`'s and `conflictResolution.sweepOvercommitted`'s own older, unrelated, still-narrow contracts — `VRS-F009`/F264 and `VRS-F008`/F263 respectively — breaking three PostgreSQL tests), plus four other failing tests it flagged as likely-stale F292 fallout pending confirmation. Closed the same day by direct precedent, reusing F292's own `bestCell`/`operation` narrow-scope mechanism through a new shared primitive; independently checking every other caller of the same pattern before ruling (not just Codex's own two) surfaced F295, a third instance the report never named — `timesheet-queries.ts::listSubmissionStatus`, live and unguarded since Stage 18 (F273), narrower still than F294's two (`VRS-F010`'s own row is Team Member "own only," not "own+team") — closed the same day on the identical precedent. The four other failures confirmed as F292's own already-decided widening propagating correctly through `audience-recompute`'s design, not a new question. Codex implemented both fixes and reported Stage 21 complete at `15d8aaf` (all four gates passed, 319 API tests passed / 2 skipped); independently verified directly against the code before merge, not the completion report's prose — see section 22 for the full completion review. Merged `--no-ff` as `414b8df`. `RST-49` moved to Done.


## 21. Stage 20 — Revenue Gap Alert (`VRS-F012`)

`VRS-001`'s Feature Register confirms `VRS-F012` as the correct next MVP-order feature after `VRS-F011` (Stage 19, merged `--no-ff` as `0e7e4d9`) — the automated bench-monitoring engine that writes a `RevenueGapAlert` once a configured threshold is crossed, carrying a daily and accumulated cost. Checked directly against the code before writing this brief: `RevenueGapAlert` is already registered (`packages/schema/src/registry/nodes.ts`, owner `VRS-F012`, `fixedProtection("Standard", 0)`) with no policy-table row yet; the `triggered_by` edge to Employee is already generalized in the edge registry; `Assignment.effective_billing_rate` is already stored per-Assignment by `VRS-F006` (Stage 14), directly readable with no second rate-card traversal; no job-scheduler or cron infrastructure exists anywhere in the codebase (`VPS-A006` is genuinely unbuilt, already correctly named in the spec's own Depends-On section).

Three findings surfaced checking this feature directly against the code before writing the brief, all closed the same day, all traced directly to existing precedent, no founder consultation needed:

- **F282** — The spec's Depends-On section, monitoring-engine description, API contracts, System States table and NFRs all still describe the engine running inside a Web Worker, computing "entirely from the local graph," with alerts "written offline sync within 1 second of reconnection" — the local-first architecture dropped project-wide before Stage 7, never corrected for this spec since this is the first time it has been read directly ahead of its own stage (unlike `VRS-F005`/F236 and `VRS-F006`/F244, both corrected at their own briefings). Ruled: corrected throughout to the server-authoritative shape every reactive engine since Stage 18 already uses — no Web Worker, no offline-sync requirement, `RevenueGapAlert` reaches the client the same way `UtilizationSnapshot`/`TimesheetAnomalyFlag` already do, via Electric's own shape sync.
- **F283** — G02 requires this feature to call `VRS-F005`'s bench computation "including... the Pitch exclusion," naming `benchForecast.computeBench(employeeId)`. Neither claim survives a direct read of `bench-forecast-queries.ts`: no function of that name exists (its two exports are a cohort-wide aggregate and a compensation-based cost query — the latter reading the Tier-1/2 `compensation` partition this feature explicitly must never touch), and the private `cohortEmployee` helper that actually computes each employee's `benchDays` has never excluded Pitch time — built at Stage 13, before Pitch time tracking existed (Stage 17/18), and never revisited; Stage 13's own briefing had already flagged this exact deferral (F239) and no later stage closed it. Ruled: the fix lands in `VRS-F005`'s own module, on the spec's own stated intent — `cohortEmployee`'s exclusion gains a third check (a logged Pitch-category `TimesheetEntry` on that date), and a new exported single-employee `computeBenchStatus` wraps the identical logic for this feature to call, replacing the non-existent `benchForecast.computeBench`.
- **F284** — The spec's Security Considerations and System States table both state cost visibility "follows `VRS-F005`'s rule" (compensation-gated), directly contradicting the same document's own Decisions Recorded correction ("cost figures derive from billing rates, not compensation... this feature never reads a salary") and the already-registered flat-Tier-0 `RevenueGapAlert` node (no partition split to gate against). Ruled: the stale bullet and System-States row removed — `VRS-F005`'s cost-visibility gate exists because *its* cost figure is compensation-derived; this feature's billing-rate-derived figure is not, so the two cannot share one rule. `RevenueGapAlert`'s cost fields are genuinely Tier 0, visible to anyone who can read the alert at all.

`docs/Vulto_Specs/VRS-F012_Revenue_Gap_Alert.md` corrected to match all three, with Decisions Recorded entries covering each. Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, Stage 20 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side only, following every prior stage's own precedent: no file under `apps/` changes; `VPS-F003` (the Inbox this alert will eventually surface in) is itself still unbuilt, an already-documented forward dependency — this stage builds the schema, engine and API only. `RST-48` is its Linear issue; the branch is `codex/stage-20-revenue-gap-alert`, started fresh from `main` carrying the F282–F284 corrections and Stage 19's merge. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**What to check in the Stage 20 report:** that `cohortEmployee`'s new Pitch exclusion and the new `computeBenchStatus` export share one implementation, not two independently-written ones that could silently disagree (F283); that `daily_cost` resolves from the most recently ended Assignment's own stored `effective_billing_rate`, never a fresh lookup or `billing_rate_default` used ahead of it (G04); that only one Active `RevenueGapAlert` node ever exists per employee across repeated evaluations, proven by a test reusing the same node ID (G05); that `revenueGapAlert.evaluate` genuinely runs under the new `revenue-gap-alert-evaluate` system principal, not the triggering caller's own; that `SYSTEM_OPERATION_TARGETS` gained exactly one new entry for `revenue-gap-alert.write`, with no interceptor branch hardcoded a fifth time (A004-T25); that Assignment creation genuinely resolves an existing Active alert end-to-end through the new `afterCommit` hook, with a real `resolved_by_assignment_id`, not a direct call to `evaluate` alone; that dismissal leaves `lifecycle_status` as `Active`, read directly off the mutation's own field list (G07); that the sweep is a callable procedure, not an actual cron — grep for any scheduling library or cron syntax and confirm none was added; that Ghost Resources are excluded at the first step of every evaluation path, not merely filtered from the sweep's own list (G09); and that no file under `apps/` appears in the diff.

## 22. Stage 21 — Skill-to-Project Matcher (`VRS-F013`)

`VRS-001`'s Feature Register confirms `VRS-F013` as the correct next MVP-order feature after `VRS-F012` (Stage 20, merged `--no-ff` as `c2583a5`) — a graph traversal engine with two modes sharing one mechanism: a persistent mode matching a Project's `requires_skill` edges against Employees'/Ghosts' `has_skill` edges, availability-filtered via `VRS-F005`'s bench computation, writing a `SkillGap` on zero matches; and an ad-hoc mode for the immediate "who can do this, and are they free" question, no persistence. This feature owns Skill's complete schema and the proficiency ordering the whole product uses. Checked directly against the code before writing this brief: `Skill` and `SkillGap` are already registered in `packages/schema/src/registry/nodes.ts` (owners `VRS-F013`, both Standard/Tier 0) with no policy-table row yet; `has_skill` and `requires_skill` are already registered in the edge registry; `VRS-F002`'s own API contract has specified `employee.attachSkill` since it was written, but the mutation was never built; Project itself carries no `start_date` field anywhere in the codebase, having only ever been bootstrapped minimally by `VRS-F005` at Stage 13.

Three findings surfaced checking this feature directly against the code before writing the brief, all closed the same day, all traced directly to existing precedent, no founder consultation needed:

- **F285** — `requires_skill` (Project/OpenRole/Pitch/CareerMilestone/Requisition → Skill) declared no `governingPartitions` entry at all, though Pitch and Requisition are both tier-split — the sixth recurrence of `staffed_on`/`logged_against`'s own class of gap (F265, F267), and the second time it has landed on Pitch specifically. Ruled: `governingPartitions: { Pitch: "identifying", Requisition: "identifying" }` declared, matching `staffed_on`'s/`logged_against`'s own precedent — a skill requirement is non-financial descriptive data on both endpoints. Fixed directly in `packages/schema/src/registry/edges.ts`, already committed ahead of this brief.
- **F286** — The spec's Interface Specification, Technical Architecture, API contracts, System States table, Acceptance Criteria and NFRs all still describe the dropped local-first architecture — "Entirely local," results resolving "from the local graph," a 10ms/200ms client-side latency budget, and an explicit offline acceptance criterion — never corrected for this foundational spec since it predates Stage 7's pivot. Ruled: corrected throughout to the server-authoritative shape every reactive engine since Stage 18 already uses — both match modes become ordinary server-side authorized reads, SkillGap detection and auto-resolution become a reactive `afterCommit` write on `has_skill`, and the ad-hoc mode's per-keystroke budget is retired along with the local scan it assumed.
- **F287** — `VRS-F002`'s API contract has read `employee.attachSkill(employeeId, skillId, proficiencyLevel) -> { edgeId }` since that document was decided at founder level, with G03 fully specifying its replace-on-duplicate semantics — but no such mutation exists anywhere in `services/api/src/mutations/employee.ts` or the mutation-type registry. `has_skill` can currently only be written through Ghost Resource creation, leaving `VRS-F013` with no path to attach a skill to a real Employee at all. Ruled: built in `VRS-F002`'s own module this stage, matching its already-specified contract and G03's semantics exactly — the same already-complete-spec, never-built-mechanism pattern F283 closed for `VRS-F005`. `employee.attachCertification`/`holds_certification` is explicitly left untouched: the executable registry names `VRS-F041` as that edge's real owner, not `VRS-F002`.

Codex began implementing this brief and, tracing `project.attachSkillRequirement` into the policy table before writing product code, stopped once more on a fourth finding:

- **F288** — Manager — the brief's own intended caller of `project.attachSkillRequirement` — holds only `READ_ANY()` on Project (`MATRIX_OVERRIDES.Project`, confirmed by direct read), not Full, while `requires_skill`'s registration declared no `readSufficientEndpoints`, so `edgeRoleDecision`'s unscoped-Full default denied the write. This is F262's own shape recurring on a second edge: F262 (Stage 16) resolved the identical Manager-`READ_ANY()`-not-Full condition for `assigned_to`/Project by declaring `readSufficientEndpoints: { Project: true }`, "mirroring `VPS-A004`'s own read-traversal rule." Ruled: the same declaration added to `requires_skill`'s registration, no policy-table change and no spec-text change — F262 itself required neither, for the same reason.

Codex resumed on that ruling and, tracing `employee.attachSkill`'s write path into the graph store before writing it, stopped once more:

- **F289** — `services/api/src/graph/store.ts::insertEdge` strips an edge's `metadata` to `{}` whenever either endpoint's node type has *any* non-Tier-0 partition anywhere (`hasProtectedPartition`), regardless of which partition the edge itself concerns. Employee is tier-split (`operational`, Tier 0; `compensation`, Tier 1), so every Employee-touching edge loses its metadata unconditionally — `has_skill` included, even though its own registration already declares `governingPartitions: { Employee: "operational" }` (F136/FDN-92, Tier 0). `insertEdge` already calls `assertRegisteredRelationship` and receives that exact registration back, unused. This defect has never surfaced before: no edge touching Employee has ever needed non-empty metadata (`pattern_for`'s data lives on the node, not the edge; Ghost Resource's existing `has_skill` writer sets none), and `store.integration.test.ts`'s own protected-partition test asserts on `managed_by`, pinning the buggy behavior. Ruled: `insertEdge` corrected to resolve each endpoint's governing partition the same way `edgeRoleDecision` already does — `getProtectionPartitions` plus the registration's own `governingPartitions[nodeType]`, failing closed when a tier-split endpoint declares none, exactly mirroring `edgeRoleDecision`'s own `declared === undefined` branch — and stripping metadata only when `resolveRegisteredProtectionTier` resolves a non-zero tier. A new generic Tier-0 primitive, `graph.updateEdgeMetadata`, extends `graph.createEdge`/`graph.closeEdge` with the identical corrected check for `has_skill`'s in-place replace-on-duplicate write. No genuine fork: the classification data and the fail-closed convention already exist for exactly this purpose in `edgeRoleDecision`; `insertEdge` was simply never updated to consult them.

Codex implemented F289's fix and, tracing `skillGap.evaluate`'s severity computation and the generic edge-write paths it had just made functional, stopped a third time on two findings raised together:

- **F290** — `SkillGap.severity` counts working days to `Project.start_date` per G07, but Project has no structural link to any calendar — no Assignment, no Entity, no Client jurisdiction — and `resolvedDayOn`/`countWorkingDays` require a real Employee (personal pattern and location included). The exact case that creates a SkillGap — zero qualifying candidates — need not have any Assignment at all, and even unrelated Employees' Entity calendars can genuinely disagree within one workspace (`entity.create` has no uniqueness constraint; multiple Entities are already structurally possible, `VRS-F064` being the unbuilt feature that would make it a real, exercised case). **Genuine architectural fork — no precedent to trace to — put directly to the founder.** Three options reasoned through: degrade to `Low` whenever no calendar resolves (simplest, but silently under-reports urgency for the single most product-critical case — a fresh, understaffed Project with a real start date, exactly the shape most SkillGaps on a new Project would take); commit a real Project→Entity link now (correct long-term, but a schema commitment beyond this stage's boundary, duplicating ground `VRS-F064` will cover regardless); or resolve opportunistically — the Project's own Assignments first, else the workspace's sole Entity (every workspace today has at most one), degrading to `Low` only in the genuinely ambiguous residual case. **Decided by the founder: the third option.** New `resolveProjectCalendarEntity` and `countWorkingDaysForEntity` implement it, sharing the same `resolveWorkingDay` pure function `countWorkingDays` already uses (G08's "one implementation" rule intact) — explicitly recorded as a forward dependency on `VRS-F064` for when multi-entity becomes real.
- **F291** — `has_skill`/`requires_skill` gained real business rules this stage (F287's server-derived `verified`/`verified_by`/`verified_at`; both edges' G03 at-most-one-active-edge rule), but the generic `graph.createEdge`/`graph.updateEdgeMetadata` mutations — reachable by any authorized caller through the shared `graph.applyMutations` pipeline (established as the standard write path since F266) — had `validate: nothing` and enforced neither, letting a caller self-attest `verified: true` or create a duplicate edge directly, entirely outside the named mutations. Ruled: not a new problem — `FEATURE_LIFECYCLE_NODE_TYPES` already solves the identical shape for nodes (`graph.createNode`/`updateNodeFields`/`softDeleteNode`/`transitionLifecycle` all reject a listed type with `MutationRejection("requires-feature-mutation")`). A parallel `FEATURE_OWNED_EDGE_TYPES` set (`has_skill`, `requires_skill`) gives the generic edge mutations the identical check, reusing the same rejection reason — edge-type-wide, matching the node-side granularity, so `requires_skill`'s four still-unowned pairings (Pitch, OpenRole, CareerMilestone, Requisition) are blocked from generic writes too, an accepted forward dependency since nothing writes them today. F285's own "real write" proof requirement is corrected alongside it: that proof now comes from an `edgeRoleDecision`/`insertEdge`-level test, since the full generic path is correctly closed.

Codex implemented F290's and F291's fixes and, resuming Stage 21 a fourth time, stopped on two access-authorization findings raised together:

- **F292** — `VRS-F013`'s Decisions Recorded settles the product question: employee operational data (name, role, skills, availability) is readable by every workspace member. But the executable `Employee:operational` row (`packages/schema/src/policy/policy-table.ts`) still carries Manager `Full (direct reports)` and Team Member `Read (own + team)`, and `PolicyCell` holds one `(outcome, scope)` pair per role/partition — `bestCell`'s row-scope check resolves an out-of-scope row straight to `none`, and `filterReadable` drops a `none` row entirely, so item 7's matcher would silently return incomplete results and could create a false `SkillGap`. Checked whether the spec's own cited precedent actually holds: it does not — `bench-forecast-queries.ts::authorizedCohort` calls the identical `listEmployees`/`filterReadable` path under the caller's own principal, so a real Manager or Team Member already can't see colleagues outside their own scope on the Bench Forecast page today. This is a second, previously undiscovered instance of the same gap, not a working example of the fix the spec assumed already existed. **Genuine architectural fork — no precedent for a policy cell holding a wider read ceiling than its write ceiling — put directly to the founder.** Two options reasoned through: a general `readScope` split on `PolicyCell` (more surface, but correctly closes the identical gap in Bench Forecast and the plain directory too, matching what the spec's own reasoning actually asked for) or a matcher-only dedicated read carve-out (smaller, but leaves those two known-and-now-documented gaps unfixed). **Decided by the founder: the general split.** `PolicyCell` gains an optional `readScope`, consulted by `bestCell` only for `operation: "read"` and only after the narrow `scope` check fails, ceilinged at `read`; `decideRead` is the only call site that passes `"read"`, so `edgeRoleDecision` and the node-write gate are unaffected. `Employee:operational`'s `manager` and `team-member` cells both gain `readScope: "any"`.
- **F293** — Item 8 requires `skillGap.evaluate` to call item 7's `matchCandidates` under the new `skill-gap-evaluate` system principal, which reads candidates via `filterReadable`. A system principal holds no `PolicyRole` at all — `decideRead`'s system branch checks only `SYSTEM_OPERATION_TARGETS`'s closed list for the named operation, never a policy cell (A004-T25) — and item 9 as originally specified granted this principal only `skill-gap.write`, targeting `SkillGap:record`, no read of any kind. Without one, every reactive evaluation would read zero candidates and create a `SkillGap` for every requirement, real matches included. Ruled by direct precedent: the identical shape F281 already solved for `utilization-snapshot-compute` (two operations, `.compute` and `.read-cohort`, declared side by side in one closed table) — item 9's own original text had already named this precedent without extending it far enough. A new `"skill-gap.read-cohort"` operation, granted alongside `"skill-gap.write"`, targeting `{ nodeType: "Employee", partitionKey: "operational" }` only — no Skill or Assignment target: item 7's Assignment-coverage reads reuse `bench-forecast-queries.ts`'s own trusted, unauthenticated `getNodes` pattern (never `filterReadable`), and matching itself compares required proficiency against the `has_skill` edge's own metadata, never Skill node content (Skill node reads belong only to `adHocSearch`'s free-text mode, under the calling member's own ordinary principal).

Codex implemented F292's and F293's fixes and, building the matcher through to a full gate run, stopped a fifth time on findings F294 and F295:

- **F294** — F292's `readScope: "any"` correctly widens `Employee:operational`'s directory-purpose read, but two older queries had no admission check of their own — they treated that same generic Employee decision as sufficient to disclose something else entirely. `pitch-queries.ts::listStaffedFor` gates an Employee's staffed-Pitch/client-name disclosure on it, though `VRS-F009`/F264 deliberately gave Team Member `None` at the generic Pitch level, sanctioned only through this proxy check on the (formerly narrow) `Employee:operational` row-scope. `conflictResolution.sweepOvercommitted` filters its cohort through the same call before disclosing overcommitment, though `VRS-F008`'s own spec is explicit that the sweep is direct-reports/own-plus-team-scoped, full stop. Neither spec was revisited or superseded by F292 (whose own Decisions Recorded covers only directory-purpose visibility); this is a mechanism problem — two callers borrowed a check that happened to equal their own boundary, and F292 correctly changed what it now means — not a product question. **Closed by direct precedent.** F292's own `bestCell`/`operation` mechanism already resolves a cell's literal, narrow `scope`, ignoring `readScope`, whenever not explicitly called with `"read"`. A new exported primitive resolves a candidate Employee against any named node type's own policy cell using that narrow tier, without requiring a real stored row of that type to exist; both queries call it against `Employee:operational` specifically — their own governing specs define exactly that scope — restoring their pre-F292 boundary with zero effect on F292's own directory fix. The four other failing tests (three audience, one matrix fidelity) are F292's own ruling propagating correctly through `audience-recompute`'s design (which derives a device's cache directly from `decideRead`'s outcome) — update them to the ruled wide scope, don't revert the fix.
- **F295** — Verifying F294's fix against every other caller of the same pattern, not only Codex's own two, surfaced a third the report never named: `timesheet-queries.ts::listSubmissionStatus` also filters its cohort through `Employee:operational`'s `filterReadable` before disclosing each Employee's timesheet compliance status. But `TimesheetEntry`/`TimesheetWeekSubmission`'s own row (F273, Stage 18) is narrower than `Employee:operational` has ever been — Team Member `Full (own only)`, not `own-plus-team` — so a Team Member has been able to see teammates' compliance status since Stage 18, independent of F292 entirely; no test exercises a non-Owner principal on this function, so nothing caught it. **Closed by direct precedent, the identical mechanism as F294.** Called against `TimesheetEntry:record`'s own cell instead of `Employee:operational`'s — narrower than F294's own fix (`own`, not `own-plus-team`), matching `VRS-F010`'s explicit text exactly.

`docs/Vulto_Specs/VRS-F013_Skill-to-Project_Matcher.md` corrected to match F286 and F287, with Decisions Recorded entries covering all three findings (F285 touches only the executable registry, no spec text to correct). F288 through F291 touch only executable code too, no spec text to correct. Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, Stage 21 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side only, following every prior stage's own precedent: no file under `apps/` changes; `VPS-F002` (Local-First Search, whose palette will eventually call the ad-hoc mode) is itself still unbuilt, an already-documented forward dependency — this stage builds the matching engine, `SkillGap` lifecycle and `employee.attachSkill`/`project.attachSkillRequirement` mutations only, not the `Cmd+K` trigger. `RST-49` is its Linear issue; the branch is `codex/stage-21-skill-to-project-matcher`, at `84d57c1` with F288's through F293's rulings already implemented (the matcher, both named mutations, the reactive evaluator and router wiring all built with no stubs) and F294's/F295's rulings now ready to resume on — not restarted. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**What to check in the Stage 21 report:** that `employee.attachSkill`'s replace-on-duplicate genuinely updates the existing `has_skill` edge in place rather than closing and recreating one (G03), and that `verified`/`verified_by`/`verified_at` are derived from the caller/target relationship, never accepted as a caller-supplied boolean (F287); that the match engine is a single shared implementation called by both `getMatchResults` and `adHocSearch`, not two independently-written traversals (G05's "never a second implementation"); that availability resolution reuses `bench-forecast-queries.ts`'s own Assignment-coverage primitives rather than a parallel query, and that a candidate covered beyond the requested `availabilityWindowDays` is genuinely excluded from results, not merely labeled with a distant date; that `SkillGap` evaluation runs under the new `skill-gap-evaluate` system principal on both trigger paths — `project.attachSkillRequirement`'s `afterCommit` and `employee.attachSkill`'s `afterCommit` — never the triggering caller's own, read directly off the mutation's own principal-substitution code; that `SYSTEM_OPERATION_TARGETS` gained exactly one new entry for `skill-gap.write`, with no interceptor branch hardcoded a sixth time (A004-T25); that `requires_skill`'s `governingPartitions` fix (F285) is genuinely exercised by at least one real write touching Pitch or Requisition, not only the Project-Skill pairing this stage's own flow uses; that a Manager with only `READ_ANY()` on Project can genuinely call `project.attachSkillRequirement` end-to-end, proven by a real test, not merely that `readSufficientEndpoints: { Project: true }` is present in the registry (F288); that `employee.attachSkill`'s `proficiency_level`/`verified`/`verified_by`/`verified_at` genuinely persist and survive a re-read after a duplicate attachment updates the edge in place, proven against real Postgres, not merely that `insertEdge`'s corrected check compiles (F289); that the corrected `store.integration.test.ts` proves metadata is still genuinely stripped for a tier-split endpoint with an undeclared or non-zero-tier governing partition, not only that it is now preserved for `has_skill`/`managed_by` (F289's protection is not weakened); that `resolveProjectCalendarEntity` genuinely picks the earliest-staffed Assignment's Employee when one exists, and falls back to the workspace's sole Entity only when none does, proven by tests covering both branches plus the residual `Low`-degrade case (F290); that a direct `graph.applyMutations` call attempting `graph.createEdge`/`graph.updateEdgeMetadata` on `has_skill` or `requires_skill` is genuinely rejected with `requires-feature-mutation`, while `employee.attachSkill`/`project.attachSkillRequirement` themselves still succeed through the same pipeline (F291); that `employee.attachCertification`/`holds_certification` was not built (F287's explicit scope boundary); that `Project.start_date` is added as a plain nullable field with no new named mutation, and that a null start date degrades `SkillGap.severity` to `Low` rather than erroring; that a Manager or Team Member genuinely reads a qualifying candidate Employee outside their own direct reports/team through item 7's matcher, proven by a real test under both roles, while that same non-report Employee remains unwritable by that Manager through the ordinary node-write gate and `edgeRoleDecision` — proving the read/write split actually split, not merely widened both (F292); that `skillGap.evaluate` run under the `skill-gap-evaluate` system principal genuinely reads real candidates and resolves an Active `SkillGap` when one newly qualifies, not just that it fails to error — proven by a real test, since a silent zero-candidate read and a genuine capability gap produce the identical visible symptom (F293); that `SYSTEM_OPERATION_TARGETS` gained exactly two new entries this stage in total — `skill-gap.write` and `skill-gap.read-cohort` — with no interceptor branch hardcoded a sixth time (A004-T25); that an unrelated Team Member calling `listStaffedFor`/`pitch.listStaffedFor` for another Employee still learns nothing — no Pitch name, no client name, no distinguishable response from a nonexistent Employee — proven by the existing no-existence-leak test passing again, not merely not erroring (F294); that `sweepOvercommitted` still returns only a Manager's direct reports and a Team Member's own team, proven by the existing two F263 tests passing again with a genuinely out-of-scope overcommitted Employee present in the fixture (F294); that `listSubmissionStatus` returns a Team Member only their own compliance status, not their team's — proven by a new test, since none previously existed for a non-Owner principal (F295); and that no file under `apps/` appears in the diff.

**Independently verified and merged, 25 September.** Codex reported Stage 21 complete at `15d8aaf` (all four gates passed; `verify:full` — database preflight and API 319 tests passed, 2 skipped, across 25 files; the focused F294/F295 suites 81/81; the matcher integration suite 9/9; `RST-49` moved to In Review). Verified directly against the code before merge, not the completion report's prose: `interceptor.ts`'s new `withinRoleScopedReach` primitive resolves a candidate Employee's admission through a *named* node type/partition's own policy cell via `bestCell`'s write-tier resolution (`operation: "write"`, so F292's `readScope` is never consulted), using a new `subjectEmployeeIdOverride` on `RowScopeContext` so no stored row of that other type needs to exist — matching the F294/F295 ruling exactly. Its three call sites match precisely: `pitch-queries.ts::listStaffedFor` and `conflict-resolution-queries.ts::sweepOvercommitted` both check against `Employee:operational` (their own governing scope, per `VRS-F009`/F264 and `VRS-F008`/F263), and `timesheet-queries.ts::listSubmissionStatus` checks against `TimesheetEntry:record` instead (`VRS-F010`/F273's own narrower row), not `Employee:operational` — the distinction F295 itself turned on. `pitch-queries.ts`'s `listStaffedFor` was correctly retyped from `Principal` to `MemberPrincipal`, and confirmed safe: `trpc.ts`'s `createContext` only ever builds `protectedProcedure`'s `ctx.principal` with `kind: "member"`, so no support/system caller could have reached the old, wider type. The new `timesheet.integration.test.ts` case proves the real boundary, not a tautology: it inserts a `managed_by` edge making a second employee (`outsiderId`) the Team Member's teammate, independently confirms via `authorizeRead` that the teammate's `Employee:operational` row is genuinely `"read"`-or-`"full"` (F292's wide grant working as ruled), and then shows `listSubmissionStatus` still returns only the Team Member's own row — while the Owner's call still sees the teammate. The pre-existing, unmodified F263/F264 tests in `capacity-conflict.integration.test.ts` and `pitch.integration.test.ts` (never touched by this fix commit) are the proof the old narrow contracts are restored, not merely re-asserted. The `audience.integration.test.ts` and `interceptor.integration.test.ts` oracle updates were checked to encode F292's ruled wide scope (`cell.readScope === "any" && READABLE.has(cell.outcome)`) rather than revert it — matching the reviewer's own reasoning that those four failures were F292's intended consequence, not a new question. `skill-gap.read-cohort`'s `SYSTEM_OPERATION_TARGETS` entry is `[{ nodeType: "Employee", partitionKey: "operational" }]` only, matching F293 exactly — no Skill or Assignment target. `matchCandidates` (`skill-matcher-queries.ts`) reads proficiency only through the `has_skill` edge's own metadata and availability only through `bench-forecast-queries.ts`'s existing trusted `assignmentsFor`/`assignmentCovers` pair, confirming F293's own reasoning that no Skill-node read is or was ever required. `employeeAttachSkill`'s write authority was checked and found correctly untouched by F292: `has_skill`'s `governingPartitions: { Employee: "operational" }` still gates the edge write at the *write*-tier outcome (Manager full/direct-reports, Owner/HR-admin full/any, Team Member read-only — unchanged since F292 only added a read-only `readScope`), so skill attachment stays exactly as restrictive as any other Employee-profile edit; the `self`/`verified` distinction in the mutation is provenance metadata, not an access grant, and only ever fires for a principal already holding write authority (confirmed against the actual test, which exercises HR Admin attaching to their own linked record, not a plain Team Member). `router.ts`'s five new endpoints are all wired under `protectedProcedure`, never exposing the `skill-gap-evaluate` system principal to a caller. `git diff --stat` against the merge base confirmed zero `apps/` files and zero governing spec/build-prompt/findings-ledger files touched by Codex on this branch. `tsc --noEmit` was run independently in this reviewing shell and passed clean. This device's own restricted shell has no Docker, no root, and could not install or reach Postgres, so the live `pnpm --filter @vulto/api test` suite could not be re-run here — consistent with Stage 19's own precedent, verification here is the direct diff and test read above, not a fresh gate run, and is consistent with the report's own gate output. Merged `--no-ff` as `414b8df`. `RST-49` moved to Done.

## 23. Stage 22 — Skill Matrix (`VRS-F014`)

`VRS-001`'s Feature Register confirms `VRS-F014` (Skill Matrix) as the correct next MVP-order feature after `VRS-F013` (Stage 21, merged `--no-ff` as `414b8df`) — a read-only grid, employees by rows and skills by columns, showing coverage and depth per skill with no query and no forecast. It adds no fact to the graph and, per its own spec, introduces no new permission logic. Checked directly against the code before writing this brief: `Skill`'s schema is already complete from Stage 21, including `lifecycle_status: "Active" | "Deprecated"` (G04's deprecated-column treatment needs no new field); `Skill`'s own policy row already grants every role at least `READ_ANY()`, Team Member included, so no policy change is needed at all; `packages/graph/src/queries/bench-forecast.ts` is a live, working precedent for exactly this shape of query — a purely local read over the device's own synced cache (`localNodes`/`localEdges`), no interceptor call, no server round trip — and that same local module already fetches `Skill` nodes and `has_skill` edges for its own filtering today, confirming this data already reaches the client; Ghost rows are `Employee` nodes with `employee_type: "Ghost"`, identical to `VRS-F013`'s own representation, not the separately-registered `GhostResource` node type (which is used for creation/promotion bookkeeping only, and carries no `has_skill` edges of its own); and the proficiency ordering (`PROFICIENCY_LEVELS`, `proficiencyMeets`) is already exported from `packages/schema/src/skill-matcher.ts` for exactly this reuse (G02).

One finding surfaced the same day, closed on the spot, no founder consultation needed — **F296**: `VRS-F013`'s own `certified` field has been hardcoded `false` since Stage 21, because nothing in the graph can produce anything else (`has_skill` has no `certified` metadata slot, `holds_certification` is registered but unwritable, and `VRS-F041` — the only feature that will ever derive it — is unbuilt and far down the MVP order). `VRS-F014`'s own spec repeats the identical unqualified claim one level further downstream ("a small filled dot in the corner marks certified"), with no caveat that the fact behind it has never existed. Traced directly to `VPS-F002`'s own already-documented forward-dependency pattern — not a defect to fix now, a documentation gap. Both `docs/Vulto_Specs/VRS-F013_Skill-to-Project_Matcher.md` and `docs/Vulto_Specs/VRS-F014_Skill_Matrix.md` corrected with a one-line note: `certified` reads `false` everywhere until `VRS-F041` exists and decides whether it becomes a genuinely separate `has_skill` metadata field. Stage 22 carries `certified: false` forward unchanged, honoring the API contract's shape exactly as Stage 21 did.

Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, Stage 22 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Entirely local — the spec's own API contract (`skillMatrix.get`, `skillMatrix.forSkill`) names no server endpoint, and none is needed: no new mutation, no new policy row, no new system principal, no file under `apps/` or `services/api/` changes; the Interface Specification (the grid, filters, Panel views, keyboard bindings) is reference material for whichever later stage wires the real UI, the same deferral every prior stage's own Interface Specification has received. `RST-50` is its Linear issue, filed and moved to In Progress; the branch is `codex/stage-22-skill-matrix`, started fresh from `main` carrying the F296 correction and Stage 21's merge. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**What to check in the Stage 22 report:** that `skillMatrix.get`/`skillMatrix.forSkill` are genuinely local — no new `services/api` file, no new router procedure, no network call in the implementation — since the spec's own API contract and G03 both require it; that coverage and depth are computed at query time from `has_skill` edges and recompute against the *filtered* set when a filter is applied, never against the unfiltered population (G01, G05) — proven by a test that changes the answer under a filter, not merely returns a smaller list; that the proficiency ordering and comparator are imported from `packages/schema/src/skill-matcher.ts` unchanged, no second ordering defined (G02); that a Ghost Resource appears as a row through the identical `employee_type: "Ghost"` check `VRS-F013`'s own matcher already uses, not a new or parallel Ghost detection (G06); that a Deprecated Skill's existing cells remain visible with the column marked, read directly off `lifecycle_status`, with no cell silently dropped (G04); that `certified` is `false` on every returned cell, matching F296's ruling, with no interim certification mechanism invented; that employee visibility is whatever the device's own local cache already contains — no second authorization check reimplemented client-side — matching `bench-forecast.ts`'s own precedent exactly (G03); and that no file under `apps/` or `services/api/` appears in the diff.

**Completion review, 25 September 2026 — independently verified against the actual diff, not accepted from Codex's report.** Read `git show --stat`/full diff for the single implementation commit (`64af8e7`): only `packages/graph/{index.ts, queries/bench-forecast.ts, queries/cache-data.ts, queries/skill-matrix.ts, queries/skill-matrix.test.ts}` plus the stage report — no `apps/` or `services/api/` file, no policy row, no mutation, no router procedure, no new system principal. `getSkillMatrix`/`getSkillHolders` (`packages/graph/src/queries/skill-matrix.ts`) contain no `fetch`/tRPC call, confirming the local-only shape. The Ghost check (`employee.record["employee_type"] === "Ghost"`) is textually identical to `VRS-F013`'s own `matchCandidates` (`row.record["employee_type"] === "Ghost"`) — confirmed by direct comparison, not assumed. `proficiencyMeets`/`PROFICIENCY_LEVELS`/`proficiencyLevelSchema` are imported unchanged from `packages/schema/src/skill-matcher.ts`, the same source `VRS-F013`'s own code imports from — no second ordering exists anywhere in the diff. `certified` is typed as the literal `false` on `SkillMatrixCell` and asserted by a dedicated test (`all.cells.every((cell) => cell.certified === false)`), matching F296.

`getSkillHolders`'s `availabilityStatus` reuses a new `assignmentCoversDate` helper factored *out of* `bench-forecast.ts` itself, rather than a second, independently-written Assignment-coverage traversal — the report's specific claim. Checked this refactor is behavior-preserving where it already applied: `getBenchForecast`'s own `employeeAssignments` array was already filtered to `nodeType === "Assignment" && lifecycleStatus === "Active"` *before* the coverage check ran, so the helper's added type/lifecycle guards are redundant-but-harmless there — confirmed `bench-forecast.test.ts` still passes unchanged (1/1). Those same guards are load-bearing, not redundant, at the new `getSkillHolders` call site, which filters `assignmentCoversDate` directly over a mixed-type node list (Employee, Skill, Assignment together) — without them it would silently match non-Assignment nodes. The dedicated ranking test proves the real boundary, not a tautology: an Active Assignment covering the query date reports `Assigned`, a Canceled Assignment covering the identical dates reports `Available` instead.

`cache-data.ts`'s `LocalEdge` gained a `record` field, parsed from `row["record_json"] ?? "{}"`. Confirmed `record_json` is a genuine, always-populated `cache_edges` column (the same `SELECT *` pattern `localNodes` already uses for `cache_nodes`, which has no such fallback) — the `?? "{}"` guard is defensive for a test fixture the report says it found missing that field, not a sign real synced data lacks it. Checked every other `localEdges` consumer in the repo (`bench-forecast.ts`, `conflict-check.ts`, `contextual-intelligence.ts`) for a shape-sensitive comparison the new field could break — none exists; all three consumers read named properties only.

Ran what this environment genuinely can, not what the report claims: after `corepack pnpm install --frozen-lockfile` (needed again to fix the same native-binding arch mismatch from Stage 21's review), `tsc --noEmit` on the `@vulto/graph` package passed clean, and its full `vitest` suite passed 85/85 — matching the report's own count exactly, including `skill-matrix.test.ts` (3/3, all three scenarios above) and `bench-forecast.test.ts` (1/1, proving the refactor's non-regression). The three failing suites in that run (`sync-browser-tests/*.spec.ts`) are pre-existing Playwright specs needing `BETTER_AUTH_SECRET`/the full stack, unrelated to this stage's diff and containing zero tests of their own. The Postgres-backed half of `verify:full` could not be run here — no Docker, no root — the same disclosed, precedented limitation as every prior review.

No findings. Merged `--no-ff` as `cd17db0`. `RST-50` moves to Done.


## 24. Stage 23 — Local-First Search (`VPS-F002`)

`VRS-001`'s Feature Register confirms `VPS-F002` (Local-First Search) as the correct next MVP-order feature after `VRS-F014` (Stage 22, merged `--no-ff` as `cd17db0`): `VRS-F015` is permanently vacant, promoted to the suite as `VPS-F002`, and the MVP order runs `VRS-F014`, `VPS-F002`, `VPS-F003`, `VPS-F004`. It is the `Cmd+K` palette's data layer — one local index over every nameable Tier 0 entity, a command group, and delegation of skill-shaped queries to `VRS-F013`'s existing ad-hoc match. Checked directly against the code before writing this brief: `packages/graph` has no search index of any kind (the device cache holds `cache_nodes`, `cache_edges`, `cache_tags` and the local tables only); `Employee`'s identifying fields (`full_name`, `preferred_name`, `job_title`) are all in the operational Tier 0 half; `Skill`, `Project` and `Client` are registered fixed-Tier-0 node types with real rows and `READ_ANY()` for every role; `Document` is registered with inherited protection and no field schema, `Policy` and `Deliverable` have no field schema for `title`; and `skillMatcher.adHocSearch` is a live server-side `protectedProcedure` (F286), not a local function.

Four findings surfaced checking this feature directly against the code, all closed the same day:

- **F297** — The spec's whole mechanism is SQLite FTS5, but the pinned `wa-sqlite@1.0.0` has no FTS module in either shipped build: `CREATE VIRTUAL TABLE … USING fts5(…)` fails with `no such module: fts5` (and likewise `fts3`, `fts4`, `rtree`), while `json_extract` and `LIKE` work — verified by running it, not read off documentation. **Genuine architectural fork — no precedent — put directly to the founder** with three options: a trigger-maintained derived table (works in the shipped dependency, keeps every guarantee the spec actually states, loses only ranking and tokenizers the spec's Out of Scope already excludes); a custom `wa-sqlite` build with FTS5 (keeps the spec literal but adds a WASM build pipeline and a re-pinned dependency to `VPS-A001`, and native React Native SQLite would need separate verification); or `json_extract` scans of `cache_nodes` with no derived table (smallest, but degrades against the 30ms budget as a workspace grows). **Decided by the founder: the first.** A derived `cache_search` table inside the existing cache, maintained by SQL triggers on `cache_nodes` generated from a single registry, queried by prefix and substring; the mechanism was probed directly — `putNode`'s `ON CONFLICT DO UPDATE`, `deleteNode` and `resetTemplate`'s bulk delete all fire ordinary triggers, including the soft-delete and hard-delete cases — and normalization is SQLite's own `lower()` on both sides so index and query cannot disagree on a character (a JavaScript `toLowerCase()` on one side would silently diverge on non-ASCII text). `CACHE_SCHEMA_VERSION` goes to 3 and `cache_search` joins `REPLICATED_TABLES`; erasure needs nothing new since `eraseDatabases` deletes the whole per-workspace IndexedDB database.
- **F298** — `search.query` is specified as entirely local yet returning `skillMatcher.adHocSearch`'s output unmodified, with offline skill delegation as an acceptance criterion; since F286 that function is a server read. Ruled on F286's and F199's precedent, no founder consultation: `search.query` returns `skillMatched: boolean` instead of `skillMatches`, and the palette calls the existing server function unmodified when it is true, degrading to `requires-connection` offline. No server change.
- **F299** — Only `Employee`, `Skill`, `Project` and `Client` are registered; `Document`, `Policy` and `Deliverable` are forward dependencies added by the stage that gives each a field schema; `search.queryProtected` is deferred until a built node type has a Tier 1–3 searchable name (no built node type has one — `Contract`, `Departure`, `Document` are all unbuilt), owned by whichever of `VRS-F020`/`VRS-F022`/`VRS-F023` builds one first. Ruled on F296's and F248's precedent and the spec's own G05. The registry refuses at registration any type or field it cannot show to be Tier 0, and `AuditEntry` by name, making G02 a property of registration rather than of query-time filtering.
- **F300** — The interceptor-filtering language does not apply to a local read: the device cache already holds exactly the person's own audience (derived from `decideRead`'s outcome, F292's `readScope` widening included), the identical reasoning Stage 22's own G03 ruling recorded. Commands are a static registry with no role gating this stage — the spec never hides a command from any role and a create-style action has no row to resolve a row-scoped policy cell against; a local role signal does exist (F261's replicated `WorkspaceMembership` via `membership_of`, `deriveEffectiveRoles`, used by `conflictCheck.evaluate`), so gating is feasible later once a decision exists for evaluating a create without a row. Recorded as a known limitation. A first draft of this ruling wrongly claimed the device had no role signal at all; that was caught while writing the finding, checked against `conflict-check.ts`, and corrected before anything was committed.
- **F301** — Codex, tracing the brief before writing product code, found the brief's Gates paragraph and report checklist claimed `verify:full` already runs a device-store browser suite. It does not; the suite is the separate `pnpm test:sync-browser`, run by `.github/workflows/slow-lane.yml`'s own `sync-browser` job on every push. The brief was wrong, not the code — closed on F259's own precedent, no founder consultation needed. `cache_search`'s triggers are the first SQL objects beyond tables and indexes to run inside the real `IDBBatchAtomicVFS` database (every unit test uses the in-memory VFS), so the suite is a genuine gate here, not a formality. Ruled: `pnpm test:sync-browser` (full suite) is an additional required gate, with the builder reporting the exact command, or — if its environment cannot run Chromium and Electric — the pushed branch's slow-lane `sync-browser` job conclusion and run URL; plus one added assertion in the existing `sync.spec.ts` (`dump()["cache_search"]` row count and content against `cache_nodes`). The A003-T56 no-protected-data scan reads every `sqlite_master` table, so it covers `cache_search` automatically. The reviewer's own shell has no Docker or GitHub credentials, so this is disclosed reliance on the builder's output, the same class of limitation as the Postgres suite.
- **F302** — Resuming on F301, Codex traced Do item 8's fixture and stopped again before product code: the browser suite has no named Employee (`seedProtected` creates one through `addNode` with universal fields only; `seedEntities` creates only `Entity` nodes). The brief, not the code, was wrong again — the reviewer had written item 8 without reading `helpers.ts`. Tracing the fixture also surfaced the more important half: that nameless Employee row already reaches devices in the suite, and the brief's own trigger would insert `label` (`NOT NULL`) from an absent `full_name`, abort `putNode` and break replication of that row. Closed on F259's/F301's precedent and the suite's own header (which names `sync-browser-support.ts` as the one place API internals are gathered for it), no founder consultation needed. Ruled: (1) the triggers index a node only when its registered label field is a non-empty string (`json_type = 'text'` and `<> ''`; other non-text indexed values treated as absent) — a nameless node is unsearchable, never an error, and the trigger has no path that can raise; (2) `seedProtected`'s Employee gets operational Tier 0 fields via `insertNode(tx, { ...nodeRecord(...), employee_type, full_name: SEARCH_FIXTURE_EMPLOYEE_NAME, job_title })`; (3) exactly one re-export line each for `nodeRecord` and `insertNode` in `services/api/src/test/sync-browser-support.ts` — the single test-only exception to the stage's `services/api/` boundary, with `addNode` untouched and no deep-path imports; (4) the assertions move to the existing role-removal test (it already uses `seedProtected`, `dump()` and an Electric move-out): a polled `cache_search`-versus-`cache_nodes` `node_id` set equality at both the initial sync and after the role change and audience recompute, plus the Employee's `search_text` containing its lowercased name. Team Member retains `Employee:operational` read (F292), so the Employee is not the row that moves out; the second check proves index consistency through move-out deletes, and no new scenario is built.
- **F303** — Resuming on F302, Codex traced the new label guard against the real Ghost writer and stopped a third time before product code: `ghostEmployeeOperationalRecord` (`packages/schema/src/employee.ts`, used by `ghost.create`) deliberately sets `full_name: null` and puts the role title in `job_title` (`VRS-F007`, settled at F253), so a guard that resolves the label from `full_name` alone excluded every real Ghost, contradicting the brief's own Ghost requirements and `VPS-F002`'s acceptance criterion. A test Ghost with an invented name would have hidden it. The guard was written for one row shape without checking every real writer of that node type. Closed on `VRS-F007`'s own contract (F253), which must not change to suit search — no founder consultation needed. Ruled: `labelFields`, an ordered list (`Employee`: `full_name`, then `job_title`; the other three `name`), with the label the first non-empty text; a Ghost is indexed under its role title with `secondaryLabel` `null`; tests build the Ghost with `ghostEmployeeOperationalRecord`; the browser suite's expected set is rows with a resolvable label; and `operationalShape` (module-private today) is exported, or a key list derived from it, for the registry's Tier 0 validation. The reviewer audited the rest of the brief's field assumptions against the code in the same pass (`Skill.name`/`category` exist with `category` nullable, `Project`/`Client` `name` read as `record["name"]` by existing queries, Assignments link through `record["employee_id"]`, `record_json` holds the record at its top level).
- **F304** — Codex built the partial implementation (preserved at `90ba633`, focused tests 24/24), ran `pnpm verify`, and it failed at `arch:check` (A003-T52): F302's authorized re-export of `nodeRecord` and `insertNode` from `services/api/src/test/sync-browser-support.ts` imports `graph/store` outside the folders `scripts/arch-check.mjs` allows, and the gate does not except `services/api/src/test/`. The reviewer had ruled that exception in without running the repository's own gate against it — the same defect as F301 and F302, a fourth time. Closed on the gate's own text (it allows `permission/` and treats `permission/test-support.ts` as the test-support file), no founder consultation needed. Ruled: `addNode(tx, workspaceId, nodeType)` in `services/api/src/permission/test-support.ts` gains an optional `fields` parameter (default `{}`, unable to override `node_id`, `node_type`, `workspace_id` or provenance fields; every existing caller unchanged) — the only `services/api/` file this stage changes; `sync-browser-support.ts` is restored to `main`'s version (it already re-exports `addNode`); `seedProtected` calls `addNode(tx, workspaceId, "Employee", { employee_type, full_name, job_title })`; `arch-check.mjs` and every gate stay unchanged. This reverses F302's clause that `addNode` not change, written to avoid a wider effect that a defaulted optional parameter does not have.
- **F305** — Codex finished the implementation on the F304 correction and stopped on the last gate: the full `pnpm test:sync-browser` ended 18 passed, 9 failed. `seedEntities` and `newEntity` (`packages/graph/sync-browser-tests/helpers.ts`) create an `Entity` node through `graph.createNode`, which `services/api/src/mutations/foundation.ts` rejects with `requires-feature-mutation` for every type in `FEATURE_LIFECYCLE_NODE_TYPES` — a set that has contained `Entity` since Stage 11 (`82c0b31`, 22 September). The suite was last changed by Stage 10 the same day, so it has been red on `main` for eleven stages, the slow lane's `sync-browser` job presumably with it (unconfirmed — no GitHub access from this shell), and the A003-T56 test, the repository's one real-browser proof that no protected value reaches the device, has not run green since. The reviewer's F301 gate was therefore unsatisfiable, written without running the suite on `main`. **Open finding, not closed.** Ruled: the repair is not Stage 23's job (the fixture needs a valid seed type — a genuine `entity.create` or a non-feature-owned Tier 0 type — which changes what earlier stages' tests assert on) and goes to its own Linear issue; Stage 23's gate becomes a baseline comparison per Part 2's pre-existing-failure rule (run the full suite on `main` at `11e404a` or later and record failing tests by name and message; on the branch the failing set must be identical by name and reason and every other test must pass; a branch-only or differently-failing test is a Stage 23 regression; if the baseline cannot be run, the gate is not claimed); and the passing role-removal test gains a real-browser sentinel check for the new table (`seedProtected` with unique Tier 1/2 sentinels, asserting neither appears anywhere in `JSON.stringify(dump())`, `cache_search` included). Nothing in the fixture, `graph.createNode`, `FEATURE_LIFECYCLE_NODE_TYPES`, the suite configuration or any gate is changed in this stage, and no test is skipped, tagged or removed.

`docs/Vulto_Specs/VPS-F002_Local-First_Search.md` corrected throughout to match (Technical Architecture, registry note, Permission filtering, Skill delegation, API contract, G01/G02/G03/G05, S01, the offline criterion, the NFR, four Decisions Recorded entries). Full brief in `docs/Claude_Code_Build_Prompt.md` Part 3, Stage 23 section — Do items, Done criteria, gates and the report checklist are all there, not duplicated here. Server-side untouched and no UI: no file under `apps/` changes and, but for `addNode`'s optional `fields` parameter in `services/api/src/permission/test-support.ts` (F302, F304), none under `services/api/`; `packages/graph/package.json` and the `wa-sqlite` pin are unchanged; the palette UI, keystroke, command execution and role-aware command visibility are reference material for a later shell stage, the same deferral every prior Interface Specification has received. `RST-51` is its Linear issue (filed; Blocked on F301, F302, F303, F304 and F305, back to In Progress once each was ruled); the branch is `codex/stage-23-local-first-search`, started from `main` carrying the F297–F300 corrections and resumed, not restarted, on the F301 correction. When its report is written and Linear is updated, stop and wait for the founder, same as every stage boundary.

**What to check in the Stage 23 report:** that the trigger SQL is generated from `SEARCHABLE_NODE_TYPES` with no per-type trigger hand-written, and every field name passes the `/^[a-z_]+$/` guard so no runtime value is ever interpolated into SQL (F297, G05); that `cache_search` is in `REPLICATED_TABLES`, `CACHE_SCHEMA_VERSION` is 3, and the registry-fingerprint pin test is real — change the registry locally and confirm it fails; that the index is maintained by triggers alone (grep the diff for any application-code write to `cache_search`) and the tests drive the real `putNode`/`deleteNode`/`resetTemplate` methods rather than raw inserts; that normalization is SQLite's `lower()` on both sides with no JavaScript case-folding on the entity-matching path, proven by a non-ASCII case, and that `%`/`_` are escaped with an explicit `ESCAPE`, not stripped; that registration's Tier 0 validation is a real check against the protection registry and Employee's operational schema, not an allow-list that happens to agree today — read the negative tests; that `search.query` returns no `skillMatches`, `skillMatched` is computed from the pre-limit match set, and nothing in the diff calls or wraps `skillMatcher.adHocSearch` (F298, G04); that the Ghost check is textually `matchCandidates`'s own `employee_type === "Ghost"`; that `getSkillHolders` now calls a shared availability helper with `skill-matrix.test.ts` byte-unchanged, `git grep assignmentCoversDate` shows no second coverage traversal, and no result row's availability came from a per-row `getBenchForecast`; that the command registry imports nothing from `apps/`, has no role gating and no executor; that `Document`/`Policy`/`Deliverable` and `search.queryProtected` were not built (F299); that the measured p95 is reported with its fixture scale; and that no file under `apps/` appears in the diff and the only `services/api/` file in it is `services/api/src/permission/test-support.ts`, changed only by `addNode`'s optional `fields` parameter (F302, F304; `sync-browser-support.ts` unchanged, `pnpm arch:check` and all of `pnpm verify` passing with no gate change), that the triggers resolve a label from the first non-empty text among the registered `labelFields`, that a nameless Employee replicates without error, and that a real Ghost built with `ghostEmployeeOperationalRecord` is indexed under its role title and returned `isGhost: true` (F302, F303), the `wa-sqlite` pin is unchanged, the `pnpm test:sync-browser` baseline from `main` and the branch comparison are both recorded by test name and message with an identical failing set and every other test passing (F305; or the slow-lane job's conclusion and run URL where the suite cannot be run locally), including the added `cache_search` assertions and sentinel check in `sync.spec.ts`, and the existing A003-T56 test unweakened and unrepaired here (F301, F302, F305) — and read the sentinel check against a mutated build to confirm it would fail if Tier 1/2 text reached `cache_search`.

**Completion review, 25 September 2026 — independently verified against the actual diff, not accepted from Codex's report.** Read `git diff --stat main...codex/stage-23-local-first-search` and every product and test file in full. Boundaries hold exactly as ruled: no file under `apps/`; the only `services/api/` file is `permission/test-support.ts`, changed by `addNode`'s optional fourth parameter alone (`insertNode(tx, { ...fields, ...record })`, so the fields cannot override `node_id`, `node_type`, `workspace_id` or provenance, and all 22 other `addNode` callers, in `services/api` tests, are three-argument and unaffected; the only four-argument caller is `seedProtected`); `sync-browser-support.ts` is unchanged; no `package.json`, lockfile or `wa-sqlite` pin change; none of `Foundations_Findings.md`, the build prompt, the handoff, the role doc, any `Vulto_Specs` file or `arch-check.mjs` touched. `git grep` confirms no application-code write to `cache_search` (only `search.ts` reads it, and `schema.ts` defines it), no `toLowerCase`/`toLocaleLowerCase` on the entity path, no FTS reference, `skillMatcher.adHocSearch` mentioned only in a comment, and `assignmentCoversDate` still the single coverage rule (`bench-forecast.ts`), reached from `search.ts` only through `employeeAvailabilityById`.

`packages/schema/src/search.ts`: `validateSearchableNodeTypes` is a real check, not an allow-list — it refuses `AuditEntry` by name, an unregistered type, a duplicate, an unsafe field name (`/^[a-z_]+$/`), a label field not among the indexed fields, and any type whose registered protection is not fixed Tier 0 or, for the tier-split `Employee`, whose fields are not all in the newly exported `operationalShape` with an operational Tier 0 partition present (F303); the registry is `Employee` (`labelFields: [full_name, job_title]`), `Skill`, `Project`, `Client`, and it runs at module load. `packages/graph/src/sync-client/schema.ts`: the three triggers are generated from the registry (`textValue`, `triggerInsertions`), each deleting the node's index row and re-inserting it only when the node's type is registered, `is_soft_deleted = 0` and the label resolves (`json_type = 'text'` and `<> ''`, `coalesce` over `labelFields`), so `label NOT NULL` cannot be violated and no trigger path can raise (F302, F303); `cache_search` is in `REPLICATED_TABLES` and `CACHE_SCHEMA_VERSION` is 3, with the fingerprint pinned to a literal in `database.test.ts` (Codex additionally perturbed the registry and showed the pin fail, then restored it). `packages/graph/src/queries/search.ts::searchQuery`: SQLite `lower(?)` on the query side against a `lower()`-built `search_text`, wildcards escaped in JavaScript with an explicit `ESCAPE '\'`, ranking (label-prefix, word-prefix, substring), Active-first, binary label then `node_id` tie-break, per-type limit applied after the full query so `skillMatched` (any `Skill` row) is computed pre-limit, `isGhost` as `record["employee_type"] === "Ghost"` (textually `matchCandidates`'s check), a Ghost's `secondaryLabel` null when it equals the label, `Project`'s from the `belongs_to` edge exactly as `bench-forecast.ts` does, Assignments loaded once and only when an Employee is in the results, no per-row `getBenchForecast`. `skill-matrix.ts`: `employeeAvailabilityById` is the one shared traversal; `getSkillHolders` now calls it and `skill-matrix.test.ts` is byte-unchanged and passes. `search-commands.ts`: static registry, symbolic targets, no role gating, no executor, no import from `apps/`.

Tests read in full: `search.test.ts` drives insert, rename, soft-delete, re-activation, `deleteNode` and `resetTemplate("nodes")` through the real `SqliteCache` methods with the index empty afterwards; proves G02 by reading `search_text` directly (`"aisha khan"` only, never the email or phone); refuses `Contract`, `Employee.base_compensation_amount`, `Employee.salary`, `AuditEntry`, an unregistered type and `bad-field`; builds the Ghost with `ghostEmployeeOperationalRecord`, never an invented name, and checks label, `isGhost`, `secondaryLabel: null`, re-labelling by a later `putNode`, and no-row for empty, non-text and neither-field rows; matches `%` and `_` literally; checks the ranking order `[a, z, i, w]` at limit 4; `skillMatched` true at `limit: 0`; the availability, cancelled-Assignment and no-Assignment cases; the Project's client name; and 150 Employees plus 50 Projects, p95 0.90 ms, no `fetch`. `database.test.ts` adds the version pin and a version-2 rebuild that recreates `cache_search` and its three named triggers. In `sync.spec.ts` the role-removal test now polls `cache_search` node ids against the resolvable-label rows of `cache_nodes` at initial sync and again after the role change and audience recompute, asserts the seeded Employee's `search_text` contains its lowercased name, and asserts neither of two unique Tier 1/Tier 2 sentinels appears in `JSON.stringify(dump())` (F305).

Run in this shell, not taken from the report: `scripts/arch-check.mjs` passes on the branch; `tsc --noEmit` clean for `packages/schema`, `packages/graph` and `services/api`; `packages/graph` vitest 97/97 (three Playwright spec files fail to load without `BETTER_AUTH_SECRET` and contain no vitest tests, identical to the Stage 22 review) and `packages/schema` 111 passed / 2 todo, both matching the report's counts exactly; `prettier --check` clean on all 13 changed `.ts` files; `eslint` clean on every in-scope changed file (its two `localStorage` hits at `helpers.ts:305` are pre-existing lines in `scanBrowserStorage`, outside `eslint src`, the package's own lint scope, and not in Codex's hunks). The Postgres-backed suite and both browser suites could not be run here — no Docker — the same disclosed limitation as every prior review; the report's F305 baseline (identical nine failures by test name, message shape differing only in `mutation_id`, on a detached clean worktree of `main` at `33fc9d6` and on the branch, 18 passed on each, the role-removal test passing on the branch) is read and consistent with the diff but is Codex's own terminal record, not an independent rerun.

Observations, none blocking. (1) The non-ASCII test I specified (Urdu script) has no letter case, so it would not fail if a future change folded case in JavaScript; the implementation is correct (SQLite `lower()` on both sides) and an accented-uppercase case (for example `Élodie`) is the discriminating test — add it next time `search.test.ts` is touched. (2) The generated `search_text` expression nests the running concatenation twice per indexed field, so its size doubles with each field; harmless at three fields, but a registry entry with many fields should move to a flatter form (SQLite 3.44's `concat_ws`). (3) `employeeAvailabilityById` skips an Active Assignment with no string `end_date`, where the old inline code would have compared against the string `"undefined"` and counted it as covering; the schema forbids such a row, so this is a strict improvement on malformed data only. (4) The "commands returned alongside entities" test runs at `limit: 0`, so entities are absent from that assertion; the composition is trivial in `searchQuery`. (5) The sentinel check is exactly as strong as the A003-T56 test it stands in for: Tier 1 and Tier 2 values never reach a device at all, so it confirms no path from them into the new table rather than exercising a hypothetical Tier 1 indexer; the registration-time Tier 0 validation and its refusal tests are the guard against that. (6) This review reinstalled `node_modules` (`pnpm install --frozen-lockfile --force`) in the shared working tree to get a Linux native binding for vitest; the Mac side restores its own binding with the ordinary `pnpm install --frozen-lockfile` that every gate sequence begins with.

No findings. Merged `--no-ff` as `b50ab85`. `RST-51` moves to Done.
