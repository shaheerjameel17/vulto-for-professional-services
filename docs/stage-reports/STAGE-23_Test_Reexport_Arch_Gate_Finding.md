# Stage 23 finding — F302's required test re-export fails A003-T52

The Stage 23 brief's Do item 8b requires `services/api/src/test/sync-browser-support.ts` to re-export `insertNode` directly from `../graph/store.js`, and permits exactly that test-only API-file exception. The same brief requires `pnpm verify` to pass. Its `arch:check` step (`scripts/arch-check.mjs`) forbids *every* import or re-export of `graph/store` outside the graph, permission, mutations, protected, audience and jobs folders. It does not except `services/api/src/test/sync-browser-support.ts`; its separate write check likewise does not classify that file as a test. The exact required line makes `pnpm verify` fail twice under A003-T52, despite the focused search/cache/Skill Matrix tests passing (24/24).

The browser helper cannot deep-import API internals or change `addNode` under Do item 8, and this stage may not edit any other `services/api/` file or its governing brief. No architecture-check workaround was made. Reviewer ruling requested: explicitly reconcile Do item 8b with A003-T52's repository gate, including the narrowly scoped test-only exception or an approved alternative fixture path. Partial Stage 23 product code is preserved on the branch, not complete or verified by all gates.
